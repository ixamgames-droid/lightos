# Offene Punkte — zentrale Übersicht

> **Einzige Quelle der Wahrheit für offene Aufgaben.** Diese Datei ersetzt die
> früher verstreuten Listen `TODO.md`, `docs/NEXT_STEPS.md`, `VISUALIZER_TODO.md`
> und `docs/SPIDER_MULTIHEAD_TODO.md` (am 2026-06-15 nach `docs/_archiv/` verschoben).
> Roadmap-/Design-Dokumente (`ROADMAP.md`, `docs/MOVING_HEAD_ROADMAP.md`,
> `docs/MASTERPLAN_2026-06-08.md`, `docs/UMBAU_*`, `docs/FEATURE_MAP.md`,
> `docs/PROJECT_AUDIT.md`) bleiben als Referenz erhalten — neue/offene *Aufgaben*
> aber werden ab jetzt nur noch hier gepflegt.
>
> **Stand: 2026-06-15.** Erstellt aus einem Multi-Agent-Audit: alle Tracking-
> Quellen + Code-Marker (`TODO`/`FIXME`) + Memory wurden gesammelt
> (173 Kandidaten → 79 eindeutige Punkte) und **jeder Punkt einzeln gegen den
> echten Code verifiziert**. Ergebnis: **67 wirklich offen**, **12 standen nur noch
> fälschlich als offen in der Doku** (Abschnitt 6).
>
> **Update 2026-06-15 (Folge-Batches):** 15 Punkte umgesetzt + getestet und nach
> Abschnitt 5 verschoben — Batch A: **F-1**, **SD-01**, **SD-02**, **F-14**,
> **AUDIT-exclude_note**, **B-9**; Batch B: **C-7**, **MP-SAV-01**, **NS-TEARDOWN**,
> **B-5**, **T-VIZ-15**; Batch C: **CODE-fn-editor-placeholder**, **T-8**,
> **AUDIT-VCXYPad-MIDI**, **EA-01**; Batch D (B1+B2): **T-4**, **EA-02**, **ARC-04**,
> **FW-4**; Batch E (B3): **F-6**, **F-16**, **F-15**, **AUTODJ**. Damit noch **44 offen**.
>
> **Update 2026-06-21:** **`U-4 / SD-03`** (DMX-Monitor mit Patch-Kontext) umgesetzt + getestet → Abschnitt 5. Ferner per Code-Verifikation als **stale** erkannt (Doku-Status nachzuziehen, kein Code mehr offen): **`F-20`** Art-Net-Merge ist auf eine echte Render-Schicht (HTP/LTP/REPLACE) umgebaut inkl. Tests = *erledigt*; **`F-17`** Layering-Priorität fertig, additives Blending bewusster *Won't-fix*; **`X-6`** Spider per-Kopf-Farbe kann Engine/Profil/VC bereits = *teilweise* (nur Programmer-UI-Schalter offen). Damit real noch **~43 offen**.

**Legende** — Status: `offen` (im Code nicht vorhanden) · `teilweise` (Kern da, Rest fehlt) · `extern/HW` (Code fertig, nur Hardware-/Fremdtest offen). Prio: Hoch/Mittel/Niedrig. Aufwand: klein/mittel/groß. 🔧 = braucht echte Hardware.

## 1. Priorisiert — Hoch & Mittel

Die einzigen Punkte mit echter Dringlichkeit. Alles Übrige (Abschnitt 2) ist Niedrig-Prio (Nice-to-have / Langfrist) oder bereits teilweise erledigt.

| Ref | Punkt | Prio | Aufwand | Status | Nächster Schritt |
|---|---|---|---|---|---|
| `X-1 / B-1-HW / MH-Stillstand` | Hardware-Verifikation der realen Show (4 PAR + 2 MH + APC) inkl. ZQ02001-Profil & MH-Stillstand | **Hoch** | klein 🔧 | extern/Hardware | Reine Hardware-Luecke, im Code nichts mehr zu tun. Am echten ZQ02001 (9ch + 11ch, Adressen 33/44) die dokumentierten Annahmen abarbeiten (Checkliste docs/MOVING_HEADS.md §9): CH7/CH9=Pan/Tilt-Speed bestaetigen, Strobe 250-255 = wirklich 'aus/offen', Gobo-FX/Sound-Kanal beobachten/dokumentieren, Reset-Haltedauer 4s pruefen (ggf. ResetActionButton.HOLD_MS anpassen), MH-Kreis spread auf der HW gegenpruefen. MH-Stillstand ist engineseitig bereits behoben (verify_komplett_demo.py bestanden), daher nur noch reale Sichtkontrolle der Bewegung. Ergebnisse in MOVING_HEADS.md eintragen und X-1 schliessen; B-1/X-5 (sACN) separat mit Wireshark/Empfaenger. |
| `F-8` | Enttec Open DMX USB Stabilisierung für Langzeit-Sessions (>8h) | **Mittel** | mittel 🔧 | teilweise (Kern vorhanden) | Auto-Reconnect/Health-Check im OutputManager-Loop ergaenzen (bei wiederholtem SerialTimeout/SerialException Geraet schliessen und periodisch per Port/VID-PID neu oeffnen, plus optionale UI-Statusanzeige), danach echten >8h-Dauerlauf mit angeschlossenem Enttec Open/Pro durchfuehren — Stabilitaet selbst ist nur per Hardware-Langzeittest verifizierbar. |
| `F-20 / RM-ArtNet-Merge` | Art-Net-Merge externer Quellen (HTP/LTP, Backup-Pult) | **Mittel** | mittel | teilweise (Kern vorhanden) | Merge als echte Render-Schicht statt Direkt-Write integrieren: eingehende Art-Net/sACN-Daten in einen Input-Layer-Puffer (pro out_universe) schreiben und in _render_frame nach den vorhandenen Schichten (Default/Funktionen/Executoren/Programmer) per HTP/LTP gegen das Scratch-Universe mergen, dann committen. So ueberleben externe Werte auch auf gepatchten Kanaelen und werden korrekt mit dem eigenen Output verrechnet (Backup-Pult). Anschliessend Tests fuer HTP/LTP/REPLACE-Verhalten gegen den Renderer ergaenzen und OPEN_POINTS_OVERVIEW aktualisieren. |
| `F-17 / RM-Effect-Layer-Prio / NS-B-Layering` | Freies Effekt-Layering / Effect-Layer-Priorität (zwei Effekte echt übereinander) | **Mittel** | gross | teilweise (Priorität umgesetzt) | **Priorität/Layering umgesetzt (2026-06-15):** `Function.priority` + stabiler (priority, Start-Order)-Sort in `function_manager.tick()` — höhere Priorität gewinnt bei Kanal-Konflikt, gleiche Priorität = Start-Reihenfolge (0 Regression bei Default 0). EFX-/Matrix-Editor-Regler + Persistenz + Tests (`test_function_priority.py`). Disjunkte Attribute (Pan/Tilt vs. Farbe vs. Dimmer) layern ohnehin über getrennte Kanäle. **Bewusst NICHT:** additives/HTP-Attribut-Blending — bräche den Per-Frame-Clear- + EE-02/WP-6-Merge-Contract; optionaler Rest-Stretch. |
| `D-5-Installer / PKG-Installer / RM-Linux/macOS` | Installer/Uninstaller auf frischem Windows testen + requirements.txt prüfen | **Mittel** | mittel 🔧 | teilweise (Kern vorhanden) | Code ist fertig - es fehlt der manuelle Test auf einer sauberen Windows-VM (x64 und idealerweise ARM64): python install.py durchlaufen, App starten, dann uninstall.py --dry-run und scharf prüfen. Parallel requirements.txt-Floors gegen die aktuell installierten/neuesten stabilen Wheels gegenprüfen (ggf. obere Grenzen pinnen). Danach die beiden Checkboxen in TODO.md (Z.28/30) und D-5 in OPEN_POINTS_OVERVIEW.md abhaken bzw. ein Fresh-Install-Protokoll ablegen. |
| `NS-B-LivePreview / P-06 / P-07` | Live-Vorschau Programmer: Wizard-Preview + animierte Matrix-Grid-Vorschau + aktive-Gruppen-Übersicht | **Mittel** | mittel | teilweise (Kern vorhanden) | (a) Effect-Wizard eine eingebettete Live-Vorschau geben (EfxPreviewWidget bzw. EffectMiniPreview wiederverwenden, an die Wizard-Parameter koppeln). (b) FixtureTilePreview von get_programmer_value auf den live gerenderten Output-Frame umstellen (z.B. AppState._render_frame-Ergebnis/Output-Universe pro Fixture lesen), damit Strobo/Lauflicht laufender Effekte animiert gespiegelt werden. (c) Oben-rechts eine kompakte Uebersicht der ausgewaehlten Gruppen + laufenden Effekte (mit Animations-/Aktiv-Indikator) ergaenzen; EffectMiniPreview wurde bewusst entfernt, also neues Uebersichts-Widget noetig. |
| `BPM-MGR` | Eigene BPM-Erkennung (Leader: AUTO+Tap+Nudge+Lock+Grenzen), BPM-Monitor + eigener Tab, Quellen (PC-Loopback/internem Player + externes Line-In) , Top-Bar-Anzeige, volle VC-Anbindung | **Mittel** | gross | ✅ umgesetzt+getestet | **ERLEDIGT 2026-06-16** — Voll-Suite 1298 grün; adversariale Review + Fixes drin. Design/Stand: `docs/BPM_MANAGER_PLAN.md`. Folge offen: tempo_bus `bpm_global` umgeht Lock/Präzedenz; AudioInputView-Param-Dedup. (8 Arbeitspakete, Backend zuerst). ~70% Infrastruktur vorhanden (`bpm_manager.py`/`beat_detector.py`/`capture.py` WASAPI-Loopback/Top-Bar-BPM/VC Tap+AUDIO_BPM+BPM-Fader). NEU: `request_bpm(source)`-Präzedenz + Single-Beat-Emitter-Fix (WP-1, inkl. os2l.py/media_player.py-Aufrufer), Detektor-Grenzen+Oktav-Faltung+Confidence (WP-2), Line-In-Quelle in capture.py (WP-3), Persistenz+AUTO-beim-Start (WP-4), Tab `bpm_manager_view.py` (WP-5/6), Top-Bar-Modus-Badge (WP-7), VC `VCBpmDisplay`+Nudge/Mode-Actions+APC-LED (WP-8). Entscheidungen: Player+PC via Loopback (kein Decoder-Tap), VC voll, AUTO sofort beim Start. |

## 2. Alle offenen Punkte nach Themen

### Effekt-Engine & Architektur  (6)

- **`F-17 / RM-Effect-Layer-Prio / NS-B-Layering` — Freies Effekt-Layering / Effect-Layer-Priorität (zwei Effekte echt übereinander)**  
  Mittel · gross · *teilweise (Priorität umgesetzt)*  
  → **Priorität/Layering umgesetzt (2026-06-15):** `Function.priority` + stabiler (priority, Start-Order)-Sort in `function_manager.tick()` — höhere Priorität tickt zuletzt und gewinnt bei Kanal-Überschneidung; gleiche Priorität behält die Start-Reihenfolge (0 Regression bei Default 0). EFX-/Matrix-Editor-Regler, Persistenz (to_dict/from_dict/apply_dict), Tests (test_function_priority.py). Disjunkte Attribute (Pan/Tilt vs. Farbe vs. Dimmer) layern bereits über die getrennten Kanäle. **Bewusst NICHT umgesetzt:** echtes additives/HTP-Blending pro Attribut — das bräche den Per-Frame-Clear- und EE-02/WP-6-Merge-Contract bei marginalem Nutzen; bleibt optionaler Stretch.
- **`EFX-BANK-EXKL` — Bank-exklusives Effekt-Stoppen (Effekte beim Bankwechsel automatisch stoppen)**  
  Niedrig · klein · *offen*  
  → Offen, aber bewusst nicht umgesetzt (David bevorzugt solo_fixtures-Pad-Option). Falls gewuenscht: optionale opt-in-Einstellung (z.B. AppState.stop_effects_on_bank_change) einfuehren und in vc_canvas.set_active_bank vor _apply_bank_visibility ein FunctionManager.stop_all() bzw. selektives Stoppen der Funktionen der verlassenen Bank ausloesen. Default AUS lassen, um Alt-Shows nicht zu veraendern.
- **`T-2 / ARC-02` — Effekte und Dimmer strikt trennen (Attribut-Isolation)**  
  Niedrig · mittel · *teilweise (Kern vorhanden)*  
  → Doku-Framing korrigieren: das eigentliche Verhalten (Farb-Effekte fassen den Dimmer nie an) ist via Per-Effekt-write()-Maske + Write-Log-Merge bereits robust umgesetzt. Wenn die ECHTE deklarative Variante gewollt ist (Effekt deklariert affected_attributes, Merge respektiert die Maske statt des Wert-Diffs), waere das ein Refactor: written_attrs/affected_attributes-Property auf Function + Subklassen (EFX=pan/tilt, Matrix=Style-abhaengig, Carousel) und app_state._render_frame von func_touched/func_driven (Wert-Diff) auf die deklarierte Maske umstellen. Reiner Architektur-Cleanup, kein Funktionsgewinn — niedrige Prioritaet.
- **`NS-B-UserEffekte / FW-3` — User-Effekt-/Chase-Bibliothek (eigene Effekte als Vorlage; Chase als eigene Snap-Datei)**  
  Niedrig · mittel · *teilweise (Kern vorhanden)*  
  → Teil 1 (Effekt-Vorlagen-Bibliothek) gilt als erledigt — Doku/Status entsprechend abschliessen. Fuer FW-3 noch offen: ein eigenstaendiges Datei-Konzept fuer Chases/Effekte analog zu Snaps bauen (Function.to_dict/from_dict -> .json in einem FUNCTIONS_DIR, plus migrate_from_disk und ein Export-/Import-Eintrag im snap_file_panel-Kontextmenue). Falls show-gebundene Persistenz als ausreichend gilt, FW-3 als bewusst verworfen markieren statt offen lassen.
- **`T-1 / ARC-06 / C-2 / NS-B(merge)` — Effekt-Erstellung fragmentiert → auf EIN Datenmodell vereinheitlichen**  
  Niedrig · gross · *offen*  
  → Bleibt offen, aber bewusst zurückgestellt (hohes Risiko, kein Quick-Win). Nicht als Sofort-Aufgabe angehen. Falls überhaupt: zunächst eine gemeinsame Abstraktion über EfxInstance/RgbMatrixInstance/Carousel/LayeredEffect definieren (z.B. eine generische, parametrisierte Effekt-Programm-Klasse mit einheitlicher to_dict/from_dict-Form), den Wizard so umbauen, dass er statt Scene+Chaser-Snapshots ein solches Effekt-Programm erzeugt, und alle Editoren auf dieses Modell migrieren — mit Migrationspfad für bestehende .lshow-Dateien. Wegen des großen Migrations-/Regressionsrisikos und Priorität Hoch in der Doku, aber praktisch niedrigem Nutzen vs. Aufwand, eher als langfristiges Refactoring planen, nicht jetzt.
- **`ARC-05 / C-2` — 'Programme'-Datenmodell: Effekte+Szenen als aufrufbare Einheiten (statt Snaps), mit Browser**  
  Niedrig · gross · *offen*  
  → Offen und bewusst zurückgestellt (hohes Risiko, langfristige Konsolidierung). Umzusetzen: (1) neue Datenstruktur 'Program' (Name/ID/Typ, Channel-States/Effekt-Referenzen, Fade-In/Out/Hold, Attribut-Maske) in src/core/engine/ definieren; (2) in .lshow speichern/laden; (3) Programm-Browser in rechter Seitenleiste statt SnapFilePanel (programmer_view._make_snap_panel) anbieten; (4) MIDI/VC-Zuweisung; (5) Layer-/Misch-Logik nach Maske+Priorität. Da Snaps/Scenes/Cues bereits ein de-facto-Repertoire bilden, vor Umsetzung Architektur-Entscheid: ARC-05 als eigenes Modell vs. Erweiterung des bestehenden Function/Snap-Stacks (ARC-06-Konsolidierung).

### Programmer & UI/UX  (17)

- **`NS-B-LivePreview / P-06 / P-07` — Live-Vorschau Programmer: Wizard-Preview + animierte Matrix-Grid-Vorschau + aktive-Gruppen-Übersicht**  
  Mittel · mittel · *teilweise (Kern vorhanden)*  
  → (a) Effect-Wizard eine eingebettete Live-Vorschau geben (EfxPreviewWidget bzw. EffectMiniPreview wiederverwenden, an die Wizard-Parameter koppeln). (b) FixtureTilePreview von get_programmer_value auf den live gerenderten Output-Frame umstellen (z.B. AppState._render_frame-Ergebnis/Output-Universe pro Fixture lesen), damit Strobo/Lauflicht laufender Effekte animiert gespiegelt werden. (c) Oben-rechts eine kompakte Uebersicht der ausgewaehlten Gruppen + laufenden Effekte (mit Animations-/Aktiv-Indikator) ergaenzen; EffectMiniPreview wurde bewusst entfernt, also neues Uebersichts-Widget noetig.
- **`U-8` — VC-Canvas-Kontextmenü entschlacken (Duplikate zur Toolbar)**  
  Niedrig · klein · *teilweise (Kern vorhanden)*  
  → Die schwerste Dopplung (Export/Import) ist bereits entfernt. Verbleibend: "Alle löschen" steht doppelt (Toolbar + Kontextmenü) und das komplette "Hinzufügen"-Untermenü dupliziert die Toolbar-Quick-Add-Buttons. Entscheidung treffen: Entweder Menü auf reines Schnell-Hinzufügen am Mauszeiger beschränken (dann "Alle löschen" aus dem Menü entfernen, da via Toolbar erreichbar) — oder bewusst beide belassen (Kontextmenü ist mauszeiger-positioniert, Toolbar nicht; das ist ein legitimer UX-Grund). Falls als done akzeptiert, OPEN_POINTS_OVERVIEW.md:90 von "offen, evtl. teilerledigt" auf erledigt/bewusst-belassen aktualisieren.
- **`T-10` — Echter 'Highlight'-Taster (Fixture lokalisieren) fehlt**  
  Niedrig · klein · *teilweise (Kern vorhanden)*  
  → Momentanen Locate-Taster ergaenzen: Button mit pressed/released (statt clicked) der eine temporaere Highlight-Schicht (intensity=open_value_for, weiss) ueber dem Render-Merge legt und beim Loslassen ohne Programmer-Aenderung zuruecksetzt; optional als VC/MIDI-bindbare ButtonAction.HIGHLIGHT (momentary). Der irrefuehrende Tooltip "voruebergehend ... lokalisieren" sollte dann passen bzw. bis dahin auf das tatsaechliche (persistente) Verhalten korrigiert werden.
- **`VC-DROP-rest` — VC-Restpunkte: Encoder/Color-Target Label==Wert (Show-Migration) + MIDI-Dispatch an versteckte Frame-Page-Kinder**  
  Niedrig · klein · *offen*  
  → Beide Teilpunkte sind bewusst offen geblieben. (1) Encoder/Color: analog zu BUTTON_ACTION_LABELS/SLIDER_MODE_LABELS stabile Code-Keys einfuehren (z.B. midi_mode="relative"/"absolute", target="programmer"/"effect"/...) plus Label-Mapping fuers Combo und Migrations-Mapping in apply_dict (alte deutsche Strings -> neue Keys), damit Alt-Shows nicht brechen. (2) MIDI/Tasten-Dispatch in VCCanvas._handle_midi/_on_hotkey um einen Sichtbarkeits-Check ergaenzen (widget.isVisible() bzw. fuer Frame-Kinder die aktuelle vc_page mit dem Frame._current_page abgleichen), damit Widgets auf verdeckten Frame-Pages nicht mehr feuern. Beides klein, geringe Prioritaet (Edge-Case).
- **`VC-SPEEDDIAL-BPM` — SpeedDial fest an BPM/Effekt-Tempo binden + Multiplikator-Taste (½ / ×2)**  
  Niedrig · mittel · *✅ umgesetzt 2026-06-16* (David-Wunsch) → [[project_vc_interactive_2026_06_16]]  
  → Ein **VCSpeedDial** soll dauerhaft an **BPM** bzw. **Effekt-Tempo** gebunden werden können (Drehen = Wert setzen), plus eine/mehrere zugehörige **Tasten, die den Wert relativ multiplizieren** — z. B. „Geschwindigkeit halbieren" (×0.5) / „verdoppeln" (×2). Idee: SpeedTarget um BPM erweitern bzw. den Dial an `SliderMode.BPM`/`EFFECT_SPEED` koppeln; Multiplikator als neue `ButtonAction` (z. B. `SPEED_MULT` mit Faktor-Param) oder als Dial-eigene Halbieren/Verdoppeln-Buttons. Vorhandene Bausteine: `vc_speedial.py` (VCSpeedDial), SliderMode.BPM/SPEED/EFFECT_SPEED, tempo_bus/bpm_manager. Komfort-Feature, kein Muss.
- **`VC-SMART-DROP` — Intelligenter Effekt-Drop in die VC (Auswahl-Dialog statt fixem Toggle-Button)**  
  Niedrig · gross · *✅ umgesetzt 2026-06-16* (David-Wunsch) → [[project_vc_interactive_2026_06_16]]  
  → Beim **Ziehen eines Effekts aus der Bibliothek** auf den Canvas soll statt der festen Button-Erzeugung ein kleiner **Auswahl-Dialog** erscheinen: „Was willst du steuern?" → **An/Aus** (FUNCTION_TOGGLE), **Flash** (FUNCTION_FLASH), **Effekt-Tempo**, **Effekt-Helligkeit**, ein **Live-Parameter** (`effect_live.list_params`) … → danach wird **automatisch das passende Widget** vorgeschlagen (Button vs. Fader vs. Encoder); bei mehreren sinnvollen Varianten hat man die Auswahl. Ziel: Drop wird intuitiver/intelligenter statt „immer Toggle-Button". Vorhandene Bausteine: `vc_canvas.handle_drag_drop`/`_make_*`-Pfade, `effect_live.list_params`/`list_actions`, BUTTON_ACTION_LABELS/SLIDER_MODE_LABELS, `MatrixLiveDialog` (MLV-Editor). Verwandt mit `VC-DROP-rest`.
- **`NS-F-Defaults` — Bessere Default-Farben/Beschriftungen im Panel (laufend)**  
  Niedrig · klein · *teilweise (Kern vorhanden)*  
  → Kernfunktion (sinnvolle Default-Farben + deutsche Beschriftungen + Tooltips + kontrastsichere Schrift) ist im Code umgesetzt und ausgereift. Da der Punkt bewusst als offene Dauer-Politur ("laufend") ohne Abnahmekriterium geführt wird, lässt er sich nicht formal abschließen. Empfehlung: in docs/NEXT_STEPS.md als 'weitgehend umgesetzt — nur noch fallweise Feinschliff' markieren oder ganz schließen; sonst nichts Konkretes offen.
- **`F-11` — Macro-System (Command-Line-Makros speichern/abrufen)**  
  Niedrig · mittel · *teilweise (Kern vorhanden)*  
  → Makro-Speicher implementieren: benannte Befehlssequenzen persistieren (z.B. AppState.command_macros -> .lshow/Show-File), neuer Parser-Befehl wie 'macro store <name>' / 'macro run <name>' (Keyword in lexer.py KEYWORDS + MacroCommand in parser.py), und an VC/MIDI/Keyboard bindbar machen. Die vorhandene Command-Line + Parser-Infrastruktur ist die Basis; nur das Speichern/Abrufen von Befehlsabfolgen fehlt.
- **`T-5 / C-1 / C-9 / D-1 / D-9` — Snapshot-Dopplungen vereinheitlichen (SnapFilePanel nur einmal einbetten, eine zentrale Snapshot-Quelle)**  
  Niedrig · mittel · *teilweise (Kern vorhanden)*  
  → Substanzielles Ziel (eine zentrale Snapshot-Quelle) ist erreicht — alle Panels sind Views auf den SnapLibrary-Singleton, VC teilt sich den Component. Offen bleibt nur die kosmetische Konsolidierung der Einbettungen: entscheiden, ob die Legacy-SnapshotsView (16-Slot-Quick-Grid in main_window.py:683) neben dem Programmer-eingebetteten SnapFilePanel noch noetig ist; falls nicht, den separaten Snapshots-Tab streichen bzw. das Quick-Grid in das Library-Panel integrieren, sodass SnapFilePanel nur an klar definierten Mount-Punkten (Programmer + VC) lebt. Da Datenkohaerenz bereits gegeben ist, geringe Prioritaet — eher UI-Aufraeumen als Funktions-Fix.
- **`T-5b / C-3 / C-5 / D-5 / D-7` — Weitere UI-Dopplungen: DMX-Anzeige zusammenlegen (Output+Monitor) + Farbwähler-Dedupe**  
  Niedrig · mittel · *offen*  
  → Beide Punkte sind im Code nicht umgesetzt — entspricht aber der bewussten Masterplan-Entscheidung "Komfort, nicht zusammenlegen". Falls doch gewollt: (D-5) OutputView+DmxGrid in eine Ansicht mit Zellen/Grid-Umschalter mergen; (D-7) die 7 QColorDialog-Call-Sites (rgb_matrix, carousel, color_sequence_editor, visualizer, vc_color/vc_widget/vc_button) optional auf das vorhandene widgets/color_picker.ColorPicker umstellen. Niedrige Prio, rein kosmetische Dedupe — kann als bewusster Verzicht geschlossen werden.
- **`T-12` — Software-Farbwechsel läuft als UI-Timer (nicht in Snaps/Shows persistierbar)**  
  Niedrig · mittel · *offen*  
  → Falls gewuenscht: eine echte Engine-Function fuer Farbrad-Slot-Rotation einfuehren (analog Carousel, mit Slot-Liste/Bereich + Tempo + paint-opt-in), die write() pro Frame den Farbrad-Kanal steppt und damit in Snaps/Szenen/Shows serialisierbar ist. Andernfalls bewusst als Won't-fix dokumentieren und aus der Offene-Punkte-Liste schliessen — Verhalten ist im Code klar als Designgrenze beschrieben.
- **`AUDIT-MITTEL-scene-preview` — scene_editor-Live-Preview wird vom 44-Hz-Render-Loop sofort überschrieben**  
  Niedrig · mittel · *offen*  
  → Preview durch die Render-Pipeline leiten statt direkt aufs Universe schreiben: z.B. die Scene während der Vorschau als temporäre Preview-Funktion im function_manager starten (toggelnder "Vorschau"-Button, beim Editor-Schließen stoppen) oder einen dedizierten Preview-Layer in _render_frame ergänzen, der über dem Default-Frame liegt und sauber freigegeben wird. So bleibt die Vorschau stabil und wird nicht pro Frame weg-committed.
- **`AUDIT-NIEDRIG-UniverseBar` — UniverseBar zeigt fest nur Universe 1 / Doppel-Refresh in mehreren Views**  
  Niedrig · mittel · *teilweise (Kern vorhanden)*  
  → UniverseBar-Teil ist fertig (Doku dort veraltet). Fuer den Rest: einen der beiden Pfade konsolidieren — entweder Views ganz auf den Sync-Bus (StateSync) umstellen und `state.subscribe`/`_callbacks` als Legacy entfernen, oder in `app_state._emit_impl` nur noch einen Pfad routen. Mindestens in patch_view/programmer_view/playback_view die doppelten `state.subscribe(_on_state_change)`-Registrierungen entfernen (Handler decken sich mit den Sync-Lambdas) und einen kurzen Coalescing-Timer fuer PATCH_CHANGED/REFRESH_ALL einbauen, damit `_refresh_table`/`_refresh_fixture_list` pro Event nur einmal laeuft.
- **`PA-02` — Live-Übersicht unten rechts im Patch-Bereich (Status-Widget)**  
  Niedrig · mittel · *offen*  
  → Status-Widget unten rechts in PatchView einbauen: Gruppen-Liste mit Aktivitäts-Lampe (Gruppe hat DMX-Output), nummerierte Fixture-Kacheln der aktiven Gruppe, optional Universe-Status. DMX>0 aus dem OutputManager/Universe-Buffer pollen via QTimer ~10 Hz (Timer bei verstecktem Tab pausieren, analog Live-View/SimpleDesk). UniverseBar bietet keine Live-Werte und müsste dafür erweitert oder durch ein neues Panel ergänzt werden.
- **`F-12` — Multi-Monitor-Layouts (Docks über mehrere Screens speichern)**  
  Niedrig · gross · *offen*  
  → Noch offen. Voraussetzung wäre der Umbau auf ein dockbares/floating-Window-Konzept (z.B. QDockWidget oder eigene Popout-Fenster pro Sektion) und das Persistieren von QScreen-Index + Geometrie pro Dock in layout_state.collect_layout/apply_layout, inkl. Robustheit bei wegfallenden Monitoren (Clamp auf availableGeometry). Mindestens: Visualizer-/Music-Popout-Geometrie pro Screen speichern als erster Schritt.
- **`F-13 / RM-Touch-Phase2` — Touch-Optimierung Phase 2 (Pinch-Zoom Patch, Swipe zwischen Tabs, groessere Hit-Targets)**  
  Niedrig · gross · *offen*  
  → Offen umsetzen: (a) Pinch-Zoom im Patch — Patch-Tab auf eine zoombare Ansicht (QGraphicsView/Canvas) heben oder per QGesture/PinchGesture + grabGesture/WA_AcceptTouchEvents bedienen (aktuell tabellenbasiert, gar kein Zoom). (b) Swipe zwischen Tabs — SwipeGesture am QTabWidget (_SubTabs/main_window) abfangen und currentIndex aendern. (c) Hit-Targets systematisch vergroessern (Pads/Fader/Buttons der Programmer-/VC-UI), nicht nur der bestehende 24px-Radius in live_view._fixture_at. Verifizierung kann ohne Touch-Hardware via Maus-Emulation/QTest erfolgen, finale Abnahme am Touch-Display empfohlen. (Doku korrekt als offen markiert.)
- **`AUDIT-02` — UI-Konsolidierung nach Inventar (FEATURE_MAP-Plan schrittweise umsetzen)**  
  Niedrig · gross · *teilweise (Kern vorhanden)*  
  → Offen bis auf C-6. Schrittweise (ein Bereich/PR, danach Smoke-Test) umsetzen: C-1/C-9 SnapFilePanel in eine gemeinsame Komponente/Instanz fuehren; C-3 Output+DMX-Monitor zu einer Ansicht mit Zellen/Grid-Umschalter; C-5 alle QColorDialog-Stellen auf widgets/color_picker vereinheitlichen; C-4 gemeinsamen Teach-/Learn-Flow; C-7 Fixture-/Kanal-Gruppen in einen klar beschrifteten Bereich. C-2 (Effekt-Datenmodell) als separates ARC-Refactoring belassen. Wegen geringen Nutzerwerts (funktioniert alles, nur redundant) niedrige Prioritaet.

### 3D-Visualizer  (4)

- **`NS-E-Bühnenlayout / NS-E-2DTopDown / T-VIZ-cross-mirror` — Visualizer-Rest: reales Bühnen-Layout-Setup, 2D-Top-Down-Ansicht neben VC, Cross-Window-Live-Mirror**  
  Niedrig · klein · *teilweise (Kern vorhanden)*  
  → Nur noch optionaler Rest: ein kompaktes 2D-Top-Down-Schnellpanel direkt im/neben dem Virtuelle-Konsole-Fenster einbetten (vorhandene Minimap/Visualizer3DView wiederverwenden). Reales Bühnen-Layout-Setup und Cross-Window-Live-Mirror sind bereits voll umgesetzt — Doku/verify_hint dazu sind veraltet. Wenn der 2D-neben-VC-Wunsch nicht mehr aktuell ist, kann der Punkt geschlossen werden.
- **`T-VIZ-13` — Kein Undo-Support für Visualizer-Operationen (Fixture/Stage-Element verschieben)**  
  Niedrig · mittel · *offen*  
  → Undo bleibt offen. In den Move-Handlern (visualizer_window.py: _on_fixture_moved_from_js/_on_fixture_pos_spin_changed fuer Fixtures, _on_stage_list_from_js/_on_stage_props_changed fuer Stage-Elemente) jeweils die alte Transform vor dem Schreiben sichern und ein Command via get_undo_stack().push_simple(label, do, undo) erzeugen (push mit execute=False, da die Aenderung schon erfolgt ist), das visualizer_positions/visualizer_rotations bzw. el.x/y/z restauriert und per Bridge an JS zuruecksendet (push_apply_fixture_transform). JS-Drags sollten zu einem Command pro abgeschlossenem Drag gebuendelt werden (Move-Begin/Move-End), nicht pro Frame.
- **`RM-Beam-Rendering` — Echtzeit-Beam-Rendering im 3D-Visualizer (Lichtkegel, Nebel, Gobo-Projektion)**  
  Niedrig · gross · *teilweise (Kern vorhanden)*  
  → Lichtkegel und Nebel/Haze sind erledigt (Doku-Teil dazu veraltet). Offen bleibt nur die Gobo-Projektion: Gobo-Slot pro Fixture als Textur auf SpotLight.map (oder Decal/projizierte Textur) bzw. echter volumetrischer Beam per Shader-Raymarch. Da langfristig/aufwendig und kein direkter Praxis-Bedarf: niedrige Prioritaet. Konkret: SpotLight.map mit Gobo-CanvasTexture aus dem Fixture-Profil verdrahten und an den vorhandenen Beam/SpotLight-Pfad in addFixture/updateFixture anhaengen.
- **`3D-SPIDER / T-VIZ-16` — 3D-Modell für Spider/Multihead + Custom-Modell-Import-Bridge (OBJ/COLLADA pro Fixture)**  
  Niedrig · gross · *offen*  
  → Offen, nicht im Code umgesetzt. Empfehlung: (1) Bridge-Feld model_path in _fixture_to_dict ergänzen und in addFixture an einen neuen loadFixtureModel-Pfad durchreichen (per-Fixture-Override statt nur fixture_type). (2) buildFixtureModel um Cases spider/derby/flower (eigene Geometrie mit mehreren Linsen) erweitern, mindestens den vorgeschlagenen Fallback „zwei moving_head-Meshes nebeneinander" für Spider-Typen. (3) Für GDTF-Share-GLB-Import einen GLTFLoader.js als drittes Loader-Script einbinden und loadModel um .gltf/.glb-Zweig ergänzen (alternativ GLB→DAE/OBJ konvertieren). Quick-Win zuerst: Spider-Builtins auf einen eigenen visualizer-Typ mappen + Doppel-Mesh-Fallback.

### Stabilität & QA  (1)

- **`T-7` — debug_swallow auf restliche stille except-pass im Kern ausrollen**  
  Niedrig · mittel · *teilweise (Kern vorhanden)*  
  → Wie dokumentiert ein bewusster Teilstand: zentraler Helper vorhanden und in den heissesten/wichtigsten Pfaden (app_state-Emit/Marshaller, cue_stack-Callbacks) verdrahtet, aber ~110 stille except-pass im Kern absichtlich nicht blanket-konvertiert. Falls weitergetrieben: gezielt die Render-/IO-Pfade ohne eigenes Logging nachruesten (engine/function_manager.py, dmx/output_manager.py, engine/rgb_matrix.py) mit debug_swallow(tag, e); kein Pauschal-Refactor noetig. Niedrige Prioritaet, reine Diagnose-Verbesserung ohne Verhaltensaenderung in Produktion (Default aus).
### Dokumentation  (2)

- **`T-3 / ARC-01` — Ist-Analyse/Architektur-Doku der Effekt-Engine schreiben (docs/EFFECT_ENGINE.md)**  
  Niedrig · klein · *teilweise (Kern vorhanden)*  
  → Entweder eine schlanke docs/EFFECT_ENGINE.md anlegen, die die bereits in ARCHITECTURE.md (Funktionstypen-Tabelle, Function-Basisklasse, FunctionManager-LTP) und OUTPUT_MERGE_CONTRACT.md (WP-6/EE-02/Schichtordnung) verstreute Effekt-Engine-Doku buendelt und vertieft (Datenfluss write()->Scratch->Merge, Fade-Kurven, intensity-Privatuniversum) - oder den Punkt schliessen und auf die vorhandenen Docs verweisen. Reine Doku-Arbeit, keine Code-Aenderung noetig.
- **`D-4-Smoke / SMOKE` — Smoke-Tests (Audio/MIDI/OSC/Web/DMX-Checkliste) regelmäßig manuell durchlaufen**  
  Niedrig · klein · *extern/Hardware* · 🔧 Hardware  
  → Nicht im Code entscheidbar — manueller Prozess. Empfehlung: Checkliste vor dem naechsten Release einmal mit angeschlossener Hardware (Soundcard/APC Mini/OSC-Client/Enttec o. Art-Net/Browser) durchgehen und die [ ]-Boxen abhaken bzw. Datum/Ergebnis am Dateikopf vermerken. Optional ein schlankes Smoke-Skript (z.B. tools/smoke_check.py) ergaenzen, das die HW-freien Teile automatisiert (OSC-Server-Start, Flask-Route 200, Universe-Write-Counter) um den manuellen Aufwand zu reduzieren.

### Packaging & Langfrist  (2)

- **`D-5-Installer / PKG-Installer / RM-Linux/macOS` — Installer/Uninstaller auf frischem Windows testen + requirements.txt prüfen**  
  Mittel · mittel · *teilweise (Kern vorhanden)* · 🔧 Hardware  
  → Code ist fertig - es fehlt der manuelle Test auf einer sauberen Windows-VM (x64 und idealerweise ARM64): python install.py durchlaufen, App starten, dann uninstall.py --dry-run und scharf prüfen. Parallel requirements.txt-Floors gegen die aktuell installierten/neuesten stabilen Wheels gegenprüfen (ggf. obere Grenzen pinnen). Danach die beiden Checkboxen in TODO.md (Z.28/30) und D-5 in OPEN_POINTS_OVERVIEW.md abhaken bzw. ein Fresh-Install-Protokoll ablegen.
- **`F-22 / RM-Langfrist-Block` — Langfrist-Block: Stage-Wizard, WYSIWYG-Export, Pixel-Mapping, Script-API 2, Netzwerk-Sync, Linux/macOS-Port, Plugin-System**  
  Niedrig · gross · *offen*  
  → Bleibt als Langfrist-Sammelpunkt offen; keiner der 7 Teile ist begonnen. Empfehlung: in einzelne Backlog-Items aufteilen und priorisieren — pragmatischer Quick-Win ist das Plugin-System (Stub src/plugins/__init__.py mit Entry-Point-Loader fuellen) und Script-API Phase 2 (sandboxed Python statt DSL). Stage-Wizard/WYSIWYG-Export bauen auf vorhandenem StageDefinition/dock_target_for auf. Pixel-Mapping, Netzwerk-Sync und Linux/macOS-Port sind eigenstaendige groessere Vorhaben.

### DMX  (7)

- **`X-1 / B-1-HW / MH-Stillstand` — Hardware-Verifikation der realen Show (4 PAR + 2 MH + APC) inkl. ZQ02001-Profil & MH-Stillstand**  
  Hoch · klein · *extern/Hardware* · 🔧 Hardware  
  → Reine Hardware-Luecke, im Code nichts mehr zu tun. Am echten ZQ02001 (9ch + 11ch, Adressen 33/44) die dokumentierten Annahmen abarbeiten (Checkliste docs/MOVING_HEADS.md §9): CH7/CH9=Pan/Tilt-Speed bestaetigen, Strobe 250-255 = wirklich 'aus/offen', Gobo-FX/Sound-Kanal beobachten/dokumentieren, Reset-Haltedauer 4s pruefen (ggf. ResetActionButton.HOLD_MS anpassen), MH-Kreis spread auf der HW gegenpruefen. MH-Stillstand ist engineseitig bereits behoben (verify_komplett_demo.py bestanden), daher nur noch reale Sichtkontrolle der Bewegung. Ergebnisse in MOVING_HEADS.md eintragen und X-1 schliessen; B-1/X-5 (sACN) separat mit Wireshark/Empfaenger.
- **`F-8` — Enttec Open DMX USB Stabilisierung für Langzeit-Sessions (>8h)**  
  Mittel · mittel · *teilweise (Kern vorhanden)* · 🔧 Hardware  
  → Auto-Reconnect/Health-Check im OutputManager-Loop ergaenzen (bei wiederholtem SerialTimeout/SerialException Geraet schliessen und periodisch per Port/VID-PID neu oeffnen, plus optionale UI-Statusanzeige), danach echten >8h-Dauerlauf mit angeschlossenem Enttec Open/Pro durchfuehren — Stabilitaet selbst ist nur per Hardware-Langzeittest verifizierbar.
- **`F-20 / RM-ArtNet-Merge` — Art-Net-Merge externer Quellen (HTP/LTP, Backup-Pult)**  
  Mittel · mittel · *teilweise (Kern vorhanden)*  
  → Merge als echte Render-Schicht statt Direkt-Write integrieren: eingehende Art-Net/sACN-Daten in einen Input-Layer-Puffer (pro out_universe) schreiben und in _render_frame nach den vorhandenen Schichten (Default/Funktionen/Executoren/Programmer) per HTP/LTP gegen das Scratch-Universe mergen, dann committen. So ueberleben externe Werte auch auf gepatchten Kanaelen und werden korrekt mit dem eigenen Output verrechnet (Backup-Pult). Anschliessend Tests fuer HTP/LTP/REPLACE-Verhalten gegen den Renderer ergaenzen und OPEN_POINTS_OVERVIEW aktualisieren.
- **`B-1 / X-5 / C5` — sACN (E1.31) Ausgabe gegen echte Hardware/Wireshark verifizieren**  
  Niedrig · klein · *extern/Hardware* · 🔧 Hardware  
  → Im Code nichts mehr zu tun — Sender ist implementiert, spec-konform und round-trip-getestet. Verbleibt nur der reine HW-Schritt: mit echtem sACN-Empfaenger (z.B. QLC+/sACNView) + Wireshark-Mitschnitt verifizieren, dass Multicast/Unicast-Pakete auf Port 5568 ankommen und korrekt geparst werden; danach B-1/X-5/C5 in docs/OPEN_POINTS_OVERVIEW.md als erledigt markieren. Verdict 'unknown', weil rein im Code nicht entscheidbar.
- **`U-4 / SD-03` — DMX-Monitor mit Patch-Kontext (Geräte-Labels + Farbkodierung pro Zelle)**  
  Niedrig · mittel · *✅ umgesetzt 2026-06-21* → siehe Abschnitt 5
- **`T-11 / MIGR-5` — Hängende Montage (mounted_inverted) + StageElement.fixture_id mit Tilt-Invertierung**  
  Niedrig · mittel · *teilweise (Kern vorhanden)*  
  → Effekt ist via manuellem 'Tilt invertieren'-Toggle bereits erreichbar, daher nicht dringend. Falls das explizite Feature gewuenscht ist: (1) optionales 'mounted_inverted'-Bool in PatchedFixture/Show-Datei ergaenzen (mit Migration/Default False = alt-kompatibel), das beim Rendern automatisch _apply_orientation/invert_tilt aktiviert; (2) StageElement.fixture_id (oder fids-Liste) einfuehren, damit der 3D-Visualizer Buehnen-Elemente↔Fixtures verknuepft und haengende Montage visuell ableitet. Alternativ Punkt als 'durch invert_tilt abgedeckt' schliessen und Doku entsprechend aktualisieren.
- **`X-6 / SPIDER-MH` — Spider: Köpfe getrennt einfärben (eigene Farbe pro RGBW-Bank)**  
  Niedrig · mittel · *offen*  
  → Offen. Pragmatischer erster Schritt (Weg A, klein/mittel, kein Kerncode): in fixture_db.py zwei disjunkte Builtin-Profile ergänzen ("Spider Kopf 1" 9ch = Pan/Tilt/Speed/Dimmer/Shutter/R1G1B1W1, "Spider Kopf 2" 4ch RGBW auf Basis+9) -> Kopf 2 bekommt sofort eigene Farbe über Programmer/Farb-Kacheln/Matrix. Echtes Kopf-Konzept (Weg B: FixtureChannel.head, programmer key um Kopf-Index, _fixture_color_and_intensity als Kopf-Farb-Liste, 3D buildSpider mit N Linsen) bleibt größeres Folge-Feature.
  → **David-Wunsch 2026-06-16 (erweitert):** Davids Spider haben **zwei Lichtleisten/Bars = zwei SEPARATE Tilt-Kanäle** (nicht Pan+Tilt; die Bars schwenken zu-/voneinander weg), und **je Bar eigene Farbe** (die zwei RGBW-Bänke). Aktuelles SPIDER14-Profil modelliert CH1/CH2 als Pan/Tilt und teilt die Farb-Bänke (gemeinsame Farbe). Nötig: pro-Kopf-Tilt + pro-Kopf-Farbe (= Weg B / X-6). **Zusatz-To-do `CFG-MULTIHEAD`:** der Fixture-**Konfigurator/Generator** (`fixture_generator.py`) soll Custom-Geräte mit **mehreren Tilt-Kanälen** und **Farbe je Tilt/Kopf** anlegen können (head-Index pro Kanal-Gruppe), damit man solche Geräte selbst sauber definieren kann.
- **`F-21 / RM-GDTF-Import-P2 / RM-Online-Fixture-Share / FEAT-GDTF` — GDTF-Import + Online-Fixture-Share (GDTF-Share-DB durchsuchen)**  
  Niedrig · gross · *offen*  
  → Echtes Feature noch zu bauen: (1) GDTF-Parser in src\core\database\ (z.B. gdtf_import.py) — .gdtf ist ZIP mit description.xml; DMX-Modes/Channels/Functions/Wheels in fixture_db-Profile mappen (Vorbild qxf_import.py). (2) Optional Online-Suche gegen GDTF-Share-API (Login/HTTP-Client) mit Such-/Download-Dialog analog qxf_import_dialog.py. (3) FEATURES.md:196 und F-21 nach Umsetzung aktualisieren. Anschliessend X-6 (Spider 3D-Modell aus GDTF-Share) wird dadurch leichter.

### Show  (1)

- **`FEAT-LTC / LTC-TIMECODE` — LTC (Linear Timecode) Empfang via Audio-Eingang**  
  Niedrig · gross · *offen* · 🔧 Hardware  
  → LTC-Decoder neu bauen: Audio-Frames aus dem vorhandenen AudioCapture-Stream (44100 Hz, mono) abgreifen und SMPTE-Biphase-Mark-Code dekodieren (Sync-Word 0x3FFD, 80-Bit-Frame -> hh:mm:ss:ff + fps). Als neues Modul src/core/timecode/ltc_reader.py mit gleicher Schnittstelle wie MTCReader (subscribe/time/fps/format), damit der Playback-Timecode-Pfad beide Quellen nutzen kann. Audio-Geraet-/Line-In-Auswahl in der UI ergaenzen. Hardware-Test mit echtem LTC-Generator noetig.
### MIDI  (5)

- **`F-19 / RM-OSC-Feedback` — OSC-Feedback vollständig bidirektional (TouchOSC-kompatibel)**  
  Niedrig · mittel · *teilweise (Kern vorhanden)*  
  → OscSender als Singleton anbieten (get_osc_sender), Lebenszyklus an _toggle_osc_server koppeln (Target-IP/Port aus Client-Adresse oder Konfig) und an State-Events (Cue/Executor-GO, Fader-Position, Blackout, Programmer-Clear) abonnieren, die per OscSender.send() das Spiegel-Adressschema (/lightos/exec/{n}/go usw.) zurückspielen — analog zu apc_mini_feedback.py. Verdrahtung kann ohne Hardware mit einem lokalen OSC-Mock getestet werden.
- **`U-7 / MIDI-01..04 / U-LED` — MIDI-LED-Komfort: Wellen-Toggle, LED-Painter-GUI, Momentary, mehr LED-Modi**  
  Niedrig · mittel · *teilweise (Kern vorhanden)*  
  → Doku fuer (03) Momentary + (04) LED-Modi als erledigt markieren (in flash umbenannt bzw. Pad-Stil-System deckt Blink/Puls/Farbe-aus-Effekt ab). Offen bleiben nur 2 Teile: (01) eine echte Wellen-Abschaltung exponieren — ripple_enabled an einen UI-Schalter haengen (z.B. neben "APC LEDs") und/oder ein per-Button-Flag plus Persistenz; (02) eine dedizierte Painter-GUI bauen (interaktives 8x8-Grid zum Setzen von Farbe/Animation/Verzoegerung pro Mapping-Slot) — existiert noch gar nicht, hier liegt der eigentliche Aufwand.
- **`NS-LED-PAD / LED-PAD-01` — LED-Pad-Spiegelung der live gebauten Color-/Chase-Liste auf Hardware-Pads**  
  Niedrig · mittel · *offen* · 🔧 Hardware  
  → In apc_mk2_feedback._collect_desired (bzw. analog apc_mini_feedback) eine optionale Erfassung von VCChaseBuilder/VCColorList ergaenzen: deren Ziel-Effekt aufloesen (effect_live.resolve_target), colors.entries auf eine konfigurierbare Pad-Reihe abbilden (Reihenfolge=Pads, Farbe via rgb_to_mk2_index, aktiver Index=PULSE, deaktiviert=DIM) und per set_pad senden. Pad-Layout/Reihen-Offset konfigurierbar machen und an echter APC mini bzw. mk2 verifizieren.
- **`F-18 / RM-Web-Remote-P2` — Web-Remote Phase 2 (Cuelisten/Patch/Programmer im Browser)**  
  Niedrig · gross · *offen*  
  → Web-Remote um Phase-2-Ansichten erweitern: (1) Cuelisten-View — neue Routen /api/cuelist (Liste der CueStacks+Cues) und /api/cuelist/<idx>/go/back/goto sowie eine Cue-Tabelle im index.html (status liefert cue_stacks bereits, nur Anzeige+pro-Stack-Steuerung fehlt). (2) Patch-Uebersicht — Read-only Endpoint /api/patch (Fixtures, Universe, Adresse, Mode) + Tabelle. (3) Programmer-Zugriff ueber clear hinaus — /api/programmer (aktuelle Werte lesen) und Setzen von Attributen/Farbe. Live-Updates per SocketIO-Broadcast statt Polling. Tests analog tests/test_web_* ergaenzen.
- **`CODE-keyboard-rawinput` — Getrennte Tastatur-Profile (Raw-Input/WM_INPUT) nicht implementiert**  
  Niedrig · gross · *offen* · 🔧 Hardware  
  → Offen wie dokumentiert. Falls gewuenscht: nativen Raw-Input-Hook (WM_INPUT via RegisterRawInputDevices/GetRawInputData) ueber QAbstractNativeEventFilter bzw. ctypes implementieren, pro Event das Geraete-Handle (GetRawInputDeviceList) auslesen und in KeyboardHotkeyFilter ein per-Tastatur-Profil-Mapping (device_handle -> subscriber/profile) ergaenzen. Realer Test braucht 2 physische USB-Tastaturen (needs_hardware). Andernfalls als bewusste, dokumentierte Grenze belassen.

## 3. Quick Wins (klein + sichtbar)

Kleine, in sich abgeschlossene Punkte mit guter Wirkung — gut für einen schnellen Aufräum-Durchlauf:

- _(Stand 2026-06-15: alle gelisteten Quick-Wins erledigt — siehe Abschnitt 5.)_


## 4. Hardware-/Fremdtest-abhängig

Code ist fertig — diese Punkte lassen sich nur am echten Gerät / mit externen Tools abschließen:

- **`X-1 / B-1-HW / MH-Stillstand`** (Hoch) — Hardware-Verifikation der realen Show (4 PAR + 2 MH + APC) inkl. ZQ02001-Profil & MH-Stillstand
- **`F-8`** (Mittel) — Enttec Open DMX USB Stabilisierung für Langzeit-Sessions (>8h)
- **`D-5-Installer / PKG-Installer / RM-Linux/macOS`** (Mittel) — Installer/Uninstaller auf frischem Windows testen + requirements.txt prüfen
- **`B-1 / X-5 / C5`** (Niedrig) — sACN (E1.31) Ausgabe gegen echte Hardware/Wireshark verifizieren
- **`FEAT-LTC / LTC-TIMECODE`** (Niedrig) — LTC (Linear Timecode) Empfang via Audio-Eingang
- **`CODE-keyboard-rawinput`** (Niedrig) — Getrennte Tastatur-Profile (Raw-Input/WM_INPUT) nicht implementiert
- **`D-4-Smoke / SMOKE`** (Niedrig) — Smoke-Tests (Audio/MIDI/OSC/Web/DMX-Checkliste) regelmäßig manuell durchlaufen
- **`NS-LED-PAD / LED-PAD-01`** (Niedrig) — LED-Pad-Spiegelung der live gebauten Color-/Chase-Liste auf Hardware-Pads

## 5. Erledigt laut Code — in alten Listen noch als „offen" geführt

Diese Punkte wurden gegen den Code verifiziert und sind **bereits umgesetzt**. Sie tauchen nicht mehr als offene Aufgaben auf und dienen nur der Nachvollziehbarkeit:

### Folge-Punkt 2026-06-21 (umgesetzt + getestet)

- **`U-4 / SD-03` — DMX-Monitor mit Patch-Kontext (Geräte-Kürzel + Funktionsfarbe pro Zelle)** — Umgesetzt. `DmxGrid` bekommt eine `set_cell_info(addr→(Kürzel,Attribut,Tooltip))`-Map; `dmx_monitor_view._refresh_patched` baut sie über `get_patched_fixtures()`+`get_channels_for_patched(f)` (lazy-Import, mock-bar). In `paintEvent` wird jede gepatchte Zelle dezent in `channel_function_color(attr)` getönt (Alpha 40) und unten eine Kürzel-Zeile „<Gerät> <Funktion>" via `channel_function_abbrev` gezeichnet; neuer `event()`-ToolTip-Handler zeigt den vollen Kanalnamen (Gerät — Kanal = Wert). Helfer 1:1 aus `simple_desk.py` (SD-01/SD-02) wiederverwendet, kein Eingriff in den Render-Loop. Legende erweitert. Tests: `tests/test_dmx_monitor_patch_context.py` (4). Voller Monitor/Simple-Desk-Regressionssatz 33 grün.

### Folge-Batch 2026-06-15 (ohne Hardware umgesetzt + Regressionstests grün)

- **`F-1 / RM-Preset-Browser / U-1 / P-04` — Preset-Browser (Suchfeld) für Paletten/Gruppen + Matrix als Standard-Gruppenansicht** — Umgesetzt. (a) Such-/Filterfeld (QLineEdit + textChanged) in `palette_view.PalettePage` (Toolbar, filtert `_refresh` nach Name/Ordner) und in `programmer_view._make_fixture_panel` (Gruppen-Box, filtert `_refresh_group_list`; bei aktiver Suche flache Treffer-Liste ohne Ordner-Collapse). (b) `programmer_view._on_group_clicked` schaltet nach `set_selected_group_id` direkt auf den gemerkten Matrix-Tab (`_matrix_tab_index`); Ordner-Kopfzeilen schalten NICHT um. Tests: tests/test_search_and_matrix_view.py (4).
- **`SD-01` — Geräte-Label + Kanal-Funktion pro Kanal im Simple Desk ('PAR 1 - R')** — Umgesetzt. `ChannelFader` hat ein kleines Funktions-Kürzel-Label (`set_function_label`, Fader-Höhe 110→124px), befüllt in `_apply_fixture_tints` aus dem Kanal-Attribut (`get_channels_for_patched`) via `channel_function_abbrev`; voller Name bleibt im Tooltip. Tests: tests/test_simple_desk_function_view.py.
- **`SD-02` — Kanal-Farbkodierung nach Funktion im Simple Desk** — Umgesetzt. Modul-Map `CHANNEL_FUNCTION_COLORS` + Helfer `channel_function_color`; Toolbar-Toggle „Farbe nach Funktion" (`_color_by_function`) schaltet in `_apply_fixture_tints` zwischen Fixture-Gruppierung (Default) und Funktions-Farbe um. Tests: tests/test_simple_desk_function_view.py.
- **`F-14` — Quick-Recording im Playback (Ein-Klick-Record)** — Umgesetzt. „⚡ Quick-Rec"-Button in der cue_toolbar (`playback_view._setup_ui`) + `playback_view._quick_record_cue`: dialogfreies Record auf den AKTUELLEN `self._current_stack` (Auto-Nummer max+1, Label „Cue N"), ohne QInputDialog. Tests: tests/test_playback_quick_rec.py (3).
- **`AUDIT-MITTEL-MIDI-exclude_note` — exclude_note/include_note in apc_mini_feedback ist toter Code** — Entfernt. `exclude_note`/`include_note`, `self._excluded` und alle `not in self._excluded`-Guards in `_update` gelöscht; `set_led` feuert nun unbedingt (verhaltensneutral, da die Menge immer leer war) — konsistent zur mk2-Variante. Tests: tests/test_apc_mini_feedback.py (2).
- **`B-9` — MIDI-Crash-Hypothesen (MIDI_CRASH_DEBUG_NOTES.md) als historisch markieren** — Erledigt. `MIDI_CRASH_DEBUG_NOTES.md` hat einen HISTORISCH-Kopf (Thread-Safety-Runde 2026-06-08 + MIDI-Page-Crash-Fix 2026-06-14); Hypothesen B/C als adressiert, A bleibt HW-offen. Reiner Doc-Change.

### Folge-Batch 2026-06-15 (B) — Quick-Wins (umgesetzt + getestet)

- **`C-7 / D-4` — Gruppen-Begriff schärfen (Fixture-Gruppen vs. Kanal-/Submaster-Gruppen)** — Umgesetzt. Tab-Titel in main_window.py eindeutig: „Gruppen" → „Fixture-Gruppen", „Channel Groups" → „Submaster/Kanal-Gruppen" (reine Labels, kein Lookup/Test betroffen; der separate Live-View-„Gruppen"-Tab bleibt unberührt).
- **`MP-SAV-01 / SHOW_FILE_FORMAT-doc` — docs/SHOW_FILE_FORMAT.md aktualisieren** — Erledigt. Header-Stand 2026-06-15; fehlende Top-Level-Keys `efx_paths`, `playlist`, `music_autoshow` ergänzt; `visualizer`-Zeile um docks/active_stage, `live_view`-Zeile um meta erweitert (gegen den show_file.py-Save-Pfad abgeglichen). Reine Doku.
- **`NS-TEARDOWN-SEGFAULT / SEGFAULT-01` — Voll-Suite-Teardown-Segfault (GC-Race nicht gestoppter DMX/MIDI-Threads)** — Behoben. tests/conftest.py `_stop_background_threads_at_end` stoppt jetzt zusätzlich die MIDI-Threads am Suite-Ende: `midi_mapper._mapper_instance.close()` (MidiFeedbackEngine) dann `midi_manager._manager.close_all()` (MidiDispatch), je über die Modul-Globals (kein Lazy-Create) und is-not-None-guarded.
- **`B-5 / T-VIZ-05` — Visualizer JS-Cleanup: ungenutzte Preset-Funktionen entfernen** — Erledigt. `setStagePreset` (+ applySettings-Block, stageChanged-Connect, `__lightos`-Export) aus stage_scene.html entfernt, Init nutzt direkt `buildGridAndFloor()`; `push_stage_preset`-Methode + `stageChanged`-Signal aus visualizer_window.py entfernt. Verhalten unverändert (der Preset-Pfad hatte keinen Treiber).
- **`T-VIZ-15` — Stage-Element-Farben ohne Live-Preview beim Scrollen** — Umgesetzt. `visualizer_window._on_pick_stage_color` nutzt jetzt einen nicht-modalen QColorDialog (`currentColorChanged` → live `el.color` + `updateStageObject`-Emit; Abbrechen stellt die Ausgangsfarbe wieder her). Test: tests/test_visualizer_stage_color.py (4).

### Folge-Batch 2026-06-15 (C) — Robustheit / Feature / Tooling (umgesetzt + getestet)

- **`CODE-fn-editor-placeholder` — Generischer Editor-Platzhalter 'Editor kommt bald'** — Erledigt. `create_function_editor` (function_manager_view.py) hat jetzt eigene EFX/RGBMatrix-Zweige, die auf den Programmer (Tab EFX/Matrix) verweisen, statt in der 'Editor kommt bald'-Sackgasse zu landen; Catch-all-Text entschärft. Test: tests/test_fn_editor_placeholder.py (3).
- **`T-8 / RM-Benchmark` — Performance-Benchmarks für mehrere Universen (8/16/32)** — Erledigt. `tools/benchmark_universes.py` misst `AppState._render_frame` (je Universum RGB-Matrix + EFX, p50/p95/max/FPS) ohne DMX-Ausgabe; Ergebnisse in `docs/PERFORMANCE.md` (ARM64: 8 Univ ≈ 6,6 ms Ø, 16 ≈ 16 ms, 32 ≈ 47 ms → 44-Hz-Budget bei 32 Universen überschritten). Smoke-Test: tests/test_benchmark_universes.py (2).
- **`AUDIT-NIEDRIG-VCXYPad-MIDI` — VCXYPad ohne MIDI-Bindung** — Umgesetzt. VCXYPad bekommt zwei absolute CC-Bindungen (Pan/Tilt) + Kanal (`midi_cc_pan`/`midi_cc_tilt`/`midi_ch`), `handle_midi`, Dialog-Spinboxes und Persistenz; nur im Positions-Modus wirksam, Bindung über den Eigenschaften-Dialog. Test: tests/test_vc_xypad_midi.py (8).
- **`EA-01` — Gruppen-Auswahl im Effekt-Assistenten** — Umgesetzt. `_FixturePage` zeigt Gruppen-Buttons (aus FixtureGroups); Klick kreuzt die Mitglieder zusätzlich an (Union via `group_fids_by_name`), `selected_fids()` bleibt unverändert. Test: tests/test_effect_wizard_group_selection.py (6).

### Folge-Batch 2026-06-15 (D) — B1+B2: Effekt-Assistent & Fade-Kurven (umgesetzt + getestet)

- **`T-4 / EA-03 / NS-B-MehrTypen` — Effekt-Assistent um weitere Typen erweitern** — Umgesetzt. Neue Presets **Wipe, Komet (mit Schweif), Random-Strobe, VU-Meter** (grün→rot, auf/ab) in `effect_wizard.py` (_generate, Scene-Chaser). Test: tests/test_effect_wizard_presets.py.
- **`EA-02` — Farb-Schritte mit Zwischenstufen (Color-Step-Interpolation)** — Umgesetzt. Checkbox + Spin im Farb-Schritt; interpoliert via `lerp_color` (Wrap für nahtlose Loops) für Farb-Chase/Rainbow/Lauflicht. Test: tests/test_effect_wizard_presets.py.
- **`ARC-04` — Fade-In/Fade-Out für Effekte (eigene Engine-Schicht)** — Umgesetzt. `env_fade_in`/`env_fade_out` (Sekunden) als Output-Multiplikator über ALLE Kanäle in `function_manager.tick()`; **Fade-Out via Release-State** (`stop()` blendet aus, `stop_all()` sofort); EFX-/Matrix-Editor-Regler; Persistenz. Test: tests/test_function_envelope.py.
- **`FW-4-Rest` — Fade-Kurven: Bibliotheks-Ansicht + Kurven für EFX/RGBMatrix** — Umgesetzt. (a) Standalone-**Kurven-Bibliothek-View** (`curve_library_view.py`, „Kurven"-Tab in Playback) mit Neu/Bearbeiten/Duplizieren/Umbenennen/Löschen, Presets gesperrt. (b) **`env_curve`** (named curve) formt die ARC-04-Hüllkurve auf EFX/RGBMatrix (und allen Functions); Editor-Combo. Tests: tests/test_curve_library_view.py + test_function_envelope.py.

### Folge-Batch 2026-06-15 (E) — B3: Show/Cue/Musik (umgesetzt + getestet)

- **`F-6` — Cue-Delay In/Out auf Attribut-Ebene** — Umgesetzt. `Cue.attr_delays {fid:{attr:float_s}}` (defensiv geparst, round-trip); `FadeState` blendet jedes Attribut auf einer um seine Extra-Verzögerung verschobenen Zeitachse (`_blend_per_attr`), `done` erst wenn auch das am längsten verzögerte Attribut fertig ist; **Kein-Delay-Pfad bit-identisch** zum alten Verhalten; manueller Scrub ignoriert Attr-Delays. Cue-Editor-UI bewusst zurückgestellt (2-Ebenen-Dict; authorbar via Show-Datei). Test: tests/test_cue_substack_and_attrdelay.py.
- **`F-16 / RM-Sequence-in-Sequence` — Sequence-in-Sequence** — Umgesetzt. `Cue.sub_stack_ref` (Index in `state.cue_stacks`) + `sub_stack_mode`; `CueStack.tick()` startet/tickt/mischt (LTP) die referenzierte Sub-Cueliste, **deadlockfrei** über einen `_rendering`-Guard VOR dem Lock (bricht A→B→A) + `_own_output`/`_output`-Trennung (keine nachhängenden Sub-Kanäle); Resolver von AppState injiziert (`set_sub_stack_resolver`, Verdrahtung in `new_cue_stack` + Show-Load). Editor-UI zurückgestellt. Test: tests/test_cue_substack_and_attrdelay.py.
- **`F-15 / RM-Timecode-Import-Audio` — BPM aus Audio** — Umgesetzt soweit ohne neue Abhängigkeit/Inhalts-Analyse sinnvoll: (a) `tag_reader.py` liest eingebettete BPM (ID3v2 TBPM / MP4 tmpo), schlägt die Dateinamen-Heuristik, `Track.bpm_source`; (b) `offline_analysis.py` = numpy-only `analyze_bpm` für **.wav** (opt-in, `HAS_NUMPY`-guarded, nie auf Import/Render-Pfad). **MP3-Offline-Inhaltsanalyse bleibt WontFix** (kein stdlib-Decoder, `audioop` in 3.14 entfernt; Memory „Offline-Analyse bewusst weggelassen“). Tests: tests/test_tag_reader.py, tests/test_offline_analysis.py.
- **`AUTODJ` — Musik-Player Roadmap** — Umgesetzt (a)+(b)+UI: (a) Per-Song-Look `Track.autoshow_function_ids`, `MusicShowDirector` tauscht ihn beim Trackwechsel aus (global-eigene Funktionen bleiben laufen); (b) `SpectrumBars`-Widget (8-Band, Loopback-VU, gekennzeichnet); Musik-View: „Auto-Show für Lied“-Dialog/Spalte, „BPM aus Datei-Tags“-Button, SSOT-Writeback. **(c) Auto-DJ-Crossfade zurückgestellt** (zweiter QMediaPlayer, reines Audio-Threading — headless nicht testbar). Tests: tests/test_track_autoshow.py, tests/test_music_view_autodj.py.

- **`F-2 / RM-Undo-Patch` — Undo im Patch (Fixture-Löschen rückgängig)** — Das Premiss des Punkts ist falsch: Undo wird NICHT im View per get_undo_stack().push() gepusht, sondern in der AppState-Schicht. src/core/app_state.py:192-212 remove_fixture(fid, undoable=True) snapshottet das Fixture (Zeile 197) und ruft self._push_undo(...) mit undo=lambda: _restore_fixture_dict(snap) + redo=remove_fixture (Zeile 206-212). Ebenso add_fixture undoable (184-190) und update_fixture undoable (253-261). patch_view.py ruft diese mit Default undoable=True auf: _delete_selected→remove_fixture (patch_view.py:366), _add_fixture→add_fixture (318/320), _on_double_click→update_fixture (394). Undo-Stack hängt am globalen Strg+Z in main_window.py:276-277 + 783-784. _push_undo in app_state.py:300-308.
- **`C-8 / D-8` — Rollen schärfen: Funktionen=Bibliothek/CRUD vs. Show Manager=Timeline** — Die Rollen sind im Code bereits sauber getrennt, keine doppelte Bearbeitungslogik. function_manager_view.py = reine Bibliothek/CRUD: Toolbar mit +Szene/+Chaser/+Sequence/... (create), Run/Stop, Loeschen (_delete_selected, Z.403), Umbenennen (_rename_selected, Z.533), MIDI-Learn; per-Typ-Editoren via create_function_editor() (Z.623). Entscheidender Beleg: Fuer eine Show liefert create_function_editor KEINEN Editor, sondern einen Platzhalter, der explizit weiterleitet — function_manager_view.py:668-670 "Show: {f.name}\n\nBearbeiten in 'Playback' -> 'Show Manager'." show_manager_view.py arbeitet ausschliesslich mit FunctionType.Show (by_type(FunctionType.Show), Z.409) und bietet nur Timeline-Anordnung: TimelineCanvas (Tracks, Drag&Drop bestehender Funktionen, Z.55 dropEvent), Transport (Play/Stop/Rewind), Track-Mute. Es dupliziert KEINE der typ-spezifischen Editoren. Einzige "Ueberschneidung" ist der triviale _new_show-Button (Z.432) = Komfort-Create, keine doppelte Edit-Logik. In docs/FEATURE_MAP.md:155/185-186 ist D-8/C-8 jedoch noch ohne ERLEDIGT-/Durchstreich-Marker gelistet (anders als D-3/C-6, die explizit als ERLEDIGT markiert sind) -> Doku stale.
- **`X-2 / VC-EFFEKT-02` — Intensity-Fader bei reiner Farb-Matrix auf Fixtures mit Dimmer (UX-Befund)** — Der exakte Audit-Fall (Fixture MIT Dimmer + reine Farb-Matrix drive_intensity=False) ist im Code behoben (M1-Fix 2026-06-13). src/core/engine/rgb_matrix.py:507-510: inten = self.intensity; has_dimmer = any(attr in intensity/dimmer/master); scale_colors = has_dimmer and not self.drive_intensity and inten < 0.999. Zeilen 547-548: bei scale_colors wird val = int(val * inten) auf color_r/g/b/w angewandt -> der Intensity-Fader dimmt jetzt sichtbar die Farbkanaele. Verkettung zum VC-Fader bestaetigt: src/ui/virtualconsole/vc_slider.py:177-182 (SliderMode.EFFECT_INTENSITY -> f.intensity = lvl). Default drive_intensity=False fuer neue Matrizen (rgb_matrix.py:315/334), Alt-Shows laden True (Zeile 1438) -> Fall trifft genau die reine Farb-Matrix. Regressionstest reproduziert exakt den Audit-Fall: tests/test_matrix_efx_audit_2026_06_13.py:69-78 (test_color_scaled_when_fixture_has_dimmer_and_not_driven) erwartet color_r==127 bei intensity=0.5, Dimmer bleibt 0; Gegenproben test_color_not_scaled_for_color_only_fixture (kein Doppel-Dimmen) und test_color_not_scaled_when_drive_intensity. Nur die Doku ist veraltet: docs/OPEN_POINTS_OVERVIEW.md:114 und docs/PROJECT_AUDIT.md:218-223 fuehren den Punkt noch als offen.
- **`VC-EFFEKT-01` — VC-Fader sollen Effekt aktivieren (Fader>0 starten, Fader=0 stoppen)** — vc_slider.py:90 Attribut `effect_autostart: bool = False`; vc_slider.py:289-318 `_autostart_targets()` startet bei `_effective_value() > 0` jeden noch nicht laufenden Ziel-Effekt (fm.start) und stoppt ihn bei Wert 0 (fm.stop); aufgerufen in `_apply()` vc_slider.py:144-146 fuer EFFECT_INTENSITY/EFFECT_SPEED/EFFECT_PARAM. Config-Dialog vc_slider.py:584-591 (Checkbox "bei 0 wirklich stoppen") + Persistenz to_dict/apply_dict vc_slider.py:722,743. Voll getestet: tests/test_vc_effect_live.py:138 test_slider_autostart_starts_and_stops_effect, :151 test_slider_without_autostart_only_regulates, :164 test_autostart_serialization_roundtrip.
- **`AUDIT-MITTEL-Snapshots-global` — Snapshots global in APPDATA statt pro Show (fid-Referenzen nach Show-Wechsel)** — Der 48er-Quick-Snap-Grid (SnapshotsView) ist bereits pro Show persistiert; die globale snapshots.json ist nur noch ein Live-Puffer. Vollstaendige Verdrahtung: - snapshots_view.py:450-472 SnapshotsView.to_dict() / load_data(): load_data baut neue Liste [Snapshot() for _ in range(SNAPSHOT_TOTAL)] und ersetzt self._snapshots vollstaendig aus Show-Payload (leere Payload -> alle 48 Slots reset). _save_to_disk() (Z.476) schreibt zusaetzlich in SNAPSHOTS_FILE nur als Live-Puffer. - Save-Bruecke: main_window.py:1239 state._snapshots_data = sv.to_dict() -> show_file.py:260 "snapshots": getattr(state,"_snapshots_data",None) or [] in show.json. - Load-Bruecke: show_file.py:570 state._snapshots_data = data.get("snapshots",[]) -> main_window.py:1472 sv.load_data(...). Da load_data die Slots beim Show-Wechsel komplett ersetzt, bleiben keine fid-Referenzen der vorigen Show haengen. Die in NEXT_STEPS.md:21 (✅ erledigt 2026-06-01) gemeinte System IST genau dieses SnapshotsView/snapshots.json-System (nicht das separate snap_file_panel/snap_library). Der Detail-Verdacht 'Grid bleibt global' ist damit widerlegt.
- **`AUDIT-NIEDRIG-Beat-MTC / MTC-VIEW` — BeatDetector/MTC Lock-/Modulo-Ungenauigkeiten + midi_view._on_mtc feuert nie** — Hauptbefund (midi_view._on_mtc QTimer.singleShot feuert nie) ist behoben: src/ui/views/midi_view.py:43 dediziertes Signal `mtc_received = Signal(int,int,int,int)`, :55 connect auf `_update_mtc_label`, :604-610 `_on_mtc` emittiert jetzt `self._log_signal.mtc_received.emit(...)` (Kommentar :605-606 dokumentiert genau den alten singleShot-Bug); :612-618 `_update_mtc_label` laeuft im GUI-Thread. Kein singleShot mehr in midi_view.py (Grep bestaetigt). Alter Bug nur noch in docs/CRASH_LOG_ANALYSIS_2026-06-14.md:56 beschrieben. BeatDetector (src/core/audio/beat_detector.py): nutzt threading.Lock korrekt (get_bpm/get_spectrum/_beat_times.append unter Lock; _last_beat_time single-producer Audio-Thread), KEINE Modulo-Arithmetik vorhanden — BPM via Intervall-Mittelung. MTC (src/core/timecode/mtc_reader.py:131): einzige verbleibende Mini-Ungenauigkeit `adj_frames = frames + 2` ohne Modulo-Wrap auf fps (rein kosmetisch, Anzeige).
- **`NS-D-Thread-C8` — Restliche Thread-Disziplin: Legacy app_state._emit ruft UI-Callbacks cross-thread** — app_state.py:1143-1159 _emit marshallt JEDEN Worker-Thread-Emit komplett: bei marshaller!=None und get_ident()!=_ui_thread_id wird das gesamte _emit_impl (Legacy _callbacks UND sync.emit) ueber marshaller(...) in den UI-Thread verlagert. _emit_impl (1161-1178) ruft beide Pfade und wird NUR aus _emit aufgerufen (kein Bypass; grep zeigt keinen externen _emit_impl-Caller). MainWindow registriert den Marshaller VOR allen Worker-Threads via Qt-Signal mit AutoConnection: main_window.py:108 (_emit_marshal_sig=Signal(object)), :116-117 (.connect(self._run_in_ui); set_ui_marshaller(self._emit_marshal_sig.emit)), _run_in_ui:1357. Alle direkten sync.emit, die _emit umgehen, sind UI-thread-safe: set_selected_fids->SELECTION_CHANGED (app_state.py:593) und _emit_dmx_changed->DMX_CHANGED (app_state.py:669) werden nur aus UI-Code aufgerufen; VC-MIDI marshallt zuvor in vc_canvas.py:89-91/257-262 (_on_midi_raw[MidiDispatch-Thread]->_midi_received Qt-Signal->_handle_midi[UI]->handle_midi->select_group_by_name). midi_mapper.py:465 ruft set_programmer_value, das ueber das marshallte _emit('programmer_changed') laeuft. function_manager._notify_functions_changed (fm.py:60-64) feuert nur aus UI-getriebenem add/remove; start/stop (121-144) emittieren nichts. OSC-Server (Worker) erreicht nur clear_programmer (marshalltes _emit) bzw. cue_stack.go/back (interne _on_cue_change/_on_output_change-Callbacks ohne Widget-Subscriber).
- **`ARC-03` — Manueller Dimmer als Override-Layer neben laufendem Effekt (Dimmer=0 -> Blackout)** — Implementiert als EE-02 im Render-Merge. src/core/app_state.py _render_frame Schritt 4 (Z.876-894): laufender Effekt + Programmer-Dimmer -> Faktor prog_factor[fidi]=int(attrs[ikey])/255.0 (Z.890), absoluter Intensitaets-Write wird via skip_intensity_for=set(prog_factor) (Z.894) uebersprungen. Schritt 4b (Z.962-973): factor = global_sub * fixture_dimmers * prog_factor; su.set_channel(a, int(su.get_channel(a)*factor)). Bei Programmer-Dimmer=0 -> factor 0.0 -> Kanal=0 = Blackout trotz laufendem Effekt (Guard Z.964 'if factor>=0.999: continue' greift bei 0 nicht). Ohne Effekt bleibt Programmer absolut (LTP). Permanenter Dimmer-Regler: src/ui/views/programmer_view.py Intensity-Tab (INTENSITY_ATTRS Z.107, Gruppierung Z.950-966) rendert AttributeSlider, der set_programmer_value(fid,attr,value) schreibt (Z.1477/1487). Tests tests/test_dimmer_master.py: test_programmer_dimmer_multiplies_running_effect (200*128/255~=100, nicht hart 128), test_programmer_dimmer_absolute_without_effect (=128 ohne Effekt), plus Submaster/Fixture-/Gruppen-Dimmer-Skalierung ueber Effekt.
- **`WP-8` — Migration verifizieren + manuelle Testfälle 1–27 dokumentieren** — Migration verifiziert+getestet: src/core/engine/rgb_matrix.py:1418-1421 (color_sequence-Migration, Alt-Shows ohne Key), _LEGACY_ALGO_MAP + after_fade-Migration (fade 0..1 -> % 0..100); tests/test_matrix_algo_migration.py (Schweif/Komet->after_fade x100, explizite Werte gewinnen, Roundtrip, Fallback, 6 Tests+17 Subcases); tests/test_matrix_fill.py (neuer zeitlicher Fill, 11 Tests, alle Styles/Modi/Loops, ColorSequence-Reihenfolge); to_dict/from_dict/apply_dict-Roundtrips inkl. ColorSequence in tests/test_matrix_dirty_save.py:248 + test_vc_effect_live.py:114-170. Testfälle 1-27 vollständig dokumentiert in docs/UMBAU_2026-06_CHANGES.md (Zeilen 14/26/40/48/62/76/88/93/106 decken Nr. 1-27 lückenlos ab). Einzig offen: Doc-Statusflag — CHANGES.md:4 ('Offen: WP-8 (Abschluss-Doku)') und PLAN.md:102-104 (Checkliste ohne Erledigt-Marker) sind stale.
- **`WP-7-rest` — Simple Desk: aufklappbare Kanal-Detailliste + Konflikt-/Fehler-Markierung + Sortierung/Filter (Feinschliff)** — Alle geforderten Teilfunktionen sind in src/ui/views/simple_desk.py implementiert: (1) Aufklappbare Kanal-Detailliste (CH->Funktion): FixtureOverviewPanel._add_fixture_item fuegt pro Kanal ein Child-QTreeWidgetItem hinzu (Z.489-505, "CH {abs_addr:03d}: {ch_label}" aus get_channels_for_patched(fx)); Tree mit setRootIsDecorated(True)/setAnimated(True) (Z.302/305), echt aufklappbar. (2) Konflikt-/Fehler-Markierung: Adress-Validierung addr_ok (Z.423-425) + state.check_address_conflict (Z.430) -> rote Einfaerbung _color_item(top,_C_RED,_C_WARN_BG) mit Tooltips fuer ungueltige Adresse (Z.469-478) und Kanal-Konflikt (Z.479-481); plus Legende (Z.278) und Status-Zeile mit Problemzahl (_update_status, Z.409-417). (3) Sortierung Universe+Adresse: sorted(fixtures, key=lambda fx:(fx.universe,fx.address)) (Z.391). (4) Filter: Universe-Filter-ComboBox + _on_filter_changed/set_universe_filter/follow_universe (Z.261-344), angewendet in rebuild (Z.400). (5) Auto-Update: sync.subscribe PATCH_CHANGED/REFRESH_ALL/DMX_CHANGED (Z.552-554). MASTERPLAN_2026-06-08.md:69 markiert WP-7 als erledigt; der dort genannte einzige Rest (farbige Fader-Gruppierung) ist via FixtureHeaderBand + _apply_fixture_tints (Z.107-185, 721-758) ebenfalls umgesetzt. MEMORY-Eintrag SimpleDesk-Audit (2026-06-13, test_simple_desk_grouping.py) bestaetigt zusaetzlichen Feinschliff.
- **`MP-MXP-03` — Matrix Style-Felder live steuerbar (dimmer/shutter min/max, white_amount) — Statuswiderspruch** — rgb_matrix.py:1138-1147 listet white_amount/intensity_min/intensity_max/shutter_min/shutter_max als live ParamSpec (kind=int, Kommentar "MXP-03: ... live steuerbar"). get_param rgb_matrix.py:1177, set_param geklemmt rgb_matrix.py:1223-1225. _render liest Felder pro Frame (423/429/536/551/556) -> sofort wirksam. effect_live.py:91 set_param_normalized + :120 adjust_param routen VC-Fader/Encoder/MIDI auf diese int-Params. matrix_live_dialog.py:43,91-101 erzeugt VC-Fader automatisch aus list_params (_FADER_KINDS enthaelt "int"). Persistenz to_dict/from_dict rgb_matrix.py:1384-1388/1441-1445. Test tests/test_matrix_offset_style_params.py: 6 passed (list/clamp/persist).
- **`Q3-SPD / MP-Q3` — Speed-Dial-Invertierung: Nutzerwahrnehmung klären** — Datei ist src/ui/virtualconsole/vc_speedial.py (verify_hint-Pfad 'vc_speeddial.py' hat Tippfehler/doppeltes d). Invert voll implementiert: VCSpeedDial.invert (Z.39), _effective_bpm spiegelt am Bereich (Z.77: _min_bpm+_max_bpm-_bpm if invert), _effective_mult analog (Z.80), Checkbox 'Invertieren (höher = langsamer)' im Dialog (Z.334-336, gesetzt Z.381), INV-Marker im paintEvent (Z.285-289), persistiert in to_dict/apply_dict (Z.422/439). Tests in tests/test_speed_dial.py: test_invert_flips_direction (Z.57-69, si_for_high < si_for_low), Persistenz-Roundtrip (Z.117-122), Default-off (Z.131). Der Detail-Text nennt die Restfrage selbst als 'reine Klärungsfrage an den Nutzer' (UX-Wahrnehmung), nicht im Code entscheidbar.

## 6. Archivierte Quell-Listen

Folgende Dateien wurden am 2026-06-15 hierher konsolidiert und nach `docs/_archiv/` verschoben (enthalten weiterhin die ausführlichen Design-Spezifikationen wie das 5-Zonen-Programmer-Layout, das ARC-Engine-Redesign und die T-VIZ-Historie):

- `TODO.md` → `docs/_archiv/TODO.md`
- `docs/NEXT_STEPS.md` → `docs/_archiv/NEXT_STEPS.md`
- `VISUALIZER_TODO.md` → `docs/_archiv/VISUALIZER_TODO.md`
- `docs/SPIDER_MULTIHEAD_TODO.md` → `docs/_archiv/SPIDER_MULTIHEAD_TODO.md`

