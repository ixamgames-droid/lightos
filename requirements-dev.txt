# LightOS - Test-/Entwicklungs-Abhaengigkeiten (NICHT zur Laufzeit noetig)
# Installieren: ./venv/Scripts/python.exe -m pip install -r requirements-dev.txt
#
# Das Test-Gate (tools/verify_loop.ps1 bzw. python -m pytest tests/) braucht
# zusaetzlich pytest-timeout, damit ein haengender Test gekillt + gemeldet wird
# statt den ganzen Lauf zu blockieren (siehe pytest.ini).

pytest>=8.0
pytest-timeout>=2.4.0   # Watchdog gegen den Voll-Suite-Random-Haenger (pytest.ini)
hypothesis>=6.0         # Property-basierte Vertrags-Tests (QA-14, tests/test_render_contract_props.py)

# VC-Galerie-Grafik-Regenerierung (tools/gen_vc_gallery.py) — nur Dev
Pillow>=12
