# LightOS — Backlog-Archiv (verdichtete done-Eintraege)

> Automatisch gepflegt von tools/backlog_compact.py --archive. Volltext der erledigten Tabellenzeilen; Kurzform bleibt in BACKLOG.md.

## Verdichtet am 2026-07-27

| ID | Prio | Status | Titel / Notiz |
|----|------|--------|---------------|
| VC3D-01 | P2 | ✅ done ([#296](https://github.com/ixamgames-droid/lightos/pull/296)) | **VC-Tasten plastisch/„3D-ig".** Neuer geteilter Paint-Helfer `src/ui/virtualconsole/vc_style.py` (`paint_button_surface`: Verlauf + Bevel + farbiger Tiefenrand + Press-Inset + Aktiv-Glow, rein QPainter). Angewandt in `vc_button.py::paintEvent`: Text + Zustands-Rahmen sitzen auf der erhabenen Taste und wandern beim Druck mit; Aktions-Farbbalken auf die runde Face-Form geclippt (`QPainterPath`) statt flach über die Ecken. Robust bei jeder Größe (Face-Maße ≥ 1 px). 6 Tests `tests/test_vc_style_3d.py` (Contract + sichtbares Press-/Aktiv-Feedback). Nur Buttons — Slider/SpeedDial optional als Folge. Showcase-Artifact im Chat. |
| VC-IMG-GC | P3 | ✅ done (dieser PR) | **VC-Asset-Cache-Aufräumung (Follow-up aus der VC-IMG-Review).** `%APPDATA%/LightOS/vc_assets/` wuchs unbegrenzt: `load_show` entpackt ALLE `assets/vc/*` in den Cache, aber nichts pruned lokal (die mark-sweep-GC in `save_show` trimmt nur das ZIP-Embed, nicht den lokalen Cache). **Fix (dieser PR):** neuer reference-aware LRU-Pruner `vc_assets.prune(keep, max_bytes, min_age)` — aufgerufen aus `load_show` mit den Keys des gerade geladenen Shows. Evictet verwaiste Assets (ältestes mtime zuerst), sobald der Cache den weichen Deckel (256 MB, per `LIGHTOS_VC_ASSET_CACHE_MB` einstellbar) überschreitet. **Invarianten:** referenzierte (live) Keys nie löschen · frische Dateien (<5 min) nie evicten (schützt parallele Sessions am geteilten Cache) · Pro-Datei-`try/except` gegen Windows-Dateilocks (laufendes QMovie/QPixmap) · verwaiste `.tmp`-Reste mitentsorgt · Löschen verlustfrei (Show entpackt beim nächsten Öffnen erneut aus dem ZIP). Tests `tests/test_vc_asset_gc.py`. |
| VC-IMG | P2 | ✅ done (dieser PR) | **VC-Button mit Hintergrund-Bild/GIF (David-Wunsch 2026-07-16).** Ein VC-Button soll ein user-gewähltes Bild ODER animiertes GIF als Hintergrund zeigen (zusätzlich zum Text), skaliert auf die Button-Fläche + mit-skalierend beim Resize. Für eigene Effekt-Buttons mit eigener Optik. **Design (Plan-Subagent):** Speicherung als **ZIP-eingebettetes Asset** (`.lshow` ist bereits ein ZIP) mit Content-Hash-Key (`bg_image="<sha1>.<ext>"`, Bytes unter `assets/vc/<sha1>.<ext>`, beim Laden in `%APPDATA%/LightOS/vc_assets/` entpackt) → portabel + `show.json` bleibt schlank (nicht base64/absoluter Pfad). Rendering: statisch `QPixmap` (KeepAspectRatioByExpanding, auf runde Face geclippt, Scrim für Text-Lesbarkeit), GIF `QMovie` (frameChanged→update, lifecycle-gebunden, headless-`isValid`-Guard + Static-First-Frame-Fallback). UI: Kontextmenü „Hintergrundbild wählen…"/„entfernen" (neuer `_extend_context_menu`-Hook in `VCWidget`). Koexistiert mit dem plastischen VC3D-Look (Bevel/Press bleibt). Berührt den atomaren Save-Pfad (`show_file.py`, STAB-16/17) → Assets in die Temp-ZIP vor `os.replace`. Traps: Portabilität, headless-QMovie-Crash, Text-Lesbarkeit auf Bild. Tests: Defaults/Roundtrip(inkl. None)/Set-Clear/Paint-Safety/GIF-Construct-Safety. |
| VC3D-02 | P3 | ✅ done (dieser PR) | **3D-Optik auf VCSlider-Griff + SpeedDial ausgeweitet.** Zwei neue `vc_style`-Helfer: `paint_slider_handle` (gewölbter Fader-Griff mit Licht-oben/Schatten-unten-Bevel + Griff-Rille, ersetzt das flache `fillRect` in `vc_slider.py`) und `paint_dial_knob` (radial gewölbte Dreh-Knopf-Fläche HINTER Arcs/Nadel in `vc_speedial.py`). Sättigungs-unabhängige Kanten (wie VC3D-03), rein QPainter. +Tests in `test_vc_style_3d.py` (Crash-Sicherheit + Wölbungs-Nachweis). Konsistenter Pad-/Fader-Look über die ganze VC. |
| VC3D-03 | P3 | ✅ done (dieser PR) | **VC3D-Politur (live gefunden):** (a) Auf voll gesättigten Farb-Pads war der 3D-Bevel kaum sichtbar (`lighter()` ist bei V=255 ein No-Op) → **Fix:** sättigungs-unabhängige Bevel-Kante — Gradient-Stift weiß-oben→dunkel-unten in `vc_style.py::paint_button_surface` (Schritt 4), Wölbung liest jetzt auch auf reinem Rot/Blau/Grün. (b) Farb-Vorschau-Badge überlappte den Tastentext („…arbwechs") → **Fix:** reine Funktion `_badge_text_right_inset` spart die oben-rechts belegte Spalte (Gobo/Badge) aus dem Text-Rechteck aus (`vc_button.py::paintEvent`), mit Floor auf halbe Face-Breite (Text bleibt auf schmalen Buttons lesbar). +5 Tests in `test_vc_style_3d.py`. Headless Vorher/Nachher-Render + adversariale Review (fing die fehlende Inset-Deckelung). |
| QOL-04 | P2 | ✅ done (dieser PR, Tab → „Assistent") | **Programmer-Tab „Hilfe" enthält die EFFEKTE/FUNKTIONEN (irreführendes Label)** | Der Attribut-Tab **„Hilfe"** (zwischen `Weitere` und `Matrix`) zeigt keine Hilfe, sondern die Funktions-/Effekt-/Szenen-Verwaltung (Effekt-Assistent, „+ Szene", „+ Chaser", „Programmer → Szene", komplette Funktionsliste, Start/Stop). Wer Effekte sucht, klickt nie auf „Hilfe". `programmer_view.py:442` `addTab(_make_effects_page(), "Hilfe")` (intern „Helper" = Hilfsfunktionen → zu „Hilfe" verkürzt). **Fix:** Tab in **„Effekte"** oder **„Funktionen"** umbenennen; Achtung: Tab-Text wird als Key genutzt (`tabText(idx)`, `:489`) → Persistenz/Lookup mitziehen. Hoher QoL-Wert (zentraler Bereich, aktuell praktisch versteckt). |
| QOL-01 | P2 | ✅ done (dieser PR) | **Start zeigt eine chaotische Bühne (Patch ohne Positionen)** | Beim App-Start ist der Patch geladen (48 Geräte aus `current_show.db`), aber die 2D-Bühne legt die Fixtures in einen dicht überlappenden Bogen mit unlesbaren Labels — die gespeicherten Positionen kommen erst nach `Datei→Zuletzt→<Show>`. Erster Eindruck = Chaos. Idee: beim Start die zuletzt benutzte **benannte** Show (mit Positionen/VC) laden ODER bei „nur Patch" ein sauberes Default-Raster statt Bogen; mind. ein „letzte Show fortsetzen?"-Hinweis. **✅ done (dieser PR):** Auto-Layout Bogen → label-sicheres **Raster** (`_grid_positions`, STEP 90 px ≥ Label-Breite) — bei Zoom 1.0 sind alle Labels frei; gespeicherte `.lshow`/3D-Positionen bleiben unangetastet. Zusammen mit den LOD-Labels (UI-26) dieser PR. |
| QOL-02 | P3 | ✅ done (dieser PR) | **„Zuletzt verwendet" listet gleichnamige Shows doppelt** | `grosses_rig_2026.lshow` erschien zweimal (verschiedene Pfade AppData vs. Repo), optisch nicht unterscheidbar → man wusste nicht, welche man öffnet. **Fix (`main_window.py`):** (a) `_recent_menu_labels()` hängt bei Namensgleichheit das *kürzeste unterscheidende* Ordner-Suffix an (`… — …\LightOS\shows` vs. `… — …\lightos-main\shows`), eindeutige Namen bleiben schlicht, voller Pfad weiter im Tooltip; (b) `_add_recent_file` dedupliziert nach `_canon_path` (Groß/Klein + Slash + `..`, ohne Filesystem-I/O), Original-Pfad bleibt für Anzeige/Autosave-Recovery erhalten. Neuer Test `tests/test_recent_menu.py`. |
| QOL-03 | P3 | ✅ done (dieser PR) | **Fixture-Namen in der Geräte-Liste abgeschnitten** | Längere Namen wurden rechts gekürzt (`[013] PAR T…`, `[009] MH Li…`) → gleich beginnende Geräte am Namen nicht unterscheidbar, kein Tooltip. **Fix:** (a) `FixtureTreeWithDrag` kürzt jetzt **mittig** (`ElideMiddle`) — der unterscheidende Namens-Schwanz bleibt sichtbar (`[009] … Links` vs `[010] …echts`); (b) jedes Fixture-Item bekommt einen **Tooltip mit Vollname** (`_refresh_fixture_list` in `live_view.py` + `_refresh_fixtures` in `fixture_group_view.py`). Neuer Test `tests/test_fixture_list_tooltip.py`. Live per Computer-Use bestätigt (mittige Kürzung sichtbar); Tooltip per Unit-Test gesichert (Qt rendert Tooltips unter synthetischem Hover nicht). |
| QOL-05 | P3 | ✅ done (dieser PR) | **2D-Bühne clustert, wenn NUR 3D-Positionen gesetzt sind** | Diagnose: `_load_positions` migriert 2D KORREKT aus 3D (`world3d_to_live`), aber ein kompaktes Rig (1 m = ~20 px < 30 px Icon) klumpte bei Default-Zoom. **Fix (Davids Wahl „Auto-Fit-Zoom", nicht-destruktiv):** beim Show-Load (und per neuem „⤢ Einpassen"-Button) zoomt/zentriert die 2D-Ansicht so, dass die Fixtures die Fläche mit Rand füllen — Positionen bleiben unverändert (treu zur 3D-Projektion). Reine Funktion `_compute_fit_zoom` + `LiveView._fit_2d_to_fixtures` (ohne `force` nur bei Klumpen < 35 % Sichtfeld → bewusst gesetzter Zoom bleibt). Neuer Test `tests/test_live_view_fit.py`. |
| FIX-FOG | P3 | ✅ done (dieser PR) | **Kein eingebautes Fog-/Hazer-/Smoke-Fixture-Profil in der Library** | Die Builtin-Library (`fixture_db._seed`) hatte PAR/MH/Spider/Laser, aber KEINE Nebelmaschine — Fog nur per Custom-Profil patchbar. **Fix:** Builtin **`EURON10`** („N-10 Nebelmaschine", `fixture_type="hazer"`) mit 2 Modi (1-Kanal Nebel · 2-Kanal Nebel+Lüfter, „Nebel"=`dimmer` → GM/Blackout skalieren mit) via `_add_fog_hazer` in `_seed` + `ensure_builtins`-Migration (bestehende DBs bekommen es idempotent nachgerüstet). Nebelmaschinen jetzt ohne Bastelei patchbar; rendert als Hazer im 3D-Visualizer + Fog-Icon 2D. Neuer Test `tests/test_fog_hazer_profile.py`; UXTEST-3-Generator nutzt jetzt `EURON10` statt Custom-`HAZE1`. |
| XPLAT-01 | P2 | ✅ done (dieser PR) | **3D-Visualizer: QtWebEngine ohne Linux-Sandbox-Fallback → Renderer bleibt auf Linux schwarz.** **Fix:** `_webengine_sandbox_flags` hängt auf Linux `--no-sandbox --disable-gpu-sandbox` an (Abwahl via `LIGHTOS_WEBENGINE_NO_SANDBOX`=falsy oder eigenes sandbox-Flag); Windows/macOS unberührt. Tests `tests/test_webengine_linux_sandbox.py`. `main.py:189` `_setup_webengine_diagnostics()` setzt nur Anti-Drossel-Flags, KEIN `--no-sandbox`/`--disable-gpu-sandbox`/`QTWEBENGINE_DISABLE_SANDBOX`. Auf verbreiteten Linux-Setups (Container/Docker, root, pip-PySide6 ohne setuid chrome-sandbox) startet der Chromium-Renderprozess nicht → eingebettete `QWebEngineView` (`visualizer_window.py:1547`) bleibt schwarz / `renderProcessTerminated`; der Auto-Reload-Guard loopt nur. Die eigenen Headless-Capture-Tools setzen `QTWEBENGINE_DISABLE_SANDBOX=1` schon — die App nicht. **Fix:** in `_setup_webengine_diagnostics()` `if sys.platform.startswith('linux')` die Flags `--no-sandbox --disable-gpu-sandbox` anhängen (via bestehende `LIGHTOS_WEBENGINE_FLAGS`-Mechanik oder Env-Weiche `LIGHTOS_WEBENGINE_NO_SANDBOX`, damit korrekt aufgesetzte Distros die Sandbox behalten können). **WinARM-Regression: none** (hinter `sys.platform`-Weiche, Windows-Pfad unberührt). |
| XPLAT-02 | P2 | ✅ done (dieser PR) | **Test-Gate/Runner ist Windows-only → auf Linux nicht ausführbar (kein CI/Dev-Gate).** (a) `tools/verify_loop.ps1:25-27` sucht ausschließlich `venv\Scripts\python.exe` → auf Linux (`venv/bin/python`) Exit 2 „venv-Python nicht gefunden", bevor ein Test läuft. (b) Delegiert an `../run_tests.ps1`, das bewusst NICHT im Repo liegt → frischer Linux-Checkout/CI hat es nie; der direkte-pytest-Fallback hängt wiederum am Windows-venv-Pfad. Der Direktweg selbst funktioniert (`QT_QPA_PLATFORM=offscreen python -m pytest tests/`, conftest setzt Guards). **Fix:** (a) Interpreter-Suche in `verify_loop.ps1` auf Kandidatenliste erweitern (`venv/bin/python(3)` zusätzlich, Windows-Pfad zuerst — byte-identisches Win-Verhalten); (b) einen eingecheckten, plattformneutralen Runner bereitstellen (`tools/verify_loop.sh` bzw. dokumentierter `python -m pytest tests/`) + Linux-Weg in WORKFLOW.md/CLAUDE.md. **WinARM-Regression: none** (erster Treffer gewinnt, Lock-Runner-Pfad unverändert). |
| XPLAT-03 | P3 | ✅ done (dieser PR) | **Art-Net-Input: Port 6454 ohne `SO_REUSEPORT` → Linux-Bind-Kollision.** `src/core/dmx/artnet_input.py:43` setzt nur `SO_REUSEADDR` + bindet `0.0.0.0:6454`. Windows-`SO_REUSEADDR` ≈ Linux-`SO_REUSEPORT` (Port teilbar), Linux NICHT → `bind()` wirft „Address already in use", sobald eine 2. Art-Net-App (QLC+, anderes Tool) auf 6454 lauscht; der except-Block schluckt es → Art-Net-Input bleibt still. Der sACN-Input (`sacn_input.py:49`) macht es bereits richtig, Art-Net wurde nicht angeglichen. **Fix:** denselben guarded Block nach `SO_REUSEADDR`: `try: sock.setsockopt(SOL_SOCKET, SO_REUSEPORT, 1) except (AttributeError, OSError): pass`. **WinARM-Regression: none** (guarded; Windows hat kein `SO_REUSEPORT` → except greift). |
| XPLAT-04 | P3 | ✅ done (dieser PR) | **App-Datenordner nicht XDG-konform: alle Nutzerdaten in sichtbarem `~/LightOS`.** Auf Linux ist `APPDATA` nicht gesetzt → überall greift der Fallback `expanduser('~')` → sichtbares, nicht-XDG `~/LightOS/` (current_show.db, stages/, vc_assets/, snaps, bpm_cache, prefs, crash.log). Kein Absturz, aber verstopft Home + kollidiert mit Backup/Sync. Muster über **~20 Dateien dupliziert** (kein zentraler Helper): u.a. `main_window.py:37,1741,2155`, `web/remote_settings.py:31`, `stage_definition.py:235`, `controller_library.py:33`, `vc_assets.py:36`, `visualizer_window.py:78`, `snap_library.py:18`, `bpm_cache.py:15`, `input/profile.py:11`, `programmer_view.py:29`, `live_view.py:28`, `vc_button.py:17` u.a. **Fix:** zentraler `src/core/paths.py:app_data_dir()` mit `sys.platform`-Weiche (win32: unverändert `%APPDATA%/LightOS`; Linux: `QStandardPaths.AppDataLocation` bzw. `$XDG_DATA_HOME`/`~/.local/share/LightOS`; macOS: `~/Library/Application Support/LightOS`), alle Fundstellen darauf umstellen (+ optionale Einmal-Migration). **WinARM-Regression: low** (viele Fundstellen — Windows-Zweig muss byte-identisch `%APPDATA%/LightOS` bleiben; sorgfältig testen). |
| XPLAT-05 | P3 | ✅ done (dieser PR) | **Hartkodierte Windows-Fontfamilien ohne Fallback → Layout-Drift auf Linux.** Dutzende `QFont('Segoe UI', …)` in den VC-Widgets (`vc_slider.py:711` u.v.a.), `QFont('Consolas')` (`script_editor.py:124/135`, `command_line.py:119`), `QFont('Courier New')` (`midi_view.py:155`), `QFont('Arial')` (`mini_icons.py:333`), `curve_editor.py:175`, `midi_teach_dialog.py:111`. Auf Linux existieren die Familien nicht → Qt substituiert still eine beliebige Default-Familie; die fein austarierten kleinen Punktgrößen (6–17px) können auf DejaVu/Noto breiter werden und enge Labels/Ziffern (BPM, Slider) clippen. (Canvas-Text + QSS haben bereits sans-serif/mono-Fallbacks.) **Fix:** beim App-Start `QFont.insertSubstitutions('Segoe UI', ['Noto Sans','DejaVu Sans','sans-serif'])` + `'Consolas'/'Courier New' → ['DejaVu Sans Mono','monospace']`. **WinARM-Regression: none** (auf Windows werden Segoe UI/Consolas zuerst gefunden → keine Änderung). |
| XPLAT-06 | P3 | ✅ done (dieser PR) | **Broadcast/Multicast ohne wählbare Ausgangs-NIC → DMX/Laser evtl. am falschen Interface (Linux Multi-NIC-Rigs).** `ArtNetSender` defaultet `255.255.255.255` (`artnet.py:25/28`), `idn.discover()` ebenso (`idn.py:190`), `SACNSender` setzt Multicast-TTL aber kein `IP_MULTICAST_IF` (`sacn.py:103`). Windows sendet Limited-Broadcast historisch auf ALLEN Interfaces, Linux nur über die Route-NIC. Auf typischem Rig (Lichtnetz an 2./USB-Ethernet-NIC ≠ Default-Route) erreichen Art-Net-Broadcast + IDN-Discovery unter Windows die Nodes, unter Linux ggf. nicht → reale Ausgabe verpufft. **Fix:** Ausgangs-Interface konfigurierbar (bestehenden `get_lan_ip()`/NIC-Enum aus `web/app.py` wiederverwenden): (a) Art-Net gerichtete Subnetz-Broadcast-Adr. bzw. `bind((iface_ip,0))`; (b) sACN optional `IP_MULTICAST_IF`; (c) IDN/Ether-Dream-Discovery dieselbe iface-IP. Alles guarded, Default = bisher. **WinARM-Regression: low** (Default unverändert; nur additive Opt-in-Interface-Wahl). |
| XPLAT-07 | P3 | ✅ done (dieser PR) | **Linux-Install-/Laufzeit-Doku fehlt (Systempakete/Backends).** Kein Code-Zwang, aber: `python-rtmidi` (C-Extension) braucht auf Linux ohne manylinux-Wheel `libasound2-dev` (ALSA) + `build-essential`, sonst schlägt `pip install -r requirements.txt` fehl → auf Linux OHNE rtmidi gibt es GAR KEIN MIDI (WinMM-Fallback ist Windows-only). `soundcard`-Loopback (`capture.py:186`) ist WASAPI-Semantik → auf Linux braucht Loopback-BPM eine PulseAudio-Monitor-Quelle + `libpulse`, sonst stumm (degradiert weich). **Fix:** Linux-Systempakete + Audio-Monitor-Hinweis in Install-Doku/CI dokumentieren; optional Linux-Zweig, der bei `source_mode='loopback'` eine PulseAudio-Monitor-Quelle bevorzugt. requirements.txt bleibt. **WinARM-Regression: none** (Doku + optionaler Linux-Zweig). |
| REPO-QABRANCHES | P3 | done (2026-07-26 — **GitHub steht jetzt auf genau EINEM Branch: `main`**. Alle 24 Alt-Branches einzeln gegen den Code geprüft: 18 verifiziert überholt und gelöscht, 2 leer/gemergt, aus 4 wurde der Rest über [PR #437](https://github.com/ixamgames-droid/lightos/pull/437) geborgen — darunter der Mehrkopf-Bug in `mapped_channel.write`, die Label-Asymmetrie im Show-Dump, der `AudioInputView`-Teardown und die Abdeckung von `LayeredEffect.write`. Ursprünglicher Text:) | **21 alte QA-/Audit-Branches auf GitHub löschen.** Beim Repo-Aufräumen 2026-07-26 wurden 65 Branches mit gemergtem PR entfernt (89 → 24). Übrig sind 21 Branches ohne PR (`qa/*`, `audit/threading-native-av`, `fix/multihead-attribute-sweep`, `fix/show-roundtrip-identity`, `feature/qa-ui-smoke`, `integration/qa-foundation`). **Verifiziert überholt:** keiner bringt eine Datei mit, die auf `main` fehlt; die drei Ausnahmen haben Äquivalente (`src/core/weak_callbacks.py` → `src/ui/weak_slots.py`, `tools/check_backlog.py` → `tests/test_backlog_lint.py` + `tools/backlog_compact.py`, `docs/THREADING_AV_AUDIT_2026_07_09.md` → `..._2026-07-13.md`), und alle acht QA-10-Testdateien liegen auf `main`. David wollte sie vorerst behalten — löschbar, sobald er drübergeschaut hat. |
| RL-01 | P3 | ✅ done ([#234](https://github.com/ixamgames-droid/lightos/pull/234)) | **Range-Lock lässt Identitäts-Modifier zurück.** `channel_range_lock_dialog.py:_on_accept` nullt beim „Lock entfernen" nur `range_min/max` statt den Eintrag zu entfernen → ein Identitäts-`ChannelModifier` (LINEAR, 0–255) bleibt im geteilten `ChannelModifierManager` registriert. **Funktional folgenlos** (Identität → kein DMX-Effekt), reine Hygiene. Fix: Eintrag ganz entfernen, wenn er nur wegen des Locks existiert (`curve==CurveType.LINEAR and not existing.custom_lut`). |
| STOPALL-PANIC | P2 | ✅ done (dieser PR) | **„STOP ALL" stoppte keine VC-getriggerten Szenen/EFX/Programmer-Matrizen** — es rief nur `playback_engine.stop_all()` (Executor-Cuestacks); FunctionManager-Funktionen (`_running_ids`) liefen weiter (Banner „Aktiver Effekt" + DMX blieben, bis die Show neu geladen wurde). Panik-Knopf war keiner. **Fix:** STOP ALL = Superset — zusätzlich `function_manager.stop_all()` an allen 4 Aufrufstellen (VC-Primäraktion / Multi-Action / Toolbar-`_stop_all` / cmdline-`StopCommand`); derselbe Call wie `STOP_EFFECTS`. Programmer bleibt bewusst unberührt (= `CLEAR`). Test `test_stop_all_stops_functions.py`. |
| FM16E-HEADCOUNT | P2 | ✅ done ([PR #425](https://github.com/ixamgames-droid/lightos/pull/425)) | **Kopf-Gruppe „… · Köpfe" zeigt „(0)" Geräte + selektiert nichts im Attribut-Editor.** `_group_fids()` (`programmer_view.py`) sowie `fixture_group_view`/`vc_slider`/`efx_view` machten `int(fid)` auf den Zellwerten und übersprangen `fid:head`-Zellen. **✅ Erledigt (konsistenter Fix, eine Quelle):** neues Leaf-Modul `src/core/group_cells.py` (`parse_group_cell` + `base_fids_in_grid_order`, dedup/Rasterreihenfolge, dependency-frei). Umgestellt: Kern-`AppState._group_lookup` (→ `group_fids_by_name`/`select_group_by_name` → VC-Slider/Buttons/Effect-Wizard/Preset-Browser) + `list_fixture_groups`; `ProgrammerView._group_fids`; `EfxView._active_group_fids`. Die zwei schon korrekten Parser (`rgb_matrix._parse_cell`, `fixture_group_view._split_cell`) **delegieren** jetzt an `parse_group_cell` → keine Drift. Matrix-Render (`grids_from_positions`) + reine-fid-Alt-Gruppen unverändert. **Adversariale Review fand + fixte 1 MEDIUM:** das **live_view-Gruppen-Panel** (dieselbe Klasse, Roh-Zellwert-Durchreichung) hob bei Kopf-Matrix-Auswahl im Live-Canvas nichts hervor (Strings in `set[int]`), zeigte „(4)" statt Geräte-Count und warf in der Detail-Box — Count/Highlight/Detail jetzt auch über `base_fids_in_grid_order`. Tests `tests/test_fm16e_headcount.py` (20). _Add-/Remove-Editierpfade des Simple-Panels reshapen eine Kopf-Matrix wie gehabt auf 1×N (Encoding bleibt); interaktives 2D-Editieren = **FM-HEADLAYOUT**._ |
| SM-CRASH | P1 | ✅ done ([#235](https://github.com/ixamgames-droid/lightos/pull/235)) | **Show-Manager „+ Neue Show"/„+ Track" crashte** (`AttributeError: 'NoneType' has no attribute 'deleteLater'`). `TrackLabelPanel.refresh()` räumte das Layout ohne None-Guard (`itemAt(i).widget()` = None bei Spacer/Stretch). F-9-Reporter fing es ab, Operation aber kaputt. Fix + Regressionstest. |
| VIZ-MASTER-FEEDBACK | P2 | done (WYSIWYG-Entscheidung: Monitor/Viz zeigen Post-GM/Blackout; visuelle Verifikation -> Computer-Use-Phase) | **Blackout & Grand Master ohne Feedback in Monitor/Visualizer.** `output_manager._send_all` wendet Blackout/GM nur im **Sende-Pfad** an (auf einer Kopie), nicht im Universe-Puffer → Output-Monitor, DMX-Monitor UND 3D-Visualizer zeigen weiter die Pre-Master-Werte. Ohne echtes Rig sieht der Nutzer beim BLACKOUT **nichts** — für eine Safety-Funktion irritierend. **Design-Entscheidung nötig:** soll der Visualizer/Monitor den **finalen** Output (nach GM/Blackout) zeigen (WYSIWYG)? Bezug: VIZ-05. |
| AUDIO-START-WARN | P3 | done ([#238](https://github.com/ixamgames-droid/lightos/pull/238)) | **Audio-Input-Tab „Start" zeigt Capture-Fehler jetzt sichtbar.** `AudioCapture.start()` setzt `last_error()` auch bei fehlendem Backend/fehlendem Default-Gerät; `AudioInputView` zeigt `last_error()` sowohl beim direkten Start-Fail als auch wenn der Capture-Thread nach einem zunächst erfolgreichen Start stirbt (z. B. „no device with id …") statt still auf „Status: gestoppt" zurückzufallen. Tests `tests/test_audio_input_view.py`. |
| EFX-PATH-BTN-LABEL | P3 | done | **EFX-Custom-Path-Buttons abgeschnitten** — „Zeichnen"/„Bearbeiten" rendern als „eich"/„beit" (zu schmale Buttons). Rein kosmetisch. Custom-Path-Zeile im EFX-Panel (`programmer_view`/EFX). |
| BH-FRAMEBANK | P2 | ✅ done | **Bank-Pins von Widgets IN einem VCFrame werden ignoriert** — VCFrame-Kinder kombinierten Seiten- und Bank-Filter nicht. **Gefixt [#257]:** Canvas propagiert Bank-Wechsel in die Frames; `VCFrame` kombiniert Seite×Bank und delegiert die Bank-Entscheidung an die getestete Autorität `VCCanvas.on_active_bank` (VCB-04-Vererbung, auch nested). |
| BH-COLORWHEEL | P2 | ✅ done | **ColorWheel: Klick-Hue ≠ gerenderter Gradient** — der Konus malt bei Schirm-Winkel `θ` die Farbe `(90-θ)%360` (offscreen an der echten QConicalGradient-Ausgabe gemessen), aber `_set_from_pos` setzte `_hue=θ`. **Gefixt:** Klick UND Marker spiegelbildlich zur Gradient-Konstruktion (`_hue=(90-θ)%360`, Marker bei `(90-hue)`) → gewählte Farbe = angeklickte Farbe, Marker bleibt unter dem Cursor; +Render-Regressionstests. |
| BH-CGRENDER | P2 | ✅ done | **Channel-Group-Slider schreibt roh ins Live-Universe** und wird vom 44-Hz-Renderer überschrieben. **Gefixt [#253]:** schreibt jetzt über die Simple-Desk-Override-Schicht (`set_simple_desk_channel`). |
| BH-CURVEDRAG | P3 | ✅ done | **Curve-Editor: Rechtsklick-Löschen während eines Linksklick-Drags** ließ `_drag_idx` stale → nächster Move korrumpierte einen Endpunkt. **Gefixt [#253]:** `_drag_idx` beim Löschen invalidiert. |
| BH-MODNAME | P3 | ✅ done | **Channel-Modifier-Dialog: Name-Spalte-Edits gingen beim Speichern verloren.** **Gefixt [#257]:** `itemChanged`-Handler schreibt den Namen sofort ins Modifier-Objekt (UserRole-Ref); Manager serialisiert `name` bereits. |
| BH-NEXTCUE | P3 | ✅ done | **Playback „Nächste"-Vorschau nutzte naives idx+1** statt der modusabhängigen Logik. **Gefixt [#253]:** `peek_next()` public gemacht und genutzt (korrekt bei loop/bounce/pingpong am Listenende). |
| LAS-01 | P1 | done ([PR #140](https://github.com/ixamgames-droid/lightos/pull/140)) | Ehaho-L2600-Builtin + Laser-Attribut-Vokabular + Klassen-Audit | Builtin `L2600LASER` (6ch Simple + 34ch Professional, alle Manual-Ranges, Gruppe A/B als Kopf 0/1, Shutter-Safety-Default 0), 13 `laser_*`-Attribute in `CHANNEL_ATTRS`/`ATTR_GROUPS['Effect']`/`ATTR_LABELS`, Audit-Test „kein Builtin ist `other`". Tests `tests/test_ehaho_l2600_profile.py`. |
| LAS-02 | P1 | done (dieser PR) | Laser-Tab im Programmer | `src/ui/views/laser_view.py` (`LaserView(follow_selection=True)`) analog EFX/Matrix in `programmer_view.py` eingehängt; sichtbar nur bei Laser-Capability (laser_*-Kanäle bzw. `fixture_type=='laser'`). Inhalt: A/B-Kopf-Umschalter (Mehrkopf attr#N, ENG-03-Guard), Modus-Kacheln aus Shutter-Ranges, Range-beschriftete Regler je Laser-Kanal, Muster-Paletten. Headless-Tests `tests/test_laser_view.py`. |
| LAS-03 | P2 | done | Muster-Paletten & Presets für Laser | `PaletteType.LASER` (Engine + Paletten-Tab), „Muster speichern…"/Anwenden-Kacheln in der LaserView (speichert pro Kopf via attr#N); Laser-Paletten sind über den **Preset-Browser** abrufbar. Snap-/Szenen-Capture und -Abruf sind durch `tests/test_laser_snap_scene.py` End-to-End verifiziert (siehe LAS-03-Rest). |
| LAS-10 | P2 | done (dieser PR) | Laser-Sicherheit von der Virtual Console bedienbar | ✅ Zwei neue `ButtonAction`s in `vc_button.py`: **`LASER_ARM`** (Scharf/Unscharf-Toggle des `LaserOutputManager`, Farbbalken lila=scharf) + **`LASER_ESTOP`** (Not-Aus, roter Balken: `estop_all` → `set_armed(False)` → `clear_estop_all`, Reihenfolge wie in der Laser-Steuerseite). Handler in `_trigger_primary`, Labels im Aktions-Dropdown. LaserView `_sync_arm_from_manager` spiegelt den Manager-Zustand (VC-/MIDI-Änderungen sichtbar). Damit ist der Laser-Not-Aus + Scharfschalten auf ein festes Pad/MIDI-Taste legbar. Tests `tests/test_laser_vc_safety.py`. |
| LAS-04 | P2 | done (dieser PR) | Netzwerk-Laser-Grundlagen (`protocol`-Feld) | ✅ `PatchedFixture.protocol` (`'dmx'` Default \| `'etherdream'` \| `'idn'`), ALTER-TABLE-Migration, `.lshow`-Serialisierung beidseitig (Alt-Shows → `'dmx'`), Undo/`update_fixture`-Allow-List, `fixture_uses_dmx()`-Gate an ALLEN vier Adress-Schreibstellen (`_rebuild_render_plan` Defaults/Spans/GM-Maske, `_flush_programmer_to_dmx`, `_apply_fixture_map`, Executor-`_flush_to_dmx`) — Platzhalter-Adressen von Netzwerk-Lasern erreichen nie das Live-Universe; Programmer-Werte bleiben für den späteren `LaserOutputManager` erhalten. Tests `tests/test_laser_protocol_field.py`. Die Protokoll-Auswahl im Patch-UI kam mit LAS-05. |
| LAS-05 | P2 | done (dieser PR) | Ether-Dream-Backend (Punkt-Streaming) | ✅ Neues Paket `src/core/laser/`: `frame.py` (neutrales `LaserFrame`/`LaserPoint`-Modell + `LaserLimits`/`clamp_frame` — Scan-Ausschlag, Punktrate, Helligkeit, Mindest-Punktzahl gegen stehende Strahlen), `etherdream.py` (voller Protokoll-Treiber nach [ether-dream.com/protocol.html](https://ether-dream.com/protocol.html): TCP-Befehle inkl. E-Stop/Clear, UDP-Discovery, reine struct/socket-Implementierung), `laser_output.py` (`LaserOutputManager`: eigener 30-fps-Thread, Kreis-Testmuster aus Programmer-Werten mit Shutter-Gate, **BLACKOUT blankt jeden Frame**, verriegelnder `estop_all()`, Reconnect-Backoff). `net_host`-Feld (Migration + Serialisierung + Undo), **Protokoll-Auswahl im Patch-Dialog** (Universe/Adresse ↔ IP umschaltbar, keine Fehl-Konfliktwarnung). Lifecycle: Start in `start_playback` (Env-Gate `LIGHTOS_NO_OUTPUT_THREAD`), Stop im MainWindow-Shutdown. Tests `tests/test_laser_etherdream.py` (Fake-DAC, ohne Hardware). **Hardware-Verifikation folgt, sobald eine Ether-Dream-DAC vorhanden ist.** |
| LAS-06 | P3 | done (dieser PR) | IDN-Stream-Backend | ✅ `src/core/laser/idn.py` — voller IDN-Stream-Treiber (ILDA Digital Network) hinter derselben `LaserFrame`-Abstraktion, Wire-Format gegen offizielle Spec Rev001/Rev002 **und** DexLogic-Referenzcode (helios_openidn) verifiziert: UDP :7255, IDN-Hello-Header + Channel-Message + Channel-Config mit Standard-XYRGB-Tag-Dictionary + Sample-Chunk, alles Big-Endian; session-freies Streaming (Command 0x40), Graceful Close (0x44) + Abort (0x46) als Not-Aus, optionaler Scan-Discovery. `IDNConnection` implementiert dieselbe Schnittstelle wie Ether Dream → `LaserOutputManager` bekommt eine **Protokoll-Weiche** (`_factory_for`), beide Backends laufen nebeneinander; Patch-UI bietet „IDN / ILDA Digital Network" als 3. Protokoll. v1: ein Frame = ein Chunk = ein UDP-Paket, zu punktreiche Frames werden geometrie-erhaltend downgesampled (App-Fragmentierung = Folgeschritt mit LAS-07). Tests `tests/test_laser_idn.py` (Golden-Bytes, Fake-UDP-Server, Manager-Weiche). **Hardware-Verifikation folgt, sobald ein IDN-Gerät vorhanden ist.** |
| VIZ-10 | P1 | ✅ done ([PR #139](https://github.com/ixamgames-droid/lightos/pull/139)) | Phase 0: Stabilitäts- & Sichtbarkeits-Sofortpaket | Plan §4/Phase 0: renderProcessTerminated-Selbstheilung, Bridge-Fehler → crash_logging (Traceback statt print), animate()-try/catch, setPixelRatio+ACES-ToneMapping, Einstellungen-Tab erreichbar, Toolbar-Labels lesbar, Palette wechselt Modus statt no-op, Statusbar/Tooltip nicht mehr stale, Nachfrage bei ungespeicherter Bühne. Alle Punkte live in GUI nachweisbar + Regressionstests. |
| VIZ-11 | P1 | ✅ done ([PR #144](https://github.com/ixamgames-droid/lightos/pull/144)) | Phase 1: Szenegraph-Datenmodell (Meter, Parenting, Stage in .lshow) | Plan §4/Phase 1: SceneNode ersetzt 5 State-Dicts; Docking = echtes Parenting (Truss-Rotation nimmt Fixtures mit); Live View 2D = Projektion; Bühnen-Snapshot in .lshow + Migration alter Shows (SHOW_VERSION-Bump). **Migrations-Gate:** alle ≥35 committeten Shows aus `shows/` laden fehlerfrei + automatisierter Vorher/Nachher-Vergleich der Welt-Transforms (Referenz „david test 2.lshow"); QUndoStack für alle Editier-Operationen (Strg+Z/Y). |
| VIZ-12 | P1 | ✅ done ([PR #149](https://github.com/ixamgames-droid/lightos/pull/149)) | Phase 2: Ein VisualizerService + persistentes Dauerfenster | Plan §4/Phase 2: Singleton-Service mit EINER Bridge + Dirty-Tracking + Batch-Push; Fenster hide/show statt destroy/rebuild (Kamera bleibt erhalten); nur noch ein DMX-Timer app-weit; reset_interaction_state bei Show-Wechsel; DPI/screenChanged-Handling. |
| VIZ-13 | P1 | done (3a #153 · 3b #157/#159 · 3c #180/#185/#187/#195/#198) | Phase 3: JS-Neuaufbau — **3a ✅** ([#153](https://github.com/ixamgames-droid/lightos/pull/153) Modul-Split) · **3b ✅** (Kamera [#157](https://github.com/ixamgames-droid/lightos/pull/157) + Gizmo [#159](https://github.com/ixamgames-droid/lightos/pull/159), Eigenbau ohne externe Controls) · **3c komplett** (dmxUpdated-Legacy-Entfernung ✅ [#180](https://github.com/ixamgames-droid/lightos/pull/180) · FixtureType-Registry **Teil 1 (build-Dispatch)** ✅ [#185](https://github.com/ixamgames-droid/lightos/pull/185) · **3c-1 Ortho-2D-Polish** ✅ [#187](https://github.com/ixamgames-droid/lightos/pull/187) (Icons behalten+verbessert lt. Reframe: permanente Umrisse + heller Unlit-Fill, par_bar/mover_bar-Glyphen mit Per-Kopf-Zellen, modusabhängiger Footer-Hint, Bühnen-Grundriss-Umrisse in Typ-Farbe) · **Registry Teil 2 (updateDmx-Split)** ✅ [#195](https://github.com/ixamgames-droid/lightos/pull/195) (updateFixture-Monolith → pro-Typ-`updateDmx`-Handler in Registry/builders, Fassade unverändert, verhaltens-identisch per Golden-Parity-Test `test_viz13c_updatedmx_registry.py`) · **3c-2 On-Demand-Rendering** ✅ [#198](https://github.com/ixamgames-droid/lightos/pull/198) (requestRender-Dirty-Flag + Live-Animation-Proben in neuem `scene/render_loop.js`, alle Quellen verdrahtet, 11 Beweis-Tests — **Phase 3 damit KOMPLETT**)) | Plan §4/Phase 3: **3a** Build-Step+Modul-Extraktion ohne Verhaltensänderung → **3b** Gizmo (zoom-korrekt, Snap) statt Verschieben/Höhe/Drehen-Buttons + Kamera-Presets Top/Front/Seite/Persp/Frei + Fit/Fit-Selected + gespeicherte Positionen → **3c** 2D-Top-Down = Ortho-Projektion derselben Szene + FixtureType-Registry + On-Demand-Rendering. Jede Teilrunde einzeln mergebar; Altfunktionen (Zielen/Nachfahren/Align/Spider) erhalten. |
| VIZ-LABELS-POPOUT | P2 | ✅ done (dieser PR) | **3D-Namens-Labels ein/aus-Toggle + Pop-out-Fenster (Zweitmonitor)** (Davids Auftrag 2026-07-18) | Schließt das VIZ-14-„Labels-Toggle-UI"-out-of-scope-Item + neues Pop-out. **Label-Toggle:** globaler Schalter `settings.showLabels` (`state.js` → `bridge.js#applySettings` → `labels.js#_visibleFor` UND-verknüpft + `requestRender`) blendet die Fixture-Namens-Sprites aus/ein. Zentrale Quelle **`AppState.show_fixture_labels`** (default AN, bewusst NICHT in `.lshow` — wie brightness/showCones, überlebt Show-Wechsel), gelesen in `_collect_settings()` der eingebetteten `Visualizer3DView` UND des Vollfensters, geschrieben vom Toolbar-Button „🏷 Labels" bzw. Checkbox `_chk_labels`; `on_shown`/`showEvent` ziehen Button/Checkbox **und JS-Settings** aus der Quelle nach (sonst geht die Wahl beim Redock/Reshow still verloren — Review-Fix). **Pop-out (3er-Panel-Entscheidung C, einstimmig):** Button „⧉ Ausklinken" löst die eingebettete Live-View-3D in ein `VisualizerPopoutWindow` (QMainWindow, `WA_DeleteOnClose`), das eine ZWEITE `Visualizer3DView`-Instanz hostet (Service-Target `live_view_popout` via neuem Ctor-Param `target_name`) — Reuse der gehärteten View (Bridge/Pull-Poll/RenderCrashGuard/dispose), kein Neubau. Beim Ausklinken fällt die eingebettete View auf 2D zurück → **nie zwei GL-Szenen gleichzeitig** (GPU-Invariante, Adreno-16-Textur-Limit); Banner „🧊 3D läuft im separaten Fenster / ⇲ Zurückholen" + Fenster-Schließen == Andocken (deferred Redock aus dem `closeEvent` + Sichtbarkeits-Gate — Review-Fixes gegen Reentrancy/Tab-Gate-Umgehung). Fenster zentriert auf Nicht-Primärschirm, gegen aktuelle Screens validiert (kein off-screen). Adversariale Review (4 Linsen × je gegen echten Code verifizierte Funde): 5 bestätigte Funde eingearbeitet. Tests: `tests/test_viz_labels_popout.py` (15) + `tests/test_viz_labels_js.py` (2, JS-Gate über Test-Seam `__lightos.updateLabelZoomVisibility`). |
| VIZ-PULL | P1 | done ([#201](https://github.com/ixamgames-droid/lightos/pull/201)) | **3D-Bearbeiten war komplett tot — Pull-Poll-Fix** | QtWebEngine stellt Python→JS-QWebChannel-SIGNALE (Push) an die eingebettete Post-Load-Seite NICHT zu (auch fokussiert nicht) → Hinzufügen/Bearbeiten/Verschieben/Drehen/Kamera/DMX **und** Live-platzierte Fixtures reagierten nicht. Fix: JS pollt `pollControl()` (Slot-Rückgabe kommt zuverlässig an), Python spiegelt JEDES Signal in den Poll — inkl. `allFixtures`/`fixtureAdded`/`fixtureRemoved` (Fixture-Meshes, im 2. Durchgang nachgezogen). LIVE end-to-end verifiziert (Stage-Add/Move/Edit, Modus, Kamera, Fixture place/remove sofort sichtbar). |
| VIZ-TRUSS-ADD | P1 | done ([#201](https://github.com/ixamgames-droid/lightos/pull/201)) | **Trasse hinzufügen scheiterte still (mit geladenen Fixtures)** | Root Cause (6-Agent-Workflow + Live-Debug): KEIN JS-Build-Fehler, sondern destruktiver Reconcile-Race in `_on_stage_list_from_js` (visualizer_window.py:~2864): ein Echo mit AKTUELLEM Token, das die frisch gepushte async-Truss transient nicht listet, löschte sie via `py_ids_to_remove`. Der Poll-Fixture-Fix erhöhte die Echo-Frequenz → öffnete den latenten Race; Truss (async + einziges Dock-Hang-Element) besonders exponiert, Plattform (sync Box) nie. FIX: `was_reloading` in den `is_stale`-Guard falten (während eines Python-Reloads ist Python autoritativ, kein Löschen). 3 Tests angepasst + neuer Regressionstest. LIVE verifiziert (Trassen adden zuverlässig mit 14 Fixtures). Nebenmerke: Stage-Add/-Edit triggern je einen Full-Reload → GUI-Ops ~1-2s pacen. |
| VIZ-TRUSS-GEOMETRY | P1 | ✅ done ([PR #250](https://github.com/ixamgames-droid/lightos/pull/250)) | **Truss-OBJ wurde aus dem Querschnitt statt entlang seiner Längsachse skaliert** | Asset-Bounds sind `0,302 × 0,300 × 2,000 m`, die echte Länge liegt auf lokal Z. Alt: horizontal `fitModelToSize(size)` und vertikal die Annahme „Länge = X“ → Ziel-Bounds sahen korrekt aus, aber 30-cm-Streben wurden ~13× langgezogen und die echten Gurte auf 15 % gestaucht. Neu: Kind Z→X/Z→Y orientieren, zentrieren, äußeren Wrapper in Weltachsen fitten. Echte OBJ-Runtime-Abnahme: H=`4×0,3×0,3`, V=`0,3×4×0,3`, kein Extremstretch. |
| VIZ-LOWSPEC | P1 | ✅ done (dieser PR) | **Low-Spec-Modus: flüssiger Visualizer auf der Surface-GPU** (Davids Auftrag 2026-07-11) | Auto-GPU-Tier-Probe (`renderer.js`, Wegwerf-Canvas vor Renderer-Bau, Override `?gputier=`): Low-Tier = kein MSAA, Pixel-Ratio-Cap 1,25 (statt 2), PCF- statt PCFSoft-Schatten, 256er Shadow-Maps, 12er Beam-Kegel; `pixelRatioSignal` respektiert den Cap. Tier-unabhängig: **Dunkel-Culling für SpotLights** (`applyGenericColor`: `visible = intNorm > 0.01`) — dunkle Lampen kosten kein Per-Pixel-Shading mehr (vorher der größte Laufzeit-Block bei 48 Fixtures). Deckt den Qualitätsstufen-Teil von VIZ-15 für die Automatik ab; manuelle Stufen-Wahl im Einstellungen-Tab bleibt VIZ-15. Smoke: `test_gpu_tier_low_reduces_geometry_and_culls_dark_spots` + `test_gpu_tier_high_keeps_full_geometry`. |
| VIZ-SHADER-BUDGET | P1 | ✅ done (dieser PR) | **Bühne bei großen Rigs komplett unsichtbar — Shader-Texture-Limit** | Root Cause des Live-Vorfalls 2026-07-10/11 (crash.log 12:45:56 + GPU-Repro): jedes Licht-Fixture bekam einen `SpotLight` mit `castShadow=true`; jede Shadow-Map kostet eine Texture-Unit im Fragment-Shader JEDES beleuchteten Materials. 48-Fixture-Demo-Rig ⇒ >16 Units auf der Adreno-GPU (Surface, `MAX_TEXTURE_IMAGE_UNITS=16`) ⇒ kein Lit-Shader kompilierte mehr (`useProgram: program not valid`-Dauerspam, GPU-Context-Losses), sichtbar blieben nur MeshBasic-Beams. FIX: deterministisches Shadow-Budget in `fixtures.js` (`maxTextures − 6`, fid-Reihenfolge, Rückverteilung beim Entfernen). Smoke `test_shadow_spot_budget_respects_texture_units` (30 echte Fixtures). |
| VIZ-3D-TABRETURN | P3 | ✅ done (nicht reproduziert, Live-Audit 2026-07-14) | **3D-View ~2 s schwarz beim Zurückwechseln — NICHT reproduziert** | Live beobachtet (2026-07-08, Computer-Use): Nach Wechsel Bühne→Virtual Console→zurück ist der 3D-Bereich ~2 s **schwarz** (Blackframe), dann rendert der Rig wieder — aber aus **veränderter Kamera-Perspektive** (vorher frontal, danach 3/4-Winkel). Deutet an, dass der Visualizer beim Sichtbarwerden **neu aufbaut** statt nur ein-/auszublenden — VIZ-12 wollte hide/show mit **erhaltener Kamera**. Prüfen: `visualizer_window`/`VisualizerService`-show/hide-Pfad (rendert on-show sofort? Kamera-State persistiert über Tab-Wechsel?). DoD: Rückkehr rendert ohne Blackframe, Kamera bleibt exakt erhalten. **✅ Live-Audit 2026-07-14:** hide→show-Zyklus (Fenster schliessen/öffnen) zeigt die Szene SOFORT gerendert, gleiche Kamera-Perspektive — kein Blackframe, kein Kamera-Sprung. (Die Bühne-Sektion ist ohnehin 2D-only → involviert kein 3D.) Nicht reproduziert → schliessen. |
| VIZ-TAB-CLICK | P3 | ✅ done (kein Bug — Artefakt, Live-Audit 2026-07-14) | **Visualizer-Panel-Tabs reagieren nicht auf synthetische Klicks — AUFGELÖST: Automations-Artefakt, KEIN Bug** | ~~In 3 Computer-Use-Sessions ließen sich die rechten Panel-Tabs NICHT per synthetischem Klick umschalten~~ → **Ursache gefunden (Live-Audit 2026-07-14):** Der Visualizer ist ein **Sekundärfenster**; isolierte synthetische Einzelklicks treffen es oft OHNE echten Windows-Foreground (der Claude-Controller re-grabbt den Foreground zwischen Einzelaktionen). Der QtWebEngine-**Child-HWND** verarbeitet Rohmaus auch ohne Foreground (deshalb ging Canvas-Drag/Fixture-Move), die **Qt-Widgets** (Tabs/Toolbar/Felder) brauchen echten Foreground. Sobald „Titelleisten-Klick (Foreground) + Widget-Klick" im SELBEN Batch laufen, schalten Tabs/Toolbar sofort. Code bestätigt: `QSplitter`, WebEngine nur in der linken Hälfte (`visualizer_window.py:1533/1550`) — kein Overlay über Toolbar/Panel. **Für David (normaler Desktop-Start, echter Foreground) funktionieren Tabs/Toolbar normal.** Konsequenz: schließen. |
| VIZ-BUILDER-FIXTYPE | P2 | ✅ done (dieser PR) | **`ShowBuilder.patch()` übernimmt `fixture_type` aus dem Profil** | ~~Skript-gepatchte Fixtures bekamen den Default `fixture_type="other"` (models.py:35) → 3D mappt DMX nicht auf Farbe/Pan/Tilt~~ → `patch()` liest jetzt `(id, fixture_type)` des Profils (neuer Helfer `_lookup_profile`) und setzt den Typ beim Anlegen (spiegelt die `sync.py`-Auto-Fix-Semantik, nur schon beim Patchen statt erst bei Validierung). Der manuelle Nachzieh-Workaround in `tools/build_grosses_rig.py` ist damit entfallen (entfernt). Test `test_showbuilder.py::test_patch_inherits_fixture_type_from_profile`. |
| VIZ-STAGE-PANEL | P3 | ✅ done (a,c erledigt; b,d nicht reproduziert, Live-Audit 2026-07-14) | **Stage-Eigenschaften-Panel: Feld-Commit-/Sync-Macken** | **(a) erledigt:** die Stage-Größen-/Positions-Felder (`_stage_spin_*`) sind `LocaleTolerantDoubleSpinBox` → Punkt/Komma gehen nicht verloren. **(c) erledigt (2026-07-10):** Baum-Auswahl schreibt ihre ID sofort fort; unvollständige Reload-Echos bleiben bis zum vollständigen Snapshot folgenlos; während der Parameter-Eingabe wird ein nachlaufendes 3D-Auswahlecho ignoriert und dieselbe Auswahl aktiv zurück in die Szene gespiegelt. Real per Desktop-UI geprüft (Publikumsfläche, X-Wert): 3D-Markierung, Tabellenzeile und Feld blieben synchron. **(b)+(d) ✅ Live-Audit 2026-07-14 NICHT reproduziert:** (b) beim Tippen von `X=0` (Back Wall) wandert das 3D-Element SOFORT ohne Enter/Tab → Feld committet live pro Tastendruck (Keyboard-Tracking), Verdacht „ENTER committet nicht" trifft nicht zu. (d) „Größe anpassen"-Toggle geht sauber AN (gelb „: AN" + Resize-Handles am Element) und wieder AUS (normal, Handles weg), kein Klemmen über Zyklen. Beide → schliessen. `visualizer_window.py` `_on_stage_property_changed`/`_on_resize_mode_toggled`. |
| VIZ-FIX-DECIMAL | P3 | ✅ done (dieser PR) | **Fixture-Positionsfeld verwirft Dezimalstellen** | ~~Eingabe „5.7" wurde unter dt. Locale verworfen (Komma erwartet, Punkt abgeschnitten)~~ → neues wiederverwendbares Widget **`LocaleTolerantDoubleSpinBox`** (`src/ui/widgets/decimal_spinbox.py`, C-Locale + Komma→Punkt in `validate`/`valueFromText`) akzeptiert Punkt UND Komma. Alle 14 `QDoubleSpinBox` der 3D-Panels (Fixture-Position/-Ausrichtung, Stage-Größe, Raster) darauf umgestellt. Test `tests/test_decimal_spinbox.py` (5, dt. Locale erzwungen, inkl. Regressionsbeleg gegen die Standard-Spinbox). |
| VC-WIDGET-DRAG | P3 | done — verifiziert (✅ geschlossen — nicht reproduzierbar (headless **+ LIVE**) | **VC-Canvas: Widgets nicht per Drag verschiebbar** | Live beobachtet (2026-07-07): Button/Fader/SpeedDial ließen sich angeblich nicht ziehen. **Headless NICHT reproduzierbar** (Drag-Pfad `VCWidget.mousePressEvent`→`_dragging`, `mouseMoveEvent`→`self.move` funktioniert für alle drei Typen, auch VCFrame-verschachtelt; Guard-Test `tests/test_vc_widget_drag.py`, 4). **LIVE-Repro 2026-07-08 (Computer-Use auf Davids „Grosses Rig 2026"):** im Bearbeiten-Modus **„MH ColorFade"-Button UND „MH Tempo"-SpeedDial per Drag verschoben** — beide folgten der Maus, selektierten korrekt (Inspector band an), Strg+Z nahm sauber zurück. → Drag funktioniert live einwandfrei; die Ursprungs-Beobachtung war ein Einmal-/Timing-Effekt. Wächter-Test bleibt als Regressionsschutz. |
| FM-1 | P1 | ✅ done ([PR #164](https://github.com/ixamgames-droid/lightos/pull/164)) | Scanner-Spiegel + Strahl animieren | Scanner = Gehäuse→Pan-Yoke→kippender Spiegelkopf; Strahl schwenkt live mit Pan/Tilt-DMX. |
| FM-2 | P1 | ✅ done ([PR #165](https://github.com/ixamgames-droid/lightos/pull/165)) | Multi-Head-Payload generalisiert (N Köpfe + pro-Kopf-Pan) | `heads`-Array beliebig viele Köpfe (aus höchstem #N-Index) + pro-Kopf-`pan`; spider-byte-identisch. Fundament für Bars. |
| FM-3 | P1 | ✅ done ([PR #168](https://github.com/ixamgames-droid/lightos/pull/168)) | N-PAR-Bar (einzeln steuerbare PARs) | ≥2 Banks ohne Bewegung → `par_bar`: N einzeln gefärbte PARs, je eigener Beam. |
| FM-4 | P1 | ✅ done (dieser PR) | N-Mover-Bar (einzeln pan/tilt/farbe pro Kopf) | ≥2 Banks + ≥2 Pan → `mover_bar`: N Mini-Moving-Heads, jeder Kopf einzeln pan/tilt/farbe. |
| FM-5 | P2 | ✅ done (dieser PR) | **Fixture-Bibliotheks-Profile für die neuen Bars** | Zwei generische Builtins in `fixture_db.py`: „LED Moving Bar 4×" (MOVBAR4, 22ch, 4×Pan/Tilt/RGB → `mover_bar`) + „LED PAR Bar 4×" (PARBAR4, 12/16ch, 4×RGB(W) → `par_bar`). Idempotent in `_seed`+`ensure_builtins`, im Klassen-Audit festgenagelt, Routing per echtem `_viz_model_for`-Test verifiziert (`test_generic_bars_profile.py`). Jetzt patch- und sichtbar. (4-PAR-Bar ging vorher nur über ADJ Dotz TPar; Mover-Bar gar nicht.) |
| FM-6 | P2 | ✅ done (dieser PR) | 2D-Symbole für par_bar/mover_bar | Dedizierte grandMA3-artige Symbole: par_bar = Gehäuse-Balken mit 4 PAR-Zellen, mover_bar = Balken mit 4 kleinen Köpfen + Beam. Im 2D-Top-Down (`live_view.FixtureRenderer`) UND als Listen-Icon (`mini_icons`: `fx_par_bar`/`fx_mover_bar`). Vorher: beide fielen aufs Spider-Symbol. (Scanner hatte schon ein eigenes Symbol.) |
| FM-7 | P3 | ✅ done (dieser PR) | Routing-Vereinheitlichung: EINE Quelle `viz_model_for` | NEU `app_state.viz_model_for()` als einzige Quelle für mover_bar/par_bar/spider — genutzt von 3D (`_viz_model_for` delegiert), 2D-Symbol (live_view), Listen-Icon (mini_icons) UND Patch-Spiegel-Option (nur noch echte Spider, nicht par_bar/mover_bar). Verhindert 2D/3D-Drift. Adversariale Review: 24 Kandidaten, 0 real. |
| FM-11 | P2 | ✅ done ([PR #254](https://github.com/ixamgames-droid/lightos/pull/254)) | **Realismus-Pass: alle prozeduralen 3D-Modelle auf echte Datenblatt-Maße** (Davids Auftrag 2026-07-12) | Jede Geräteklasse gegen ein recherchiertes Referenzgerät dimensioniert (Quellen in `builders.js`-Kommentaren): PAR = PAR-64-Dose Ø 0,23 m statt Ø 0,44 (+ Doppelbügel, bei `.dae`-Overlay ausgeblendet); MH = Intimidator-260-Klasse H ~0,48 m statt 0,85; Spider = 8×10W-Klasse 0,40×0,25×0,20 m statt 0,88×0,54; 4er-PAR-Bar = Dotz-TPar ~1,05 m statt 1,82; Mover-Bar = AXIS-Klasse ~1,05 m/Ø-0,10-Köpfe statt 2,05 m; LED-Bar = Stairville-1m-Klasse 1,07×0,088×0,065; Strobe = Superstrobe 460×140×240; Dimmer = 4ch-Truss-Pack 0,30×0,13×0,19 (Torblenden → Ausgangs-Dosen); Scanner = Dynamo-Klasse 0,40 m, **Spiegel am Gehäuse-Ende** statt mittig; Laser = Ehaho L2600 201×66×160 flach + Bügel + Frontfenster; Nebel = N-10-Klasse mit Düse + Bügel; Hazer = HZ-100-Klasse hochkant + Griff + Ausblasgitter oben. **LowRes-Anschluss:** neuer `segs()`-Helfer halbiert Gehäuse-Rundkörper-Segmente im Low-Tier (analog Beam-Kegel); Mover-Bar-Beam-Anker hängt jetzt an der Linsen-Position statt an einem hart kodierten Offset. Tests: `test_fixture_models_have_realistic_dimensions` (Bounding-Box-Abnahme in echtem QtWebEngine) + Tier-Tests um Gehäuse-Segmente erweitert. |
| FM-12 | P2 | ✅ done ([PR #255](https://github.com/ixamgames-droid/lightos/pull/255)) | **Fixture-Generator: 3D-Modell wählbar + Automatik-Vorschlag** (Davids Auftrag 2026-07-12) | Neues Feld `FixtureProfile.viz_model` ("" = Automatik; ALTER-TABLE-Migration in `migrate_fixtures_db`). `viz_model_for` liefert den Profil-Override VOR der Kanal-Heuristik — dadurch schaltet EINE Wahl 3D-Modell, 2D-Live-View-Symbol, Listen-Icon und Patch-Spiegel-Option gemeinsam um (FM-7-Muster `viz_model_for(f) or fixture_type`). Heuristik in `suggest_viz_model()` extrahiert (eine Quelle, DB-frei) → der Generator zeigt in der neuen „3D-Modell"-Combo live „Automatisch (Vorschlag: X)" für den gerade bearbeiteten Modus; 13 explizite Modelle wählbar (PAR/MH/Scanner/LED-Bar/PAR-Bar/Mover-Bar/Spider/Strobe/Dimmer/Laser/Nebel/Hazer/Other). Payload/`create_user_profile` persistieren die Wahl; `clear_channel_cache` invalidiert den neuen Override-Cache mit, `_rebuild_render_plan` wärmt ihn vor (kein DB-Roundtrip im 20-FPS-Paint-Pfad). **Adversariale Review (3 Linsen × 2 Skeptiker): 3 Funde bestätigt + gefixt** — Spider-Override ohne Head-Banks fällt auf Basis-RGB zurück statt dunkel zu bleiben (HIGH), Cache-Prewarm (MEDIUM), DB-Fehler nicht mehr als „kein Override" gecacht (MEDIUM); 4 Kandidaten widerlegt. 17 Tests (`test_viz_model_override.py`) + QtWebEngine-Spider-Fallback-Regression. |
| FM-8 | P3 | ✅ done | Modell-Fidelity: Moving-Head ✅ ([#182](https://github.com/ixamgames-droid/lightos/pull/182)) + LED-Bar Pro-Pixel ✅ (dieser PR) | **Moving-Head:** detailliertes prozedurales Modell (Bodenplatte + Hals → U-Yoke mit Motor-Gehäusen → Kopf-Trommel + emissive Linse); `moving_head.dae` verworfen (grober Low-Poly-Klotz, per Render belegt, Davids Entscheidung). **LED-Bar Pro-Pixel:** `buildParBar(n, pixelStyle)` — led_bar-Typ mit ≥6 Banks rendert als schlanke Bar mit N einzeln gefärbten Segmenten (4er-Bars behalten bewusst den PAR-Dosen-Look). Pro-Zellen-FARBE gab's seit FM-3/7 via par_bar-Routing; jetzt stimmt auch der LOOK. **Rest-Idee (nicht blockierend):** Scanner-Strahl physikalisch am Spiegel ablenken → VIZ-15 Optik. Für künftige Asset-Upgrades: hochwertiges .glb/.dae nötig, Split-/Rig-Verfahren liegt bereit. |
| FM-16 | P2 | ✅ done (a/c/d/b/e + Preview-Nachtrag [PR #423]) | **Multi-Head-Fixtures als fest angelegte Matrix/Gruppe — pro-Kopf per Matrix-Programmer UND EFX** (David-Wunsch 2026-07-18, GENERELL für ALLE Mehrkopf-Geräte: Spider/MOVBAR4/Hydrabeam/künftige) | **Problem (Architektur-Karte 2026-07-18, Explore-Recall):** Die Kopf-Struktur existiert bereits als `attr#N` (`channel_occurrence_keys` `app_state.py:2616`, `resolve_attr_channels` `:2634`), aber die zwei Engine-Write-Pfade adressieren nur GANZE `fid`s, nie einzelne Köpfe: **Matrix** (`RgbMatrixInstance.write` `rgb_matrix.py:797-836`) matcht bei einer Grid-Zelle ALLE `color_r`-Vorkommen uniform → ein Multi-Head = ein einfarbiger Pixel (die 4 Köpfe sind NICHT 4 Pixel); **EFX** (`efx.py`) targetet 1 `EfxFixture` pro `fid` (`efx.py:54-59/110`, `efx_view.py:1486`) und bewegt nur Kopf-0-Pan — `_spider_head_tilts` `efx.py:398-432` erzeugt zwar bereits pro-Kopf-**TILT** (`tilt#k`), aber **kein pro-Kopf-PAN** (`pan#N` wird im EFX nie befüllt). **Ziel:** die N Köpfe jedes Mehrkopf-Fixtures automatisch als adressierbare Matrix/Gruppe behandeln — Matrix-Programmer (jeder Kopf = ein Pixel) UND EFX (Chase/Phasenversatz über die Köpfe „nacheinander"). **Slices:** **(a) Fundament** — zentrale `heads_for_fixture(f) -> [{attr: channel}]`-Enumeration (kanonische „gib N Köpfe als adressierbare Einheiten") neben `channel_occurrence_keys` (`app_state.py:2616`), aufbauend auf `_multihead_count`-Logik (`visualizer_service.py:129-137`) + `tilt_head_count` (`app_state.py:2735`); EINE Quelle für Matrix + EFX. **(b) EFX pro-Kopf** — Ziel-Expansion (Multi-Head-Mover → N `EfxFixture(fid, pan_attr="pan#h", tilt_attr="tilt#h")` statt 1; `efx_view.py:1486`, `assign_movers_auto` `efx.py:498-522`, `mover_fids`/`is_mover_fixture` `app_state.py:2763-2788`) + pro-Kopf-PAN-Welle analog `_spider_head_tilts` (`efx.py:398-432`, Gate/Write `efx.py:557-575`); Schreibpfad `resolve_attr_channels` (`efx.py:580`) ist bereits `attr#N`-fähig, wird nur nie mit `pan#N` befüllt. Ergebnis: echter Pan+Tilt-Chase über die 4 Köpfe eines Fixtures. **(c) Matrix pro-Kopf** — Grid-Zelle kann `(fid, head)` halten statt nur `fid` (`positions_json`-Wertsemantik `models.py:162-163`, `grid_from_positions` `rgb_matrix.py:144-157`, `fixture_grid` `rgb_matrix.py:441`); `write()` nutzt `channel_occurrence_keys`, um pro Zelle nur das h-te `color_r#h/g#h/b#h/w#h` (ggf. `intensity#h`) zu treffen (`rgb_matrix.py:797-836`); **.lshow-Rückwärtskompat PFLICHT** — alte Shows = reine `fid`-Zellen müssen weiter laden (`show_file.py:234-271`, Fallenklasse #3). **(d) Auto-Gruppe/Matrix beim Patchen** (Komfort, setzt a+c voraus) — bei erkanntem Multi-Head automatisch eine 1×N-`FixtureGroup` mit Kopf-Zellen anlegen (`patch_view.py:544`, `app_state.add_fixture` `app_state.py:480-483`; analog `live_view.create_group_from_selection` `live_view.py:2529`, aber `(fid,head)`-Zellen). **Reihenfolge:** a → {b, c parallel} → d. **Fallenklassen:** #5 attr#N (Kopf-Unabhängigkeit, kein Phantom-Head), #3 .lshow-Rückwärtskompat (Grid-Kopf-Encoding). **(e) Kopf-Matrizen ZUSAMMENLEGEN** (David-Wunsch 2026-07-18, Nachtrag) — mehrere auto-entstandene Kopf-Matrizen (z. B. 2× Hydrabeam = 2×4 Köpfe) im Programmer/Gruppen-Editor zu EINER größeren Matrix mergen (Zeilen stapeln / Spalten erweitern); der Gruppen-Editor (`fixture_group_view`) muss dazu Kopf-Zellen `(fid,head)` verstehen + platzieren (heute nur ganze fids) und eine „Gruppen zusammenlegen"-Aktion bekommen. **Reihenfolge:** a → {b, c parallel} → d → e. **✅ Erledigt (dieser PR, Matrix-Hälfte a/c/d):** `color_head_count`/`channels_for_head` (`app_state.py`), Zell-Encoding `"fid:head"` + `grids_from_positions` + `RgbMatrixInstance.head_grid` + Pro-Kopf-`write()` (nur h-tes color_r#h/g#h/b#h/w#h, geteilte Dimmer/Shutter) + to_dict/apply_dict (.lshow-rückwärtskompat), `rgb_matrix_view` zieht `head_grid` aus der Gruppe, Auto-Kopf-Matrix-Gruppe beim Patchen (`create_head_matrix_group`, idempotent). Tests `tests/test_matrix_perhead.py`. **(b) ✅ Erledigt (EFX-Kopf-Welle):** `_head_pan_tilts` verallgemeinert die tilt-only Spider-Kopf-Welle (`_spider_head_tilts`) zu Pan+**Tilt** pro Kopf; `write()` gated auf `pan_heads>=2 ODER tilt_heads>=2` — Voll-Mehrkopf-Mover (MOVBAR4/Hydrabeam) bekommen jetzt pro-Kopf-Pan (statt Kopf-0-Spiegelung via `resolve_attr_channels`), reine Dual-Tilt-Spider (0 Pan) bleiben tilt-only unverändert; invert_pan/invert_tilt/swap je Kopf via derselben `apply_pan_tilt_orientation` (bitidentisch zu Kopf 0); mirror/counter/fan/RANDOM/16-bit mitgetragen; **keine neue Persistenz** (nutzt bestehendes `head_spread`). **Ziel-Zuweisung UNVERÄNDERT** (1 `EfxFixture`/fid, Kopf-Expansion im `write()` — bewusst KEIN invasives N-`EfxFixture`-Split, das die i-indizierte Fan/Counter/Mirror-Logik + save/load bräche). Tests `tests/test_efx_perhead_pan.py` (12). **Preview-Nachtrag ✅ erledigt ([PR #423](https://github.com/ixamgames-droid/lightos/pull/423), 2026-07-22):** `EfxPreviewWidget` zeichnet für Mehrkopf-Mover jetzt N phasenversetzte Kopf-Punkte (+ Wellenlinie + Kopf-Nummern) statt 1 Punkt/Gerät. Neue state-freie `EfxInstance.head_phase_points` (Render + Vorschau = EINE Positions-Quelle; `_head_pan_tilts` delegiert → write()-DMX bit-identisch), `app_state.pan_tilt_head_count` (write()-Gate + Vorschau = EINE Kopfzahl-Quelle); Kopfzahl pro Paint frisch aus dem Patch (kein Stale-Cache, Fallenklasse #6b). Tests `tests/test_efx_preview_perhead.py` (18); adversariale Review (5 Linsen × Skeptiker) bestätigte Bit-Identität + Kopf-0==Render-Kopf-0 für alle Kombis, 1 MEDIUM (Fallback-Cache) vor Merge gefixt. **(e) ✅ Erledigt (Kopf-Matrizen zusammenlegen):** `AppState.merge_head_matrix_groups` stapelt N (Kopf-)Matrix-Gruppen vertikal zu EINER größeren Matrix (`_stack_group_grids`, rein/getestet; z. B. 2× Hydrabeam 1×4 → eine 4×2), nicht-destruktiv, jede Zelle behält `fid:head` → Matrix-Engine spricht die Köpfe weiter einzeln an. Gruppen-Editor (`fixture_group_view`) versteht jetzt `"fid:head"`-Zellen (`_split_cell`): `_load_group` verwirft sie nicht mehr still (Kopf-Matrix erschien vorher LEER), Paint zeigt „fid·K{n}", `_group_fids` liefert die Basis-fids; neuer „⧉ Matrizen zusammenlegen…"-Button (Mehrfach-Auswahl-Dialog). Tests `tests/test_fm16e_head_matrix_merge.py` (15). **Computer-Use-Folgeschritt läuft als eigenständiges FM-HEADLAYOUT (P1):** interaktives Pro-Kopf-Platzieren im Editor (Drag einzelner Köpfe) + Live-UX-Abnahme (der `EfxPreviewWidget`-Kopf-Punkte-Nachtrag ist mit [PR #423] erledigt). **FM-16 damit komplett** (a–e + Preview alle gelandet). Koppelt an **FM-9** (Pro-Kopf-Programmer-Steuerung), **FM-10** (Pro-Kopf-Aim), **FM-14** (Pixel/Segment). |
| FLA-1 | P1 | ✅ done ([PR #176](https://github.com/ixamgames-droid/lightos/pull/176)) | PARTYLASER wurde als Spider statt Laser gerendert | Builtin „Laser Stage Lighting" hat zwei `color_r`-Kanäle → `is_spider_fixture` True → `viz_model_for` liefert 'spider' statt 'laser' (falsches 3D-Modell + 2D-Symbol in beiden Renderern + sinnlose Patch-Spiegel-Option). Fix: `is_spider_fixture` gatet `fixture_type=='laser'` zentral aus (Laser = Punkt-Scanner). DMX-Output unberührt (`_multihead_count`). Adversariale Review: SICHER. `tests/test_laser_not_spider.py`. |
| FLA-2 | P1 | ✅ done ([PR #176](https://github.com/ixamgames-droid/lightos/pull/176)) | CMY-Farbmischer fielen aus dem Color-Tab | `ATTR_GROUPS['Color']` + `_DIM_COLOR_ATTRS` führten tote Tokens `cyan/magenta/yellow`; real emittiert werden `cmy_c/m/y` (QXF-Import/Fixture-Editor) → `classify_attr('cmy_c')` fiel auf 'Other' → importierte CMY-Mover verloren Color-Tab/Picker + GM-Farbmaske. Fix: `cmy_c/m/y` in beide Mengen (konsistent mit `engine/palette.py`). |
| FLA-3 | P2 | ✅ done ([PR #176](https://github.com/ixamgames-droid/lightos/pull/176)) | DOTZTPAR-Strobe ohne Range | Shutter-Kanal der 9/18ch-Voll-Modi als einziges Fixture ohne Range-Block. Fix: Standard-`_SIMPLE_STROBE` (0=Aus/open, 1-255=Strobe). |
| FLA-4 | P3 | ✅ done ([PR #250](https://github.com/ixamgames-droid/lightos/pull/250)) | Geräteklasse `other` mit eigenem 3D-Modell/2D-Icon | Real erreichbar via QLC+-Import (Fan/Effect/Other → `other`). Jetzt eigener prozeduraler Builder (neutrales technisches Gehäuse, Haltebügel, Ident-/Statusfeld; ≤16 Drawables inkl. Beam) sowie dedizierte Symbole im 3D-Top-Down, in der nativen 2D-Live-View und in Listen/Bäumen. Rein kosmetisch: generischer DMX-/Beam-Vertrag bleibt erhalten; unbekannte Legacy-Typen wie `color` fallen weiterhin auf PAR zurück. Echte QtWebEngine-DMX-/Icon-Abnahme + Painter-Smokes. |
| FLA-5 | P3 | ✅ done | Kleinvieh aus dem Audit | (a) **stale** — `speed` ist längst in der `Effect`-ATTR_GROUPS-Menge (`attr_groups.py:50`) + Label „Speed" (`:76`); (b) **✅ done (dieser PR)** — Doku-Kommentar an `_add_adj_dotz_tpar` (`fixture_db.py`): DOTZTPAR-Typ ist `led_bar` (Default), die `par_bar`-Optik entsteht modusabhängig aus der gepatchten Kanalzahl über `_viz_model_for` — kein Bug, nur nicht offensichtlich. |
| VCL-01 | P2 | done ([PR #132](https://github.com/ixamgames-droid/lightos/pull/132)) | Live-Edit: Anzeige neu synchronisieren, wenn Tempo/Params von ANDERER Stelle geändert werden | `ui/virtualconsole/vc_multi_live_editor.py` — Tempo-Modus-Buttons (Aus/BPM/Tap) + versteckte/gezeigte Regler lesen ihren Wert nur beim `_refresh_body`-Bau. Ändert eine ANDERE Fläche (VC-Bus-Selector, MIDI, CLI) `tempo_bus_id`/Effekt-Params derselben Funktion, bleibt die Live-Edit-Anzeige stale bis zum nächsten Moduswechsel/Rebuild. Fix: auf das passende Change-Signal abonnieren (bzw. beim Sichtbarwerden `_refresh_body`) und die betroffenen Regler/Tempo-Buttons nachziehen. Regressionstest. (LOW aus PR #126/#128-Adversarial-Review.) |
| VCL-02 | P3 | done ([PR #132](https://github.com/ixamgames-droid/lightos/pull/132)) | Live-Edit: Touch-Resize-Band effektiv auf 8px geklemmt | `ui/virtualconsole/vc_multi_live_editor.py:_reposition_content` — der Content wird nur um `HANDLE_SIZE` (8) eingerückt; die per Dwell aufklappenden `TOUCH_HANDLE_SIZE` (24) breiten Greif-Bänder sind damit vom Content überdeckt und unerreichbar (Touch-Resize degradiert auf die schmale Maus-Zone). Fix: bei aktivem Touch-Resize (bzw. sobald `_big_handles`) den Content um die Touch-Griffbreite einrücken. Cosmetic/Touch-UX. |
| VCL-03 | P3 | done ([PR #132](https://github.com/ixamgames-droid/lightos/pull/132)) | Live-Edit: nicht gemappte Auswahl-Optionen zeigen den Roh-Key | `ui/virtualconsole/vc_multi_live_editor.py` (`_DIR_LABELS`/`_build_segmented`) — Bewegungs-/Stil-Optionen ohne Eintrag in der Label-Map (z. B. `normal`) rendern den englischen Roh-Token als Segment-/Combo-Beschriftung. Fix: freundliche deutsche Labels für die restlichen Bewegungs-/Stil-Tokens ergänzen (zentrale `_option_label`-Map statt nur `_DIR_LABELS`). Polish. |
| VCL-04 | P2 | done ([PR #133](https://github.com/ixamgames-droid/lightos/pull/133)) | „Neue Show" — VC-Canvas zuverlässig leeren (verifizieren) | Beim Live-Test **einmal** beobachtet: nach „Neue Show" blieb ein Live-Edit-Widget auf dem VC-Canvas sichtbar. Verifizieren, ob `reset_show`/„Neue Show" die VC-Canvas-Widgets wirklich entfernt (vs. Stale-Repaint / einmaliger Anzeige-Glitch). Falls ein Widget real überlebt → Lifecycle-Bug fixen (Bezug: VCB-05-Reset-Pfad, `core/show/show_file.py:reset_show` + `vc_canvas`-Teardown). Repro + Regressionstest. |
| UXT-01 | P1 | ✅ done (dieser PR) | **Fader „Programmer-Attribut" bekam ein UI-Feld für das Ziel-Attribut** | `vc_slider.py` `_open_properties()`: editierbare Attribut-Combo (aus `attr_groups.ATTR_LABELS`, 55 Attribute inkl. aller Laser-Attribute; Freitext für Power-User), sichtbar nur bei Modus==PROGRAMMER, Rücklesen per itemData/Freitext. **Der LAS-Speed-Fader (gobo_rotation 192–223) ist jetzt per GUI baubar.** Tests: `test_laser_vc_speed.py` (Feld da/vorbefüllt/Auswahl+Freitext) + Sichtbarkeits-Gate verifiziert. |
| UXT-03 | P2 | ✅ done (dieser PR) | **Snap→Szene-Brücke** + Chase-Weg klargestellt | **Zwei Teile:** (1) Der Snap→**Chase**-Weg existierte schon (Snap-Kontextmenü „Chase aus Auswahl erstellen" baut aus ≥2 Snaps eine Sequence) — der Agent nahm nur den falschen Button („Chase +" im Editor). (2) Der echte Gap war Snap→**Szene**: neue Kontextmenü-Aktion **„Als Szene(n) übernehmen"** (`snap_file_panel._create_scene_from_selection`) wandelt je markierten Snap über `programmer_to_scene_values` in eine Scene-Funktion, die danach im Chaser-Editor unter „+ Hinzufügen" wählbar ist. Tests `test_snap_to_scene.py` (Einzel/Mehrfach/leer/ohne-Patch). Doku-Hinweis dazu steckt bereits in EFFEKTE.md (UXT-04). |
| UXT-04 | P2 | ✅ done (dieser PR) | **EFFEKTE.md-Tabname korrigiert** (kleiner als gedacht) | **Verifikation gegen den Code ergab:** der „Helper"-Tab existiert weiter — er heißt im sichtbaren Reiter seit QOL-04 **„Assistent"** (vorher „Hilfe"; `programmer_view.py`, Zeilenangabe 2026-07-26 entfernt, weil gedriftet), mit unveränderten Buttons Effekt-Assistent/„+ Szene"/„+ Chaser"/„Programmer → Szene". Der Agent hatte „Hilfe" gesehen, aber nicht als Effekt-Seite erkannt — seine „Funktionalität weg"-These war falsch. Fix: „Helper"→„Hilfe" durchgängig + Klarstellung **Snap vs. Szene** (Chaser nimmt Szenen, nicht Snaps → „Programmer → Szene") im Intro. |
| UXT-05 | P2 | ✅ done (dieser PR) | **Laser-Endnutzer-Anleitung geschrieben** | NEU `docs/anleitung_laser/ANLEITUNG_LASER.md`: zwei Laser-Klassen (DMX-Muster vs. Netzwerk), Patchen, Laser-Tab + Betriebsart, Bank/Muster + Klartext-Wertebereiche, Muster-Palette speichern, Werksmuster-Kacheln + Foto, VC-Knopf + Tempo-Fader (`gobo_rotation` 192–223), Zeichen-Studio, Sicherheit. In `ANLEITUNGEN.md` (Nr. 12) verlinkt. |
| UXT-06 | P2 | ✅ done ([#190](https://github.com/ixamgames-droid/lightos/pull/190)) | **VC-Werkzeugleiste: neue Elemente staffeln + auswählen** | **Ursache:** `virtual_console_view._add_widget` legte JEDES neue Widget exakt in der Canvas-Mitte ab → der zweite lag deckungsgleich auf dem ersten (wirkte wie „nichts passiert"; ein Element WURDE erzeugt, nur unsichtbar gestapelt). Fix: diagonale **Kaskade** (Versatz `GRID*3` je Klick, nach 8 Stufen Wrap, Reset je Bearbeiten-Sitzung) + das neue Widget wird **ausgewählt** (Inspector bindet → sichtbare Bestätigung). Tests in `test_views.py`. (Der „Toolbar-Drag"-Punkt war kein Bug: die Toolbar ist Klick-zum-Anlegen; Drag gibt es nur aus der rechten Bibliothek — der Tooltip „auf eine Taste ziehen" gehört zur Bibliothek.) |
| UXT-07 | P2 | ✅ done (dieser PR) | **„Großes Fenster"-Popout wirkte folgenlos** — behoben | **Ursache:** die Popout-Fenster (`_toggle_editor_popout` in 7 Editoren + EFX-Vorschau-Popout) riefen `win.show()` **ohne** `raise_()`/`activateWindow()` → das nicht-modale Fenster erschien HINTER dem maximierten Hauptfenster (der Editor-Bereich wurde leer → „kein Effekt"). Fix: nach jedem `show()` `raise_()`+`activateWindow()` (rgb_matrix, chaser, audio, carousel, effect_layer, scene, sequence, efx — inkl. EFX-Vorschau-Erstöffnung). Der „Großes Fenster"-Knopf ist damit die Lösung für das Höhenproblem (voll scrollbares Fenster, Buttons sichtbar). Test `test_matrix_meta_view.py` verifiziert raise+activate+isVisible. |
| UXT-08 | P3 | ✅ done (dieser PR) | **Patch-Validierungswarnung entschärft (Modus-Umbenennung)** | Ist der gespeicherte Modusname nur eine Kurzform des vollen DDF-Namens (z. B. „34-Kanal" ⊂ „34-Kanal (Professional DMX)"), meldet die Validierung (`sync.py`) das jetzt als harmlose Umbenennung („Modus … aktualisiert / wird als … geführt") statt als alarmierendes „Mode … fehlt". Beide Warn-Pfade. Test in `test_uxt_kleinvieh.py`. |
| UXT-09 | P3 | ✅ done (dieser PR) | **NOT-AUS bestätigt jetzt sichtbar** | Neuer SyncEvent `LASER_ESTOP`, zentral in `laser_output.estop_all()` emittiert (egal ob VC-Button oder Laser-Steuerseite auslöst). Das Hauptfenster zeigt darauf einen **prominenten rot eingefärbten Statuszeilen-Alarm** („⏹ LASER NOT-AUS ausgelöst — Netzwerk-Ausgabe verriegelt & unscharf", 6 s, selbst zurücksetzend) — bewusst nicht-blockierend (kein wegzuklickender Dialog im Ernstfall). Test in `test_laser_vc_safety.py`. |
| UXT-12 | P2 | ✅ done (dieser PR) | **NOT-AUS macht DMX-Muster-Laser (L2600) jetzt wirklich dunkel** | `estop_all()` verriegelte nur den Netzwerk-Streamer; ein L2600 lief über DMX weiter. Fix: Latch `app_state.laser_estop_active` (gesetzt zentral in `estop_all` via `set_laser_estop(True)`); `_rebuild_render_plan` cacht die DMX-Adressen aller Laser-Fixtures (`_laser_estop_addrs`) + deren fids (`_laser_fids`); `_render_frame` zwingt bei aktivem Latch als **oberste Ebene** (nach allen Merges, vor Commit) alle Laser-Kanäle auf 0 — überschreibt auch Effekte/Simple-Desk. Latch löst beim nächsten bewussten Laser-Programmerwert (Muster-Abruf/Regler = „wieder an"); Show-Load lässt ihn stehen (dunkel = sicher). Annahme: Betriebsart/Shutter 0 = aus (gilt L2600). Tests `test_laser_dmx_estop.py` (5, inkl. echtem Render: Laser 0, PAR unberührt). |
| UXT-10 | P3 | ✅ done (dieser PR) | **Intensity-Tab erklärt RGBW-Dimmen** | `programmer_view.py`: bei einem Gerät ohne Intensity-Kanäle, das aber Farbkanäle hat, steht statt „Keine Intensity-Kanäle gefunden" jetzt „Dieser Modus hat keinen separaten Dimmer — die Helligkeit läuft über die Farbe (Color-Tab)." |
| PATCH-GRP-01 | P1 | ✅ done (dieser PR) | **Gruppen-Auswahl bleibt stabil + Drag-Drop rastet ein (kein stiller Overwrite)** | `_reload_group_list` sprang bei jedem Neuaufbau auf `groups[0]` (alphabetisch erste) → nach „+ Neu"/„Speichern" (via `GROUP_CHANGED`) traf die Auswahl unbemerkt die falsche Gruppe (Spiders→MovingHeads überschrieben). Fix: Auswahl per ID erhalten (`select_gid`), „+ Neu" selektiert die neue Gruppe. Externer Drop: `place_fixture`/`_nearest_free_cell` weichen belegten Zellen auf die nächste freie aus (kein stilles Überschreiben), Rand geklemmt, grünes Live-Ziel-Highlight (`resolve_drop_cell`). Plus Shortcut „Alle → Raster" (`_add_all_fixtures`). Tests `tests/test_fixture_group_grid_ux.py` (16). |
| UI-15 | P2 | done ([PR #135](https://github.com/ixamgames-droid/lightos/pull/135)) | Sektions-Tab-Beschriftungen clippen in der Haupt-Navigation | `ui/main_window.py` (Section-Buttons) — bei 1440 px sind fast alle Titel abgeschnitten („Bühnen-Lay", „Programm", „Virtual Cons", „Simple Des", „Eingabe / Aus"). Fix: Breite aus `sizeHint` der Beschriftung statt fixer Breite; bei echtem Platzmangel elidieren + Tooltip (kein harter Clip). Screenshot-Vorher/Nachher im PR. |
| UI-16 | P2 | done ([PR #135](https://github.com/ixamgames-droid/lightos/pull/135)) | Grand-Master-Prozentwert liest sich als „00%" | `ui/main_window.py` (Kopfzeile, GM-Slider) — der Slider-Griff überlappt die erste Ziffer des Labels. Fix: Label mit fester Mindestbreite RECHTS neben dem Slider (nie unterm Griff). Sicherheitsrelevant (wichtigster globaler Regler). |
| UI-17 | P2 | done ([PR #135](https://github.com/ixamgames-droid/lightos/pull/135)) | EFX-View: Buttonleisten unlesbar geclippt | `ui/views/efx_view.py` — Buttons unter der Effektliste zeigen „Ne/eic/sch/Sta/St"; auch „Custom Path"-Mini-Buttons betroffen. Fix: `minimumSizeHint` statt harter Breiten (Vorbild: Matrix-View daneben, dort voll lesbar). |
| UI-18 | P2 | done ([PR #135](https://github.com/ixamgames-droid/lightos/pull/135)) | Checkbox-Indikatoren im Dark-Theme unsichtbar | Live-Edit-QSS (`vc_multi_live_editor.py`) + globales QSS prüfen — unangehakte `QCheckBox::indicator` haben keinerlei sichtbare Box. Fix: 1 px heller Rand (`#8b949e`) auch im unchecked-Zustand. |
| UI-19 | P2 | ✅ done (dieser PR) | Rohe Tokens app-weit eindeutschen (Options-Labels in die Meta-Schicht) | **Fix:** die Label-Map + reine `option_label(val, key)`/`prettify_option` (Fallback-Kette by_key→Richtung→Option→Prettify, inkl. `loop_mode:"reverse"`→„Rückwärts leeren") aus dem Live-Editor nach `vc_effect_meta.py` (Qt-frei, geteilt) gehoben; `vc_multi_live_editor` delegiert jetzt statt Duplikat. Angewandt zur **Anzeige-Zeit** in: Programmer-Matrix-Combos (color_order + dynamische Select-Params, `rgb_matrix_view.py`) — Label sichtbar, **Rohwert bleibt userData** (kein Save/Load-Bruch); VCStepper/VCEncoder-Untertitel = `_param_label()` (ParamSpec.label statt „runner_count"/„speed") + Options-Wert = Label. **Bewusst ausgeklammert:** Algorithmus-/Stil-Combos (deren `.value` ist der Save-Key, per Bestandsdesign unübersetzt). ParamSpec.options NICHT auf Tupel umgebaut (bräche flache Engine-Consumer). Neuer Test `test_ui19_option_labels.py`; adversariale Review = kein Persistenz-Bruch (alle Combos schreiben currentData). |
| UI-20 | P3 | done | Programmer-Leerzustand: doppeltes „Kein Gerät ausgewählt" ohne Anleitung | `ui/views/programmer_view.py` — Meldung erscheint 2× übereinander, ohne Hinweis, dass links ein Gerät zu wählen ist. Fix: eine Meldung mit Handlungsanweisung; Doppel-Label entfernen. |
| UI-21 | P3 | ✅ done (dieser PR) | Live-View-Kopfzeile: Gerätezähler ohne Initial-Pull | `LiveView.__init__` (`src/ui/views/live_view.py`) rief `_refresh_info()` nie selbst — der Zähler „0 Geräte im Patch" wurde nur über den `_info_timer` (alle 500 ms) aktualisiert (die Präzisierung ggü. der Notiz: es ist der Timer, nicht `PATCH_CHANGED` — `_on_patch_changed` ruft `_refresh_info` gar nicht). Beim Bau einer bereits gepatchten Show blieb der Zähler bis zum ersten Tick (≤500 ms) auf 0 (Bug-Klasse UI-05/UI-09). Fix: `_refresh_info()` am Ende von `__init__` (Initial-Pull). Test `test_live_view_fixes.py::test_info_label_initial_pull`. |
| UI-22 | P3 | done (Batch 12: Anzeige-Sprachkonsistenz; classify_attr-Labels bewusst offen) | Sprachkonsistenz: englische Kategorie-/Widget-Titel neben deutschen | Preset-Browser-Kategorien („Color", „Beam"), VC-Widget-Titel („Cue List" vs. „Cueliste" im Playback). Fix: Anzeige-Mapping vereinheitlichen (keine Datenmigration). |
| UI-23 | P3 | done ([PR #135](https://github.com/ixamgames-droid/lightos/pull/135)) | Live-Edit breit: Vorschau oben ausrichten | `vc_multi_live_editor.py` (Body-Split seit PR #131) — Vorschau-Spalte zentriert vertikal; oben ausrichten (AlignTop), damit kein toter Leerraum über der Vorschau entsteht. |
| UI-24 | P3 | done (Batch 12) | Kleinvieh-Batch aus dem Visual-Audit | Sammel-Item, je 1-Zeilen-Fixes: (a) DMX-Monitor: Rot in Legende erklären + Kontrast; (b) Empty-State-Hinweise für Show Manager/Playback/BPM; (c) BPM „Erkennungsqualität"-Balken mit Kontur bei 0 %; (d) VCColorList: zwei Leerzustands-Hinweise zu einem zusammenfassen; (e) Statuszeile „Enttec: nicht gefunden" klickbar → Output-Einstellungen; (f) Bibliothek-Punktfarben (gelb/blau) Tooltip. |
| UI-25 | P3 | ✅ done (dieser PR) | Sektions-Tab-Breitenverteilung bei Davids realer Fensterbreite (1344 px) unfair | QHBoxLayout verteilte den Platzmangel unfair — `Virtual Console` blieb voll, `E/A` kollabierte zu reinem `...`. **Fix:** die 8 Tabs liegen jetzt in einem eigenen Container `_SectionBar`, der ihre Breite mit einer reinen **Max-Min-Verteilfunktion** `_allocate_tab_widths` fair aufteilt (kurze Labels behalten Volltext, nur lange Titel eliden, Summe ≤ verfügbar → kein Überlauf) + Elide-Guard in `SectionButton` (nie reines `...`, sonst erste 2 Zeichen). Headless-Vergleich belegt die Fairness: bei 1600px zeigte das Original `E/A`=`…` während `Virtual Console` breit blieb — jetzt `E/A`=**`E/A`** voll, `VC` elidiert. Neue Tests `test_ui25_tab_allocation.py` (reine Funktion + Fenster-Fairness + Overlap-frei koordinaten-korrekt); bestehende Overlap-Harness bleibt grün. Hinweis: headless-Tofu-Fonts überzeichnen die Enge; die reale 1344px-Optik am besten per Computer-Use gegenprüfen. |
| UI-26 | P2 | done ([PR #304](https://github.com/ixamgames-droid/lightos/pull/304)) | **2D-Bühne: Fixture-Labels überlappen bei dichter Platzierung → unlesbar** | Live gefunden (Computer-Use, Show `Autonomous_Full_Demo_Rig_2026`): In der 2D-Top-Down-Live-View laufen die Fixture-Namen bei eng platzierten Geräten (obere Moving-Head-Reihe, mittlere PAR-Reihe) komplett ineinander („MH 25MH 25MHLSF 33…") und sind unlesbar; die weiter gespreizte untere PAR-Reihe ist sauber. Zoom hilft NICHT (Label-Text skaliert mit — world-space). Fix-Idee: Labels bei Kollision kürzen/ausblenden/stapeln oder erst ab Zoomstufe/ausreichendem Abstand zeichnen; ggf. screen-space-Mindestabstand. `live_view.py` (Fixture-Label-Zeichnung). **✅ done (dieser PR):** Sub-Agent-Panel entschied — Kernproblem ist das ungemanagte Label-Zeichnen (nicht Icon-Größe; Davids Skalier-Ideen verschieben nur die Schwelle, weil man rauszoomen kann). Umsetzung: **LOD-Labels** (`_lod_for_screen_gap` + `_compute_label_gaps`-Cache am Positions-Choke-Point + `FixtureRenderer.draw(lod=…)`): Detailgrad nach Bildschirm-Nachbarabstand (voll → nur Nummer → aus), Selektion/Hover immer voll — lesbar bei JEDER Dichte/JedemZoom, Nutzer-Platzierung unangetastet. Plus QOL-01-Raster-Default + Icon-Größe per Pref (`fixture_size`, Davids „kleiner"-Wunsch als Self-Service). |
| UI-05 | P2 | done | Programmer-Fokus beim Start initialisieren ([PR #95](https://github.com/ixamgames-droid/lightos/pull/95)) | `ui/views/programmer_view.py` — `programmer_focus` bleibt `None` (Intensity-Tab feuert kein `currentChanged`) → „Intensity gewinnt" erst nach Tab-Wechsel. Fix: einmal `_on_main_tab_changed(currentIndex())` nach connect; Test. [Codex #9](https://github.com/ixamgames-droid/lightos/pull/9#discussion_r3455648382) |
| UI-06 | P2 | done (wontfix bewusst, 2026-07-20 — David: Seeding BEHALTEN; Codex' „nicht persistieren" verworfen, da sonst der Kopf-Unabhängigkeits-Bug zurückkehrt; dokumentiert im `_seed_separate_head`-Docstring) | Color-Tab: Default-Köpfe nicht persistieren — **Design-Entscheidung nötig** | `ui/views/programmer_view.py` (`_seed_separate_head`). **Trade-off (Audit 2026-06-30):** Codex' Fix („nur Anzeige-Fallback, nicht persistieren") **kollidiert mit Davids bewusstem Feature** der Getrennt-Kopf-Unabhängigkeit: Das Tab-Bau-Seeding ist der EINZIG robuste Weg, dass Bewegen von Kopf 0 die anderen Köpfe NICHT mitzieht (deckt Slider, Color-Picker, Palette, VC ab — der DMX-Flush fällt sonst auf Kopf 0 zurück). Ohne Seeding kehrt Davids ursprünglich gemeldeter Bug zurück. → Siehe „🎨 Design-Entscheidungen" unten. Details: [Codex #35](https://github.com/ixamgames-droid/lightos/pull/35#discussion_r3462619556) |
| UI-07 | P2 | done | QuickBars: Representative-Range nur auf kompatible Fixtures ([PR #95](https://github.com/ixamgames-droid/lightos/pull/95)) | `ui/views/programmer_view.py` — gemischte Auswahl nutzt einen Fixture-Range als Template für alle → „open"/Strobe schreibt fremden Range-Mittelwert in inkompatiblen Kanal. Fix: QuickBars auf range-kompatible Fixtures filtern; Test. [Codex #28](https://github.com/ixamgames-droid/lightos/pull/28#discussion_r3462419284) |
| UI-08 | P2 | done | Orientierungs-Tri-State auf Pan/Tilt filtern ([PR #95](https://github.com/ixamgames-droid/lightos/pull/95)) | `ui/views/programmer_view.py` (`_build_orientation_bar`/`_set_orientation`) — statische Fixtures verfälschen Tri-State + Flags werden auf nicht-fähige Fixtures persistiert. Fix: nur Pan/Tilt-fähige berücksichtigen; Test. [Codex #28](https://github.com/ixamgames-droid/lightos/pull/28#discussion_r3462419288) |
| UI-09 | P2 | done | Externe Auswahl in ProgrammerView spiegeln ([PR #95](https://github.com/ixamgames-droid/lightos/pull/95)) | `ui/views/programmer_view.py` — abonniert `SELECTION_CHANGED` nicht → Preset-Browser-Gruppenauswahl im Attribut-Tab unsichtbar/funktionslos. Fix: subscriben + `_selected_fids`/Liste nachziehen; Test. [Codex #13](https://github.com/ixamgames-droid/lightos/pull/13#discussion_r3459492513) |
| ENG-03 | P2 | done | Palette: Phantom-Köpfe vermeiden ([PR #96](https://github.com/ixamgames-droid/lightos/pull/96)) | `core/engine/palette.py` (`apply_to_programmer`) — Mehrkopf-Keys auf Einkopf-Fixture schreiben Bogus-`attr#N`, landen in Snaps/Paletten. Fix: Head-Keys gegen echte Kanal-Vorkommen der Ziel-Leuchte filtern; Test. [Codex #20](https://github.com/ixamgames-droid/lightos/pull/20#discussion_r3461923078) |
| ENG-04 | P2 | done | Palette-Overwrite: stale Werte bereinigen ([PR #96](https://github.com/ixamgames-droid/lightos/pull/96)) | `ui/views/palette_view.py` + `Palette.record_from_programmer` — selektives Überschreiben merged nur, alte Fixture-Werte bleiben und werden später angewendet. Fix: bei Auswahl-Overwrite nicht-Ziel-fids leeren/ersetzen; Test. [Codex #22](https://github.com/ixamgames-droid/lightos/pull/22#discussion_r3462027119) |
| ENG-05 | P2 | done | Preset-Browser: Gruppen-Identität (ID/fids statt Name) ([PR #96](https://github.com/ixamgames-droid/lightos/pull/96)) | `core/engine/preset_search.py` + `app_state` — gleichnamige Gruppen → `select_group_by_name` (`scalar_one_or_none`) scheitert („Gruppe ohne Geräte"). Fix: Gruppen-ID/fids im Eintrag statt nur Name; Lookup per ID; Test. [Codex #13](https://github.com/ixamgames-droid/lightos/pull/13#discussion_r3459492508) |
| ENG-06 | P2 | done | EFX: Spider-Modus nach Auto-Zuweisung aktualisieren ([PR #96](https://github.com/ixamgames-droid/lightos/pull/96)) | `ui/views/efx_view.py` (`_auto_assign_if_empty`) — Auto-Zuweisung nur von Spider-Fixtures ruft `_update_spider_mode()` nicht → Spider-Controls bleiben versteckt. Fix: nach Auto-Zuweisung `_update_spider_mode()` in `_add_efx`/`_start_efx` aufrufen; Test. [Codex #11](https://github.com/ixamgames-droid/lightos/pull/11#discussion_r3458551131) |
| STAB-09 | P1 | done | `reset_show`: re-entranten `patch_changed`-Emit unterdrücken ([PR #93](https://github.com/ixamgames-droid/lightos/pull/93)) | `core/show/show_file.py` (`reset_show`) — der DEMO-03-`clear_patch()` läuft NACH `_replace_patch_from_data` (das `_suppress_emits` im `finally` auf `False` zurückstellt) → feuert mitten im Reset `patch_changed`, während programmer/functions/VC noch alt sind → re-entranter Refresh (genau der STAB-07-Pfad, AV-Gefahr). Fix: den harten `clear_patch()` im `_suppress_emits`-Fenster ausführen; der finale gebündelte `patch_changed`-Emit bleibt einzige Notification; Test. [Codex #90](https://github.com/ixamgames-droid/lightos/pull/90#discussion_r3495290942) |
| ENG-09 | P2 | done | `classify_attr`: `#N`-Kopf-Suffix vor Klassifikation strippen ([PR #97](https://github.com/ixamgames-droid/lightos/pull/97)) | `core/attr_groups.py` (`classify_attr`) — Mehrkopf-Schlüssel `prism_rotation#1` verfehlt den Exact-Match und fällt über den Substring `prism` in **Beam** statt **Effect** → falsches Snap/Szenen-Label für den 2. Kopf. Fix: `attr.split("#",1)[0]` vor der Klassifikation (generell für ALLE Multi-Head-Attribute); Test. [Codex #89](https://github.com/ixamgames-droid/lightos/pull/89#discussion_r3495260316) |
| ENG-10 | P2 | done | `sync_phase`: Stall-Bus-Cache beim Re-Ankern löschen ([PR #97](https://github.com/ixamgames-droid/lightos/pull/97)) | `core/engine/rgb_matrix.py` (`sync_phase`) — nach Bus-Stall gilt `_last_bus_pos == pos`; `sync_phase` re-ankert `_beat_anchor`, lässt aber `_last_bus_pos` stehen → der DEMO-04-Stall-Check überspringt im nächsten Frame die Bus-Sync und free-runt → Sync-Aktion/Phasen-Offset ignoriert, bis der Bus vorrückt. Fix: `_last_bus_pos=None` im Bus-Zweig von `sync_phase`; Test. [Codex #88](https://github.com/ixamgames-droid/lightos/pull/88#discussion_r3495186868) |
| UI-14 | P2 | done | VC-Badge-Farben nach Live-Color-Edit invalidieren ([PR #97](https://github.com/ixamgames-droid/lightos/pull/97)) | `ui/virtualconsole/vc_button.py` (`_color_badge_colors`) — Cache-Key `(action, snap_ids, function_id)` ignoriert den Inhalt der live-editierbaren `ColorSequence` → nach Live-Edit (VCColor `EFFECT`/`add_color`/`toggle_color`) bleibt `_badge_colors` stale (zeigt entfernte/alte Farben + falschen Cycle-Zustand). Fix: Sequence-Signatur (`enabled_colors`) in den Cache-Key; Test. [Codex #87](https://github.com/ixamgames-droid/lightos/pull/87#discussion_r3495059354) |
| DEMO-06 | P3 | done | Generatoren: `_gen_env`-Import auch im Modul-Import-Pfad ([PR #97](https://github.com/ixamgames-droid/lightos/pull/97)) | `tools/build_*.py` — `import _gen_env` resolved nur bei Direkt-Ausführung (Python legt `tools/` auf `sys.path`), nicht bei `python -c "import tools.build_x"` (nur Repo-Root auf Path). Fix: `tools/`-Verzeichnis vor dem Import zu `sys.path` ergänzen (oder paket-relativer Fallback) — zentral in `tools/_gen_env.py`-Importpfad; Test. [Codex #90](https://github.com/ixamgames-droid/lightos/pull/90#discussion_r3495290945) |
| CDX-01 | P1 | done | NET-07 nutzt `_log.warning` statt projekt-`print()` (AGENTS.md-Verstoß) | `app_state.py:~1345` — die NET-07-Warnung nutzt `_log.warning`; AGENTS.md verlangt `print(f"[modul] …")` (kein `logging`), und jede andere Diagnose der Datei nutzt `print`. Fix: auf `print(f"[app_state] …")` umstellen; wenn `import logging`/`_log` danach ungenutzt → entfernen. [Codex-Review](https://github.com/ixamgames-droid/lightos/pull/280) |
| CDX-02 | P2 | done | NET-07: `input_unconfigured` wird in der UI nie gelesen → Status lügt weiter „Aktiv" | `app_state.py:~1347` setzt den Zähler, aber kein `src/ui/`-Code liest ihn (verifiziert: 0 Treffer) → das von NET-07 versprochene „Aktiv, aber wirkungslos" fehlt. Fix: `output_config`-Statuslabel liest `input_unconfigured` und zeigt den Zustand; Test. [Codex-Review](https://github.com/ixamgames-droid/lightos/pull/280) |
| CDX-03 | P2 | done | QXF-Import: FEHLENDES `Number` kollidiert weiter (FIMP-02-Rest) | `qxf_import.py:~524` — `int(ch_ref.get("Number","0"))+1`: ein fehlendes `Number` wird per Default `"0"` → Kanal-Nr 1 → Kollision mit dem echten 1. Kanal; der `except` fängt nur NON-numeric Strings, nicht die fehlende Angabe. Fix: `.get("Number")` ohne Default, `None` → Kanal überspringen + melden (wie non-numeric); Test. [Codex-Review](https://github.com/ixamgames-droid/lightos/pull/280) |
| CDX-04 | P2 | done | VC-PARAMKEY-FN nicht an Slider-Modus gebunden → falsch-positiver Speicher-Block | `validate.py:~294` — der Check feuert für JEDEN `VCSlider`, auch in Modi (EffectSpeed/EffectIntensity/Level), die `param_key` gar nicht nutzen (nur `EFFECT_PARAM` liest ihn). Ein stale `param_key` an so einem Slider → ERROR → `assert_show_dict` blockt Speichern (gleiche Klasse wie die QA-19-Regression). Fix: den `param_key`-Check nur ziehen, wenn der Modus ihn wirklich nutzt (EFFECT_PARAM) bzw. bei Encoder/Stepper; Test. [Codex-Review](https://github.com/ixamgames-droid/lightos/pull/277) |
| CDX-05 | P2 | done | QA-14-Property-Tests schützen CI nicht (hypothesis fehlt + nicht im CI-Lauf) | `ci.yml` installiert nur `pytest pytest-timeout` (kein `hypothesis`) und führt eine feste Datei-Teilmenge ohne `test_render_contract_props.py` → die QA-14-Properties werden in CI still geskippt/nie ausgeführt. Fix: `hypothesis` in den CI-Install (bzw. `requirements-dev.txt`) + `test_render_contract_props.py` in den CI-Lauf aufnehmen. [Codex-Review](https://github.com/ixamgames-droid/lightos/pull/278) |
| CDX-06 | P3 | done | `get_lan_ip` nutzt nur die Internet-Route → falsches NIC/Loopback auf VPN/Multi-NIC/offline-LAN | `web/app.py:~40` — `connect(("8.8.8.8",80))` liefert die Default-Route-Adresse (VPN/kein Internet → Loopback-Fallback), obwohl die Remote-URL genau für isolierte Venue-LANs gedacht ist. Fix: Nicht-Loopback-IPv4-Interfaces enumerieren als Fallback statt nur Loopback. [Codex-Review](https://github.com/ixamgames-droid/lightos/pull/276) |
| CDX-07 | P2 | ✅ done (dieser PR) | Fog-Modus „2-Kanal (Nebel + Lüfter)" — Lüfter nicht getrennt steuerbar | `fixture_db.py:~1078` (`_fog_hazer_modes_data`) vergibt BEIDEN Kanälen das Attribut `dimmer`; `programmer_view.py:1126-1134` dedupliziert Slider pro Attribut → nur EIN „Nebel"-Slider; `app_state.py:~1186` spiegelt den Wert auf `dimmer#1` → der beworbene separate Lüfter-Kanal lässt sich in der UI nicht unabhängig fahren. **Einziger Produktions-Fund** (in meiner FIX-FOG-Arbeit). Fix: Lüfter eigenes Attribut geben (eigener Slider) ODER den 2-Kanal-Modus streichen, wenn getrennte Steuerung nicht vorgesehen ist. |
| CDX-08 | P3 | ✅ done (dieser PR) | Demoshow-Generatoren: Farb-Pads erzeugen kein sichtbares Licht | `tools/build_uxtest3_full.py:63-68` (UND `tools/build_laser_gobo_test.py`: PAR Rainbow/Chase): `b.matrix(...)` läuft mit `drive_intensity=False` → treibt den ZQ01424-Dimmer (Default 0) nicht → PARs bleiben dunkel, wenn nicht separat ein Dimmer-Effekt läuft. `MH ColorFade` (UXTEST-3) schreibt zudem via RGB-Matrix auf `color_wheel` (kennt `rgb_matrix.write` nicht) → totes Pad (im neuen Generator korrekt als `color_wheel`-**Chaser** gelöst, nicht Matrix). Für die „jedes Pad beweist seinen Effekt"-Audit-Shows: `drive_intensity=True` bzw. Basis-Dimmer seeden. |
| CDX-09 | P3 | ✅ done (dieser PR) | Auto-Fit schrumpft bereits gespreizte Ein-Reihen-Rigs (marginal) | `live_view.py:~2162` — der `and`-Guard schützt nur, wenn BEIDE Achsen ≥35 % Sichtfeld füllen; ein breites, flaches Truss/Row-Rig fällt durch und wird von `_compute_fit_zoom` auf die breite Achse eingepasst (kann Fixture-Abstand verringern). Ob schädlich ist debattierbar (ein über-breites Rig einzupassen ist arguably gewollt). Fix (falls angegangen): EINE ausreichend gefüllte Achse ODER ein flächen-/abstandsbasiertes „nicht-geklumpt"-Kriterium. |
| CDX-10 | P3 | ✅ done (dieser PR) | CHANGELOG-Unterabschnitte weichen von AGENTS.md Regel 3 ab (systemisch) | `CHANGELOG.md` nutzt durchgängig `#### Neu / Tests` (VC3D-02, DOC-10, VIZ-14, QOL …), `AGENTS.md:33-35`/Checkliste `:118` nennt nur `#### Neu / Hinzugefuegt`/`#### Behoben`. Entscheidung nötig (EINE Stelle): Header normalisieren ODER AGENTS.md an die gelebte Konvention anpassen. |
| CDX-11 | P3 | ✅ done (dieser PR) | UI-25-Test sichert „kurzer Tab bleibt voll" nicht ab | `tests/test_ui25_tab_allocation.py:108-131` prüft nur den relativen Kürzungs-Anteil (`frac(E/A) ≥ frac(VC)`); eine Regression, die `E/A` teil-elidiert während es noch passt, bliebe unbemerkt. Fix: für die engen Breiten (wo E/A rechnerisch noch passt) explizit `assertEqual(E/A.text(),"E/A")` — ohne den Font-Unabhängigkeits-Kompromiss zu brechen. |
| CDX-12 | P2 | ✅ done (dieser PR) | **[LASER-SAFETY]** Estop: Fenster zwischen Flag-Set und Mask-Push → Channel-Modifier öffnet Laser 1 Frame | `src/core/app_state.py:1070-1072` (`set_laser_estop`-Aktivierungspfad): Flag `laser_estop_active=True` und `_push_laser_estop_mask()` laufen zwar gemeinsam unter `_get_estop_lock`, aber der Render-/Sendepfad nimmt den Lock NICHT — `_send_all` liest das Flag roh (`:2050`) und der OutputManager wendet die zuletzt gepushte Maske separat an. Ein Frame im Fenster zwischen Flag=True und installierter Maske sieht `active=True` (nullt Roh-Laser im Puffer), findet die OutputManager-Maske aber noch leer → ein INVERSE/Range-Lock-Modifier auf einer Laser-Adresse öffnet den Laser für genau diesen Frame wieder (das A3D-01-Sub-Frame-Restfenster). Fix: im **Aktivierungspfad** die Maske VOR dem Flag pushen (Deaktivierung bleibt Flag-dann-leere-Maske → failt safe) ODER Sende-/Modifier-Pass denselben Estop-Zustand atomar beobachten lassen; Regressionstest. [Codex #330](https://github.com/ixamgames-droid/lightos/pull/330#discussion_r3599423350) **✅ done (dieser PR):** `set_laser_estop` ist jetzt reihenfolge-ASYMMETRISCH — **Aktivieren: Ebene-2-OM-Maske installieren, BEVOR das Flag gesetzt wird** (an jedem Interleaving gilt: Maske installiert ODER Flag noch aus → nie ein offenes Fenster); **Deaktivieren bleibt Flag-dann-leere-Maske** (failt safe: alte Maske hält den Laser einen Frame extra dunkel). Dazu `_push_laser_estop_mask(target_active=None)` parametrisiert, damit Maske-vor-Flag nicht aus dem noch-`False`-Flag eine leere Maske ableitet (No-Op); Plan-Rebuild/Latch-Clear-Aufrufer (None → aus Flag) unverändert. Regressionstests in `tests/test_laser_estop_modifier_bypass.py` (Aktivierungs-Ordering — empirisch gegen die Buggy-Reihenfolge als Fail bestätigt · Deaktivierungs-Failsafe · Fenster-Hazard-Kontrollnachweis · Plan-Rebuild-Union). **Zusätzlich (aus der adversarialen Safety-Review, 3 Linsen × verifiziert): dieselbe Fehlerklasse im `_rebuild_render_plan` mitgefixt** — wird ein Laser bei AKTIVEM NOT-AUS re-adressiert, erweitert der Rebuild die Ebene-2-Maske jetzt VOR dem Adress-Swap auf die Vereinigung alt+neu (sonst deckte die Maske im Rebuild-Fenster nur die alte Adresse, während Ebene 1 schon die neue nullte → Modifier konnte die neu adressierte Laser-Adresse öffnen). Guarded (`if laser_estop_active`), deadlock-frei. |
| CDX-13 | P2 | done (2026-07-20 — David: Latch ENG halten/Safety; `intensity` bleibt bewusst draußen, Rearm über gobo_wheel/FB4-Cue; dokumentiert am `_LASER_REARM_ATTRS` + Safety-Test `test_intensity_on_laser_keeps_latch`) | **[LASER-SAFETY/Design]** Rearm-Whitelist ohne `intensity` → FB4-Laser (Dimmer = `intensity`) per Helligkeitsregler nicht wieder-scharfschaltbar | `src/core/app_state.py:59` (`_LASER_REARM_ATTRS = {shutter, laser_bank, gobo_wheel, macro}`) + `set_programmer_value:1094`. FB4-Laser-Modi exponieren Helligkeit als `intensity` (`fixture_db.py:897`, `:982`); nach NOT-AUS hebt ein Programmer-Write auf `intensity` den Latch NICHT auf. **Design-Entscheidung nötig (wie UI-06):** `intensity` in die Whitelist = FB4-Regler entriegelt wieder, ABER `intensity` ist sehr breit — ein Szenen-/Snapshot-Apply mit Laser-`intensity>0` über `set_programmer_value` würde den NOT-AUS ungewollt lösen (genau das, was A3D-02 eng halten wollte). Ein Rearm-Pfad existiert bereits (FB4-„Cue" = `gobo_wheel`, whitelisted). David wählt: `intensity` aufnehmen (Usability) vs. Latch eng halten (Safety). [Codex #331](https://github.com/ixamgames-droid/lightos/pull/331#discussion_r3600490396) |
| CDX-14 | P2 | ✅ done (dieser PR) | BPM: A3D-17-Race bleibt ordnungsabhängig — `_source`-Write und `set_bpm` nicht atomar — **Fix:** `set_bpm(bpm, source=…)` schreibt Quelle+Wert unter EINEM Lock-Hold; alle 3 internen Aufrufer (`_set_manual`/`request_bpm`/`_apply_detected_bpm` inkl. Audio-Pfad) reichen die Quelle atomar durch, externe Low-Level-Aufrufer unverändert; deterministischer Regressionstest `tests/test_bpm_manager_source_race.py`. | `src/core/engine/bpm_manager.py:184-187` (`set_bpm`): `request_bpm:208-210`/`_set_manual:193-196` schreiben `_source` in einem SEPARATEN Lock-Fenster VOR `set_bpm`. Schlägt `reset()` (setzt unter Lock `_source='off'`/`_bpm=0`) zwischen Source-Write und `_bpm`-Zuweisung zu, schreibt `set_bpm` ein positives `_bpm`, ohne die Source zu restaurieren (der `<=0`-Zweig greift nicht) → `_bpm>0` bei `current_source=='off'`. Der A3D-17-Fix koppelte nur `_bpm`+abgeleitetes `_source` INNERHALB `set_bpm`, nicht die Source der Aufrufer. Fix: Source + BPM atomar unter EINEM Lock-Hold (z. B. `set_bpm(bpm, source=…)` oder Lock über beide Writes in request_bpm/_set_manual); Test. [Codex #334](https://github.com/ixamgames-droid/lightos/pull/334#discussion_r3604018193) |
| CDX-15 | P2 | ✅ done (dieser PR) | VC-Asset-Cache: `store_extracted` prüft Bytes nicht gegen Content-Hash → globaler Cache dauerhaft vergiftbar — **Fix:** Hash-Verifikation (`content_matches_key`) verwirft Bytes, deren sha1 nicht zum Key passt; Test `tests/test_vc_asset_poison.py`. | `src/core/show/vc_assets.py:97-102` (`store_extracted`) akzeptiert beliebige Bytes für einen syntaktisch gültigen `<sha1>.<ext>`-Key; `_write_atomic` überschreibt bestehende Dateien nicht. Eine korrupte/untrusted `.lshow` mit `assets/vc/<sha1-of-good>.png` = anderer Inhalt vergiftet den Key im globalen Cache (`%APPDATA%/LightOS/vc_assets/`) DAUERHAFT → spätere legitime Shows mit demselben Key rendern/re-speichern das falsche Asset. `store_bytes` hasht selbst und ist sicher; nur der Entpack-Pfad vertraut dem Key. Fix: vor dem Schreiben `hashlib.sha1(data).hexdigest()` gegen den 40-Hex-Präfix prüfen, bei Mismatch verwerfen (wie ungültiger Key); Test. [Codex #324](https://github.com/ixamgames-droid/lightos/pull/324#discussion_r3597866305) |
| CDX-16 | P2 | ✅ done (dieser PR) — **Zahl im Ur-Text war zu hoch: nicht 34, sondern 19 offene Zeilen waren wirklich gekappt** (bei den übrigen passte der Text in die Kappungsbreite). Regeneriert aus `docs/AUDIT_3D_DEEP_2026-07-16.md`: **40 von 40** A3D-Zeilen konnten zugeordnet werden (Titel-Präfix-Match nach Strippen des `[Bereich]`-Präfixes, **Dokument-Reihenfolge als unabhängige Gegenprobe: 40/40 an gleicher Position**). Repariert wurden **6 Titel + 12 DoD**; Diff exakt 13↔13 Zeilen (keine Zeile verloren oder dupliziert). **Bewusst konservativ:** (a) `done`-Zeilen wurden NIE angefasst — sie tragen handgeschriebene Abschluss-Notizen, die in der Audit-Quelle gar nicht stehen; (b) das `**Ort:**`-Präfix jeder DoD blieb erhalten (es stand am Zellenanfang und war nie gekappt, trägt aber die Codex-Nummer), ersetzt wurde nur ab `**Bug:**`; (c) die P2-Befunde bekommen zusätzlich das `**Szenario:**` aus der Quelle, das der Ur-Generator komplett verworfen hatte. Verifiziert: 0 offene Zeilen enden noch mitten im Wort. | `BACKLOG.md` (A3D-Sektion, ~Z. 680+). Der Generator, der die 40 A3D-Zeilen aus dem Audit erzeugte, kappte Bug- UND DoD-Text pro Feld: A3D-01 „…Zeile 300-301 \`ge" (offener Code-Span), A3D-03 „…poch-Pruef", A3D-05 endet „…gemeinsamer Helfe", A3D-20 „…Das Ergebn" — verlorener Fix/DoD. Betrifft die **34 offenen (todo) Items** (A3D-03..15, A3D-20..40), die der Loop als Arbeitsquelle nutzt (die 6 „done" sind unkritisch). Fix: A3D-Zeilen ungekürzt aus der Quelle `docs/AUDIT_3D_DEEP_2026-07-16.md` neu erzeugen/nachtragen. [Codex #329](https://github.com/ixamgames-droid/lightos/pull/329#discussion_r3599276770) |
| CDX-17 | P3 | ✅ done (dieser PR) | Zombie-Release: re-gepatchte Adresse bleibt in `_pending_release` → neu-gepatchtes Fixture 1 Frame auf 0 — **Fix:** finale Nullung filtert `rel` gegen `patched_set[univ]` (nur wirklich-ungepatchte nullen), `pop`-Konsum bleibt unbedingt; Test in `tests/test_zombie_channel_release.py`. | `src/core/app_state.py:2084-2090`. Wird eine Adresse entfernt und vor dem nächsten Render-Tick wieder gepatcht (Bulk-Show-Load, schnelles Undo/Redo), enthält `_pending_release` sie noch aus dem früheren Rebuild (`_release_unpatched_addrs` merkt nur beim Entfernen vor, räumt beim Re-Patch nicht ab); der `pop`+bedingungslose Nullung NACH dem Span-Commit zwingt das neu-gepatchte Fixture für diesen Frame auf 0. Fix: `rel` gegen den aktuellen `patched_set[univ]` filtern (nur wirklich-ungepatchte nullen) ODER re-gepatchte Adressen beim Rebuild aus `_pending_release` entfernen; Test. [Codex #333](https://github.com/ixamgames-droid/lightos/pull/333#discussion_r3600797419) |
| CDX-19 | P3 | ✅ done (dieser PR) | Referenz-Show `build_komplette_animierte_show`: „Nebel an"/„BLACKOUT" liegen ausserhalb des erreichbaren VC-Canvas | `tools/build_komplette_animierte_show.py:146,149` platziert Buttons bei x=1220/1370 (Breite 140). `VCCanvas` hat `setMinimumSize(1200,800)` (`src/ui/virtualconsole/vc_canvas.py:122`) und die Scroll-Area ist `setWidgetResizable(False)` (`src/ui/views/virtual_console_view.py:397`), ohne Kinder-Rect-Wachstum → Widgets bei x≥1200 sind im normalen VC-View nicht erreichbar/scrollbar. Fix: die letzten Buttons in eine zweite Reihe umbrechen (x<1060) ODER den Canvas in der generierten Show explizit breiter setzen; Show neu generieren. [Codex #327](https://github.com/ixamgames-droid/lightos/pull/327#discussion_r3598531360) |
| CDX-20 | P3 | ✅ done (dieser PR) | Referenz-Show `build_komplette_animierte_show`: „PAR Strobe" blitzt nur den Dimmer, keine Farbe → physisch schwarz | `tools/build_komplette_animierte_show.py:115-124` (`par_strobe`-Chaser) alterniert nur `intensity`/`dimmer` 255↔0; die ZQ01424-RGBW-Kanäle starten bei 0 (`src/core/database/fixture_db.py:371-375`) → der PAR bleibt schwarz, obwohl der Dimmer blitzt. Der Button soll einen echten Strobe eigenständig demonstrieren. Fix (wie CDX-08): in den Strobe-Schritten zusätzlich einen sichtbaren Farb-/Weiß-Wert setzen (oder den Shutter-Kanal treiben); Show neu generieren. [Codex #327](https://github.com/ixamgames-droid/lightos/pull/327#discussion_r3598531365) |
| CDX-22 | P1 | ✅ done ([PR #433](https://github.com/ixamgames-droid/lightos/pull/433)) | STAB-19b-Folge: `blackout_output=False` verhindert den Blackout-Puls NICHT — jeder Live-Show-Load blitzt die alten Adressen 1 Frame schwarz | `src/core/show/show_file.py:669` (`_reset_state` ruft **bedingungslos** `_replace_patch_from_data(state, [])`), erreicht aus `load_show` reset-first bei `:995` (`_reset_state(..., blackout_output=False)`). Der leere `replace_patch([])` läuft durch `_reload_patch_cache`→`_rebuild_render_plan`→`_release_unpatched_addrs` (`app_state.py:1097`+`:1141`), das JEDE zuvor gepatchte Adresse SOFORT im Live-Universe auf 0 setzt (`uni.set_channel(a,0)`) + in `_pending_release` vormerkt. Der 44-Hz-Output-Thread liest die genullten Universes und sendet 0 auf alle alten Adressen, bis der neue Patch geladen+gerendert ist → genau der Puls, den `blackout_output=False` vermeiden sollte. Der `blackout_output`-Guard (`:733`) überspringt nur den EXPLIZITEN `_u.clear()`/`_flush_all_to_dmx()`, nicht die `replace_patch([])`-Nullung. Fix: den Patch-/Render-Plan im reset-first NICHT leeren (der folgende `_replace_patch_from_data(patch)` baut ihn ohnehin neu) ODER die Unpatched-Address-Freigabe unterdrücken, bis der Ersatz-Patch steht; Regressionstest (Live-Load nullt keine noch-gepatchte Adresse). [Codex #386](https://github.com/ixamgames-droid/lightos/pull/386#discussion_r3615722099) **✅ GELANDET (2026-07-26, PR #433) — es waren ZWEI Vektoren, das Item nannte nur den ersten.** Nachgewiesen, indem jeder `set_channel(addr, 0)` während eines Live-Loads mitgeschnitten wurde: ohne Fix `[21, 22, 21, 22]` (2 Adressen × 2 Vektoren), mit Fix keiner. (1) Der leere Zwischen-Patch → neue **`AppState.deferred_unpatched_release()`** bündelt die A3D-18-Freigabe über den gesamten, mehrstufigen Patch-Tausch (im Fenster nur merken, beim Verlassen EINMAL gegen den dann gültigen `_patched_set` freigeben; re-entrant, `finally`-fest). Der Patch-/Render-Plan wird also weiterhin geleert (Mirror-Pflicht + DEMO-03-`clear_patch` unangetastet) — nur die Adress-Freigabe wartet. (2) **NEU gefunden:** der Programmer-Clear in `_replace_patch_from_data` flushte, **während der ALTE Patch noch geladen war**, jedes alte Fixture auf seine Kanal-Defaults (Dimmer 0) → `clear_programmer(flush=False)` unterdrückt genau diesen DMX-Flush im Ladepfad (der Loader flusht direkt danach gegen den NEUEN Patch; alle anderen Aufrufer behalten `flush=True`, mit Gate-Test). **Safety-Ausnahme:** Adressen bisheriger DMX-Laser werden auch im Fenster sofort freigegeben — solange der Plan sie nicht kennt, greift an ihnen weder die Renderer-Nullung (Ebene 1) noch die OM-Maske (Ebene 2) eines ERST DANN ausgelösten NOT-AUS. Test `test_cdx22_load_no_blackout_pulse.py` (11). |
| CDX-23 | P2 | ✅ done ([PR #435](https://github.com/ixamgames-droid/lightos/pull/435), Status nachgezogen 2026-07-27 — stand noch auf „PR folgt") ( `set_bpm(..., only_if_auto=True)` = **Compare-and-Set** — der Modus-/Lock-Re-Check läuft unter DEMSELBEN Lock-Hold wie der Write, statt den Lock in den Aufrufern über die Callback-Emits zu spannen. `request_bpm`/`_apply_detected_bpm` setzen das Flag; ein verworfener Write unterdrückt auch den State-Emit. Manuelle/restaurierende Pfade (`_set_manual`, Tap/Nudge, Freeze-Auftauen in `tempo_bus.toggle_freeze`) bleiben unverändert, weil `only_if_auto` opt-in ist. Test `test_cdx23_bpm_turnoff_race.py` (8) mit DETERMINISTISCH injiziertem Fenster — der reine Thread-Stresslauf reproduzierte den Bug NICHT, die Injektion (Flip in `_clamp_bounds` bzw. um den Write herum) schon: 3 Race-Tests rot auf dem alten Code, grün mit Fix) | A3D-17b-Folge: `turn_off` serialisiert nicht mit in-flight AUTO-BPM-Writes → „0/aus" springt doch zurück | `src/core/engine/bpm_manager.py` — die Modus-Guards in `request_bpm:214` und `_apply_detected_bpm:488` (`if _mode != AUTO: return`) lesen `_mode` UNGELOCKT; das folgende `set_bpm(bpm, source=…)` (`:172`) schreibt `_bpm`/`_source` unter Lock, prüft `_mode` aber NICHT erneut. Hat ein AUTO-Thread (Audio/OS2L/Timeline) den Guard schon passiert, BEVOR `turn_off:319` `_mode=MANUAL` flippt + `reset()` nullt, schreibt sein `set_bpm` danach ein positives `_bpm` bei `_mode==MANUAL` → der von A3D-17b bekämpfte „springt zurück"-Effekt kehrt zurück. **Distinkt von CDX-14/14b** (die koppelten nur `_source`+`_bpm` in `set_bpm`, NICHT den Modus-Guard+Write). Der A3D-17b-Fix (MANUAL zuerst unter Lock) schließt nur das Fenster für Guards NACH dem Flip, nicht die bereits in-flight Guard+Write-Sequenz. Fix: Guard+Write in den AUTO-Writern atomar unter EINEM Lock-Hold ODER `set_bpm` re-prüft `_mode==AUTO` für source-getaggte AUTO-Writes; Test mit injiziertem in-flight-Beat. [Codex #389](https://github.com/ixamgames-droid/lightos/pull/389#discussion_r3616172913) |
| CDX-24 | P2 | ✅ done (2026-07-27, [PR #456](https://github.com/ixamgames-droid/lightos/pull/456)) | web-remote-Anleitung bewirbt Sicherheits-Bedienelemente, die es in der UI nicht gibt | **Umsetzung:** **Davids Entscheidung: UI nachziehen + Doku.** Neuer Dialog „Ausgabe → Web-Remote: Verbindung & Token…" (oeffnet sich auch beim Einschalten statt der bisherigen reinen Info-Box): Adresse + Token, Direkt-Link als selektierbares Feld mit „Link kopieren", Button „Token neu erzeugen" (mit Rueckfrage) und Checkbox „LAN-/Handy-Remote". Bewusst ein EIGENER Menuepunkt — der in der Anleitung dokumentierte Incident-Pfad („bei Verdacht Token neu erzeugen") muss WAEHREND einer laufenden Show begehbar sein. **Zwei Fallen, die beim Verifizieren auffielen und mitgefixt sind:** (1) `regenerate_token()` allein wirkt NICHT am laufenden Server — der Gate liest das Token aus `app.config`, dort landet es nur beim `create_app`; der Server akzeptierte also weiter den ALTEN `?k=`-Link. Neuer public Helfer `app.refresh_running_token()` (das `_flask_app` ist modulprivat), und der **falsche Docstring** von `regenerate_token` („wirkt SOFORT am laufenden Server") ist korrigiert. (2) `is_lan_remote_enabled()` wird NUR in `start_server` gelesen — ein Umschalten am laufenden Server waere wirkungslos gewesen: der Nutzer glaubt „aus", der Port bleibt im LAN offen (NET-09-Falle). Der Toggle startet den laufenden Server daher neu (`stop_server` gibt den Socket sofort frei) und meldet das in der Statusleiste. Doku angeglichen inkl. der Nebendrift „Bind-Adresse: 0.0.0.0" (real bedingt seit NET-01); das im Design-Doc als Follow-up geparkte **NET-02-UI** ist damit geschlossen, der tote BACKLOG-Verweis darauf entschaerft. Test `tests/test_cdx24_web_remote_ui.py` (8). **Beim Review-Vorlauf zusaetzlich gefunden und gefixt (Safety):** die Rotation war ein HALBES Versprechen — das `before_request`-Gate sperrt jede HTTP-Anfrage der alten Epoche, ein SCHON VERBUNDENER Socket.IO-Client laeuft aber nicht mehr durch dieses Gate, und die Event-Handler (`go`/`back`/…) pruefen einzeln gar nichts. Ein vor der Rotation verbundenes Handy haette also weitergesteuert. Jetzt: `on_connect` prueft zusaetzlich die **Epoche** (nicht nur `authed`), und neue `disconnect_all_clients()` kappt beim Rotieren alle offenen Verbindungen (defensiv — ein Fehler dort darf die Rotation nie kippen). — `docs/anleitung_web_remote/ANLEITUNG.md:~121-125` verweist auf „**Token neu erzeugen**" (macht alte Links ungültig) und den Toggle „**LAN-/Handy-Remote**" als begehbaren Sicherheitspfad (kompromittiertes Token / nur-lokal). Aber `regenerate_token()` (`src/web/remote_settings.py:113`) und `set_lan_remote_enabled()` (`:129`) werden NUR aus Tests aufgerufen; die UI (`src/ui/main_window.py`) liest nur `is_lan_remote_enabled()` (`:1854`) und zeigt das Token in einer Info-`QMessageBox` (`_toggle_web_server`) — kein „Token neu erzeugen"-Knopf, kein LAN-Toggle. Der Leser kann dem dokumentierten Safety-Pfad nicht folgen. Fix: die Doku markiert die Controls als (noch) nicht UI-erreichbar bzw. dokumentiert den echten Preference-Editier-Weg — ODER (besser) die zwei Controls werden in der UI nachgezogen. [Codex #382](https://github.com/ixamgames-droid/lightos/pull/382#discussion_r3613918673) |
| CDX-25 | P2 | ✅ done (dieser PR) — **Ur-Befund war ein False-Positive, drei echte Lücken daneben gefixt.** Der Button IST global: `estop_all()` ruft selbst `self._state.set_laser_estop(True)` (`laser_output.py`, seit UXT-12/PR #194), und `clear_estop_all()` hebt den Latch NICHT auf (es setzt nur `_estopped=False` + öffnet die Netzwerk-Session). Die Doku-Zeile `01_button.md:90` ist damit korrekt — die im Item vorgeschlagene „Netzwerk-only"-Umscopung wäre eine **Verschlechterung** gewesen. Beim Verifizieren gefunden und gefixt: **(1) Reihenfolge (safety-relevant):** der DMX-Latch wurde erst NACH der Netzwerk-I/O-Schleife gesetzt; `conn.estop()` ist ein Socket-Roundtrip mit `timeout=0.5` je Verbindung (`etherdream.py:146`, `idn.py:236`) → bei einem hängenden/abgezogenen DAC blieben DMX-Laser bis zu 0,5 s **pro Gerät** hell, obwohl NOT-AUS gedrückt war. Jetzt fail-safe: billige lokale Verriegelung ZUERST, teure Netzwerk-Zustellung danach — dieselbe Asymmetrie wie CDX-12. **(2) Stiller Fehler:** `except Exception: pass` um genau diese Verriegelung liess einen misslungenen NOT-AUS wie einen erfolgreichen aussehen → wird jetzt protokolliert (Netzwerk-Ebene läuft unabhängig weiter). **(3) Kein End-to-End-Netz:** `test_laser_vc_safety.py` prüft den VC-Button nur gegen einen Fake-Manager. Neuer Test `tests/test_cdx25_laser_estop_order.py` (6): Reihenfolge-Beweis über eine Fake-Verbindung, die den Latch-Zustand im Moment des `conn.estop()` mitschreibt (3 Tests auf altem Code **rot verifiziert**), Latch trotz werfender Verbindung, Fehler-Sichtbarkeit, Renderer-Ende-zu-Ende, `clear_estop_all` löst den Latch nicht, und VC-Button gegen den ECHTEN Manager. | ~~`src/ui/virtualconsole/vc_button.py:1060-1074` (`LASER_ESTOP`) ruft ausschließlich `state.ensure_laser_output().estop_all()/set_armed(False)/clear_estop_all()` (Netzwerk-Laser-Streamer) — NIE `AppState.set_laser_estop()` (die DMX-Laser-Verriegelung aus A3D-01/CDX-12). Ein NOT-AUS auf DMX-Laser-Fixtures (z. B. L2600) hebt daher nichts an. Die Doku `docs/anleitung_vc_widgets/01_button.md:90` verspricht „Laser-Not-Aus: sofort dunkel + entwaffnen" GLOBAL, während Zeile 89 (Arm/Disarm) bewusst „**Netzwerk**-Laser-Ausgang" scopt. Ein Nutzer verlässt sich für DMX-Laser-Blackout auf diesen Knopf. Fix (Safety-korrekt): den Button zusätzlich `set_laser_estop(True)` auslösen lassen (echter globaler Laser-Kill) — ODER minimal die Doku-Zeile 90 auf Netzwerk-only scopen wie Zeile 89. **David entscheidet (wie CDX-13):** echter Global-Estop (Code, sicherer) vs. Doku-Scope (schnell).~~ (Ur-Text durchgestrichen: die Prämisse „ruft NIE `set_laser_estop`" stimmt nicht — der Aufruf sitzt eine Ebene tiefer in `estop_all()`. Die Entscheidungsfrage entfällt damit.) [Codex #383](https://github.com/ixamgames-droid/lightos/pull/383#discussion_r3614888361) |
| CDX-26 | P2 | ✅ done (dieser PR) — Zeile `Laser-Muster (Palette)` in die Einstellungs-Tabelle von `01_button.md` ergänzt, an der **echten Dialog-Position** (zwischen `Gruppe (SelectGroup)` und `Tempo-Bus`, verifiziert an `vc_button.py:1967`). Zusätzlich als **Pflichtfeld** markiert mit dem expliziten Hinweis, dass die Aktion ohne Auswahl ein stilles No-Op ist (`vc_button.py:1100` `if press and self.laser_palette`) — genau die Falle aus dem Befund — plus Querverweis auf `anleitung_laser/ANLEITUNG_LASER.md`, wo die Muster angelegt werden. | button-Anleitung: der für „Laser-Muster abrufen" nötige Palette-Selektor fehlt in der Einstellungs-Tabelle | `docs/anleitung_vc_widgets/01_button.md` (Abschnitt „## Einstellungen", Tabelle ab `:49`) hat keine Zeile für den Palette-Selektor. Die Aktion `Laser-Muster abrufen` ist aber ein No-Op ohne gesetzte Palette (`vc_button.py:1082` `if press and self.laser_palette`); der Dialog exponiert sie als „**Laser-Muster (Palette)**" (`vc_button.py:1708-1719`). So kann ein Nutzer die Aktion hinzufügen, aber den Pflicht-Selektor nicht entdecken. Fix: eine `Laser-Muster (Palette)`-Zeile in die Einstellungs-Tabelle neben der neuen Aktion. [Codex #383](https://github.com/ixamgames-droid/lightos/pull/383#discussion_r3614888366) |
| CDX-27 | P2 | ✅ done (dieser PR) — **war zur Hälfte stale:** der Guide `02_fader.md` listet seit PR #415 (Commit `866279f`) bereits alle 13 Modi inkl. `Feature-Dimmer (Gruppe)` — in BEIDEN Tabellen (Einstellungs-Zeile „Modus" und „Die Modi im Klartext", je 13 Einträge nachgezählt). Real offen war nur das falsche Verdikt im datierten Report `ANLEITUNGEN_AUDIT_2026-07-20-p3b.md:11`. **Davids Entscheidung: als revidiert markieren statt still korrigieren** — die Zeile steht jetzt auf `⚠️ revidiert` und unten im Report ein Nachtrag mit Datum/Begründung/Beleg. Damit ist zugleich die **Konvention für nachträglich falsche Audit-Verdikte** etabliert (erster Präzedenzfall im Repo, betrifft auch CDX-29): datierte Reports werden nie rückwirkend umgeschrieben, sondern revidiert + ergänzt — eine stille Korrektur würde genau die Information vernichten, dass der Audit sich geirrt hat. | fader-Anleitung + p3b-Audit sagen 12 Modi „exakt", `SLIDER_MODE_LABELS` hat 13 (`Feature-Dimmer (Gruppe)` fehlt) | `src/ui/virtualconsole/vc_slider.py` `SLIDER_MODE_LABELS` listet **13** Modi inkl. `(SliderMode.FEATURE_DIMMER, "Feature-Dimmer (Gruppe)")`; `docs/anleitung_vc_widgets/02_fader.md:42` zählt nur **12** auf (ohne Feature-Dimmer). `docs/ANLEITUNGEN_AUDIT_2026-07-20-p3b.md:11` markiert das als „12-Modi-Liste = SLIDER_MODE_LABELS … **exakt**" → verdeckt einen stalen Guide. Fix: `Feature-Dimmer (Gruppe)` in `02_fader.md` ergänzen (Modus-Zeile + Erklärung) und den Audit-Eintrag auf 13/korrigiert setzen; Test/Doc-Check. [Codex #383](https://github.com/ixamgames-droid/lightos/pull/383#discussion_r3614888369) |
| QA-26 | P1 | ✅ done | CI ist rot auf `main`: Schritt „Test-Ergebnis zusammenfassen" scheitert reproduzierbar nur unter Python 3.11 | `.github/workflows/ci.yml` — Der Job `Tests (Python 3.11)` schlägt seit mindestens 4 aufeinanderfolgenden Läufen fehl (zuletzt [Run #598](https://github.com/ixamgames-droid/lightos/actions/runs/29226430309), 2026-07-13, Commit `dff75bf` — ein **reiner Docs-Commit**), obwohl der Schritt `Unit-Tests ausfuehren` im selben Job **grün** ist und der Job `Tests (Python 3.12)` komplett durchläuft. Fehlschlagender Schritt ist ausschließlich `Test-Ergebnis zusammenfassen`, der **dieselbe Suite ein zweites Mal** im selben Workspace fährt (`python -m pytest tests/test_show_file.py tests/test_core_engine.py --tb=no -q`). **Verdacht:** nicht-idempotenter Zweitlauf — der erste Lauf hinterlässt SQLite-State (`data/current_show.db` / `fixtures.db`), mit dem der Zweitlauf kollidiert (gleiche Bugklasse wie QA-06, „already exists"); warum es nur 3.11 trifft, ist offen (Log-Download braucht Admin-Rechte, HTTP 403). **DoD:** Ursache am Actions-Log belegt (Traceback/`file:line`); Fix bevorzugt durch **Entfernen des redundanten Doppellaufs** — Summary aus dem ersten Lauf erzeugen (z. B. `--junitxml` + Report-Step) statt die Suite zweimal auszuführen; Regression: ein Lauf auf `main` grün für 3.11 **und** 3.12. Gemeldet von David 2026-07-13. **Verifiziert 2026-07-16 (Claude):** CI auf `main` ist aktuell gruen (letzte 6 Runs `success`, 3.11 **und** 3.12) — die rote Serie ist nicht mehr reproduzierbar. Der redundante Doppellauf besteht laut Code-Pruefung weiter (`ci.yml`), aber die vermutete SQLite-Ursache traegt nicht: die Suite nutzt `sqlite:///:memory:` pro Prozess (zwei pytest-Prozesse teilen keine In-Memory-DB). Restarbeit = reine CI-Hygiene (redundanten Doppellauf entfernen), kein rotes Gate mehr. **✅ Abgeschlossen 2026-07-17 (Claude):** der redundante Doppellauf wurde mit [#297](https://github.com/ixamgames-droid/lightos/pull/297) entfernt — `ci.yml` hat jetzt nur noch EINEN Testschritt (`Unit-Tests ausfuehren`, exit-gehaertet via #281). Letzte 8 main-Runs `success`, aktueller Run job-genau gruen fuer **3.11 UND 3.12** verifiziert. DoD erfuellt. |
| QA-24 | P2 | ✅ done (2026-07-08, Runner-Tooling) | Lock-Runner klassifizierte nativen Crash nicht-deterministisch als Failure → falsch-rote Gates | `run_tests.ps1 -Isolate` zählte nur `rc=139`/`-1073741819` als CRASH; **jeder** andere Nicht-Null-Code → FAIL/rot. Dieselbe viz13-QtWebEngine-Teardown-Instabilität crasht aber mit wechselndem nativem Code (mal `-1073741819` AV → grün, mal `-2147483645` 0x80000003 Breakpoint → FAIL/rot) → identische Baseline kippte das Gate zufällig. **Fix:** Crash-Erkennung auf die ganze Familie erweitert — Unix 128+Signal (`129..192`) ODER Windows-NTSTATUS (große negative Codes, `rc < -100000000`); echte pytest-Failures (Exit 1..5) bleiben FAIL/rot. Verifiziert (alle Codes + E2E: `2 ok, 0 Failures, 5 Crashes → exit 0`). **Hinweis:** `run_tests.ps1` liegt AUSSERHALB des Repos (Davids sitzungsübergreifendes Tooling im äußeren Ordner) → nicht versioniert; Doku: SecondBrain `reference_pytest_lock`. |
| QA-23 | P2 | ✅ done ([PR #199](https://github.com/ixamgames-droid/lightos/pull/199)) | MainWindow-Tests hängen am Autosave-Recovery-Dialog (Baseline-Bruch, entdeckt 2026-07-07 im PR-#198-Gate) | Lag eine Autosave-Datei neuer als alle Recents vor, öffnete `_check_autosave_recovery` beim MainWindow-Bau (via `singleShot(500)`) ein modales `QMessageBox.question` → headless blockierte in den pytest-Timeout: `test_ui_polish_audit.py` + `test_vc_canvas_clear_on_new_show.py` (die einzigen zwei MainWindow-bauenden Tests) fielen zustandsabhängig. **Fix:** `_recovery_prompt_suppressed()` (`LIGHTOS_NO_RECOVERY_PROMPT`-Env, conftest-gesetzt, ODER `QT_QPA_PLATFORM=offscreen`) — Guard VOR jedem Dateizugriff + `singleShot` gar nicht erst geplant; Live-Recovery in der echten App unverändert. Regressionstest `tests/test_autosave_recovery_headless.py` (A: headless fragt nie/kein Modal; B/C: Live-Logik fragt genau einmal, Yes stellt wieder her; `_autosave_path` strikt auf Temp). |
| QA-06 | P2 | done | `test_vc_tempo_live_coupling` fixtures.db-Setup-Kollision ([PR #104](https://github.com/ixamgames-droid/lightos/pull/104)) | Root-Cause war eine **TOCTOU-Lücke in `create_all(checkfirst=True)`**: greifen zwei Verbindungen/Läufe auf dieselbe SQLite-Datei zu, reflektiert die eine `sqlite_master`, bevor der parallele CREATE der anderen committet ist → eigener CREATE kollidiert mit „already exists". `checkfirst` allein verhindert das nicht. Fix: neuer Helfer `models.create_all_idempotent` schluckt nur „already exists", verdrahtet in `open_show`/`create_db`/`get_engine` (conftest darf fixtures.db NICHT umlenken — feste `fixture_profile_id`). Baseline danach 274/274 grün. |
| VCB-01 | P1 | done | VCFrame.switch_page traversiert Grandchildren → versteckt Kinder innerer Frames ([PR #100](https://github.com/ixamgames-droid/lightos/pull/100)) | `vc_frame.py:switch_page` |
| VCB-02 | P1 | done | add_child_to_page: `widget.show()` überschreibt `setVisible` (falsche Seite sichtbar) ([PR #100](https://github.com/ixamgames-droid/lightos/pull/100)) | `vc_frame.py:add_child_to_page` |
| VCB-03 | P1 | done | _open_properties: Seitenzahl-Reduktion klemmt `_current_page` nicht ([PR #100](https://github.com/ixamgames-droid/lightos/pull/100)) | `vc_frame.py:_open_properties` |
| VCB-04 | P1 | done (bereits impl. vc_canvas.on_active_bank, verifiziert) | VCFrame-Kinder erben Bank-Index statt -1 → MIDI/Hotkey stumm nach Bank-Wechsel. **Deferiert:** der einfache Fix (`bank=-1` beim Umhängen) ist nur korrekt, wenn der Frame selbst `bank=-1` ist; bei Frame auf fester Bank würde das Kind auf allen Bänken reagieren. Braucht saubere Lösung (MIDI-Dispatch wertet die Bank des Eltern-Frames für Frame-Kinder) → genauere Analyse. | `vc_canvas.py:_add_widget` / `vc_frame` |
| VCB-05 | P1 | done | `fixture_dimmers` (F-25) bei reset/load nicht geleert → Ghost-Dimmer ([PR #100](https://github.com/ixamgames-droid/lightos/pull/100); `feature_dimmers`/F-26 nicht auf main → Teil von F-26b) | `show_file.py:reset_show`/`load_show` |
| VCB-06 | P1 | done | „Alle löschen" (Kontextmenü) nicht Undo-bar ([PR #100](https://github.com/ixamgames-droid/lightos/pull/100)) | `vc_canvas.py:_context_menu` |
| VCB-07 | P1 | done | `set_param_normalized`: color/action-Kinds → AttributeError (`spec.min/max`) ([PR #100](https://github.com/ixamgames-droid/lightos/pull/100)) | `effect_live.py:set_param_normalized` |
| VCB-08 | P1 | done | Negativer `function_id` → letzter Executor statt Stop ([PR #100](https://github.com/ixamgames-droid/lightos/pull/100)) | `vc_button.py:_trigger_primary` |
| VCB-09 | P1 | done | Negativer `snapshot_index` → letzter Snapshot statt Abbruch ([PR #100](https://github.com/ixamgames-droid/lightos/pull/100)) | `vc_button.py:_apply_snapshot` |
| VCB-10 | P1 | done | VCEffectColors.mousePressEvent: fehlendes `event.accept()` → Klick verschiebt Widget ([PR #100](https://github.com/ixamgames-droid/lightos/pull/100)) | `vc_effect_colors.py:mousePressEvent` |
| QA-07 | P1 | ✅ done (dieser PR) | Test: 2 Universen, 2 verschiedene Adapter — beide bekommen ihre Frames (S) | `tests/test_output_manager.py::TestOutputManagerMixedSend`: `add_universe(1)`+`add_universe(2)`, `_enttec_outputs[1]`=FakeEnttec (`send_dmx(data)`), `_artnet_outputs[2]`=FakeArtNet (`send_dmx(universe,data)`), Kanal-1-Werte 11/22; ein `_send_all()` → Enttec bekam genau U1 (11), Art-Net genau U2 (22) mit externer Universe-Nr `univ_num-1`=1, keiner fremde Daten. Nagelt den zentralen Mixed-Send-Pfad (`output_manager.py:303-321`) gegen Routing-/Universe-Vertausch-Regression fest. |
| AUD-01 | P2 | done (Batch 7) | Audit: Multi-Universe + gemischte Adapter (Zuweisung, Persistenz, Render-Pfad) (M) | Gelandet als [`docs/MULTI_UNIVERSE_AUDIT_2026-07-13.md`](docs/MULTI_UNIVERSE_AUDIT_2026-07-13.md) (Pfad 2026-07-26 korrigiert; der ursprünglich genannte `docs/OUTPUT_MULTI_UNIVERSE_AUDIT.md` existiert nicht): beschreibt je Universum den Pfad `universes.json` → `apply_output_config` (`app_state.py`) → `add_enttec/add_artnet/add_sacn` → `_send_all`; belegt mit `file:line` die 3 Befunde — (a) Art-Net sendet `univ_num-1` vs. sACN `univ_num` (`output_manager.py:314` vs `:319`); (b) totes UI-Feld „Art-Net Startuniversum" (`output_config.py:102-104`, nirgends gelesen); (c) `_apply_artnet/_apply_sacn`-Loop überschreibt ALLE Universen (`output_config.py:285-287/362-364`); nennt die abgeleiteten OUT-/QA-Items. |
| QA-08 | P2 | ✅ done (dieser PR) | Test: `universes.json` Round-Trip — `apply_output_config` richtet Mixed-Adapter ein (S) | `tests/test_output_manager.py::TestApplyOutputConfigRoundtrip`: temporäre `universes.json` (Enttec/ArtNet/sACN auf U1/U2/U3) → `AppState.apply_output_config` (auf einem Stub, der nur `output_manager`+`universes` nutzt; Enttec-Port-Open via `_make_enttec_device`-Mock umgangen) → alle 3 Universen existieren, jeder Adapter im RICHTIGEN Registry-Dict, keine Kreuz-Einträge; zweiter Test: ein Enttec-Fehler bricht den Loop NICHT ab (ArtNet danach trotzdem gesetzt). Schließt die ungetestete Start-Rekonstruktion (`app_state.py:apply_output_config`). |
| OUT-03 | P2 | done (Batch 9) | Art-Net/sACN: externe Universe-Nummer pro Universum konfigurierbar (M) | `universes.json`-Zeile trägt optional `out_universe`; `apply_output_config` reicht sie an OutputManager durch; `_send_all` nutzt die konfigurierte Nummer statt fix `univ_num-1`/`univ_num` (Default abwärtskompatibel: Art-Net=num-1, sACN=num); Universe-Manager-Tab zeigt/editiert die Spalte. Test: ArtNetSender/SACNSender bekommen die erwartete externe Universe-Nr. Macht das tote „Startuniversum"-Feld nutzbar (`output_config.py:102-104`). |
| OUT-04 | P2 | ✅ done (dieser PR) | „Übernehmen"-Buttons überschreiben nicht mehr alle Universen (per-Universum) (S) | Art-Net- und sACN-Tab haben jetzt je ein **Ziel-Universe-Feld** (`_spin_artnet_univ`/`_spin_sacn_univ`, analog `_spin_enttec_univ`); `_apply_artnet`/`_apply_sacn` wirken nur auf **dieses eine** Universum (legen es bei Bedarf an) statt per Schleife über alle — `_persist_output` aktualisiert nur diese eine Zeile, andere `universes.json`-Zuweisungen bleiben erhalten. Das behebt zugleich AUD-01-Befund (c) (Apply-Loop überschrieb alle). Das separate „Startuniversum"-Feld (externe Nr.) blieb unangetastet → gehört zu **OUT-03**. Test NEU `tests/test_output_config.py` (4: Art-Net/sACN nur 1 Universum, deaktiviert=noop, fehlendes Zieluniversum wird angelegt). |
| DOC-01 | P3 | done | Anleitung: 2 Universen über 2 verschiedene Adapter (Schritt für Schritt) (S) | Gelandet als [`docs/anleitung_zwei_universen/ANLEITUNG.md`](docs/anleitung_zwei_universen/ANLEITUNG.md) (Pfad 2026-07-26 korrigiert): (1) 2. Universum im Universe-Manager-Tab anlegen, (2) U1=Enttec+COM, U2=ArtNet+IP setzen+Speichern (→`universes.json`), (3) Fixtures auf U2 patchen, (4) im Output-Monitor (`output_view.py`, Universe-Spinner) prüfen, dass beide Universen Werte zeigen; nennt explizit die Stolperfalle der globalen „Übernehmen"-Buttons. |
| AUD-02 | P1 | ✅ done (dieser PR) | Audit: Render-Pfad & Channel-Merge (`_render_frame`) (L) | [`docs/RENDER_AUDIT_2026_07_08.md`](docs/RENDER_AUDIT_2026_07_08.md) — 6-Dimensionen-Workflow (36 Agenten, 6 CONFIRMED, 2 PLAUSIBLE, 7 adversarial zurückgewiesen). Kern: Clamp lückenlos (`set_channel`), kein Thread-Death (Callback-Isolation), GM/WP-6-Merge **by design** (2× widerlegt), Coverage **breit** (≥8 Suiten — Backlog-Annahme „nur test_render_frame.py" war veraltet). Echte Defekte als **STAB-13/14/15 + QA-25** abgeleitet. |
| AUD-03 | P1 | ✅ done (dieser PR) | Audit: DMX-Output & Netzwerk-Sender (ArtNet/sACN/Serial) (M) | [`docs/DMX_OUTPUT_AUDIT_2026_07_08.md`](docs/DMX_OUTPUT_AUDIT_2026_07_08.md) — 5-Dimensionen-Workflow (19 Agenten, 6 CONFIRMED + 1 PLAUSIBLE). Kern: **Bytes spec-konform, Loop doppelt abgesichert (kein Loop-Tod), Locks robust, Universe→Wire korrekt** (send-Robustheit-Hypothese adversarial widerlegt). Ein harter Defekt **OUT-05** (P2), Rest P3 → **OUT-05/06, NET-04, SERIAL-01/02** abgeleitet. |
| AUD-04 | P1 | ✅ done (dieser PR) | Audit: Show-Datei-Persistenz (.lshow Round-Trip) (M) | [`docs/SHOW_FILE_AUDIT_2026_07_08.md`](docs/SHOW_FILE_AUDIT_2026_07_08.md) — 5-Dimensionen-Workflow (33 Agenten, 12 CONFIRMED, 2 zurückgewiesen → 9 distinkte Defekte). Kern: **Round-Trip vollständig** (alle 29 Keys symmetrisch, kein stiller Feld-Verlust), `reset_show` sauber. Echte Defekte in der **Fehler-Behandlung** → **STAB-16..20** abgeleitet (2× P1: nicht-atomarer Save + functions leer bei to_dict-Fehler). |
| AUD-05 | P1 | ✅ done (dieser PR) | Audit: Web-Remote (Flask + SocketIO, `src/web/app.py`) (M) | [`docs/WEB_REMOTE_AUDIT_2026_07_08.md`](docs/WEB_REMOTE_AUDIT_2026_07_08.md) — 4-Dimensionen-Workflow (21 Agenten, 16 CONFIRMED, jeder Befund adversarial gegen den Code verifiziert). Kern: **kein** Out-of-range-/Index-/Clamp-Bug (Guards korrekt), `SECRET_KEY` korrekt (random). Echte Befunde als **NET-/WEB-Items** unten abgeleitet. |
| NET-01 | P1 | done (Panel-Entscheidung + Umsetzung) | **Web-Remote UND OSC ohne Auth im ganzen LAN erreichbar** | `app.py:170` bindet `0.0.0.0` ohne Token/PIN → jeder im WLAN kann Blackout/Kanäle/GO/Fader auslösen. **AUD-08-Parität (OSC-02):** `osc_server.py:27/48` bindet ebenso `0.0.0.0:7770` ohne Auth → dieselbe LAN-Exposition. Fix: Bind default `127.0.0.1`, LAN nur per Option; optional PIN/Token; **querschnittlich für ALLE externen Eingänge** (Web + OSC, ggf. Art-Net/sACN-Input). Dialog benennt die Exposition (NET-02). |
| NET-02 | P2 | done | **Verbindungs-Dialog zeigt „localhost:5000" trotz `0.0.0.0`-Bind** | `main_window.py:1606` — irreführend, Nutzer unterschätzt LAN-Exposition. Fix: echte LAN-IP + Warnung; koppelt an NET-01. |
| NET-03 | P2 | done (Panel-Entscheidung + Umsetzung) | **`cors_allowed_origins="*"` ohne CSRF/Origin-Schutz** | `app.py:36` — beliebige Webseite kann sich mit dem SocketIO verbinden. Fix: Origins beschränken; mit Auth (NET-01) entschärft. |
| WEB-01 | P2 | done (Panel-Entscheidung + Umsetzung) | **`/api/channel` UND `/lightos/ch` schreiben am 44-Hz-Renderer vorbei (halten auf gepatchten Kanälen nicht)** | `app.py:108` — `set_channel` direkt; `_render_frame` überschreibt gepatchte Spans jeden Frame → Web-Wert flackert/hält ~23 ms. **AUD-08-Parität (OSC-01):** `osc_server.py:125-138` `_handle_channel` hat exakt dasselbe Verhalten. Fix (gemeinsam für beide Remote-Kanal-Schreiber): über eine Input-/Override-Ebene leiten (analog `input_layer`), oder dokumentieren/auf ungepatchte Adressen beschränken. |
| WEB-02 | P3 | ✅ done (dieser PR) | **Ungekapselte `int()/float()`-Coercion → HTTP 500** | Neuer Helfer `_num(data,key,default,cast)` in `app.py` fängt `TypeError/ValueError/OverflowError` (auch `int(inf)/int(NaN)`) → Default statt 500; verdrahtet in `api_fader`, `api_channel`, `on_fader`. Clamp bleibt einzige Wert-Leitplanke. |
| WEB-03 | P3 | ✅ done (dieser PR) | **`on_fader`/`on_blackout` crashen bei leerem SocketIO-Payload** | `def on_fader(data=None)`/`on_blackout(data=None)` + `data = data or {}` → leerer Emit crasht nicht mehr (analog `on_go`/`on_back`). |
| WEB-04 | P3 | ✅ done (dieser PR) | **TOCTOU auf `state.cue_stacks[0]`** | `stacks = _get_state().cue_stacks; if stacks: stacks[0]...` in `api_go`/`api_back`/`on_go`/`on_back` → lokale Referenz, kein IndexError bei nebenläufigem Leeren. |
| WEB-05 | P3 | ✅ done (dieser PR) | **`/api/status` iteriert `cue_stacks`/`universes` ohne Lock** | `for s in list(state.cue_stacks)` (Snapshot-Kopie); `universes` nutzte bereits `list(...)`. |
| WEB-QA | P3 | ✅ done (dieser PR) | **0 Tests für den Web-Remote** | NEU `tests/test_web_app.py` (17 Tests, Flask `test_client` + SocketIO-`test_client`, `get_state` gemockt): Clamping (Fader 0–1/Kanal 0–255), Bereichs-Guards (out-of-range/slot=0 = No-op), Payload-Fehlertoleranz (WEB-02, kein 500), SocketIO ohne/kaputtem Payload (WEB-03), Routing, leerer Cue-Stack. |
| NET-09 | P2 | ✅ done (dieser PR) | **Web-Interface „Stopp" schliesst den Server NICHT — Port 5000 bleibt LAN-offen** | Live per Computer-Use gefunden: Ausgabe→„Web-Interface (Port 5000)" AUS → Menü unchecked + Status-Label „Web: aus", **ABER der Server läuft weiter** (`curl localhost:5000/`→200, `/api/status`→403-Gate = lebt, `netstat`→`TCP 0.0.0.0:5000 LISTENING`, PID = die LightOS-App). Der LAN-Listener bleibt offen bis App-Ende → der Nutzer glaubt „aus", das Remote ist aber weiter erreichbar (mit Token) — sicherheits-/erwartungsrelevant. **Ursache:** `main_window.py:1678` ruft `stop_server()` (`app.py:419`); der setzt nur `_running=False` + `_socketio.stop()` (`:423`); der Server läuft via `sio.run(..., allow_unsafe_werkzeug=True)` im **Daemon-Thread** (`:409-415`) → `socketio.stop()` beendet den Werkzeug-Listener aus dem Qt-Fremd-Thread NICHT (nur aus Request-Kontext zuverlässig). **Fix:** `werkzeug.serving.make_server()`-Referenz halten + `srv.shutdown()` (thread-safe) beim Stopp statt `sio.run()`; Menü-„aus" erst nach echtem Socket-Close setzen. Gegenprobe: START + NET-01-Auth funktionieren einwandfrei (403 ohne Token / 200 mit richtigem / 403 mit falschem). **✅ Fix (dieser PR):** `start_server` haelt jetzt einen expliziten `werkzeug.serving.make_server(host, port, app, threaded=True)` (SocketIO-Middleware sitzt an `app.wsgi_app` → HTTP+SocketIO unveraendert), `stop_server()` ruft `srv.shutdown()`+`server_close()`+Thread-Join → Port ist SOFORT beim Toggle 'aus' frei und der Server neu startbar. +2 Lifecycle-Tests (`test_remote_auth.py::TestServerLifecycle`: stop gibt Port frei + Restart; idempotenter Stop). |
| STAB-13 | P2 | ✅ done (dieser PR) | **`feature_dimmers` im Renderer ungelockt iteriert → Dropped-Frame-Stutter** | `app_state.py:1542` baut `active = [s for s in self.feature_dimmers.values() …]` ohne Snapshot/Lock; UI-Thread mutiert die dict-Größe (`set_feature_dimmer` Slot anlegen/`pop` bei Schwelle 0.999; `clear_feature_dimmers` beim Show-Load, Z. 1777/1779/1784) → `RuntimeError: dictionary changed size during iteration` (Block nicht in try/except) → Commit (Schritt 5) entfällt, alle Universen behalten den Vorframe. Einzige Render-Ebene ohne Lock-Snapshot. **Fix:** `_fd_lock` (analog `_prog_lock`), Setter unter Lock, Renderer snapshottet vor Z. 1542. **DoD:** Regressionstest (nebenläufige Mutation wirft nicht mehr / Frame committet). |
| STAB-14 | P2 | ✅ done (dieser PR) | **Repatch verwirft Engine-Extra-Freigabeliste → Roh-Kanal-Zombie (bleibt an)** | `_rebuild_render_plan` setzt `_engine_extra_prev = {}` (`app_state.py:675`) ohne die Live-Werte zu nullen. Eine `ScriptFunction` (setdmx auf ungepatchte Adresse) committet einen Roh-Kanal (gemerkt in `_engine_extra_prev`, Z. 1662); nach einem Patch-Change + Skript-Stopp ist `prev=None` → `if cur or prev` (Z. 1656) false → **keine Freigabe** → `live[addr]` bleibt dauerhaft an (Strobe/Shutter/Beam sicht-/sicherheitsrelevant). **Fix:** Tracking über Repatch erhalten ODER beim Repatch die gemerkten Roh-Adressen aktiv auf 0 committen; `_engine_extra_prev` cross-thread-sicher stellen. **DoD:** Test (Roh-Kanal committen → Repatch → Skript stoppt → Adresse wird auf 0 freigegeben). |
| STAB-15 | P3 | done | **Patch-Render-Plan wird ungelockt & nicht-atomar getauscht → 1-Frame-Glitch beim Umpatchen** | `_rebuild_render_plan` rebindet `_fix_index`/`_default_frame`/`_commit_spans`/`_patched_set`/`_engine_extra_prev` als 5 separate Statements ohne Lock (`app_state.py:669-675`), während `_render_frame` sie lock-frei quer über den Frame liest (Z. 1259/1267 früh, 1648/1653 spät). Torn Read → Spurious-/dunkler Wert an einer eben umgepatchten Adresse; selbstheilend, nur bei **manuellem** Umpatchen, kein Crash/Datenverlust. **Fix:** Plan-Strukturen zu einem `RenderPlan`-Snapshot bündeln, **einen** atomaren Rebind; Renderer zieht `plan = self._plan` am Frame-Anfang. |
| QA-25 | P3 | done (Batch 3) | **Render-Coverage: Fail-Safe + Multi-Universe-Engine-Extra + EE-02-Color-only** | (a) Universe-Stub, dessen `set_channel` in einer späten Stage wirft → asserten, dass der zuletzt committete Frame steht (Fail-Safe, kein Halb-Commit). (b) 2 Universen mit Roh-Script, in einem stoppen → nur dort Release; + Repatch-Zombie-Regression (nach STAB-14). (c) Color-only-Fixture (RGB, kein Dimmer), Effekt→`color_r=200`, programmer intensity=128 → assert ~100 (EE-02-Multiply). _Der Rest des Render-Pfads ist bereits breit getestet (≥8 Suiten) — dies sind die einzigen echten Lücken._ |
| STAB-16 | P1 | ✅ done (dieser PR) | **Nicht-atomarer Save → korrupte `.lshow` + vorherige Show zerstört** | `show_file.py:424-425` — `zipfile.ZipFile(path,"w")` truncatet die gute Datei sofort, `json.dumps` (ohne `default=`) läuft erst danach im offenen Handle. Crash/voller Datenträger/`TypeError` (programmer/base_levels by-ref) → halb geschrieben + Vorversion weg. Kein `temp+os.replace`, auch nicht im Autosave. **Fix:** `json.dumps` **vor** ZIP-Open; Temp-Datei + `os.replace()` atomar; programmer/base_levels defensiv kopieren. **DoD:** Test (Serialisierungsfehler lässt Zieldatei unangetastet; erfolgreicher Save ersetzt atomar). Zusammen mit STAB-17 fixen. |
| STAB-17 | P1 | ✅ done (dieser PR) | **`functions`-Block bei `to_dict()`-Fehler still LEER gespeichert → Totalverlust (inkl. EFX/Matrix)** | `show_file.py:301-305` — `function_manager.to_dict()` ohne Per-Funktion-Guard; eine kaputte Funktion → `except` (Z.304, kein `_lenient`) → `functions_data` bleibt `{"functions":[]}`, Save schreibt leer. EFX/Matrix leben **nur** hier. **Fix:** bei Serialisierungsfehler eines nicht-leeren Managers Save **abbrechen** statt leer speichern (fällt mit STAB-16 serialize-before-open zusammen). **DoD:** Test (werfender Manager → Save wirft, Zieldatei unverändert). |
| STAB-18 | P2 | ✅ done (dieser PR) | **Ein kaputter Wert löscht den GESAMTEN programmer/base_levels-Block still** | `show_file.py:711` (programmer) / `722-730` (base_levels) — `int(v)`-Comprehension nicht pro-Wert geschützt → äußerer `except` → `state.programmer={}` / `base_levels={}` (alle Fixtures weg, still). **Zusatz:** `_rebuild_render_plan()` (Z.727) steht **im** base_levels-`try` → ein unabhängiger Render-Fehler löscht base_levels + kippt `implicit_brightness`. **Fix:** `int(v)` pro Wert kapseln (wie fid/attrs schon); `_rebuild_render_plan()` aus dem `try` ziehen. **DoD:** Test (ein null-Wert → nur dieser Eintrag fällt, Rest bleibt). |
| STAB-20 | P3 | ✅ done (dieser PR) | **`.lshow`-Robustheit gegen alt/korrupt** | (a) Non-Object-JSON (`list`/`null`/`string`) → unbehandelter `AttributeError` statt sauberem Fehler; (b) kein Versions-Gate (zu neue `SHOW_VERSION` still fehlinterpretiert); (c) Legacy-EFX/RGB-Migration (`~829-840`) bricht beim ersten kaputten Eintrag ab, verliert alle folgenden. **Fix:** `isinstance(data, dict)`-Guard am Load-Anfang; `read_show_version` gegen `SHOW_VERSION` prüfen/warnen; Migrationsschleifen pro Eintrag kapseln (wie cue_stacks). |
| OUT-05 | P2 | ✅ done (dieser PR) | **Kein Remove/Disable eines Adapters pro Universe → Typ-Wechsel/„Disabled" sendet weiter (Doppel-Output + Leak)** | `output_manager.py:163-188`/`app_state.py:754-772` — `add_*` schreiben nur in ihre eigene Registry, kein `remove_*`/Disable, kein „Disabled"-Zweig. Typ-Wechsel (ArtNet→sACN) → beide Adapter senden; „Disabled"-Universe gibt weiter Licht aus (nur per Neustart stoppbar); Alt-Adapter-Handle nie `close()`t. **Fix:** `remove_output(universe)`/`disable_universe` (pop unter `_io_lock` + `close()`, Muster wie `_swap_device`); `apply_output_config` entfernt Alt-Typ vor dem Einrichten + behandelt „Disabled". **DoD:** Test (Typ-Wechsel → nur neuer Adapter aktiv, alter `close()`t; „Disabled" → kein Adapter für das Universe). |
| SERIAL-01 | P3 | done (Batch 11) | **Toter/falscher COM-Port zeigt dauerhaft „Verbunden" (kein Fehlersignal)** | `serial_process.py:167` — `is_open()` gibt `p.is_alive()` (Prozess lebt) zurück, nicht Port-offen; Worker setzt `ST_DISABLED`, aber kein UI-Code liest `is_disabled()`. Rig bleibt dunkel, UI meldet „Verbunden". **Fix:** `is_open()`/`output_live` am `ST_OK/ST_DISABLED`-Status ausrichten; Output-Config `is_disabled()` pollen → „Kein Signal". |
| SERIAL-02 | P3 | done (Batch 11) | **Reconnect auf feste COM-Nummer festgenagelt → USB-Replug mit neuer COM heilt nie** | `serial_process.py:94`/`enttec_pro.py:131` öffnen nur `self.port`; `find_enttec_port()` (VID/PID) beim Reconnect ungenutzt; Docstring verspricht „ohne App-Neustart". Randfall (FTDI behält meist COM-Nr). **Fix:** Reconnect optional per VID/PID neu auffinden; sonst UI-Warnung. |
| AUD-06 | P1 | ✅ done (dieser PR) | Audit: Art-Net/sACN-EINGANG & RX-Thread-Lebenszyklus (M) | [`docs/DMX_INPUT_AUDIT_2026_07_08.md`](docs/DMX_INPUT_AUDIT_2026_07_08.md) — 5-Dimensionen-Workflow (15 Agenten, 5 CONFIRMED). Kern: **Parser robust, Locks sauber, Multicast-Join korrekt** (2 Dimensionen sauber). Defekte im **Quellen-Lebenszyklus** → **NET-05** (P1, kein Source-Timeout → eingefrorene Kanäle), **NET-06** (P2, RX-Thread-Tod ohne `_running`-Reset), NET-07/08 (P3, Merge-Config). |
| NET-05 | P1 | ✅ done (dieser PR) | **Kein Source-Timeout → verlorene Art-Net/sACN-Quelle friert Kanäle dauerhaft ein** | `app_state.py:1631-1654` — Receiver rufen `apply_input_merge` nur bei Paketempfang; hört die Quelle auf, bleibt der letzte Wert in `input_layer` und der 44-Hz-Renderer mischt ihn weiter (HTP-Boden/REPLACE-Freeze). **Sicherheitsrelevant:** Input wird nicht vom Blackout/Submaster skaliert. `clear_input_merge` existiert (Docstring verspricht genau das), hat aber **keinen** Produktiv-Aufrufer; `input_layer` hat keinen Zeitstempel. **Fix:** `time.monotonic()`-Stempel pro `out_univ` in `apply_input_merge`; in `_render_frame` 4b-Input Universen älter ~2,5 s (E1.31 Network-Data-Loss) überspringen + verwerfen (bzw. Watchdog). **DoD:** Test (Quelle stempelt, dann „Zeit vergeht" → Kanal fällt auf 0/Default statt zu hängen). |
| NET-06 | P2 | ✅ done (dieser PR) | **RX-Thread-Tod ohne `_running`-Reset → `is_running()` lügt, Eingang bleibt stumm** | `artnet_input.py:153-159`/`sacn_input.py:171-176` — `except OSError: break`/äußeres `except: break` beenden den RX-Thread ohne `self._running=False` → `is_running()` (nur `return _running`) bleibt True → UI-Restart-Guard `if not rx.is_running(): rx.start()` verpufft, `start()` no-oppt. Nach einem Netz-Blip permanent stumm, UI sagt „Aktiv". **Fix:** in beiden `break`-Pfaden `_running=False`; `is_running()` an `_thread.is_alive()` koppeln. **DoD:** Test (Thread-Tod → `is_running()` False → Restart-Guard greift). |
| NET-07 | P3 | done (Batch 5) | **Merge in nicht konfiguriertes `out_universe` still verworfen (Status lügt „Aktiv")** | `app_state.py:1642-1644` — `scratch.get(out_univ) is None → continue`; empfangene Kanäle verpuffen, UI zeigt „Aktiv". **Fix:** `out_univ`-Existenz prüfen (Warnung/anlegen) bzw. Status-Label. |
| NET-08 | P3 | done (Batch 6) | **Umkonfigurieren des Eingangs-Universums lässt alte Merge-Config + Multicast-Join aktiv** | `output_config.py:428-429` ruft nie `remove_merge`/`clear_merges` → alte Quelle (z. B. U5) mischt nach Umstellung auf U7 weiter in dasselbe out-Universe. **Fix:** vor neuem Merge den alten `in_u` räumen. |
| AUD-07 | P2 | done (Batch 7) | Audit: MIDI (Manager, Mapper, APC-Feedback) (M) | `docs/MIDI_AUDIT_<datum>.md`: verifizierte Bug-Liste (`file:line`) zu Cross-Thread-Marshalling der Callbacks (`midi_manager.py` MidiDispatch-Thread → UI), Queue-Overflow-/Reconnect-Verhalten (bounded Queue `maxsize=4096`) und Mapping-/Teach-Korrektheit (`midi_mapper.py`, `apc_mk2/mini_feedback.py`). Nur `tests/test_midi_mapper.py` existiert. P1-Bugs als Items. |
| AUD-08 | P2 | ✅ done (dieser PR) | Audit: OSC- & Timecode/MTC-Remote-Eingang (S) | [`docs/OSC_TIMECODE_AUDIT_2026_07_08.md`](docs/OSC_TIMECODE_AUDIT_2026_07_08.md) — 4-Dimensionen-Workflow (22 Agenten, 7 CONFIRMED). **Kein neuer P1/P2** — OSC/MTC ist solide (robuste Handler, Cross-Thread gelockt, MTC spec-konform). Die 2 P2 sind **Parität** zu WEB-01 (`/lightos/ch` am Renderer vorbei) + NET-01 (`0.0.0.0` auth-los) → dort vermerkt. Neu nur P3 → **OSC-04, MTC-01/02/03**. |
| OSC-04 | P3 | ✅ done (dieser PR) | **`_handle_blackout` invertiert bei String-Args (`bool("0")`==True)** | `osc_server.py:89` — `val = bool(args[0])` → String-Typetag „0"/„off" → Blackout AN statt AUS (getypte int/float von TouchOSC/Lemur sind ok). **Fix:** typ-tolerant (numerisch `float(raw)>=0.5`, String gegen `{"0","off","false","no",""}`). |
| MTC-02 | P3 | ✅ done (dieser PR) | **MTC feuert bei `piece==7` ohne Vollständigkeitsprüfung → gemischte Nibbles** | `mtc_reader.py:117` — bei Mid-Stream-Attach/Rückwärts-Transport Frame aus alten+neuen Nibbles → kurz falscher Timecode. **Fix:** Bitmaske der empfangenen Pieces, nur bei vollständigem 0..7-Satz feuern. |
| MTC-01 | P3 | done | **MTC `+2`-Frame ohne Wrap → Frame-Nr ≥ fps** | `mtc_reader.py:131` — `adj_frames = frames+2` ohne Sekunden-Carry → „:31" bei 30 fps. **Fix:** Timecode normalisieren — **Achtung Drop-Frame (29,97)**, eigene sorgfältige Runde. |
| MTC-03 | P3 | done (Batch 5) | **Lock-freie `time()`/`format()` → Cross-Thread-Torn-Read (latent)** | `mtc_reader.py:167/173` — heute kein Live-Defekt (einziger Consumer nutzt die konsistenten `_fire`-Callback-Args), aber Falle für künftige Poll-Consumer. **Fix:** Tupel unter Lock lesen/kopieren. |
| AUD-09 | P2 | done (Batch 5) | Audit: Audio/BPM-Engine (Beat-Detektor, Capture, BPM-Manager) (M) | `docs/AUDIO_BPM_AUDIT_<datum>.md`: verifizierte Bug-Liste (`file:line`) zu Thread-Safety/Callback-Marshalling in `bpm_manager.py` (`_emit_beat`/Timer-Thread, „im Qt-Mainthread"-Garantie) & `audio/capture.py`, Beat-Anker-/Drift-Korrektheit (`beat_detector.py`) und `offline_timeline.py`-Persistenz. BPM treibt EFX/Matrix/Chaser-Sync (DEMO-04/ENG-10-Kopplung). `BPM_MANAGER_PLAN.md` ist Plan, kein Audit. P1-Bugs als ENG-Items. |
| AUD-10 | P2 | done (Batch 4) | Audit: QXF/QXW-Fixture-Import & Attribut-Mapping (M) | [`docs/FIXTURE_IMPORT_AUDIT_2026-07-13.md`](docs/FIXTURE_IMPORT_AUDIT_2026-07-13.md): verifizierte Bug-/Risiko-Liste (`file:line`) zu Kanal-/Capability-Parsing und Attribut-Mapping in `qxf_import.py` (Vollständigkeit QXF-Kanaltyp→Attribut, Multi-Head-`#N`-Ableitung), QLC+-Workspace-Import `qxw_importer.py`, Fehlertoleranz bei korrupten QXF; mind. 1 Mapping-/Klassifikations-Verdachtsfall belegt/widerlegt (analog ENG-07, dessen Attributname aus genau diesem Import stammt). Nur `tests/test_qxf_import_color_attr.py` existiert. Befunde als ENG-/QA-Items. |
| FIMP-02 | P2 | done (Batch 5) | **Kaputte/fehlende Mode-`Number` → `channel_number`-Kollision (verschobene DMX-Adressen)** | `qxf_import.py` (Mode-Parsing, ~`:525`) — fehlt/defekt fällt die Kanal-Nummer auf `len(ch_refs)` zurück → zwei Kanäle bekommen dieselbe Nummer, DMX-Belegung verschiebt sich still. Belegt (zwei Kanäle beide auf 3). **Fix:** fehlende/ungültige `Number` als Fehler behandeln (Kanal überspringen + Warnung) statt kollidierender Default; Test. |
| FIMP-04 | P2 | done (Batch 5) | **QXW verwirft ganze Fixtures still bei einem defekten Zahlenfeld; Erfolgsmeldung überzählt** | `qxw_importer.py` — breiter `except → None` bei z. B. `Address='notanumber'` verwirft das ganze Fixture lautlos, die Import-Meldung zählt es trotzdem (3 Fixtures, 1 defekt → „Importiert: 2 Fixtures", aber Meldung nennt 3). Belegt. **Fix:** pro-Feld-Fehler sammeln + melden (welche Fixtures warum übersprungen); Zählung an tatsächlich importierte koppeln; Test. |
| FIMP-01 | P3 | done (Batch 6) | **QXF `SpeedPanTilt*`-Kanäle → Attribut `speed`, das `attr_groups` weder klassifiziert noch labelt** | `qxf_import.py` (Kanaltyp→Attribut) — `SpeedPanTiltSlowFast` u. ä. mappen auf `speed`; `classify_attr('speed')=='Other'`, `attr_label('speed')=='speed'` → gleiche Fallenklasse wie ENG-07. Belegt. **Fix:** `speed` in `attr_groups` klassifizieren/labeln (oder Import auf einen bekannten Attributnamen mappen); Test analog `test_qxf_import_color_attr`. |
| FIMP-05 | P3 | done (Batch 6) | **QXW-Import parst nur, wendet Adressen/Modes nie auf die Show an** | `main_window.py:1471-1492` (`_import_qxw`) — das Parse-Ergebnis wird nie in die Show übernommen (verworfen) → Nutzer importiert, es passiert effektiv nichts. **Fix:** geparste Fixtures/Adressen/Modes in die Patch-Struktur übernehmen (bzw. UI-Hinweis, falls bewusst nur Vorschau); Test. _(FIMP-03 HSV/Indigo/Lime/CTO+Fine-Bytes→raw ist bewusste Feature-Grenze, im Audit dokumentiert — kein Item.)_ |
| MIDI-CONFLICT-WARN | P3 | done (Batch 11) | **Kein Konflikt-Hinweis, wenn eine MIDI-Nachricht global-Mapping UND VC-Binding auslöst (STAB-12-Doppel-Trigger)** | Aus [`docs/MIDI_VC_CONFLICT_AUDIT_2026-07-13.md`](docs/MIDI_VC_CONFLICT_AUDIT_2026-07-13.md): `MidiMapper` (Callback-Idx 0) und `VCCanvas` (Idx 1) abonnieren denselben `get_midi_manager()`-Bus; `_rx_loop` (`midi_manager.py:328-332`) ruft alle Subscriber ohne consumed/Kurzschluss → eine Note feuert beides. Struktureller Doppel-Subscriber, keine risikofreie 1-Zeilen-Fix. **Sicherste kleine Maßnahme (Option B):** nicht-behaviorale Konflikt-Warnung beim MIDI-Learn, wenn Note/CC bereits global ODER an einem VC-Widget belegt ist. (Option A „VC vor global" braucht Konsumierungs-Protokoll + volle Subscriber-Tests.) |
| BPM-02 | P2 | done (Batch 6) | **Capture-Orphan-Thread bei Geräte-Hang → Doppel-Capture (F3)** | `audio/capture.py:152-156` — `stop()` setzt `_thread=None` nach Join-Timeout; hängt das Gerät, läuft der alte Capture-Thread weiter, ein folgender `start()` (`:135`) erzeugt einen zweiten → zwei Capture-Threads gleichzeitig (`:171-173`). **Fix:** alten Thread als „sterbend" markieren + neuen Start blocken/koaleszieren, bis der alte wirklich tot ist; Test. |
| BPM-01 | P3 | done (Batch 10) | **`_emit_beat` ruft Subscriber im Hintergrund-Thread — Docstring-Garantie „Qt-Mainthread" ist falsch (F1)** | `bpm_manager.py:345`/`:349` — `_emit_beat` ruft `cb(idx)` direkt aus Timer-Thread `BPM-Beat` (`_loop`) und Audio-Capture-Thread (`_on_audio_beat`); der Docstring `:7` verspricht Mainthread. Heute halten alle Subscriber die Konvention selbst (marshallen), aber ein neuer Subscriber, der dem Docstring vertraut + ein QWidget anfasst → Cross-Thread-Qt-Crash. **Fix:** entweder im Manager per Qt-Signal/QueuedConnection marshallen ODER Docstring korrigieren + Kontrakt (Subscriber marshallt) klar dokumentieren; ggf. Assert. |
| BPM-03 | P3 | done (Batch 6) | **BPM-Cache-Schreiben ohne temp+rename → Truncation-Korruption + Lost-Update (F5)** | `bpm_cache.py:39-41` schreibt direkt (Absturz/paralleler Schreiber → halbe/verlorene Datei); `:26-31`/`:56-63` Read-modify-write ohne Lock bei Stapelanalyse. **Fix:** atomar per temp-Datei + `os.replace`; Merge/Lock beim RMW; Test (korrupte Datei → sauberer Rebuild). |
| BPM-04 | P3 | done (Batch 10) | **`reset()` schreibt `_bpm=0.0` außerhalb `_lock` → Ordering-Race mit Audio-Thread (F7)** | `bpm_manager.py:261` — `reset()` setzt `self._bpm=0.0` lock-frei, während der Audio-Thread `set_bpm` aufrufen kann → kurzer Ordering-Race (Timer könnte weiterlaufen). **Fix:** `_bpm`-Reset unter `_lock`; Test. |
| BPM-05 | P3 | done (Batch 10) | **`OfflineTimeline.from_dict` validiert `downbeats ⊆ beats` nicht → korrupter Cache → falsches `beat_in_bar` (F6)** | `offline_timeline.py:186-194`/`:140-144` — `beats_ms`/`downbeats_ms` werden unabhängig sortiert, keine Konsistenzprüfung. **Fix:** beim Laden `downbeats ⊆ beats` prüfen (sonst verwerfen/neu ableiten); Test. |
| ENG-12 | P2 | done (Batch 8) | **`update_fixture` NICHT undo-bar → Ctrl+Z auf Patch-Änderungen wirkungslos** | Aus QA-15: die Undo/Redo-Closures (`app_state.py:~549`) rufen `update_fixture(fid, undoable=False, **before)`, wobei `before` (aus `_fixture_to_dict`) selbst einen `fid`-Key trägt → `TypeError: got multiple values for argument 'fid'`, im UndoStack still verschluckt. **Fix:** `fid` vor `**kwargs` aus dem Dict entfernen; den `tests/test_undo_coverage.py`-xfail zu einem echten Test machen. |
| ENG-13 | P3 | ✅ done (dieser PR) | **Cue-Renumber im Playback-Editor sortiert `stack.cues` ohne `_reindex_after_mutation` → `_current_idx`-Desync bei laufender Cueliste** | Beim A3D-16-Fix entdeckt: `src/ui/views/playback_view.py:_on_cue_edited` (Z. 519–520) setzt `cue.number` und ruft `self._current_stack.cues.sort(...)` DIREKT von aussen — bypassed die einzige konsistenz-wahrende Stelle `CueStack._reindex_after_mutation` (fuehrt `_current_idx`/`_manual_target` identitaets-treu nach). Renummeriert der Nutzer eine Cue-Nummer, waehrend die Liste laeuft, zeigt `_current_idx` danach auf die falsche Cue (Replay/Skip); ausserdem ohne `_lock`. Rar (Editor-Aktion), aber latent — der A3D-16-Objekt-Anker haelt beim Commit trotzdem (identitaets-basiert). **Fix:** Renumber ueber eine CueStack-API leiten (z. B. `renumber_cue`/bestehendes `update_cue`), die unter `_lock` sortiert und `_reindex_after_mutation` ruft — `.cues` nicht von aussen mutieren. |
| MU-01 | P2 | done (Batch 8) | **Live-Tab: Cross-Typ-Adapterwechsel → Doppel-Output/Leak (OUT-05 im Live-Pfad)** | `output_config.py:284-289/302-306/383-388` — `_connect_enttec`/`_apply_artnet`/`_apply_sacn` rufen kein `remove_output` vor `add_*`; der OUT-05-Fix lebt nur im Rehydrierungs-Pfad (`app_state.py:816`). Nach Enttec+Art-Net auf demselben Universum senden beide (`_send_all`). **Fix:** vor `add_*` das Universum via `remove_output` räumen; Test. |
| MU-02 | P2 | done (Batch 8) | **„Deaktivieren"-Checkbox ist No-Op → Adapter sendet weiter** | `output_config.py:291-294/372-375` — setzt nur das Label + `return`; der Adapter bleibt registriert. **Fix:** `remove_output`/disable beim Abwählen; Test. |
| MIDI-RECONN | P2 | done (Batch 8) | **APC-Reconnect ohne Handle-Ersatz → stumm bis Neustart (AUD-07 F3)** | `midi_manager.py:106-108` — `open_input` kehrt sofort zurück, solange der Portname im Dict steht; tote Handles werden nie evakuiert. Nach USB-Replug bleibt der APC stumm trotz 4s-Auto-Connect (`main_window.py:290-298`). **Fix:** bei Reconnect toten Handle erkennen + ersetzen; Feedback-LEDs neu aufbauen. |
| MIDI-QDROP | P2 | done (Batch 8) | **RX-Queue-Overflow droppt Note-Off → hängender Flash/Button (AUD-07 F2)** | `midi_manager.py:307-312` — `put_nowait`-Overflow (Queue 4096) droppt still; ein gedropptes Note-Off bei durchgekommenem Note-On lässt `_flash_active` True (`executor.py:40/47`). **Fix:** bei Full Note-Off/CC bevorzugen ODER Resync nach Overflow + Drop-Zähler/Log. |
| DOC-02 | P1 | done (Batch 3) | DOC-Index + Konvention + Template (`docs/components/README.md`) (S) | Neu: `docs/components/README.md` mit (a) Zweck/Scope, (b) Pfadkonvention (`views/<modul>.md`, `vc/<widget>.md`, `engine/<typ>.md`, `output/<modul>.md`, `input/<modul>.md`), (c) verbindliches Markdown-Template mit fixen Abschnitten (Zweck · Bedienung/Optionen · Verknüpfungen · zugehörige Tests · Quelle `file:line`), (d) lebende Status-Tabelle (Komponente → Doc ja/nein). Plus 1 voll ausgefülltes Beispiel (`docs/components/vc/vc_button.md`). Grundlage für DOC-03..07 + QA-10. |
| DOC-03 | P2 | done (Batch 7) | Doku für alle VC-Widgets (`docs/components/vc/<widget>.md`) (L) | Pro `vc_*.py`-Widget (außer Infrastruktur `vc_widget/vc_canvas`, im Index als „Basis" markiert) eine Doc nach Template: Zweck, Modi/Optionen (z.B. `SliderMode` bei vc_slider, action-Kinds bei vc_button), Drop-/Bind-Verhalten (`_droppable_types`), Serialisierung (`to_dict/apply_dict`), gekoppelte Engine-Teile (`effect_live`, `show_file`), Tests. DoD: jede `vc_*.py` hat eine Doc oder ist im Index begründet ausgenommen. Hochgekoppeltes Subsystem (vgl. VC-Audit). |
| DOC-04 | P2 | done (Batch 7) | Doku für alle Views (`docs/components/views/<view>.md`) (L) | Pro `src/ui/views/*.py`-View (außer Helfer-Widgets) eine Doc nach Template: Zweck, Navigations-Ort (Sektion/Sub-Tab laut `main_window.py`/`FEATURE_MAP.md`), Bedienelemente, abonnierte/emittierte Bus-Signale (z.B. `SELECTION_CHANGED`, `patch_changed`), gekoppelte AppState-/Engine-Teile, Tests. DoD: jede Nicht-Helper-View hat eine Doc oder ist als Helper/Basis markiert. |
| DOC-05 | P2 | done (Batch 4) | Doku für alle Engine-Funktionstypen (`docs/components/engine/<typ>.md`) (M) | Pro `FunctionType`-Wert (`function.py`: Scene, Chaser, Sequence, Collection, Show, EFX, RGBMatrix, Audio, Script, MappedChannelChange) eine Doc nach Template: Zweck/Verhalten, Parameter (Dataclass-Felder + `set_param`-Keys, z.B. EFX `speed` nicht `speed_hz`), Render-Beitrag (Schritt in `show_engine`), Serialisierung, gekoppelte Module, Tests. DoD: alle 10 FunctionType-Werte haben eine Doc, im Index verlinkt. |
| DOC-06 | P2 | done (Batch 4) | Doku für DMX-/Output-Komponenten (`docs/components/output/<modul>.md`) (M) | Pro `src/core/dmx/*.py`-Modul (output_manager, universe, artnet, sacn, enttec_pro, serial_process, artnet_input, sacn_input) eine Doc nach Template: Zweck, Konfiguration/Schalter (z.B. `LIGHTOS_SERIAL_INPROC`, `FAIL_LIMIT`, Universe-Mapping), Threading-/Prozessmodell, Fehler-/Watchdog-Verhalten, gekoppelte Module, Tests. DoD: alle 8 Nicht-`__init__`-Module haben eine Doc. |
| DOC-07 | P3 | done (Batch 3) | Doku für Input-Komponenten (MIDI/OSC/Timecode/Hotkeys) (M) | Pro Input-Modul (`midi_manager`, `midi_mapper`, `midi_backend_winmm`, `osc_server`, `mtc_reader`, `input/keyboard_hotkeys`, `input/profile`) eine Doc nach Template: Zweck, unterstützte Nachrichten/Adressen (MIDI-CC/Note, OSC-Pfade, MTC-Frames), Mapping-/Learn-Mechanik, gekoppelte VC-/Engine-Teile, Tests. DoD: jedes Modul hat eine Doc, im Index verlinkt. `KEYBOARD_MAPPING.md` deckt nur Tastatur ab. |
| DOC-08 | P2 | done | Referenz-Doku: Env-Flags & Config-Dateien (`docs/CONFIG_REFERENCE.md`) (S) | Neu: `docs/CONFIG_REFERENCE.md` mit (a) Tabelle aller `LIGHTOS_*`-Env-Flags (LIGHTOS_DEBUG, _STRICT, _SHOW_DB, _NO_OUTPUT_THREAD, _NO_AUDIO_AUTOSTART, _SERIAL_INPROC, _FLASK_SECRET): Name·Wirkung·Default·wo gelesen (`file:line`)·Test-Relevanz; (b) Tabelle der `data/`-Configs (universes.json, midi_mappings.json, channel_groups.json): Pfad·Schema-Kurzform·Subsystem. Per `ls docs/` verifiziert: kein `*CONFIG*`-Doc. Aus README/INSTALL.md verlinkt. |
| DOC-09 | P3 | done | Anleitung: Web-Remote (Handy als Konsole) einrichten & bedienen (S) | Gelandet als [`docs/anleitung_web_remote/ANLEITUNG.md`](docs/anleitung_web_remote/ANLEITUNG.md) (Pfad 2026-07-26 korrigiert). **Achtung:** der DoD-Punkt „Sicherheitshinweis (offenes Netz, keine Auth)" ist durch NET-01 überholt — der Web-Remote hat seit 2026-07-14 ein Token-Gate und bindet ohne LAN-Toggle nur auf 127.0.0.1. Inhalt: (1) Server starten (Port 5000, Bind `0.0.0.0`, ggf. `LIGHTOS_FLASK_SECRET`), (2) eigene IP finden + am Handy im WLAN öffnen, (3) was die Buttons tun (GO/BACK/Blackout/Fader/Executor — Bezug auf `src/web/app.py`-Routen), (4) Sicherheitshinweis (offenes Netz, keine Auth). Aus README.md + `docs/ANLEITUNGEN.md` verlinkt; `REMOTE_KONSOLE_PLAN.md` (reines Konzept) davon abgegrenzt. |
| DOC-11 | P2 | ✅ done ([#266](https://github.com/ixamgames-droid/lightos/pull/266)) | **`docs/FIXTURE_3D_GALLERY.md` — gerendertes Fixture-3D-Modell-Schaufenster** (Davids Auftrag 2026-07-12) (M) | **Erledigt:** alle 13 `viz_model`-Klassen als originalgetreue Renders (aus den echten Produktions-Buildern via `gallery_render.html` + `tools/gallery_server.py`, Browser-GPU) in `docs/img/fixture_gallery/`, gruppiert mit FM-11-Maßen + Auto-Zuordnungs-Abschnitt; aus README + FIXTURE_LIBRARY verlinkt. Ursprünglicher Auftrag: | Repository-Schaufenster, das **jedes** prozedurale Fixture-3D-Modell zeigt (die 13 `viz_model`-Klassen aus `src/ui/visualizer/scene_src/fixtures/builders.js`: PAR · MH · Scanner · LED-Bar · PAR-Bar · Mover-Bar · Spider · Strobe · Dimmer · Laser · Nebel · Hazer · Other). Pro Modell ein **gerendertes Bild** (aus dem Three.js-Visualizer — bevorzugt headless via Browser-Tools auf der Visualizer-HTML/`builders.js`, sonst Computer-Use auf der laufenden App; Bilder nach `docs/img/fixture_gallery/<klasse>.png`) + Tabelle: Klasse · Referenzgerät & Maße (aus FM-11) · Kanal-Heuristik, die `suggest_viz_model()` darauf mappt. Plus Abschnitt **„Automatische Gruppen-Zuordnung"**: wie `viz_model_for`/`suggest_viz_model` das Modell je Fixture wählt und dass Fixtures ihr Modell in Gruppen behalten (Bezug FM-12). Aus `README.md` + `docs/FIXTURE_LIBRARY.md` verlinkt. DoD: Seite mit allen 13 Renders + Zuordnungs-Abschnitt, Bilder eingecheckt, Links gesetzt. |
| DOC-12 | P3 | ✅ done | **README-/Doku-Landing aufhübschen** (Davids Auftrag 2026-07-12) (S) | **Erledigt:** README erfüllt die Landing-DoD bereits (Hero-Screenshot, Quick-Start = „Erste Schritte", vollständiger Feature-Überblick, `ANLEITUNGEN.md`-Index) — ergänzt um den prominenten Verweis auf die 3D-Modell-Galerie (DOC-11) aus der Einstiegs-Box UND der „3D Visualizer"-Feature-Sektion. Bestehende Bilder wiederverwendet (keine neuen Screenshots). Ursprünglicher Auftrag: | Einstiegs-Präsentation schöner + konsistenter: `README.md` mit (a) Hero-Screenshot (Hauptansicht) + 3D-Visualizer-Bild + Verweis auf die 3D-Galerie (DOC-11), (b) knappe Feature-Übersicht mit Links in `docs/FEATURE_MAP.md`/`FEATURES.md`, (c) klarer „Erste Schritte"-Block + Verweis auf den konsolidierten `docs/ANLEITUNGEN.md`-Index. **Bestehende Bilder wiederverwenden** wo passend (nur fehlende Hero-Shots neu aufnehmen). DoD: README rendert sauber auf GitHub, alle Verweise gültig, Galerie/Anleitungen von der Landing erreichbar. Läuft nach DOC-10/DOC-11 (nutzt deren Bilder/Index). |
| QA-09 | P1 | done | Enumerierender Headless-Smoke: ALLE Views + alle WIDGET_REGISTRY-Widgets bauen ohne Crash (M) | `tests/test_ui_smoke_enumerated.py`: inventarisiert alle 24 öffentlichen no-arg `*View`-Klassen per `pkgutil`/`importlib`/`inspect`; neue oder umbenannte Views machen den Inventar-Test rot. Jede wird headless gebaut; alle 19 `WIDGET_REGISTRY`-Widgets durchlaufen `to_dict()`→`apply_dict()`-Roundtrip. |
| QA-10 | P1 | done | Smoke-Tests für die 8 Views ohne jede Test-Abdeckung (S) | `tests/test_ui_smoke_enumerated.py` baut AudioEditor, CarouselEditor, CollectionEditor, EffectLayerEditor, MidiView, OutputView, SceneEditor und ScriptEditor mit minimalen echten Engine-Objekten und prüft je ein Kern-Widget. **34 neue Tests grün**; gemeinsam mit `test_views.py` und `test_web_app.py`: 63 grün. **Nachtrag 2026-07-26:** Alle acht Einzeltests sind auf `main` verifiziert (`test_output_view` · `test_midi_view` · `test_audio_editor` · `test_carousel_editor` · `test_collection_editor` · `test_effect_layer_editor` · `test_scene_editor` · `test_script_editor`) — die damalige „noch nötig: die acht Branches integrieren"-Auflage ist erfüllt (u. a. über [PR #418](https://github.com/ixamgames-droid/lightos/pull/418)). Die acht Fortschritts-Zeilen (1/8…8/8) und eine doppelte QA-09-Zeile wurden dabei entfernt — jede Runde hatte eine neue Zeile angehängt statt die vorhandene zu aktualisieren. |
| OUT-U32 | P2 | done | **Universe 17–32 nur anzeigbar, nicht ausgebbar** (Review-Fund 2026-07-11) | Der U32-Fix (`b86d252`) öffnete nur den Output-MONITOR (`output_view.py`); die Ausgabe-Routing-Dialoge in `src/ui/widgets/output_config.py` (Enttec/Art-Net/sACN, Zeilen ~81/103/130) kappen die Universe-Auswahl weiter bei `setRange(1, 16)` → auf U17–32 gepatchte Fixtures erreichen kein reales Ausgabe-Ziel. DoD: alle drei Spinboxen bis 32 + Test analog `test_output_view.py`. |
| QA-P95-FLAKE | P3 | done | **p95-Render-Gate (QA-20) gegen Umgebungs-Flakiness absichern** (Review-Fund 2026-07-11) | `tests/test_benchmark_universes.py` assertet p95<20 ms über nur 30 Frames ohne Warm-up/Retry — läuft im lokalen Pflicht-Gate, das laut `reference_pytest_lock` explizit mit parallelen Claude-/Cowork-Sessions rechnen muss; unter Last reißt das Budget fälschlich und blockiert Merges. DoD: Warm-up-Frames + 1 Retry (oder Median-von-3), Budget bleibt 20 ms. |
| QA-11 | P2 | ✅ done | CI-Gate auf Show-Lint + View-Smoke ausweiten (S) | **Erledigt:** `.github/workflows/ci.yml` ruft jetzt zusätzlich `tests/test_show_lint.py` + `tests/test_views.py` (beide pytest-Schritte, `QT_QPA_PLATFORM=offscreen`); Install-Schritt ergänzt um `PySide6>=6.7.0` + `PySide6-Addons>=6.7.0` (wie requirements.txt) für den View-Smoke. Lokal laufen die 4 CI-Tests grün (143). Ein kaputter committeter Show-`param_key` bzw. eine nicht mehr baubare View lässt CI jetzt auf GitHub rot werden, nicht nur lokal. |
| QA-12 | P2 | done (Batch 12) | `docs/UI_VERIFICATION_CHECKLIST.md`: maschinen-prüfbare Pro-Element-Checkliste (M) | Neue `docs/UI_VERIFICATION_CHECKLIST.md` mit (1) Tabelle aller Views (`src/ui/views/*.py`) + VC-Widgets (WIDGET_REGISTRY), Spalten: headless-instanziierbar / hat Tooltip-od.-Label / Aktion-od.-Signal verdrahtet / Regressionstest (Pfad) / Doc; (2) je Zeile ein ausführbarer Verifikationspfad (Testname od. „manuell: SMOKE_TEST.md §x"). Offene Zellen als Lücke markiert. Löst die rein manuelle `docs/SMOKE_TEST.md` als Backing-Store für QA-13 ab. |
| QA-13 | P3 | done (Batch 3) | Tooltip-/Label-Coverage-Audit für interaktive Steuerelemente (M) | Headless-Test (oder `tools/`-Skript) baut jede no-arg View, durchläuft `findChildren(QAbstractButton)` und meldet Buttons mit leerem `text()` UND leerem `toolTip()` als Verstoß; Ergebnis in `docs/UI_VERIFICATION_CHECKLIST.md`. Abdeckung heute ungleich: `show_manager_view.py` (7 Buttons, 0 Tooltips), `dmx_monitor_view.py` (0) vs. `efx_view.py` (28). Mindestziel: show_manager + dmx_monitor dokumentiert/behoben. |
| STAB-10 | P1 | ✅ done ([#270](https://github.com/ixamgames-droid/lightos/pull/270)) | .lshow Save→Load→Save Round-Trip-Identitätsjagd (M) | **Erledigt:** `tests/test_show_roundtrip_identity.py` (Klemm-Symmetrie + `load∘dump`-Fixpunkt + alle 6 committeten Shows). Verifizierte Drift behoben: `_fixture_to_dict` klemmt `address`/`channel_count` jetzt symmetrisch zu `_patched_fixture_from_data` auf `[1,512]` (+ `universe = max(1,…)`), beide Zweige. Nebenbefund `label`-Default (`""` vs `Fixture N`) ist KEINE echte Round-Trip-Drift (State-Label nach Load nie leer). Checker-Falsch-Positiv im DOC-10-Gate separat gefixt ([#269](https://github.com/ixamgames-droid/lightos/pull/269)). |
| ENG-11 | P1 | ✅ done | Multi-Head-Attribut (`attr#N`) Pfad-Sweep über alle Erzeuger/Verbraucher (M) | **Erledigt:** `tests/test_multihead_sweep.py` sweept den `#N`-Pfad an SPIDER14 (2 Bänke = head0 vs `#1`, der fundamentale Split): Kopf-Werte unabhängig (set→get→DMX), head0-Spiegelung, `#N`-Keys überleben save→load, **kein Phantom-Kopf** (head jenseits der Bänke schreibt keinen Kanal), Pro-Kopf-Label distinkt + kopf-indiziert (`attr_label` „#N → (Kopf N+1)"). **Keine Inkonsistenz gefunden** — der Pfad ist über die getesteten Stellen konsistent. Erweiterung auf 3+ Köpfe/EFX-write/palette-record als Folge-Sub-Item möglich. |
| STAB-21 | P1 | ✅ done | Threading/native-AV-Härtungsaudit (re-entrante Emits, close/send-Races) (M) | **Erledigt:** `docs/THREADING_AV_AUDIT_2026-07-13.md` (4 Grenzen × Skeptiker-Verifikation). **1 echte Lücke behoben:** `record_cue` (`app_state.py:1976`) snapshottete den Programmer OHNE `_prog_lock` → paralleler MIDI-RX-`set_programmer_value` konnte mitten in der Iteration ein fid einfügen → `RuntimeError: dict changed size` (unabgefangen, Record bricht ab); Fix = Snapshot unter `_prog_lock` (wie `_render_frame`), +`tests/test_record_cue_thread_safe.py`. **4 als sicher widerlegt** (GIL-atomare `list(dict.items())`/`dict(attrs)`-Snapshots, Qt-QueuedConnections serialisieren gegen Destruktion, MIDI-Thread-Join vor Widget-Teardown, Render-Blanket-catch), **2 Grenzen clean** (MIDI-RX marshallt korrekt, keine re-entranten Emits). **2 low-prio Hardening-Nits** dokumentiert: `remove_fixture`-pop-Lock, `executor.set_page`-`list()`-Snapshot (heute crash-neutralisiert). |
| QA-14 | P2 | done (Batch 4) | Render-Vertrag (WP-6/EE-02/LTP) als Property-Tests statt Einzelfälle (M) | `hypothesis` als Test-Dep + neuer `tests/test_render_contract_props.py` mit `@given`-Strategien über (Anzahl Funktionen, Kanal-Typ Intensität/Farbe/Position, Werte, Start-Reihenfolge), der WP-6 (Funktions-Kanalschutz), EE-02 (Programmer-Dimmer multiplikativ) und LTP (später gestartete gewinnt) als Eigenschaften assertet. Mind. 3 Properties grün; jede generativ gefundene Vertragsverletzung als ENG/STAB-Sub-Item. ARCHITECTURE/OUTPUT_MERGE_CONTRACT nennen selbst einen by-design-Grenzfall. |
| STAB-12 | P2 | done (Batch 3) | MIDI-Mapping (global) vs VC-Binding Konflikt-Audit (S) | Audit + Test `tests/test_midi_vc_conflict.py`: simuliert eine MIDI-Message, die sowohl ein VC-Widget-Binding als auch ein globales `MidiMapper`-Mapping trifft (beide abonnieren denselben `get_midi_manager()`-Bus, `vc_canvas.py:_setup_midi`); dokumentiert das beobachtete Verhalten (beide feuern?) + DoD = klare Aussage „erwartet/unerwartet" mit Empfehlung (Priorität VC vor global, oder Konflikt-Warnung beim Learn). ARCHITECTURE markiert die globalen Mappings ausdrücklich als Konfliktquelle. Doppel-Trigger als STAB-Sub-Item. |
| QA-15 | P2 | done (Batch 7) | Undo-Abdeckungs-Matrix — jede mutierende Aktion auf Undo-barkeit prüfen (S) | `docs/UNDO_COVERAGE_MATRIX.md`: alle mutierenden AppState/Engine/UI-Aktionen mit Spalte „undo-bar? (ja/nein/teilweise)" + Quellzeile; identifizierte Nein-Lücken als priorisierte STAB/UI-Sub-Items. Heute nur punktuell `undoable=` (add/remove/update_fixture, auto_patch, set_programmer_value); Verdachtskandidaten `clear_programmer` (`app_state.py:674`, kein undoable-Param) + base-level-Setter. DoD: mind. 3 echte Lücken benannt. Bestehende Lücken (VCB-06, UI-02) belegen die Klasse. |
| QA-16 | P1 | done ([#213](https://github.com/ixamgames-droid/lightos/pull/213), [#231](https://github.com/ixamgames-droid/lightos/pull/231)) | Web-Remote-Endpunkte headless abgesichert | `tests/test_web_app.py` deckt Flask- und SocketIO-Routing, Clamp-/Bereichs-Guards, fehlerhafte Payloads, leere Cue-Stacks sowie STOP und Fader-Initial-Sync ab. Die ursprüngliche Behauptung „0 Tests" ist damit überholt. |
| QA-17 | P2 | ✅ done | Doc-Link-Checker für tote `*.md`-Querverweise (S) | **Erledigt:** `tools/check_doc_links.py` + `tests/test_doc_links.py` prüfen alle 343 relativen Markdown-Querverweise in `docs/**.md` + README/BACKLOG/ROADMAP/CHANGELOG (Bilder + `_archiv/`-Ordner + Code-Spans ausgenommen). 2 tote Links behoben (Guides verwiesen auf nicht-committete `.lshow`-Shows → Code-Span statt Link). **Tote Links = 0**, dauerhaft gegatet. Ursprünglich: | Neues `tools/check_doc_links.py` + `tests/test_doc_links.py`: parst alle relativen Markdown-Links (`](…md…)`) in `docs/*.md`, `BACKLOG.md`, `ROADMAP.md`, `CHANGELOG.md` und meldet nicht existierende Ziele; grün, sobald aktuell tote Links (z.B. die neuen `docs/components/*` aus DOC-02..07) existieren oder als bekannt-ausgenommen gelistet sind. ~65 Docs / ~168 Querverweise, heute ungeprüft. |
| QA-18 | P1 | ✅ done | Backlog-Linter: ID-/PR-/Status-Konsistenz in `BACKLOG.md` (S) | **Erledigt:** `tests/test_backlog_lint.py` prüft jede Tabellenzeile auf gültige Prio (P1/P2/P3, deko-tolerant `**P1**`), ein anerkanntes Status-Keyword (todo/done/wip/review/blocked/decision/teils/defer/verifiziert/…) und wohlgeformte GitHub-`pull`/`issues`-Links im Status. Bewusst lenient (dekorierte Status + `wip (3/8)`-Fortschritts-Logs zulässig; tote Querverweise deckt QA-17 ab). Fing 13 Inkonsistenzen, alle normalisiert: 11× `**P1**`→`P1`, `in progress`→`wip`, fehlende Prio (`—`)→`P3`. Ursprünglich: Neuer `tests/test_backlog_lint.py` prüft Warteschlange + Tabellen auf eindeutige IDs, zulässige Status, gültige PR-Links, intakte Links. |
| DOC-10 | P2 | ✅ done (dieser PR) | AUDIT_COVERAGE-Tracker: welche Subsysteme sind auditiert (M) | **[`docs/AUDIT_COVERAGE.md`](docs/AUDIT_COVERAGE.md)** angelegt: Tabelle Subsystem → letztes Audit-Doc/Datum/Status (auditiert/teilweise/offen), gruppiert nach Ausgabe/Netzwerk/Engine · Show/Fixtures/Audio · UI. Deckt 17+ vorhandene Audit-Docs ab, nennt die Lücken (dediziertes 3D-Visualizer-/Programmer-Audit fehlt, VC-Audit auffrischen). +`tests/test_audit_coverage_docs_exist.py` (jede verlinkte Doc muss existieren → fängt Umbenennungen). |
| QA-19 | P2 | done | Show-Lint: `param_key` gegen die echten Params der gebundenen Funktion prüfen (M) | `validate.py`: bei gesetzter `function_id` wird `param_key` gegen die Param-Keys des konkreten gebundenen Funktionstyps geprüft (neuer Code, z.B. `VC-PARAMKEY-FN`) statt nur gegen `caps.all_param_keys` (Union aller Effekte). Test: Slider `param_key='speed'` gebunden an eine Funktion ohne speed-Param → ERROR-Finding; korrekt gebundener bleibt sauber. Genau die QA-05-Bugklasse (`speed_hz` global gültig, für die Funktion wirkungslos) rutscht heute durch. |
| QA-20 | P2 | done (2026-07-10) | Performance-Regression-Gate: 44-Hz-Render-Budget im Test prüfen (S) | `tests/test_benchmark_universes.py` misst bei einem kleinen 1-Universum-Rig 30 Frames und fordert p95 < 20 ms. Die Schwelle liegt mit Headroom unter dem 44-Hz-Budget von 22,7 ms und fängt echte Verzögerungen im Renderpfad. |
| QA-21 | P3 | done (Command /ui-verify-round in .claude/commands/, untracked per Konvention; Backlog-Vermerk) | `/lightos-loop`-Routine-Task: wiederkehrende UI-Element-Verifikation (S) | Neue Mini-Spec `.claude/commands/ui-verify-round.md`: definiert einen Loop-tauglichen Task, der (1) die oberste rote Zeile aus `docs/UI_VERIFICATION_CHECKLIST.md` (QA-12) wählt, (2) Headless-Test + ggf. Tooltip/Doc ergänzt, (3) die Checkliste-Zeile auf grün setzt, (4) via `verify_loop.ps1` testet; mit Auswahlkriterium + Abbruchbedingung (alle grün). Macht die Verifikation zur Routine statt Einmal-Doc. Hängt an QA-12. |
| QA-22 | P2 | done (Batch 9) | UI-Drive-Toolset konsolidieren: `tools/app.ps1` (restart/stop/wait/shot) + EIN Klick/Screenshot-Werkzeug (M) | Transcript-Mining 2026-07-06: ~700 ad-hoc getippte Kill-Start-Sleep-/Klick-/Shot-Ketten über `docs/_walkthrough/lo.ps1` UND `docs/tutorial_matrix/_ui.ps1`/`_cap.ps1`/`_fg.ps1` (zwei redundante Toolsets für denselben Zweck). Neu: `tools/app.ps1` mit Subkommandos `restart/stop/wait/shot` (kapselt den Get-CimInstance-Kill per CommandLine-Filter mit Worktree-Pfad-Parameter, venv-Start, Fenster-Wartezeit) + Konsolidierung der zwei Walkthrough-Toolsets zu einem (`fg/click/dclick/key/type/shot/crop/scroll`, einheitliche Screenshot-Ordner-Konvention, `dlg.ps1`-Modal-Handling integriert). DoD: beide alten Toolsets rufen intern das neue auf (oder sind ersetzt); ein kompletter Mini-Walkthrough (Show laden, 3 Screenshots) läuft nur über das neue Tool; `/lightos-demoshow` + `/lightos-audit` (`.claude/commands/`) verweisen darauf. |
| A3D-01 | P2 | ✅ done (dieser PR) | [LASER-SAFETY] Channel-Modifier (Inverse/Range-Lock) macht Laser-NOT-AUS-Zeroing wieder nonzero -> DMX-Laser bleibt an | **Ort:** `src/core/dmx/output_manager.py:301` (Codex 215). **Bug:** Die Laser-NOT-AUS-Verriegelung zwingt im Renderer alle Laser-Adressen als OBERSTE Ebene auf 0 (app_state.py:1722-1728, `if laser_estop_active: su.set_channel(addr, 0)`). Das ist aber NICHT der letzte Schritt: output_manager._send_all liest anschliessend `universe.get_all()` (Zeile 297) und wendet die Channel-Modifier an (Zeile 300-301 `ge **Fix (dieser PR):** OutputManager bekommt eine `_laser_estop_mask` (spiegelt `set_gm_address_mask`); `_send_all` erzwingt die verriegelten Laser-Adressen FINAL — nach Channel-Modifier **und** Grand-Master/Blackout — auf 0 (die allerletzte Transformation vor Anzeige-Snapshot/Senden). AppState-Helfer `_push_laser_estop_mask()` pflegt die Maske bei jeder Änderung von `laser_estop_active` (set_laser_estop / Latch-Clear in set_programmer_value / Plan-Rebuild). Regressionstest `tests/test_laser_estop_modifier_bypass.py` (INVERSE→0, Range-Lock→0, schlägt GM/Blackout, nur maskierte Adressen, AppState-Push-Integration, End-to-End Renderer+Sendepfad). **Adversariale 3-Linsen-Review:** ein realer Fund gehärtet — Flag-Write + Mask-Push jetzt unter `_get_estop_lock` gekoppelt (sonst Race Latch-Clear[MIDI-Thread] ↔ set_laser_estop[UI-Thread] → stale Push mit `active=True`/leerer Maske = Backstop aus). A3D-02 (harmloses Attribut löst Latch) folgt separat. |
| A3D-02 | P2 | ✅ done (dieser PR) | [LASER-SAFETY] Setzen eines HARMLOSEN Laser-Attributs (laser_x/zoom) loescht den NOT-AUS-Latch -> Laser oeffnet wieder | **Ort:** `src/core/app_state.py:920` (neu). **Bug:** set_programmer_value loescht den DMX-Laser-NOT-AUS-Latch fuer JEDES Attribut eines Laser-Fids (Zeile 920-922: Guard prueft nur `fid in _laser_fids`, NICHT das Attribut). NOT-AUS zeroed nur den DMX-OUTPUT, nicht den Programmer-Zustand — der Programmer behaelt seinen vor dem Estop gesetzten Shutter-/Betriebsart-Wert (z. B. 'offen'=200). **Fix (dieser PR):** Whitelist-Konstante `_LASER_REARM_ATTRS = {shutter, laser_bank, gobo_wheel}` (die emissions-/betriebsart-relevanten Gates); der Latch-Clear-Guard in `set_programmer_value` prüft jetzt zusätzlich `attribute in _LASER_REARM_ATTRS` (rohes Attribut → gilt für alle Köpfe). Ein harmloses Attribut (Position/Zoom/Farbe/Twist/…) hält den NOT-AUS jetzt gedrückt; nur ein bewusster Muster-/Bank-/Shutter-Abruf = „wieder an". Tests in `tests/test_laser_dmx_estop.py` (harmlos hält Latch + bleibt dunkel, alle Gates lösen). **Adversariale Review-Härtung:** `macro` in die Whitelist (deckt macro-getriebene Party-Laser wie das PARTYLASER-Builtin ohne laser_bank/gobo_wheel ab — Programm-Auswahl = „wieder an") + Basisnamen-Check via `attribute.split("#",1)[0]`, damit auch die Snap-/VC-Restore-Pfade (die den Composite-Key `attr#N` als attribute durchreichen) für Kopf>0-Gates korrekt entriegeln. |
| A3D-03 | P2 | ✅ done | [Web/OSC] Web-Remote bindet 0.0.0.0:5000 voellig ohne Authentifizierung (NET-01) | **Ort:** `src/web/app.py:156` (Codex 288). **Bug:** Der komplette Auth-Layer, den die Projekt-Invariante beschreibt (before_request-Gate mit session[epoch] vs. get_auth_epoch, Socket.IO-Gate) existiert im AKTUELLEN Code NICHT. create_app() (Z.37-52) registriert nur _register_routes/_register_socketio; es gibt keinen @app.before_request, keine Login-Route, keine session-/authed-/epoch-Pruef **Fix:** before_request-Gate + Session-Login (Passwort/Token) wiederherstellen und im Socket.IO connect-Handler dieselbe Auth+Epoch pruefen; cors_allowed_origins auf konkrete Origins statt '*' einschraenken. **✅ Verifiziert erledigt (Backlog-Hygiene, kein Codeaenderung): Auth-Layer NET-01 deckt DoD ab — before_request-Gate (`_register_auth`/`_auth_gate`, `app.py:185-207`: `session['authed'] is True` UND `session['epoch'] == get_auth_epoch()`) + Token-Login (`?k=<token>` constant-time gegen `LIGHTOS_REMOTE_TOKEN`, `app.py:212-231`) + Socket.IO-connect-Auth (`on_connect` disconnected unauthentisierte WS, `app.py:331-341`) + CORS-Allowlist statt '*' (`cors_allowlist()`, `app.py:108/161`, NET-03). Vermutlich in der NET-01-Runde geschlossen; der 3D-Audit (2026-07-16) war bei der Aufnahme bereits stale.** |
| A3D-04 | P2 | ✅ done (dieser PR) — `_poll_set_dmx` mergt jetzt pro fid in ein dict statt den letzten Batch zu behalten; `pollControl` serialisiert die Werte. **Adversariale Vorprüfung fand 3 Blocker in der naiven Umsetzung, alle vermieden:** (1) `out["dmx"]` MUSS ein JSON-**String** bleiben — JS macht `JSON.parse(s.dmx)`; eine Liste dort hinein hätte geworfen, der Wurf wäre im äusseren `catch` des Poll-Handlers gelandet und hätte den DANACH folgenden `events`-Block übersprungen, **während Python die Event-Queue beim Ausliefern schon geleert hat** → stiller Totalverlust aller Einmal-Events (Kamera/Transform/Stage) in jedem Poll mit DMX. Zusätzlich hat der `dmx`-Block in `bridge.js` jetzt ein eigenes try/catch. (2) `_poll_set_dmx` trägt **keinen** `_bridge_slot_guard`, und `visualizer_service` bedient seine Targets in einer Schleife **ohne** try/except → ein Wurf hätte das zweite Target (Live-View-Spiegel) um seinen Batch gebracht, also genau die Verlustklasse verschlimmert; Merge-Body daher defensiv. (3) **Ganz-Payload-Ersatz pro fid, niemals `dict.update`** — `heads` hängt nur bei `head_count>=2` am Payload; ein Key-Merge hätte ein veraltetes `heads`-Array stehen lassen, das JS dauerhaft cacht (`if (heads) f.lastHeads = heads`) → permanent falsche Pro-Kopf-Farben bei Spider/PAR-Bar/Mover-Bar. Dazu ein Backstop-Deckel analog zum 512er von `_poll_events` (bei totem Renderer pusht der Service weiter in einen Puffer, den niemand leert). Test `tests/test_a3d04_poll_dmx_merge.py` (7), davon 3 auf altem Code rot verifiziert. | ~~[3D-Bridge] pollControl coalesciert differentielle dmxBatch-Frames -> persistent verlorene Fixture-Updates | **Ort:** `src/ui/visualizer/visualizer_window.py:442` (neu). **Bug:** Der Pull-Poll ist laut Code-Kommentar (Z.376-387) der EINZIGE zuverlaessige Python->JS-DMX-Weg (QtWebEngine drosselt Push-Signale an die eingebettete Post-Load-Seite). `_poll_set_dmx` (Z.441-443) speichert aber nur den LETZTEN Batch: `self._poll_dmx = batch_json` (Ueberschreiben, kein Merge). Die Batches vom VisualizerService sind DIFFERE **Fix:** In `_poll_set_dmx` die Batches per fid mergen statt ueberschreiben (Dict {fid: payload}, beim Poll als Array serialisieren) — differentielle Frames duerfen zwischen zwei Polls nicht verloren gehen.~~ (Ur-Text durchgestrichen: Befund war korrekt, die naive Umsetzung aber nicht — s. Status.) |
| A3D-05 | P2 | ✅ done ([#356](https://github.com/ixamgames-droid/lightos/pull/356)) | [3D-Beams] showCones/showFloorSpots-Toggle resynct Multi-Head-Per-Head-Beams (PAR-Bar / Mover-Bar / Spider) nicht | **Ort:** `src/ui/visualizer/scene_src/bridge/bridge.js:172` (Codex PR#168). **Bug:** applySettings() (bridge.js:170-177) iteriert alle Fixtures und setzt beim Settings-Wechsel NUR f.beam (172), f.floorSpot (173) und f.laserBeams (176) neu. Die Per-Head-Kegel der Multi-Head-Typen werden nie angefasst: PAR-Bar f.parHeads[*].beam (in addFixture gesetzt, fixtures.js:167-174), Mover-Bar f.moverHeads[*].beam (fixtures.js:178-18 **Fix:** In der Fixture-Schleife von applySettings zusaetzlich f.parHeads / f.moverHeads / f.bars durchlaufen und ihre .beam(s) analog zu f.beam auf settings.showCones && view.mode==='3D' && opacity>0.01 setzen (gemeinsamer Helfe |
| A3D-06 | P2 | ✅ done (dieser PR) — **Item war FALSCH spezifiziert; empirisch nachgemessen.** Die DoD verlangte `_write_back_to_live_view` nach jedem Setzen von `visualizer_positions`. Seit VIZ-11 sind `visualizer_positions` (`_SceneBackedDict`) und `live_view_positions` (`_LiveViewDict`) aber **zwei Projektionen DESSELBEN SceneGraph** — sie koennen gar nicht driften. Messung auf echtem AppState: `pos=(3,6,-2)` → `live=(360.0,160.0)`; nach `undo()` → `pos=(0,6,0)`, `live=(300.0,200.0)`, **ohne jeden Write-Back**. Der geforderte Fix waere also ein No-op — und schaedlich: der Roundtrip `world3d_to_live`→`live_to_world3d` verfaelscht die Weltposition um 1 ULP (`12.345` → `12.344999999999999`). **Der echte Schaden war ein anderer:** `_on_fixture_pos_spin_changed` emittierte GAR KEIN `LIVE_VIEW_CHANGED` → per Spinbox gesetzte Positionen machten die Show nie „dirty", der Autosave uebersprang sie still (einziger Abnehmer des Events ist `_mark_autosave_dirty`; einen 2D-Repaint-Subscriber gibt es nicht). Gefixt: genau ein Emit je Commit und je Undo/Redo-Anwendung (`on_applied`-Callback, `scene_commands` bleibt UI-frei). ⚠️ Ein Test „live_view folgt nach undo" waere auch OHNE Fix gruen — taugt also nicht als Beleg. | [3D-Coords] 3D-Positions-Spinbox (und Transform-Undo/Redo) schreiben X/Z NICHT in die Live View zurueck -> 2D/3D-Desync + stiller Revert der Eingabe | **Ort:** `src/ui/visualizer/visualizer_window.py:2250` (neu). **Bug:** Der Spinbox-Commit `_on_fixture_pos_spin_changed` setzt `self._state.visualizer_positions[fid] = (x, y, z)` (Zeile 2250) und ruft danach nur `push_apply_fixture_transform` (reines Emit an JS, visualizer_window.py:1197-1206, kein State) sowie `push_transform_and_dock_fixture` auf. Dessen `_apply` (src/core/stage/scene_commands.py:176) mutiert wiederum nur `state.visualizer_positions` — KEINER dieser Pfade ruft `_write_back_to_live_view` bzw. `world3d_to_live` auf. Damit wird die dokumentierte Invariante 'Live View = Single Source of Truth fuer X/Z; nie visualizer_positions ohne _write_back setzen' verletzt. Zum Vergleich: der Drag-Pfad `fixturePositionChanged` (Zeile 565), `fixtureGestureEnd` (Zeile 649) und `place_fixture_at` (Zeile 1080) schreiben alle korrekt zurueck; nur der Spinbox-Edit einer bereits platzierten Fixture und der Undo/Redo-Apply der Transform-Commands fehlen. **Fix:** Nach jedem Setzen von visualizer_positions[fid] auch `_write_back_to_live_view(fid, x, z)` aufrufen — sowohl in `_on_fixture_pos_spin_changed` als auch im `_apply` von `push_transform_and_dock_fixture`/`push_transform_fixtures` (z.B. per on_change-Callback), damit Undo/Redo die Live-View-X/Z konsistent mitfuehren. |
| A3D-07 | P2 | ✅ done (dieser PR) | [3D-GPU] removeFixture disposed die SpotLight-Shadow-Map nicht — GPU-RenderTarget-Leak pro Show-Reload | **Bug:** `removeFixture` (fixtures.js) entfernt ein Fixture via `f.group.traverse(disposeObj)`. Der Per-Fixture-`SpotLight` (`root.add(spot)`, `shadow.mapSize` gesetzt) wird mit-traversiert, aber `disposeObj` (scene/grid_floor.js) gab NUR geometry+material frei, NICHT `spot.shadow` → das GPU-Shadow-RenderTarget leckte pro Show-Reload (wächst bis Context-Loss auf schwacher GPU). **Fix (robuste Variante):** `disposeObj` gibt zusätzlich `light.shadow` frei (`if (o.isLight && o.shadow && typeof o.shadow.dispose==='function') o.shadow.dispose()`). Deckt den Spot über den bestehenden Traverse ab + etwaige Pro-Kopf-Lichter, an EINER Stelle. Verifiziert: `disposeObj` läuft nur über Fixture-/Stage-/Grid-Objekte, NIE über die persistenten Szenen-Lichter (kein Fehl-Dispose). (`floorSpot` ist ein `Mesh`-Disc, kein Light → kein Shadow.) Test-Seam `__lightos.disposeObj`; neuer Test `tests/test_viz_shadow_dispose.py` (3: Light-Shadow-Dispose, geometry/material-Regression, kein Throw ohne Shadow). |
| A3D-08 | P2 | ✅ done (dieser PR) — **Befund korrekt, der im Item vorgeschlagene Fix aber NICHT.** Die DoD verlangte „`findDockTarget` aufrufen und `_pendingDock` setzen (analog Translate-Pfad)". Die adversariale Vorprüfung zeigte, dass das die Lage verschlechtert hätte: (a) **`floor` ist ein Andock-Ziel** (`DOCK_TOP_TYPES`) und `findDockTarget` nimmt den obersten Treffer eines Strahls von oben — ein von der Traverse weggedrehtes Gerät hätte also den **Boden** als Ziel bekommen und wäre danach „steht auf dem Boden" auf 7 m Höhe gewesen; (b) alle bisherigen `_pendingDock`-Schreiber setzen im selben Zug `f.group.position.y = dt.y` — dieser Y-Snap mitten in einer Drehung zerreisst die Gruppe, und ohne ihn holt `moveDockedFixtures` den Sprung beim nächsten Bewegen des Bühnenelements ohnehin nach (nur später und unerklärlicher); (c) bei **Einzel**-Auswahl ist `start == pivot` → die Rotation verschiebt gar nichts, hätte aber trotzdem ein bewusst per Y-Achse freigestelltes Gerät wieder angedockt (direkt gegen den Kommentar „Explizite Höhe → Andocken lösen"). **Umgesetzt stattdessen: Bindung LÖSEN**, sobald die Drehung das Gerät wirklich versetzt hat — exakt das, was der Bugtitel verlangt („bleibt gedockt"), und dieselbe Entscheidung mit derselben Begründung wie der bestehende Ausrichten/Verteilen-Pfad in `bridge.js`. | ~~[3D-Pointer] Multi-Select-Gizmo-Rotation laesst weggedrehtes Fixture an alter Trasse gedockt | **Ort:** `ui/visualizer/scene_src/interaction/pointer.js:369` (Codex 159). **Bug:** Der Rotate-Zweig des Gizmo-Drags (handlePointerMove, dragMode==='gizmoDrag' && mode==='rotate', Z. 346-381) veraendert bei Multi-Select die Fixture-POSITIONEN (Orbit um den gemeinsamen Pivot, Z. 367-372), ruft aber KEIN findDockTarget und setzt _pendingDock NICHT (anders als der Translate-Zweig Z. 331-335 und der Body-Drag Z. 288-292). In **Fix:** Im Rotate-Zweig pro bewegtem Fixture bei settings.dockEnabled findDockTarget(x,z) aufrufen und f._pendingDock setzen (analog Translate-Pfad Z. 331-335), inkl. Dock-Highlight/Badge-Update. |
| VIZ-CLICKDOCK | P2 | ✅ done (dieser PR) | [3D-Pointer] **Ein blosser Auswahl-Klick loescht die gespeicherten Andockungen — der GANZEN Auswahl** | **Beim Verifizieren von A3D-08 gefunden, stand in keinem Item.** `handlePointerDown` setzt `dragMode='fixtureDrag'` bei JEDEM Treffer eines Fixture-Koerpers (und `'gizmoDrag'` bei jedem Gizmo-Handle) — **ohne jede Bewegungsbedingung**; der Commit-Zweig in `handlePointerUp` hatte keinen Guard (`mouseMovedDuringClick` existiert, wird aber NUR fuers Aim/Trace-Werkzeug ausgewertet). Bei `settings.dockEnabled == false` — dem **Default** (`state.js`, opt-in) — faellt der else-Zweig auf `newDock = null` → `hasDockChange` → Python popt `visualizer_docks[fid]`. Ein einziger Klick zum Auswaehlen loeschte damit die Andockung, und weil die Schleife ueber `view.selectedFids` laeuft, **gleich fuer alle markierten Geraete** (Klick in eine 20er-Auswahl = 20 entdockte Geraete). Gilt auch fuer einen Klick auf ein Gizmo-Handle ohne Mausbewegung. **Fix:** die Dock-Entscheidung als reine Funktion `resolveDockOnGestureEnd` herausgezogen (Test-Seam `window.__lightos.__resolveDockOnGestureEnd`) und auf „Position hat sich wirklich geaendert" gegatet. `pointer.js` hatte bis dahin NULL Verhaltensabdeckung — neuer Test `test_gesture_end_dock_decision` (6 Faelle) in `tests/test_viz13_scene_modules_smoke.py`, auf der Alt-Logik rot verifiziert. |
| A3D-09 | P2 | ✅ done (dieser PR) — Neuer Bridge-Slot `fixturesTransformBatch` + `push_transform_and_dock_fixtures` (Plural); die Singular-Funktion ist jetzt ein 1-Element-Delegate (ein Codepfad, bestehende Tests unveraendert gruen). JS sammelt die Gestik und schickt EINEN Aufruf, dreistufiger Fallback (Batch → Einzel-Buendel → Legacy-Slots). **Warum kein `begin_group`/`end_group` im UndoStack** (3-Linsen-Panel + Judge): die Vervielfachung entsteht in JS-SCHLEIFEN, nicht in einem Python-Frame — es gaebe gar keinen Ort zum Klammern, `begin_group` braeuchte ZWEI zusaetzliche Bridge-Slots statt des einen Batch-Wegs. Dazu: der UndoStack ist global geteilt (Patch/Programmer/VC), eine ueber die Bridge-Grenze offene Gruppe finge fremde Commands ein, und ein verlorenes `end_group` (Renderer-Crash) toetete Strg+Z fuer die restliche Sitzung — dieselbe Bug-Form hatte das Modul mit `_reloading_stage` schon einmal. `VIZ11_SCENEGRAPH_DESIGN.md` lehnt „Koaleszenz-Logik im Stack" ausdruecklich ab. **Nebenbei gefixt:** `MAX_SIZE=100` mit hartem Abschneiden — ein Multi-Drag ueber 100 Fixtures loeschte die komplette Undo-Historie der Sitzung. | [3D-SceneGraph] Multi-Select-3D-Drag erzeugt EIN Undo-Command PRO Fixture statt eines fuer die ganze Geste | **Ort:** `src/ui/visualizer/scene_src/interaction/pointer.js:518` (Codex 159). **Bug:** Beim Drag-Ende (dragMode 'fixtureDrag'/'gizmoDrag') laeuft in pointer.js eine Schleife `for (const fid of view.selectedFids)`, die `bridge.fixtureGestureEnd(...)` EINMAL PRO ausgewaehltem Fixture aufruft (Z.518-551). Jeder Aufruf landet in VisualizerBridge.fixtureGestureEnd (visualizer_window.py:594) und pusht ueber `_scmd.push_transform_and_dock_fixture` (visualizer_window.py:654) einen EIGENEN Undo-Command auf den globalen Stack (scene_commands.py:187). Der UndoStack (src/core/undo.py) hat KEINE Gruppen-/Transaktions-Klammer, es entstehen also N getrennte Commands. Der Kommentar pointer.js:515 ('Python buendelt zu EINEM Undo-Command') ist falsch: Python buendelt nur Position+Rotation+Dock EINES Fixtures, nicht mehrere Fixtures. Der Codex-Fund aus PR#159 ist im aktuellen Code weiterhin LEBEND. **Fix:** JS soll die Geste als EIN Batch-Event (Liste aller fids mit neuen Transforms) senden, die genau EINEN push_transform_fixtures/push_transform_and_dock-Command mit allen Eintraegen erzeugt; alternativ begin_group/end_group-Klammer im UndoStack. |
| A3D-10 | P2 | ✅ done (dieser PR) — `fixturesTransformBatch`/`fixtureGestureEnd` pushen jetzt mit `apply_push`, sodass do/undo/redo die 3D-Ansicht mitfuehren. Der Dock reist dabei als **optionales Feld an der bestehenden `applyFixtureTransform`-Payload** mit statt als neues Signal: Python→JS-Signale erreichen die Post-Load-Seite nicht zuverlaessig, ein neues Signal muesste zusaetzlich in die Poll-Spiegel-Liste und wuerde den „22 Signale"-Vertrag des Smoke-Tests brechen — und einen Python→JS-Dock-Kanal gab es ueberhaupt nicht (`f.dockedTo` kam sonst nur aus der `addFixture`-Payload). Sentinel `_KEEP_DOCK` statt `None`, weil `None` ein GUELTIGER Wert ist („kein Dock"): sonst haette jeder Alt-Aufrufer still entdockt. Verifiziert, dass daraus KEINE Echo-Schleife entsteht — `jsApplyFixtureTransform` mutiert nur Mesh/Icon und ruft keinen Slot zurueck; zweite Ebene ist der `_suspended`-Guard im UndoStack. | [3D-SceneGraph] Undo/Redo einer 3D-Drag-Geste aktualisiert 3D-Ansicht und Dock nicht -- State/Visual desynchronisieren | **Ort:** `src/ui/visualizer/visualizer_window.py:654` (neu). **Bug:** fixtureGestureEnd ruft `push_transform_and_dock_fixture` OHNE `apply_push` und OHNE `on_dock_change` auf (visualizer_window.py:654-660). Der Command-_apply (scene_commands.py:175-185) setzt daher bei do()/undo()/redo() nur den AppState, pusht aber nichts an JS und ruft keinen Dock-Callback. Kein kompensierender Mechanismus: der Visualizer **Fix:** In fixtureGestureEnd `apply_push` (wie Spinbox-Pfad Z.2270) und ein `on_dock_change` mituebergeben, damit do/undo/redo 3D-Ansicht und Dock-Zustand mitfuehren. |
| A3D-11 | P2 | ✅ done (verifiziert stale 2026-07-21 — war bereits gefixt, Backlog nachgezogen) | [3D-SceneGraph] Loeschen eines Buehnen-Elements teleportiert gedockte Fixtures zum Ursprung (keine Welt-Erhaltung) — **bereits gefixt:** `scene_graph.py` `remove()` nutzt schon `self.reparent(child.id, None, keep_world=True)` (Z.168, mit Kommentar), Docstring auf Welt-Erhaltung korrigiert, `test_scene_graph.py::TestRemove` deckt genau das ab (world_pos bleibt nach remove erhalten; 21 Tests grün). Z.516 (`parent_id=None`) ist ein SEPARATER Legit-Fall (ungueltige Parent-Refs aus defekten Dateien verwerfen — kein Welt-Transform zu erhalten). | **Ort:** `src/core/stage/scene_graph.py:162` (neu). **Bug:** SceneGraph.remove(..., reparent_children_to_root=True) setzt fuer jedes Kind nur `child.parent_id = None` (Z.161-163) OHNE die lokale Transform per keep_world in Welt-Koordinaten umzurechnen. Die Docstring (Z.157-158) verspricht aber ausdruecklich 'sie schweben an ihrer aktuellen Welt-Position weiter'. Da eine gedockte Fixture ihre Transf **Fix:** In remove() die Kinder per `self.reparent(child.id, None, keep_world=True)` loesen statt parent_id direkt zu nullen; Docstring und test_scene_graph.py auf Welt-Erhaltung korrigieren. |
| A3D-13 | P2 | ✅ done (dieser PR) | [3D-Kameras] Benannte Kameras + Kamera-Menue werden bei show_loaded nicht neu synchronisiert | **Erledigt (dieser PR):** Im `show_loaded`-Zweig von `_on_state` (`visualizer_window.py`) werden jetzt `cams = list(getattr(self._state, "visualizer_named_cameras", []) or [])`, `self._bridge.push_named_cameras(cams)` und `self._rebuild_camera_menu()` aufgerufen — 1:1 wie in `_push_initial_state`. Damit uebernimmt ein BEREITS offenes Visualizer-Fenster beim Show-Wechsel die benannten Kameras + das Toolbar-Menue der neuen Show (leere Liste raeumt alte Kameras weg). Regressionstest `tests/test_a3d13_show_loaded_camera_resync.py` (rot ohne Fix: `push_named_cameras` 0×; deckt Push, Menu-Rebuild, Leerliste-Clear, fehlendes State-Attr, `patch_changed`-Nichtberuehrung, Fehler-Swallow). Schliesst **A3D-22** (selber Codex-157-Fund) mit. _(Ur-Befund: `_on_state('show_loaded')` rief weder `push_named_cameras` noch `_rebuild_camera_menu`; der Resync lief nur in `_push_initial_state` beim ersten Andocken.)_ |
| A3D-14 | P2 | ✅ done (bereits in `main`) | [DMX-Output] Per-Tab-Apply (Art-Net/sACN/Enttec) entfernt Fremd-Adapter desselben Universums nicht -> Doppel-Output | **Verifiziert erledigt (diese Runde, kein Codeaenderung noetig):** Auf aktuellem `main` rufen alle drei Tab-Handler `remove_output(univ)` VOR dem `add_*`: `_connect_enttec` (`output_config.py:301`→`:302` add_enttec), `_apply_artnet` (`:330`→`:331`), `_apply_sacn` (`:437`→`:438`) — dazu raeumen `_apply_artnet`/`_apply_sacn` zusaetzlich das zuvor aktive Universe (`_artnet_active_univ`/`_sacn_active_univ`) ab. Pro Universe existiert damit genau ein Adapter, kein Doppel-Output. Codex-287-Befund war bei der Ernte bereits geschlossen. |
| A3D-15 | P2 | ✅ done (dieser PR) | [DMX-Output] Art-Net-Startuniversum-Spinbox ist komplett unverdrahtet — externe Universe-Nummer nicht einstellbar | **Ort:** `src/ui/widgets/output_config.py:109` (neu). **Bug:** _spin_artnet_start_univ (Range 0-32767) wird in _setup_ui angelegt, aber NIRGENDS ausgelesen (Grep: nur die drei Erzeugungszeilen 109-111, keine .value()-Referenz). _apply_artnet (:291) liest nur ip und univ, nie das Startuniversum. _send_all sendet Art-Net hart mit univ_num-1 (output_manager.py:335). Die externe Art-Net-Universe ist dami **Fix:** Startuniversum in _apply_artnet auslesen und als Art-Net-Universe an den Sender/Send-Pfad durchreichen (statt hart univ_num-1), oder das Feld entfernen, wenn nicht unterstuetzt. **✅ done (dieser PR):** `_apply_artnet` liest jetzt `_spin_artnet_start_univ` und reicht sie als `out_universe` an `add_artnet` durch (Send-Logik existierte schon: `_send_all` ehrt `_out_universe`, Default `univ_num-1`); `_persist_output` erweitert → externe Universe überlebt Neustart (`apply_output_config` reicht sie bereits durch), abwärtskompatibel „leer=Default" wie die Tabellen-Spalte OUT-03. **Kein Regressions-Surprise:** neue `_sync_artnet_start_univ_default` stellt die Spinbox beim Universumswechsel/Setup auf die gespeicherte externe Universe bzw. Default `univ-1` — untouched → `out_universe=None` → exakt Alt-Verhalten (kein stiller Universe-0-Apply). sACN unberührt. Tests `tests/test_a3d15_artnet_start_universe.py` (4); Backend-Pfad weiter durch `test_external_universe_number.py`. Adversariale Review (Send-Regression/Persistenz/Edge) am Output-Pfad Pflicht. |
| A3D-16 | P2 | ✅ done (dieser PR) | [Engine] manual_crossfade Commit ohne Bounds/Identity-Check auf _manual_target -> IndexError-Crash bzw. falscher Cue mit alten We | **Ort:** `core/engine/cue_stack.py:275` (Codex 259). **Bug:** In manual_crossfade() wird beim Armieren _manual_target = nxt (ein Index in self.cues zum Arm-Zeitpunkt) gemerkt und der Fade auf self.cues[nxt].values gestartet. Beim Commit (pos>=1.0, Zeile 269-275) wird OHNE erneute Pruefung self._current_idx = self._manual_target gesetzt und commit_cb = (self._current_idx, self.cues[self._current_idx] **Fix:** Beim Armieren die Ziel-Cue-Referenz (Objekt) statt nur den Index merken und beim Commit den aktuellen Index per Identity in self.cues neu suchen; ist die Ziel-Cue nicht mehr vorhanden, Commit abbrechen/Fade verwerfen. **✅ Erledigt (dieser PR):** Der Crash-/IndexError-Teil war bereits durch den frueheren F4-Fix gedeckt (Bounds-Check im Commit + `_reindex_after_mutation`-Klemmung). Diese Runde schliesst die verbliebene IDENTITAETS-Luecke: die Ziel-Cue wird beim Armieren als Objekt-Anker (`_manual_target_cue`) gemerkt, der Commit loest den Index per Identity neu auf (Ziel entfernt -> Fade sauber verworfen statt falscher/ungueltiger Commit), `_reindex_after_mutation` fuehrt den Anker bei jeder Mutation nach (in-bounds-aber-verschoben trifft nicht mehr die falsche Cue), GO/BACK verwirft einen armierten Scrub. Regressionstests `tests/test_cue_stack_live_mutation.py::ManualCrossfadeTargetIdentityTest` (Insert-/Remove-Verschiebung + GO-Abbruch; auf altem Code rot verifiziert). |
| A3D-18 | P2 | ✅ done (dieser PR) | [AppState/Merge] Entfernen/Umadressieren eines Fixtures nullt seine bisherigen DMX-Adressen NICHT -> eingefrorener Zombie-Kanal im Live-U | **Ort:** `src/core/app_state.py:687` (neu). **Bug:** _rebuild_render_plan baut _default_frame/_commit_spans/_patched_set neu, gibt aber NUR die Engine-Extra-Kanaele frei (_release_engine_extra). Fuer Adressen, die zuvor durch ein jetzt entferntes/umadressiertes Fixture GEPATCHT waren, passiert nichts: _render_frame committet in Schritt 5 nur die neuen Spans → die alte Adresse behaelt ihren letzten Wert. **Fix (dieser PR):** `_rebuild_render_plan` merkt vor dem Tausch einen Snapshot des alten `_patched_set`; neuer Helfer `_release_unpatched_addrs(old, new)` nullt die Differenz `alt − neu` im Live-Universe (weiter gepatchte/umadressierte Adressen bleiben, sie committet der naechste Frame). Defensiv/thread-safe wie `_release_engine_extra`. **Adversariale Review-Härtung:** ein bestätigter TOCTOU-Fund — ein Render-Frame mit stale altem Plan-Snapshot konnte die alte Span NACH dem UI-Thread-Nullen erneut committen (permanenter Zombie). Fix: die freigegebenen Adressen zusätzlich in `_pending_release` vormerken; `_render_frame` nullt sie in Schritt 5 NACH dem Span-Commit deterministisch aus dem Render-Thread (single-threaded → nächster Frame konsumiert das Pending). Test `tests/test_zombie_channel_release.py` (Entfernen/Umadressierung + Race-Resilienz: nachlaufender Alt-Commit wird final überschrieben). |
| A3D-19 | P2 | ✅ done (dieser PR) | [Persistenz] OverflowError bei 1e999 im Programmer/Base-Level-Load umgeht Per-Value-Guard und loescht den GESAMTEN Programmer bzw. al | **Ort:** `src/core/show/show_file.py:779` (Codex 220). **Bug:** Im programmer-Load (Zeilen 776-783) konvertiert `int(v)` jeden Attributwert; der Per-Value-Guard faengt aber nur `except (TypeError, ValueError)`. `json.loads` parst `1e999` (und `-1e999`, `1e400`) zu `float('inf')`; `int(float('inf'))` wirft `OverflowError`, die NICHT von (TypeError, ValueError) erfasst wird → schlug bis zum aeusseren `except` durch und setzte `state.programmer = {}` (Verlust-Amplifikation). **Fix (dieser PR):** beide Per-Wert-Guards (Programmer `show_file.py:804` UND Base-Levels `:831`) auf `except (TypeError, ValueError, OverflowError)` erweitert → nur der eine inf-Wert faellt, der Rest ueberlebt. Test `tests/test_show_file_overflow_guard.py` (1e999 + -1e999 im Programmer und in Base-Levels; guter Wert bleibt, inf uebersprungen, kein Total-Reset). |
| A3D-22 | P3 | ✅ done (dieser PR) | [3D-Bridge] show_loaded pusht bei offenem Fenster die benannten Kameras der neuen Show nicht neu | **Erledigt (dieser PR):** Duplikat von **A3D-13** (selber Codex-157-Fund, selbe Codestelle `_on_state('show_loaded')`) — mit demselben Fix erledigt: `push_named_cameras(cams)` + `_rebuild_camera_menu()` im `show_loaded`-Zweig. Siehe A3D-13 + `tests/test_a3d13_show_loaded_camera_resync.py`. |
| A3D-24 | P3 | ✅ done ([#356](https://github.com/ixamgames-droid/lightos/pull/356)) | [3D-Beams] View-Wechsel 2D->3D stellt Multi-Head-Per-Head-Beams nicht wieder her (bleiben unsichtbar) | **Ort:** `src/ui/visualizer/scene_src/stage/view_mode.js:31` (Codex PR#170). **Bug:** setViewMode() (view_mode.js:26-40) resynct beim Moduswechsel gezielt f.beam (31) und f.laserBeams (35-39) — genau wegen des VIZ-03-Problems, dass deren .visible-Flag den Term view.mode==='3D' enthaelt und sonst auf dem 2D-Stand 'false' klebt. Exakt derselbe view.mode==='3D'-Term steht in den Multi-Head-Sichtbarkeitsbed **Fix:** In der Fixture-Schleife von setViewMode dieselben Multi-Head-Beam-Listen wie in bridge.js resyncen (gemeinsamer Helfer). |
| A3D-33 | P3 | ✅ done ([#360](https://github.com/ixamgames-droid/lightos/pull/360)) | [DMX-Output] Universe-Manager '#'-Spalte akzeptiert beliebige Zahl (-1 / 70000) -> Art-Net wirft, sACN wrappt still | **Ort:** `src/ui/widgets/output_config.py:355` (Codex 289). **Bug:** Die '#'-Spalte des Universe-Tables ist ein freies QTableWidgetItem. _univ_save (output_config.py:355) parst nur int(), ohne Range-Clamp, und persistiert num nach universes.json. apply_output_config (app_state.py:766-785) nutzt num direkt als Universe-Key und add_artnet/add_sacn. Anders als die 1-32-Spinboxen der Tabs g **Fix:** In _univ_save (oder _univ_add) die Universe-Nummer auf 1..32 (bzw. gueltigen Bereich) clampen/ablehnen, bevor persistiert und angewendet wird. |
| A3D-34 | P3 | ✅ done (dieser PR — Kern war schon durch CDX-03 (Zwei-Pass) gefixt; die im Finding genannten Rest-Edges geschlossen: leeres `Number=""` wird wie „ohne Nummer" behandelt (Pass 2 → naechste freie), negative `Number` (`int("-1")+1==0`) wird sichtbar verworfen statt eine ungueltige channel_number ≤ 0 zu patchen. Tests `test_qxf_import_missing_number.py` +2) | [Fixture-DB] QXF <Channel> ohne Number-Attribut kollidieren alle auf channel_number==1 | **Ort:** `core/database/qxf_import.py:523` (Codex 280). **Bug:** In der Mode-Schleife wird die DMX-Offset-Nummer via `num = int(ch_ref.get("Number", "0")) + 1` bestimmt. Fehlt das Number-Attribut komplett (hand-editierte/aeltere QXF), liefert `.get("Number", "0")` den Default "0" -> `int("0")+1 == 1`. Der `except (ValueError, TypeError)` greift NUR bei nicht-numerischen Strings, nic **Fix:** Absenz/None des Number-Attributs wie ungueltig behandeln: raw = ch_ref.get("Number"); bei None/"" auf die enumerate-Position zurueckfallen (nicht len(ch_refs)), negative Werte ablehnen. |
| A3D-35 | P3 | ✅ done (dieser PR — 3-Agent-Debatte → C_hybrid: `_lookup_profile` waehlt jetzt TOTAL-deterministisch `ORDER BY builtin-first, dann id ASC` (statt arbitrary `.first()` ohne ORDER BY); bei Mehrdeutigkeit lautes `[showbuilder] WARN` mit voller Kandidatenliste (1×/short_name/Builder); opt-in strict-Modus (`ShowBuilder(strict_profiles=True)` / env `LIGHTOS_STRICT_PROFILES=1`) → harter `BuildError` fuer CI (Raise ausserhalb des DB-try/except, nicht als „nicht lesbar" fehl-umgewickelt). Tests `test_a3d35_lookup_profile_dedup.py`, 10) | [Fixture-DB] _lookup_profile nutzt .first() ohne ORDER BY -> patcht bei doppeltem short_name stilles falsches Profil | **Ort:** `core/show/showbuilder/builder.py:105` (Codex 203). **Bug:** `_lookup_profile` selektiert `FixtureProfile.id/fixture_type WHERE short_name == short_name` und nimmt `.first()`. FixtureProfile.short_name hat KEINEN Unique-Constraint (models.py:34: mapped_column(String(40), default="")). Doppelte short_names sind real erreichbar: der QXF-Import dedupliziert nur ueber (manufacturer_ **Fix:** Auf Eindeutigkeit pruefen: .all() laden; bei >1 BuildError mit Kandidatenliste werfen oder deterministisch priorisieren (Builtin/kleinste id) und dokumentieren. |
| A3D-37 | P3 | ✅ done (dieser PR) | [AppState/Merge] CMY-only-Fixture (kein Dimmer) wird als Intensity-Fallback multiplikativ Richtung 0 gedimmt -> Blackout/GM oeffnet CMY = | **Ort:** `src/core/app_state.py:1893` (Codex 176). **Bug:** _fixture_intensity_addrs (Z.1878-1893) liefert bei fehlendem echten Dimmer die Farbadressen als Intensity-Fallback zurueck (`return inten if inten else color`). `_DIM_COLOR_ATTRS` enthaelt bewusst cmy_c/cmy_m/cmy_y (Z.41-48). Diese Fallback-Adressen werden anschliessend MULTIPLIKATIV Richtung 0 skaliert: im 4b-Dimmer-M **Fix:** Subtraktive (CMY-)Farbkanaele nicht als Intensity-Fallback fuer Dimmer-Master/GM/Blackout verwenden; entweder aus dem Fallback ausschliessen oder invertiert skalieren (Richtung 255 = geschlossen). |
| A3D-38 | P3 | ✅ done | [AppState/Merge] Remote /api/channel schreibt direkt ins Live-Universe -> auf gepatchten Kanaelen sofort vom naechsten Render-Frame ueber | **Ort:** `src/web/app.py:144` (Codex 288). **Bug:** api_channel (app.py Z.138-145) schreibt den Remote-Wert per `state.universes[universe].set_channel(...)` direkt ins Live-Universe, ausserhalb der Render-Pipeline (kein input_layer, kein Merge). Fuer gepatchte Adressen committet _render_frame Schritt 5 jeden Frame die _commit_spans neu (app_state.py Z.1730-1737) -> der  **Fix:** Remote-Kanalwerte in eine echte Render-Schicht (eigener Remote-Layer analog input_layer/simple_desk) legen und im _render_frame mischen, statt direkt ins Live-Universe zu schreiben. **✅ Verifiziert erledigt (Backlog-Hygiene, kein Codeaenderung): WEB-01 deckt DoD ab — `api_channel` (`app.py:310-320`) schreibt nicht mehr direkt ins Live-Universe, sondern ueber die Input-Override-Schicht `state.set_input_channel(universe, channel, value, source="web")` (`app_state.py:1702`, REPLACE/vom Source-Timeout ausgenommen, Range-Guards in set_input_channel) → der 44-Hz-Renderer ueberschreibt den Remote-Wert nicht mehr.** |
| A3D-39 | P3 | ✅ done (dieser PR) | [Persistenz] playback_slot=None (bewusst ungesetzt) ist beim Load nicht von fehlendem Key unterscheidbar -> stale function_id wird zurückmigriert — **Fix:** Legacy-Migration nur noch bei `"playback_slot" not in d` (fehlender Key), explizites null bleibt None; Tests `tests/test_vc_slider_playback_slot.py`. | **Ort:** `src/ui/virtualconsole/vc_slider.py:1108` (Codex 202). **Bug:** apply_dict liest `_ps = d.get('playback_slot')` (Zeile 1108). Sowohl bei fehlendem Key als auch bei explizitem JSON-null liefert d.get denselben Wert None. Die Migrationsregel (Zeile 1109-1110) setzt dann `_ps = d.get('function_id')`. to_dict (Zeile 1072) schreibt playback_slot IMMER mit; ein bewusst geloester Executor **Fix:** None und Abwesenheit trennen: `if 'playback_slot' not in d and self.mode == SliderMode.PLAYBACK:` fuer die Legacy-Migration, statt `_ps is None`. |
| A3D-40 | P3 | ✅ done (dieser PR) | [Web/OSC] WEB-04 'TOCTOU-sicher (lokale Ref)' ist ein No-Op — IndexError bei Show-Load weiterhin moeglich — **Fix:** echter Snapshot `list(cue_stacks)` auf allen 6 Web-Endpunkten (go/back/stop + Socket.IO) + den 2 OSC-Handlern (`_handle_go/_handle_back`); Regressionstests `tests/test_web_app.py` (nebenläufiges clear() → kein Crash). | **Ort:** `src/web/app.py:89` (Codex 213). **Bug:** api_go (Z.89-91), api_back (Z.96-98), api_stop (Z.106-108) und die Socket.IO-Pendants on_go/on_back/on_stop (Z.162-178) machen stacks = _get_state().cue_stacks; if stacks: stacks[0].go(). Der Kommentar 'WEB-04: TOCTOU-sicher (lokale Ref)' ist falsch: die lokale Variable haelt eine Referenz auf DASSELBE Listenobjekt, ke **Fix:** Echten Snapshot ziehen: stacks = list(_get_state().cue_stacks) (wie in /api/status), oder Index-Zugriff in try/except kapseln. |
| GDS-1 | **P2** | ✅ done (dieser PR) | **VC-Grandmaster-Fader zeigt beim Laden 0 % trotz GM 100 % — erste Berührung = Blackout-Sprung; kein Header↔VC-Sync** | Ein `SliderMode.GRANDMASTER`-VCSlider ohne `value`-Attr serialisiert `value=0` (`showbuilder/builder.py:258`). `apply_dict()` ruft `_apply()` beim Laden nur für GROUP_DIMMER/SUBMASTER/FEATURE_DIMMER — GRANDMASTER ist ausgenommen (`vc_slider.py:216-220`) → die geladene 0 wird nie gepusht, live bleibt `grand_master=1.0`. Fader zeigt 0 %, Rig steht auf voll; die **erste Maus-/Wheel-Berührung schreibt die 0-Position** in `set_grand_master` → Blackout-Sprung. Zusätzlich subscriben weder Header-GM noch VC-Fader `output_manager.subscribe_grand_master` → beide driften (VC auf 90 % ziehen lässt Header auf 100 %). **DoD:** GRANDMASTER-Fader initialisiert beim Laden aus dem live `grand_master` (bzw. Default voll statt 0) UND Header+VC spiegeln sich (`subscribe_grand_master`); Regressionstest. `vc_slider.py:1160/:142/:216-220` · `main_window.py:750-755/1192-1198`. |
| GDS-4 | P3 | ✅ done (dieser PR) | **Demo: Effekt-Buttons nicht solo/exclusive → gleiche-Geräte-Effekte stapeln** — **Fix:** `_btn`-Helfer um `solo`-Param erweitert (`w.solo_fixtures=True`); an den 4 konkurrierenden PAR-Voll-Looks (Rainbow/Chase/ColorFade/Lauflicht) gesetzt → ein neuer Look löst den alten auf denselben PARs ab (`solo_fixtures` stoppt nur andere Effekte auf DENSELBEN Geräten). PAR Strobe (Shutter) bewusst OHNE solo (komplementär, mit Farb-Look kombinierbar); MH/Spider unangetastet (deren eigene Konkurrenz-Sets = Folge-Item, kein Über-Greifen). Verifiziert am Generator-Output (`.lshow`: die 4 haben `solo_fixtures=True`, Strobe `False`). | Alle Effekt-Buttons sind schlichte `FUNCTION_TOGGLE` ohne `solo_fixtures`/`exclusive` (beide existieren, `vc_button.py:198-199`, per `**attrs` durchreichbar `builder.py:264-265`) → PAR Rainbow/Chase/ColorFade laufen gleichzeitig auf denselben PARs. **DoD:** `solo_fixtures=True` (bzw. `exclusive`) an den Effekt-Buttons → neuer Effekt löst den alten auf denselben Geräten ab. `build_grosse_demo_show_2026.py:187-211`. |
| GDS-5 | P3 | ✅ done (dieser PR) | **Demo: VC-Farbwähler hing an der (leeren) Programmer-Selektion → unzuverlässig** — **Fix:** Demo-Farbwähler `target='Programmer/Selektion'` → `target='Alle Fixtures'`. Die Demo-VC bietet kein SELECT_GROUP-Pad; der Empty-Programmer-Fallback färbte zwar alle, aber nur solange nichts anderes selektiert war (stale Selektion → nur diese). „Alle Fixtures" macht das Ziel explizit + robust. Verifiziert am Generator-Output. | `b.color('Farbe', target='Programmer/Selektion')` (`:228`) färbt die Programmer-Auswahl, aber die VC bietet kein SELECT_GROUP-Pad/keine Auswahl → im reinen VC-Flow bewirkt der Picker nichts. **DoD:** `target='Alle Fixtures'` ODER SELECT_GROUP-Pads für die angelegten Gruppen ergänzen. `vc_color.py:19-21`. |
