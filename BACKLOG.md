# LightOS — Backlog (Loop-Modus)

> **Single Source of Truth für den autonomen Loop.** Die Arbeitswarteschlange
> direkt unterhalb ist verbindlich: Der Loop nimmt immer das erste ausführbare
> Item. Die späteren Themenblöcke liefern Kontext, Befunde und Akzeptanzkriterien,
> sie sind **keine** alternative Reihenfolge. Vision/Langfrist steht in
> [ROADMAP.md](ROADMAP.md).

## Legende
- **Status:** `todo` (ausführbar) · `decision` (Produktentscheidung nötig) ·
  `blocked` (Hardware/Zugang/extern) · `wip` (in Arbeit) · `review` (PR offen) ·
  `done` (gemerged/verifiziert). Historische Varianten werden schrittweise durch
  diese Werte ersetzt.
- **Prio:** `P1` hoch · `P2` mittel · `P3` später
- **ID:** `<Bereich>-<NN>` — UI · ENG (Engine) · OUT (Ausgabe) · STAB (Stabilität) · NET (Netzwerk) · VIZ (Visualizer) · QA (Tests/CI)

## Wie du Ideen hinzufügst
Häng einfach eine Zeile unter dem passenden Themenblock an — Minimum: Titel + 1 Satz,
was du willst. Der Loop ordnet sie nach Prüfung in die Arbeitswarteschlange ein.

---

## 🎯 Arbeitswarteschlange (kanonisch)

_Ein Durchlauf bearbeitet genau ein Item, inklusive Tests, Dokumentation, Commit und
Push. Bei einer notwendigen Produktentscheidung oder fehlender Hardware wird nicht
geraten, sondern zum nächsten ausführbaren Eintrag gewechselt._

### Jetzt ausführbar

1. **QA-LIVE** — verbliebene reale Klickpfade aus dem Verifikationsplan abschließen.
2. **QA-09 + QA-10** — alle Views/VC-Widgets deterministisch headless bauen; QA-10
   umfasst nach #235/#238 noch acht statt zehn ungetesteter Views.
3. **STAB-10** — `.lshow`-Round-Trip-Identität und die adress-/kanalzahl-Symmetrie.
4. **ENG-11** — Multi-Head-Attributpfad durchgängig absichern.
5. **STAB-21** — Threading-/native-AV-Härtungsaudit (umbenannt, damit `STAB-11`
   eindeutig für das Weak-Callback-Thema bleibt).
6. **QA-11**, danach **QA-17** und **QA-18** — CI-/Dokumentations-/Backlog-Gates.
7. **VIZ-14** — erst nach den Sicherheits- und Verifikationsgates in kleine,
   einzeln abnehmbare UX-Schritte schneiden.

### Wartet auf Produktentscheidung

- **NET-01/02/03** — sichere LAN-Policy für Web/OSC: empfohlen ist LAN als sichtbares
  Opt-in mit Token/PIN; das darf die vorhandene Handy-Remote nicht still brechen.
- **VIZ-MASTER-FEEDBACK** — empfohlen: Output-Monitor und Visualizer zeigen finalen
  Output nach Grand Master/Blackout (WYSIWYG); Rohwerte später optional.
- **VIZ-05**, **VCB-04**, **DQ-1/3/4** — dokumentierte Designentscheidungen.

### Wartet auf Hardware/externen Input

- **LAS-09** (nur 6ch-CH5 prüfen), **LAS-18c**, **LAS-20**, Ether-Dream-/IDN-
  Hardware-Verifikation sowie ShowNET/Moncha-Profile.

## 📚 Befund- und Detailregister

_Die folgenden Blöcke bleiben als nachvollziehbares Detailregister erhalten. Ein
`done`-Eintrag wird beim nächsten Berühren in den Kurz-Log verdichtet; neue Arbeit
wird ausschließlich über die Arbeitswarteschlange priorisiert._

### 🔬 Aus Feature-Verifikations-Sweep 2026-07-09
_Vollautomatischer Verifikations-Durchlauf (Import-/Konstruktions-/Show-Load-Smoke + volle Suite 337 grün) + Multi-Agent-Feature×Test-Abdeckungs-Inventar (21 Subsysteme, **519 Funktionen, 85 high-risk**) + adversariale Bug-Verifikation. Voller Bericht + **priorisierter Live-Test-Plan**: [`docs/FEATURE_VERIFICATION_2026_07_09.md`](docs/FEATURE_VERIFICATION_2026_07_09.md). **3 bestätigte Bugs bereits gefixt:** STOP ALL nur aktuelle Page ([#230](https://github.com/ixamgames-droid/lightos/pull/230)), Web-Remote STOP tot + Fader-Initial 100 % ([#231](https://github.com/ixamgames-droid/lightos/pull/231)), Tests verschmutzten echtes crash.log ([#232](https://github.com/ixamgames-droid/lightos/pull/232))._

| ID | Prio | Status | Titel / Notiz |
|----|------|--------|---------------|
| RL-01 | P3 | ✅ done ([#234](https://github.com/ixamgames-droid/lightos/pull/234)) | **Range-Lock lässt Identitäts-Modifier zurück.** `channel_range_lock_dialog.py:_on_accept` nullt beim „Lock entfernen" nur `range_min/max` statt den Eintrag zu entfernen → ein Identitäts-`ChannelModifier` (LINEAR, 0–255) bleibt im geteilten `ChannelModifierManager` registriert. **Funktional folgenlos** (Identität → kein DMX-Effekt), reine Hygiene. Fix: Eintrag ganz entfernen, wenn er nur wegen des Locks existiert (`curve==CurveType.LINEAR and not existing.custom_lut`). |
| QA-LIVE | P2 | teils (2026-07-10) | **Live-Klick-Durchlauf der 85 high-risk Funktionen.** ✅ **Großer Teil live per Computer-Use abgearbeitet** (siehe „Aus Live-Durchlauf" unten): alle 8 Sektionen + Sub-Tabs + Hauptwerkzeuge + 3D-Fenster. Sehr viel funktioniert einwandfrei (Fan-Tool, Farb-Werkzeug+Popout, EFX-Custom-Path, VC-Bedienung, 3D-Bearbeiten-Push, Curve-Editor, Matrix-Vorschau …). 1 Crash gefunden+gefixt ([#235](https://github.com/ixamgames-droid/lightos/pull/235)) + 3 UX-Punkte. **Neu erledigt:** Fixture-Editor Save/Load-Rundtrip ohne Metadatenverlust. **Rest offen:** Effect-Layer-Editor (11/12 Layer), Scene-Editor „Vorschau senden", Show-Manager-Timeline-Drag (nach #235), Palettes, Web-Remote-Lifecycle. Reihenfolge: `docs/FEATURE_VERIFICATION_2026_07_09.md` §4. |

### 🖱️ Aus Live-Durchlauf 2026-07-09 (Computer-Use, Davids „Grosses Rig 2026")
_Kompletter Klick-Durchlauf aller 8 Sektionen + Sub-Tabs + Hauptwerkzeuge + 3D-Fenster auf der laufenden App (Teil-Erledigung von QA-LIVE). **Sehr viel funktioniert einwandfrei.** ✅ Live bestätigt: Command-Line (`all @ full`/`clear`), Programmer-Intensität, **Fan-Tool** (4 Modi × 5 Kurven rechnen), **Farb-Werkzeug** (Rad+RGB+Popout/Redock), **EFX-Custom-Path-Zeichner** (Punkte, Linear/Spline, Pan/Tilt-Mapping), Matrix-Editor (live Rainbow, 10 Algorithmen), Laser-Tab (Klartext-Bereiche), **VC-Bedienung** (Effekt triggern/toggle), **3D-Visualizer-Fenster** (Kamera, Selektion, Positions-Bearbeiten-Push, Dezimal — VIZ-PULL „Bearbeiten-tot" NICHT vorhanden), Simple Desk, Channel-Groups-Anlegen, Executor-Konfig, **Curve-Editor** (Punkt → Kurve biegt), Output-/DMX-Monitor (+Filter), MIDI, BPM-Manager, Patch-Tabelle._

| ID | Prio | Status | Titel / Notiz |
|----|------|--------|---------------|
| SM-CRASH | P1 | ✅ done ([#235](https://github.com/ixamgames-droid/lightos/pull/235)) | **Show-Manager „+ Neue Show"/„+ Track" crashte** (`AttributeError: 'NoneType' has no attribute 'deleteLater'`). `TrackLabelPanel.refresh()` räumte das Layout ohne None-Guard (`itemAt(i).widget()` = None bei Spacer/Stretch). F-9-Reporter fing es ab, Operation aber kaputt. Fix + Regressionstest. |
| VIZ-MASTER-FEEDBACK | P2 | todo | **Blackout & Grand Master ohne Feedback in Monitor/Visualizer.** `output_manager._send_all` wendet Blackout/GM nur im **Sende-Pfad** an (auf einer Kopie), nicht im Universe-Puffer → Output-Monitor, DMX-Monitor UND 3D-Visualizer zeigen weiter die Pre-Master-Werte. Ohne echtes Rig sieht der Nutzer beim BLACKOUT **nichts** — für eine Safety-Funktion irritierend. **Design-Entscheidung nötig:** soll der Visualizer/Monitor den **finalen** Output (nach GM/Blackout) zeigen (WYSIWYG)? Bezug: VIZ-05. |
| AUDIO-START-WARN | P3 | done ([#238](https://github.com/ixamgames-droid/lightos/pull/238)) | **Audio-Input-Tab „Start" zeigt Capture-Fehler jetzt sichtbar.** `AudioCapture.start()` setzt `last_error()` auch bei fehlendem Backend/fehlendem Default-Gerät; `AudioInputView` zeigt `last_error()` sowohl beim direkten Start-Fail als auch wenn der Capture-Thread nach einem zunächst erfolgreichen Start stirbt (z. B. „no device with id …") statt still auf „Status: gestoppt" zurückzufallen. Tests `tests/test_audio_input_view.py`. |
| EFX-PATH-BTN-LABEL | P3 | todo | **EFX-Custom-Path-Buttons abgeschnitten** — „Zeichnen"/„Bearbeiten" rendern als „eich"/„beit" (zu schmale Buttons). Rein kosmetisch. Custom-Path-Zeile im EFX-Panel (`programmer_view`/EFX). |

### 🔦 Laser-Support (Epic, Plan 2026-07-02)
_Davids Auftrag: Laser steuern — der eigene „3D Partylight L2600" (= **Ehaho L2600**, Manual verifiziert: nur 6ch + 34ch existieren) sofort per DMX, perspektivisch Profi-Laser über Netzwerk (Ether Dream → IDN); eigene Programmer-UI mit Muster-Paletten + gut abgesichertem freiem Zeichenmodus. Voller Plan mit Recherche (Manual/Protokolle), Ist-Analyse und Safety-Konzept: [`docs/LASER_PLAN.md`](docs/LASER_PLAN.md). **3D-Viewer bewusst ausgeklammert** (parallele VIZ-Strecke)._

#### 🔎 Stand & entdeckte offene Punkte (Update 2026-07-04, von Claude gepflegt)
> Feasibility-Verdikt (Research-Workflow, hart belegt): **Der Ehaho L2600 kann über DMX KEINE eigene/gemalte Figur ausgeben** — DMX ~44 Hz vs. ~20–30 kpps nötig, kein Per-Punkt-Galvo-Kanal, kein Upload-Weg (Ehaho-FAQ: „does not support custom homemade image/animation creation"; Gerät hat nur DMX+Strom+Sensitivity-Knob). Eigene Figuren gehen nur auf Netz-/ILDA-Lasern (Punktstrom). Darum: Zeichen-Studio **capability-gated** (LAS-12), L2600 nutzt Werksmuster + Abruf. Details: SecondBrain `project_laser_support_2026_07_02`.

**Erledigt in Session 2026-07-04 (gemerged):** LAS-11 Regler gruppiert ([#160](https://github.com/ixamgames-droid/lightos/pull/160)) · LAS-12 `laser_capability()`-Gate ([#161](https://github.com/ixamgames-droid/lightos/pull/161)) · LAS-13 Vollbild-Zeichen-Studio + Ehrlichkeits-Banner ([#162](https://github.com/ixamgames-droid/lightos/pull/162)) · LAS-14a Formmathematik `figure_ops` ([#163](https://github.com/ixamgames-droid/lightos/pull/163)) · LAS-14b Formwerkzeuge drag-to-draw ([#166](https://github.com/ixamgames-droid/lightos/pull/166)) · LAS-15 Freihand+RDP ([#167](https://github.com/ixamgames-droid/lightos/pull/167)) · LAS-18 VC-Button „Laser-Muster abrufen" ([#169](https://github.com/ixamgames-droid/lightos/pull/169)) · LAS-16 Undo/Redo + Raster-Einrasten ([#171](https://github.com/ixamgames-droid/lightos/pull/171)) · LAS-17 Bibliotheks-Leiste ([#173](https://github.com/ixamgames-droid/lightos/pull/173)) · LAS-Speed VC-Geschwindigkeitsregler ([#175](https://github.com/ixamgames-droid/lightos/pull/175)) · LAS-19 Bild-Import → Vektorisierung (dieser PR). **→ Zeichen-Studio komplett + VC-Bedienung (Muster-Button + Speed-Fader) für den L2600 fertig.**

**Noch offen / muss man noch anpassen (entdeckt):**
| ID | Prio | Status | Titel / Notiz |
|----|------|--------|---------------|
| LAS-18b | P2 | ✅ done ([#184](https://github.com/ixamgames-droid/lightos/pull/184)) | L2600-**Werksmuster-Picker**: Box „Werksmuster (Gerät)" in der LaserView (nur Klasse A mit `gobo_wheel`). „➕ Muster merken…" nimmt aktuelle Bank/Muster-Werte + Name + optionales Foto; Kachel = Foto-Thumbnail oder B/M-Nummer; Linksklick ruft ab, Rechtsklick löscht. Persistenz `laser_patterns` in der `.lshow` (NEU `pattern_slots.py`). David befüllt die Bibliothek am Gerät (Foto je Slot). |
| LAS-18c | P3 | blocked (Hardware) | Figur→Werksmuster-**Näherungs-Matcher** (`pattern_match.py`): Zeichnung → nächstpassendes eingebautes Muster als LASER-Palette. |
| — | — | ✅ gelöst (Handbuch S. 8) | Chart vollständig gegengelesen: Ch20 = „Keine Funktion"/leer bestätigt, Gruppe B = +17-Wiederholung; **laser_y-Bewegungs-Labels ans Handbuch angeglichen** (= X). Offen nur noch: 6ch-CH5 (nicht in den Fotos). |
| LAS-20 | P3 | blocked (Hardware) | `.ild`-Exporter für ShowNET/ILDA+SD-Laser — nur bauen, wenn solche Hardware da ist (sonst toter Code). |
| LAS-03-Rest | — | ✅ verifiziert (dieser PR) | Laser in **Snaps/Szenen**: war seit LAS-01 implizit komplett (laser_* in `ATTR_GROUPS` → Save-Dialog; `resolve_attr_channels` → Szenen; Apply schreibt `attr#N` 1:1 zurück). Jetzt per `tests/test_laser_snap_scene.py` End-to-End festgenagelt (Dialog-Capture inkl. Kopf-Keys, Gruppen-Abwahl, SnapLibrary-Roundtrip, Szenen-Kanalauflösung Gruppe A/B). |
| ~~LAS-09~~ | — | ✅ gelöst (Handbuch) | 34ch bestätigt (nicht 32); CH18: 0=Alle AUS, 255=Gruppe A AUS/B EIN → Shutter-Default 0 korrekt. |
| — | P3 | blocked | **Hardware-Verifikation** Ether Dream + IDN (nur gegen Fakes getestet, kein DAC/Gerät). ShowNET/Moncha-Profile (Charts fehlen). |

| ID | Prio | Status | Titel | Akzeptanzkriterium (Definition of Done) |
|----|------|--------|-------|------------------------------------------|
| LAS-01 | P1 | done ([PR #140](https://github.com/ixamgames-droid/lightos/pull/140)) | Ehaho-L2600-Builtin + Laser-Attribut-Vokabular + Klassen-Audit | Builtin `L2600LASER` (6ch Simple + 34ch Professional, alle Manual-Ranges, Gruppe A/B als Kopf 0/1, Shutter-Safety-Default 0), 13 `laser_*`-Attribute in `CHANNEL_ATTRS`/`ATTR_GROUPS['Effect']`/`ATTR_LABELS`, Audit-Test „kein Builtin ist `other`". Tests `tests/test_ehaho_l2600_profile.py`. |
| LAS-02 | P1 | done (dieser PR) | Laser-Tab im Programmer | `src/ui/views/laser_view.py` (`LaserView(follow_selection=True)`) analog EFX/Matrix in `programmer_view.py` eingehängt; sichtbar nur bei Laser-Capability (laser_*-Kanäle bzw. `fixture_type=='laser'`). Inhalt: A/B-Kopf-Umschalter (Mehrkopf attr#N, ENG-03-Guard), Modus-Kacheln aus Shutter-Ranges, Range-beschriftete Regler je Laser-Kanal, Muster-Paletten. Headless-Tests `tests/test_laser_view.py`. |
| LAS-03 | P2 | done | Muster-Paletten & Presets für Laser | `PaletteType.LASER` (Engine + Paletten-Tab), „Muster speichern…"/Anwenden-Kacheln in der LaserView (speichert pro Kopf via attr#N); Laser-Paletten sind über den **Preset-Browser** abrufbar. Snap-/Szenen-Capture und -Abruf sind durch `tests/test_laser_snap_scene.py` End-to-End verifiziert (siehe LAS-03-Rest). |
| LAS-10 | P2 | done (dieser PR) | Laser-Sicherheit von der Virtual Console bedienbar | ✅ Zwei neue `ButtonAction`s in `vc_button.py`: **`LASER_ARM`** (Scharf/Unscharf-Toggle des `LaserOutputManager`, Farbbalken lila=scharf) + **`LASER_ESTOP`** (Not-Aus, roter Balken: `estop_all` → `set_armed(False)` → `clear_estop_all`, Reihenfolge wie in der Laser-Steuerseite). Handler in `_trigger_primary`, Labels im Aktions-Dropdown. LaserView `_sync_arm_from_manager` spiegelt den Manager-Zustand (VC-/MIDI-Änderungen sichtbar). Damit ist der Laser-Not-Aus + Scharfschalten auf ein festes Pad/MIDI-Taste legbar. Tests `tests/test_laser_vc_safety.py`. |
| LAS-04 | P2 | done (dieser PR) | Netzwerk-Laser-Grundlagen (`protocol`-Feld) | ✅ `PatchedFixture.protocol` (`'dmx'` Default \| `'etherdream'` \| `'idn'`), ALTER-TABLE-Migration, `.lshow`-Serialisierung beidseitig (Alt-Shows → `'dmx'`), Undo/`update_fixture`-Allow-List, `fixture_uses_dmx()`-Gate an ALLEN vier Adress-Schreibstellen (`_rebuild_render_plan` Defaults/Spans/GM-Maske, `_flush_programmer_to_dmx`, `_apply_fixture_map`, Executor-`_flush_to_dmx`) — Platzhalter-Adressen von Netzwerk-Lasern erreichen nie das Live-Universe; Programmer-Werte bleiben für den späteren `LaserOutputManager` erhalten. Tests `tests/test_laser_protocol_field.py`. Die Protokoll-Auswahl im Patch-UI kam mit LAS-05. |
| LAS-05 | P2 | done (dieser PR) | Ether-Dream-Backend (Punkt-Streaming) | ✅ Neues Paket `src/core/laser/`: `frame.py` (neutrales `LaserFrame`/`LaserPoint`-Modell + `LaserLimits`/`clamp_frame` — Scan-Ausschlag, Punktrate, Helligkeit, Mindest-Punktzahl gegen stehende Strahlen), `etherdream.py` (voller Protokoll-Treiber nach [ether-dream.com/protocol.html](https://ether-dream.com/protocol.html): TCP-Befehle inkl. E-Stop/Clear, UDP-Discovery, reine struct/socket-Implementierung), `laser_output.py` (`LaserOutputManager`: eigener 30-fps-Thread, Kreis-Testmuster aus Programmer-Werten mit Shutter-Gate, **BLACKOUT blankt jeden Frame**, verriegelnder `estop_all()`, Reconnect-Backoff). `net_host`-Feld (Migration + Serialisierung + Undo), **Protokoll-Auswahl im Patch-Dialog** (Universe/Adresse ↔ IP umschaltbar, keine Fehl-Konfliktwarnung). Lifecycle: Start in `start_playback` (Env-Gate `LIGHTOS_NO_OUTPUT_THREAD`), Stop im MainWindow-Shutdown. Tests `tests/test_laser_etherdream.py` (Fake-DAC, ohne Hardware). **Hardware-Verifikation folgt, sobald eine Ether-Dream-DAC vorhanden ist.** |
| LAS-06 | P3 | done (dieser PR) | IDN-Stream-Backend | ✅ `src/core/laser/idn.py` — voller IDN-Stream-Treiber (ILDA Digital Network) hinter derselben `LaserFrame`-Abstraktion, Wire-Format gegen offizielle Spec Rev001/Rev002 **und** DexLogic-Referenzcode (helios_openidn) verifiziert: UDP :7255, IDN-Hello-Header + Channel-Message + Channel-Config mit Standard-XYRGB-Tag-Dictionary + Sample-Chunk, alles Big-Endian; session-freies Streaming (Command 0x40), Graceful Close (0x44) + Abort (0x46) als Not-Aus, optionaler Scan-Discovery. `IDNConnection` implementiert dieselbe Schnittstelle wie Ether Dream → `LaserOutputManager` bekommt eine **Protokoll-Weiche** (`_factory_for`), beide Backends laufen nebeneinander; Patch-UI bietet „IDN / ILDA Digital Network" als 3. Protokoll. v1: ein Frame = ein Chunk = ein UDP-Paket, zu punktreiche Frames werden geometrie-erhaltend downgesampled (App-Fragmentierung = Folgeschritt mit LAS-07). Tests `tests/test_laser_idn.py` (Golden-Bytes, Fake-UDP-Server, Manager-Weiche). **Hardware-Verifikation folgt, sobald ein IDN-Gerät vorhanden ist.** |
| LAS-07 | P2 | teils (dieser PR) | Freier Zeichenmodus (abgesichert) | ✅ **07a** (PR #155) – Safety-Fundament (Arming/Not-Aus, Default unscharf → geblankt) + `LaserFigure`-Modell + Framequelle-Umschaltung. ✅ **07b** (dieser PR) – **interaktiver XY-Zeichen-Editor** `src/ui/widgets/laser_draw_editor.py` (`LaserDrawCanvas`/`LaserDrawDialog`: Punkte per Klick setzen/ziehen/löschen, Farbe je Punkt aus 7-Farb-Palette, Blank-Segmente, offen/geschlossen; normierte −1..+1-Koordinaten, +y=oben, Live-Vorschau). **Live-Streaming** beim Zeichnen (`on_live_update` → `set_figure` an die Netzwerk-Laser der Auswahl, sichtbar nur bei scharfem Laser). **Figur-Persistenz** in `.lshow` (`AppState.laser_figures` + save/load/reset in `show_file.py`). LaserView: „✏️ Zeichnen…"-Button, gespeicherte Figuren (★) in der Ausgabe-Combo. Tests `tests/test_laser_draw_editor.py` + Show-Roundtrip in `test_show_file.py`. **Offen:** IDN-App-Fragmentierung für Frames > MTU (aktuell downgesampled auf ~194 Punkte; für hochauflösende gezeichnete Figuren später). |
| LAS-08 | P3 | teils (dieser PR) | Profi-Laser-DMX-Profile | ✅ **Pangolin FB4** als Builtin `PANGFB4` (fixture_type laser): offizielles 16-Kanal-„FB3"-Profil + 39-Kanal-Profil (Pangolin-Wiki `hardware:fb4:dmx16`/`dmx39` verifiziert; Setup/Playback-Duplikate als Mehrkopf Kopf 0/1, 16-bit-Feinkanäle als raw, Safety-Defaults Ch1=Blackout/Dimmer=0/Z-Rotation=Stillstand); neues Attribut `laser_scan_rate`. Tests `tests/test_pangolin_fb4_profile.py`. **Offen:** LaserWorld-ShowNET-Art-Net-Trigger + Moncha.NET (Charts noch beschaffen). |
| LAS-09 | P3 | blocked | Hardware-Verifikation L2600 (David am Gerät) | Aus dem Handbuch geklärt: 34ch, Ch18-Semantik und Bank-Grenzen. Offen ist nur noch die Funktion von **6ch-CH5**; bei abweichendem Ergebnis Profil per Signatur-Migration (`ensure_builtins`) nachziehen. |

### 🧊 3D-Visualizer-Komplettüberarbeitung (Plan 2026-07-02)
_Davids Auftrag: 3D-Viewer komplett überarbeiten — intuitiv, schön, stabil, später eigenständiges immer-sichtbares Fenster mit Live-Rendering. Voller Plan mit Diagnose, Konkurrenz-Recherche (grandMA3/Capture/Depence/Lightkey/MagicVis/Easy View) und Akzeptanzkriterien: [`docs/VIZ3D_OVERHAUL_PLAN.md`](docs/VIZ3D_OVERHAUL_PLAN.md). Phasen sind abhängigkeits-sortiert; VIZ-10 ist unabhängig vorziehbar; VIZ-13 ist in drei mergebare Teilrunden (3a/3b/3c) geschnitten. Offene Design-Entscheidungen für David in Plan §7. (Nicht verwechseln: **VIZ-05** oben ist ein separates Alt-Thema — Dimmer/Farb-Default-Kollision — und gehört NICHT zu dieser Strecke.)_

| ID | Prio | Status | Titel | Akzeptanzkriterium (Definition of Done) |
|----|------|--------|-------|------------------------------------------|
| VIZ-10 | P1 | ✅ done ([PR #139](https://github.com/ixamgames-droid/lightos/pull/139)) | Phase 0: Stabilitäts- & Sichtbarkeits-Sofortpaket | Plan §4/Phase 0: renderProcessTerminated-Selbstheilung, Bridge-Fehler → crash_logging (Traceback statt print), animate()-try/catch, setPixelRatio+ACES-ToneMapping, Einstellungen-Tab erreichbar, Toolbar-Labels lesbar, Palette wechselt Modus statt no-op, Statusbar/Tooltip nicht mehr stale, Nachfrage bei ungespeicherter Bühne. Alle Punkte live in GUI nachweisbar + Regressionstests. |
| VIZ-11 | P1 | ✅ done ([PR #144](https://github.com/ixamgames-droid/lightos/pull/144)) | Phase 1: Szenegraph-Datenmodell (Meter, Parenting, Stage in .lshow) | Plan §4/Phase 1: SceneNode ersetzt 5 State-Dicts; Docking = echtes Parenting (Truss-Rotation nimmt Fixtures mit); Live View 2D = Projektion; Bühnen-Snapshot in .lshow + Migration alter Shows (SHOW_VERSION-Bump). **Migrations-Gate:** alle ≥35 committeten Shows aus `shows/` laden fehlerfrei + automatisierter Vorher/Nachher-Vergleich der Welt-Transforms (Referenz „david test 2.lshow"); QUndoStack für alle Editier-Operationen (Strg+Z/Y). |
| VIZ-12 | P1 | ✅ done ([PR #149](https://github.com/ixamgames-droid/lightos/pull/149)) | Phase 2: Ein VisualizerService + persistentes Dauerfenster | Plan §4/Phase 2: Singleton-Service mit EINER Bridge + Dirty-Tracking + Batch-Push; Fenster hide/show statt destroy/rebuild (Kamera bleibt erhalten); nur noch ein DMX-Timer app-weit; reset_interaction_state bei Show-Wechsel; DPI/screenChanged-Handling. |
| VIZ-13 | P1 | done (3a #153 · 3b #157/#159 · 3c #180/#185/#187/#195/#198) | Phase 3: JS-Neuaufbau — **3a ✅** ([#153](https://github.com/ixamgames-droid/lightos/pull/153) Modul-Split) · **3b ✅** (Kamera [#157](https://github.com/ixamgames-droid/lightos/pull/157) + Gizmo [#159](https://github.com/ixamgames-droid/lightos/pull/159), Eigenbau ohne externe Controls) · **3c komplett** (dmxUpdated-Legacy-Entfernung ✅ [#180](https://github.com/ixamgames-droid/lightos/pull/180) · FixtureType-Registry **Teil 1 (build-Dispatch)** ✅ [#185](https://github.com/ixamgames-droid/lightos/pull/185) · **3c-1 Ortho-2D-Polish** ✅ [#187](https://github.com/ixamgames-droid/lightos/pull/187) (Icons behalten+verbessert lt. Reframe: permanente Umrisse + heller Unlit-Fill, par_bar/mover_bar-Glyphen mit Per-Kopf-Zellen, modusabhängiger Footer-Hint, Bühnen-Grundriss-Umrisse in Typ-Farbe) · **Registry Teil 2 (updateDmx-Split)** ✅ [#195](https://github.com/ixamgames-droid/lightos/pull/195) (updateFixture-Monolith → pro-Typ-`updateDmx`-Handler in Registry/builders, Fassade unverändert, verhaltens-identisch per Golden-Parity-Test `test_viz13c_updatedmx_registry.py`) · **3c-2 On-Demand-Rendering** ✅ [#198](https://github.com/ixamgames-droid/lightos/pull/198) (requestRender-Dirty-Flag + Live-Animation-Proben in neuem `scene/render_loop.js`, alle Quellen verdrahtet, 11 Beweis-Tests — **Phase 3 damit KOMPLETT**)) | Plan §4/Phase 3: **3a** Build-Step+Modul-Extraktion ohne Verhaltensänderung → **3b** Gizmo (zoom-korrekt, Snap) statt Verschieben/Höhe/Drehen-Buttons + Kamera-Presets Top/Front/Seite/Persp/Frei + Fit/Fit-Selected + gespeicherte Positionen → **3c** 2D-Top-Down = Ortho-Projektion derselben Szene + FixtureType-Registry + On-Demand-Rendering. Jede Teilrunde einzeln mergebar; Altfunktionen (Zielen/Nachfahren/Align/Spider) erhalten. |
| VIZ-14 | P1 | todo | Phase 4: Bedien-UX (2 Modi, Drag&Drop+Ghost, Identify, Labels, Array-Tool) | Plan §4/Phase 4: Modi Ansehen/Bauen mit sichtbarem Indikator (eine State-Machine); Drag&Drop aus Liste mit Ghost-Preview + Auto-Hang; Identify + bidirektionale Selektion (3D↔Liste↔Programmer); Labels-Overlay-Toggle; Empty-State; Array-Werkzeug (Reihe/Kreis/Grid). Abnahme: Bühne mit 4 PARs + 2 MHs an Truss in <2 min per Computer-Use-Selbsttest. |
| VIZ-15 | P2 | todo | Phase 5: Optik (Bloom, Beam-Shader, glTF-Modelle, Qualitätsstufen) | Plan §4/Phase 5: Bloom+Beam-Shader+Boden-Pools; glTF statt .dae inkl. animierendem MH-Modell; Fixture-Maßstäbe; Qualitätsstufen + pro-Fixture Beam-Toggle + Max-Beam-Range. Vorher/Nachher-Screenshots; 60 fps bei 40 Fixtures in „Mittel". |
| VIZ-16 | P3 | todo | Phase 6: Kür (Quad-View, Kalibrierung, Follow-Ausbau, MVR) | Plan §4/Phase 6 — erst nach VIZ-10..15 anfassen. |

#### 🐛 Aus 3D-Live-Durchspielen (Session 2026-07-07, Computer-Use)
_Beim kompletten Demo-Show-Bau live gefunden. Kontext/Root-Cause: Second-Brain `project_viz3d_editing_dead_qwebchannel_throttle_2026_07_07`._

| ID | Prio | Status | Titel | Notiz / DoD |
|----|------|--------|-------|-------------|
| VIZ-PULL | P1 | done ([#201](https://github.com/ixamgames-droid/lightos/pull/201)) | **3D-Bearbeiten war komplett tot — Pull-Poll-Fix** | QtWebEngine stellt Python→JS-QWebChannel-SIGNALE (Push) an die eingebettete Post-Load-Seite NICHT zu (auch fokussiert nicht) → Hinzufügen/Bearbeiten/Verschieben/Drehen/Kamera/DMX **und** Live-platzierte Fixtures reagierten nicht. Fix: JS pollt `pollControl()` (Slot-Rückgabe kommt zuverlässig an), Python spiegelt JEDES Signal in den Poll — inkl. `allFixtures`/`fixtureAdded`/`fixtureRemoved` (Fixture-Meshes, im 2. Durchgang nachgezogen). LIVE end-to-end verifiziert (Stage-Add/Move/Edit, Modus, Kamera, Fixture place/remove sofort sichtbar). |
| VIZ-TRUSS-ADD | P1 | done ([#201](https://github.com/ixamgames-droid/lightos/pull/201)) | **Trasse hinzufügen scheiterte still (mit geladenen Fixtures)** | Root Cause (6-Agent-Workflow + Live-Debug): KEIN JS-Build-Fehler, sondern destruktiver Reconcile-Race in `_on_stage_list_from_js` (visualizer_window.py:~2864): ein Echo mit AKTUELLEM Token, das die frisch gepushte async-Truss transient nicht listet, löschte sie via `py_ids_to_remove`. Der Poll-Fixture-Fix erhöhte die Echo-Frequenz → öffnete den latenten Race; Truss (async + einziges Dock-Hang-Element) besonders exponiert, Plattform (sync Box) nie. FIX: `was_reloading` in den `is_stale`-Guard falten (während eines Python-Reloads ist Python autoritativ, kein Löschen). 3 Tests angepasst + neuer Regressionstest. LIVE verifiziert (Trassen adden zuverlässig mit 14 Fixtures). Nebenmerke: Stage-Add/-Edit triggern je einen Full-Reload → GUI-Ops ~1-2s pacen. |
| VIZ-STAGE-RELOAD-PACING | P3 | todo | **Rapide Stage-Ops droppen (Full-Reload je Op)** | Jedes Stage-Add UND jede Property-Feld-Änderung stößt `push_stage_definition`→`loadStageJson` (Full-Rebuild) an; folgt die nächste Op vor dem Settle, geht sie verloren. In der GUI muss ~1-2s gewartet werden. Wäre schöner: inkrementelle Add/Update-Pfad (der `push_add_stage_object`/`{t:'addStage'}`-Weg existiert, hat aber KEINEN Caller) statt Full-Reload je Op. |
| VIZ-3D-TABRETURN | P3 | todo | **3D-View ~2 s schwarz beim Zurückwechseln auf den Bühne-Tab + Kamera-Perspektive verändert** | Live beobachtet (2026-07-08, Computer-Use): Nach Wechsel Bühne→Virtual Console→zurück ist der 3D-Bereich ~2 s **schwarz** (Blackframe), dann rendert der Rig wieder — aber aus **veränderter Kamera-Perspektive** (vorher frontal, danach 3/4-Winkel). Deutet an, dass der Visualizer beim Sichtbarwerden **neu aufbaut** statt nur ein-/auszublenden — VIZ-12 wollte hide/show mit **erhaltener Kamera**. Prüfen: `visualizer_window`/`VisualizerService`-show/hide-Pfad (rendert on-show sofort? Kamera-State persistiert über Tab-Wechsel?). DoD: Rückkehr rendert ohne Blackframe, Kamera bleibt exakt erhalten. |
| VIZ-BUILDER-FIXTYPE | P2 | ✅ done (dieser PR) | **`ShowBuilder.patch()` übernimmt `fixture_type` aus dem Profil** | ~~Skript-gepatchte Fixtures bekamen den Default `fixture_type="other"` (models.py:35) → 3D mappt DMX nicht auf Farbe/Pan/Tilt~~ → `patch()` liest jetzt `(id, fixture_type)` des Profils (neuer Helfer `_lookup_profile`) und setzt den Typ beim Anlegen (spiegelt die `sync.py`-Auto-Fix-Semantik, nur schon beim Patchen statt erst bei Validierung). Der manuelle Nachzieh-Workaround in `tools/build_grosses_rig.py` ist damit entfallen (entfernt). Test `test_showbuilder.py::test_patch_inherits_fixture_type_from_profile`. |
| VIZ-STAGE-PANEL | P3 | teils (dieser PR) | **Stage-Eigenschaften-Panel: Feld-Commit-/Sync-Macken** | **(a) Dezimal-Aspekt behoben (dieser PR):** die Stage-Größen-/Positions-Felder (`_stage_spin_*`) sind jetzt `LocaleTolerantDoubleSpinBox` → ein getippter Dezimalwert mit Punkt geht nicht mehr verloren (war Teil von „übernehmen getippten Wert NICHT"). **Offen:** (b) ENTER committet nicht, nur TAB/Feldwechsel; (c) Szene↔Panel-Selektion desynct (Element in Szene klicken re-synct); (d) „Größe anpassen"-Toggle bleibt hängen/klemmt. `visualizer_window.py` `_on_stage_property_changed`/`_on_resize_mode_toggled`. |
| VIZ-FIX-DECIMAL | P3 | ✅ done (dieser PR) | **Fixture-Positionsfeld verwirft Dezimalstellen** | ~~Eingabe „5.7" wurde unter dt. Locale verworfen (Komma erwartet, Punkt abgeschnitten)~~ → neues wiederverwendbares Widget **`LocaleTolerantDoubleSpinBox`** (`src/ui/widgets/decimal_spinbox.py`, C-Locale + Komma→Punkt in `validate`/`valueFromText`) akzeptiert Punkt UND Komma. Alle 14 `QDoubleSpinBox` der 3D-Panels (Fixture-Position/-Ausrichtung, Stage-Größe, Raster) darauf umgestellt. Test `tests/test_decimal_spinbox.py` (5, dt. Locale erzwungen, inkl. Regressionsbeleg gegen die Standard-Spinbox). |
| VC-WIDGET-DRAG | — | ✅ geschlossen — nicht reproduzierbar (headless **+ LIVE**) | **VC-Canvas: Widgets nicht per Drag verschiebbar** | Live beobachtet (2026-07-07): Button/Fader/SpeedDial ließen sich angeblich nicht ziehen. **Headless NICHT reproduzierbar** (Drag-Pfad `VCWidget.mousePressEvent`→`_dragging`, `mouseMoveEvent`→`self.move` funktioniert für alle drei Typen, auch VCFrame-verschachtelt; Guard-Test `tests/test_vc_widget_drag.py`, 4). **LIVE-Repro 2026-07-08 (Computer-Use auf Davids „Grosses Rig 2026"):** im Bearbeiten-Modus **„MH ColorFade"-Button UND „MH Tempo"-SpeedDial per Drag verschoben** — beide folgten der Maus, selektierten korrekt (Inspector band an), Strg+Z nahm sauber zurück. → Drag funktioniert live einwandfrei; die Ursprungs-Beobachtung war ein Einmal-/Timing-Effekt. Wächter-Test bleibt als Regressionsschutz. |

#### 🔦 Fixture-Modelle & Animation (Davids Zusatz-Vision, 2026-07-04)
_David: professioneller grandMA3-Look, aber leicht bedienbar; volle 3D-Modelle + Animation für alle Fixture-Typen inkl. Multi-Head-Bars, Nebel/Laser/Kegel. Kontext + Ist-Stand-Audit: Second-Brain `project_viz_guided_build_vision`._

| ID | Prio | Status | Titel | Akzeptanzkriterium (Definition of Done) |
|----|------|--------|-------|------------------------------------------|
| FM-1 | P1 | ✅ done ([PR #164](https://github.com/ixamgames-droid/lightos/pull/164)) | Scanner-Spiegel + Strahl animieren | Scanner = Gehäuse→Pan-Yoke→kippender Spiegelkopf; Strahl schwenkt live mit Pan/Tilt-DMX. |
| FM-2 | P1 | ✅ done ([PR #165](https://github.com/ixamgames-droid/lightos/pull/165)) | Multi-Head-Payload generalisiert (N Köpfe + pro-Kopf-Pan) | `heads`-Array beliebig viele Köpfe (aus höchstem #N-Index) + pro-Kopf-`pan`; spider-byte-identisch. Fundament für Bars. |
| FM-3 | P1 | ✅ done ([PR #168](https://github.com/ixamgames-droid/lightos/pull/168)) | N-PAR-Bar (einzeln steuerbare PARs) | ≥2 Banks ohne Bewegung → `par_bar`: N einzeln gefärbte PARs, je eigener Beam. |
| FM-4 | P1 | ✅ done (dieser PR) | N-Mover-Bar (einzeln pan/tilt/farbe pro Kopf) | ≥2 Banks + ≥2 Pan → `mover_bar`: N Mini-Moving-Heads, jeder Kopf einzeln pan/tilt/farbe. |
| FM-5 | P2 | ✅ done (dieser PR) | **Fixture-Bibliotheks-Profile für die neuen Bars** | Zwei generische Builtins in `fixture_db.py`: „LED Moving Bar 4×" (MOVBAR4, 22ch, 4×Pan/Tilt/RGB → `mover_bar`) + „LED PAR Bar 4×" (PARBAR4, 12/16ch, 4×RGB(W) → `par_bar`). Idempotent in `_seed`+`ensure_builtins`, im Klassen-Audit festgenagelt, Routing per echtem `_viz_model_for`-Test verifiziert (`test_generic_bars_profile.py`). Jetzt patch- und sichtbar. (4-PAR-Bar ging vorher nur über ADJ Dotz TPar; Mover-Bar gar nicht.) |
| FM-9 | P2 | todo | Pro-Kopf-UNABHÄNGIGE Programmer-Steuerung für Bars | DMX-Pfad + 3D unterstützen pro-Kopf-Farbe/Pan/Tilt (`color_r#N`/`pan#N`), ABER das Farbmodell ist „eine Farbe pro Fixture" (`color_attrs_for_fixture` schreibt alle Banks gleich) → alle Köpfe zeigen im Programmer dieselbe Farbe. Pro-Kopf-Individualität kommt heute nur über Matrix-/Pixel-Mapping bzw. EFX-Geräte-Verhältnis. Für echtes „jeden Kopf einzeln im Programmer" braucht es Pro-Kopf-/Sub-Fixture-Selektion (größeres Feature, Programmer/Farbmodell). |
| FM-6 | P2 | ✅ done (dieser PR) | 2D-Symbole für par_bar/mover_bar | Dedizierte grandMA3-artige Symbole: par_bar = Gehäuse-Balken mit 4 PAR-Zellen, mover_bar = Balken mit 4 kleinen Köpfen + Beam. Im 2D-Top-Down (`live_view.FixtureRenderer`) UND als Listen-Icon (`mini_icons`: `fx_par_bar`/`fx_mover_bar`). Vorher: beide fielen aufs Spider-Symbol. (Scanner hatte schon ein eigenes Symbol.) |
| FM-7 | P3 | ✅ done (dieser PR) | Routing-Vereinheitlichung: EINE Quelle `viz_model_for` | NEU `app_state.viz_model_for()` als einzige Quelle für mover_bar/par_bar/spider — genutzt von 3D (`_viz_model_for` delegiert), 2D-Symbol (live_view), Listen-Icon (mini_icons) UND Patch-Spiegel-Option (nur noch echte Spider, nicht par_bar/mover_bar). Verhindert 2D/3D-Drift. Adversariale Review: 24 Kandidaten, 0 real. |
| FM-10 | P3 | todo | Pro-Kopf-Aim (IK „auf Punkt zeigen") für mover_bar im 3D | `_is_moving_head` (visualizer_window) schließt `is_spider_fixture` aus → mover_bar-Köpfe nutzen die Rotations-/aim_orientation-Bahn statt der pan/tilt-IK („auf Punkt ausrichten"). Vorbestehend (mover_bar war schon immer is_spider), kein Regress — aber für echte Mover-Bars wäre Pro-Kopf-Aim wünschenswert. Koppelt an FM-9 (Pro-Kopf-Steuerung). |
| FM-8 | P3 | ✅ done | Modell-Fidelity: Moving-Head ✅ ([#182](https://github.com/ixamgames-droid/lightos/pull/182)) + LED-Bar Pro-Pixel ✅ (dieser PR) | **Moving-Head:** detailliertes prozedurales Modell (Bodenplatte + Hals → U-Yoke mit Motor-Gehäusen → Kopf-Trommel + emissive Linse); `moving_head.dae` verworfen (grober Low-Poly-Klotz, per Render belegt, Davids Entscheidung). **LED-Bar Pro-Pixel:** `buildParBar(n, pixelStyle)` — led_bar-Typ mit ≥6 Banks rendert als schlanke Bar mit N einzeln gefärbten Segmenten (4er-Bars behalten bewusst den PAR-Dosen-Look). Pro-Zellen-FARBE gab's seit FM-3/7 via par_bar-Routing; jetzt stimmt auch der LOOK. **Rest-Idee (nicht blockierend):** Scanner-Strahl physikalisch am Spiegel ablenken → VIZ-15 Optik. Für künftige Asset-Upgrades: hochwertiges .glb/.dae nötig, Split-/Rig-Verfahren liegt bereit. |

### 🔍 Aus Fixture-Library-Audit 2026-07-04 (Davids Prüfauftrag: 3D-Modelle · Programmer-Profile · Geräteklassen · Kohärenz)
_Vollständiges Audit aller ~30 Builtins × 4 Achsen (Geräteklasse ↔ Kanäle ↔ 3D-Modell ↔ Programmer-Profil), 6-Agenten-Workflow, jedes Finding adversarial gegen den Code verifiziert. **Fazit: ~90 % schlüssig** — Geräteklassen sauber (Klassen-Audit deckt ALLE Builtins ab, nie `other`), jede Klasse außer `other` hat einen echten 3D-Builder, alle `.dae`-Assets vorhanden & gültig, Laser-Safety-Defaults + Laser-Tab-Capability korrekt, `raw`/`reset` sauber. Drei echte Befunde behoben ([PR #176](https://github.com/ixamgames-droid/lightos/pull/176)); die 2D-Bar-Icon-Findings deckten sich mit **FM-6/FM-7** (parallel via [#174](https://github.com/ixamgames-droid/lightos/pull/174) erledigt). SecondBrain: `project_lightos_fixture_library_audit_2026_07_04`._

| ID | Prio | Status | Titel | Akzeptanzkriterium (Definition of Done) |
|----|------|--------|-------|------------------------------------------|
| FLA-1 | P1 | ✅ done ([PR #176](https://github.com/ixamgames-droid/lightos/pull/176)) | PARTYLASER wurde als Spider statt Laser gerendert | Builtin „Laser Stage Lighting" hat zwei `color_r`-Kanäle → `is_spider_fixture` True → `viz_model_for` liefert 'spider' statt 'laser' (falsches 3D-Modell + 2D-Symbol in beiden Renderern + sinnlose Patch-Spiegel-Option). Fix: `is_spider_fixture` gatet `fixture_type=='laser'` zentral aus (Laser = Punkt-Scanner). DMX-Output unberührt (`_multihead_count`). Adversariale Review: SICHER. `tests/test_laser_not_spider.py`. |
| FLA-2 | P1 | ✅ done ([PR #176](https://github.com/ixamgames-droid/lightos/pull/176)) | CMY-Farbmischer fielen aus dem Color-Tab | `ATTR_GROUPS['Color']` + `_DIM_COLOR_ATTRS` führten tote Tokens `cyan/magenta/yellow`; real emittiert werden `cmy_c/m/y` (QXF-Import/Fixture-Editor) → `classify_attr('cmy_c')` fiel auf 'Other' → importierte CMY-Mover verloren Color-Tab/Picker + GM-Farbmaske. Fix: `cmy_c/m/y` in beide Mengen (konsistent mit `engine/palette.py`). |
| FLA-3 | P2 | ✅ done ([PR #176](https://github.com/ixamgames-droid/lightos/pull/176)) | DOTZTPAR-Strobe ohne Range | Shutter-Kanal der 9/18ch-Voll-Modi als einziges Fixture ohne Range-Block. Fix: Standard-`_SIMPLE_STROBE` (0=Aus/open, 1-255=Strobe). |
| FLA-4 | P3 | todo | Geräteklasse `other` ohne dediziertes 3D-Modell/2D-Icon | `registry.js` hat kein `case 'other'` → `default: buildPar()` (PAR-Fallback); auch kein dediziertes 2D-Icon. Real erreichbar via QLC+-Import (Fan/Effect/Other → `other`, `qxf_import.TYPE_MAP`). Optik (PAR/Kreis) ≠ Klasse. Fix: neutraler `other`-Builder („unbekanntes Gerät") + Icon, ODER bewusst dokumentieren+testen. Rein kosmetisch, nur Importe (kein Builtin ist `other`), keine Funktions-/DMX-Auswirkung. |
| FLA-5 | P3 | todo | Kleinvieh aus dem Audit | (a) `speed` in keiner ATTR_GROUPS-Menge → Sammel-Tab „Weitere" (Control existiert, nur Roh-Label — optional in `Effect` aufnehmen wie `palette.py`); (b) DOTZTPAR rendert modusabhängig `led_bar` (3/5/9ch, 1 Bank) vs. `par_bar` (12/18ch, 4 Banks) — funktional korrekt, nur ein Doku-Kommentar an `_viz_model_for`/`_add_adj_dotz_tpar` wünschenswert. |

### 🎛️ Aus Live-Edit-Rework + Testshow-Bau (Findings, Session 2026-07-01)
_Beim Umbau des Live-Edit-Widgets zum **VC-Canvas-Widget** ([PR #126](https://github.com/ixamgames-droid/lightos/pull/126)) + **Bearbeiten/Bedienen-Trennung mit visuellen Reglern** ([PR #128](https://github.com/ixamgames-droid/lightos/pull/128)) und beim Live-Bau/Test einer 3-Seiten-Testshow (Computer Use, 11 Effekte) entdeckt. Kern funktioniert; das sind Feinschliff-/Verifikations-Punkte für kommende Runden. Doku: [`docs/LIVE_EDIT_FENSTER.md`](docs/LIVE_EDIT_FENSTER.md) + [`docs/anleitung_testshow_liveedit/`](docs/anleitung_testshow_liveedit/ANLEITUNG_TESTSHOW_LIVEEDIT.md)._

| ID | Prio | Status | Titel | Akzeptanzkriterium (Definition of Done) |
|----|------|--------|-------|------------------------------------------|
| VCL-01 | P2 | done ([PR #132](https://github.com/ixamgames-droid/lightos/pull/132)) | Live-Edit: Anzeige neu synchronisieren, wenn Tempo/Params von ANDERER Stelle geändert werden | `ui/virtualconsole/vc_multi_live_editor.py` — Tempo-Modus-Buttons (Aus/BPM/Tap) + versteckte/gezeigte Regler lesen ihren Wert nur beim `_refresh_body`-Bau. Ändert eine ANDERE Fläche (VC-Bus-Selector, MIDI, CLI) `tempo_bus_id`/Effekt-Params derselben Funktion, bleibt die Live-Edit-Anzeige stale bis zum nächsten Moduswechsel/Rebuild. Fix: auf das passende Change-Signal abonnieren (bzw. beim Sichtbarwerden `_refresh_body`) und die betroffenen Regler/Tempo-Buttons nachziehen. Regressionstest. (LOW aus PR #126/#128-Adversarial-Review.) |
| VCL-02 | P3 | done ([PR #132](https://github.com/ixamgames-droid/lightos/pull/132)) | Live-Edit: Touch-Resize-Band effektiv auf 8px geklemmt | `ui/virtualconsole/vc_multi_live_editor.py:_reposition_content` — der Content wird nur um `HANDLE_SIZE` (8) eingerückt; die per Dwell aufklappenden `TOUCH_HANDLE_SIZE` (24) breiten Greif-Bänder sind damit vom Content überdeckt und unerreichbar (Touch-Resize degradiert auf die schmale Maus-Zone). Fix: bei aktivem Touch-Resize (bzw. sobald `_big_handles`) den Content um die Touch-Griffbreite einrücken. Cosmetic/Touch-UX. |
| VCL-03 | P3 | done ([PR #132](https://github.com/ixamgames-droid/lightos/pull/132)) | Live-Edit: nicht gemappte Auswahl-Optionen zeigen den Roh-Key | `ui/virtualconsole/vc_multi_live_editor.py` (`_DIR_LABELS`/`_build_segmented`) — Bewegungs-/Stil-Optionen ohne Eintrag in der Label-Map (z. B. `normal`) rendern den englischen Roh-Token als Segment-/Combo-Beschriftung. Fix: freundliche deutsche Labels für die restlichen Bewegungs-/Stil-Tokens ergänzen (zentrale `_option_label`-Map statt nur `_DIR_LABELS`). Polish. |
| VCL-04 | P2 | done ([PR #133](https://github.com/ixamgames-droid/lightos/pull/133)) | „Neue Show" — VC-Canvas zuverlässig leeren (verifizieren) | Beim Live-Test **einmal** beobachtet: nach „Neue Show" blieb ein Live-Edit-Widget auf dem VC-Canvas sichtbar. Verifizieren, ob `reset_show`/„Neue Show" die VC-Canvas-Widgets wirklich entfernt (vs. Stale-Repaint / einmaliger Anzeige-Glitch). Falls ein Widget real überlebt → Lifecycle-Bug fixen (Bezug: VCB-05-Reset-Pfad, `core/show/show_file.py:reset_show` + `vc_canvas`-Teardown). Repro + Regressionstest. |
| VIZ-05 | P3 | ✅ verifiziert → 🎨 Design | 2D-Live-View: PARs erschienen dunkel (verifizieren/reproduzieren) | **Headless verifiziert (2026-07-02): echter Befund, aber KEIN Anzeige-Bug** — der 2D-View spiegelt treu den realen DMX-Output. Ursache: Default-Kollision `drive_intensity=False` (neue Matrizen, `rgb_matrix.py:436`) × `implicit_brightness=False` (neue Shows, `app_state.py:127`) → frische Farb-Matrix auf Dimmer-PARs treibt NICHTS den Intensity-Kanal → Licht physisch dunkel (Farbe im Signal vorhanden). Beweis-Repro + 5 Fälle (A–E): `docs/UI_VISUAL_AUDIT_2026_07_02.md`. **Lösungswahl liegt bei David** → „🎨 Design-Entscheidungen" unten. |

### 🎭 Aus UX-Dogfooding 2026-07-05 (Opus/Sonnet-Agenten bauen Shows per Computer Use)
_Zwei Computer-Use-Agenten bauten je eine komplette Show über die echte GUI: **UXTEST-1 „Beach Party"** (mobiler DJ — 4 PAR + 2 MH, Snaps, Matrix-Chase, VC-Seite, gespeichert ✅ 7/7) und **UXTEST-2 „Laser Club"** (Lichttechniker — L2600 34ch + 2 PAR, Laser-Tab, Muster-Palette, Werksmuster-Picker, VC, gespeichert ✅). Ziel: echte Reibungspunkte + Doku-Abgleich. Reports: `scratchpad/uxtest1_report.md`, `uxtest2_report.md`. **Sehr gelobt:** Patch-Dialog (Auto-Adressen/Batch), Programmer-Zonen-Layout + Live-Vorschau, Neue-Show-Warnung, VC-Drag&Drop mit „Effekt einrichten"-Dialog, Laser-Klartext-Wertebereiche („40-47 Blau"), Werksmuster-Kacheln (B32/M77). Die Befunde:_

| ID | Prio | Status | Titel | Stelle / Akzeptanzkriterium |
|----|------|--------|-------|------------------------------------------|
| UXT-01 | **P1** | ✅ done (dieser PR) | **Fader „Programmer-Attribut" bekam ein UI-Feld für das Ziel-Attribut** | `vc_slider.py` `_open_properties()`: editierbare Attribut-Combo (aus `attr_groups.ATTR_LABELS`, 55 Attribute inkl. aller Laser-Attribute; Freitext für Power-User), sichtbar nur bei Modus==PROGRAMMER, Rücklesen per itemData/Freitext. **Der LAS-Speed-Fader (gobo_rotation 192–223) ist jetzt per GUI baubar.** Tests: `test_laser_vc_speed.py` (Feld da/vorbefüllt/Auswahl+Freitext) + Sichtbarkeits-Gate verifiziert. |
| UXT-02 | P2 | ⏳ nicht reproduzierbar (Wächter gesetzt) | **VC-Inspector: einmaliger Datenverlust bei Tab statt Enter** | Nur EINMAL im Live-Test beobachtet (mit Enter hielt es), vom Tester selbst als „nicht 100 % deterministisch" markiert. **Headless nicht reproduzierbar:** neuer Regressionstest `test_vc_inspector_panel.py::test_config_survives_rebind_after_switch` (bind A→editieren→bind B→zurück zu A) ist GRÜN — der Live-Apply committet bereits pro Tastendruck, kein Logikfehler im Inspector-Code. Vermutete Ursache: Qt-Fokus-/IME-Timing-Rennen (koppelt an den beobachteten `Textinputhost`-Blocker). Wächter fängt eine echte Regression; für einen echten Fix braucht es eine Live-Repro. |
| UXT-03 | P2 | ✅ done (dieser PR) | **Snap→Szene-Brücke** + Chase-Weg klargestellt | **Zwei Teile:** (1) Der Snap→**Chase**-Weg existierte schon (Snap-Kontextmenü „Chase aus Auswahl erstellen" baut aus ≥2 Snaps eine Sequence) — der Agent nahm nur den falschen Button („Chase +" im Editor). (2) Der echte Gap war Snap→**Szene**: neue Kontextmenü-Aktion **„Als Szene(n) übernehmen"** (`snap_file_panel._create_scene_from_selection`) wandelt je markierten Snap über `programmer_to_scene_values` in eine Scene-Funktion, die danach im Chaser-Editor unter „+ Hinzufügen" wählbar ist. Tests `test_snap_to_scene.py` (Einzel/Mehrfach/leer/ohne-Patch). Doku-Hinweis dazu steckt bereits in EFFEKTE.md (UXT-04). |
| UXT-04 | P2 | ✅ done (dieser PR) | **EFFEKTE.md-Tabname korrigiert** (kleiner als gedacht) | **Verifikation gegen den Code ergab:** der „Helper"-Tab existiert weiter — er heißt im sichtbaren Reiter nur **„Hilfe"** (`programmer_view.py:434`), mit unveränderten Buttons Effekt-Assistent/„+ Szene"/„+ Chaser"/„Programmer → Szene". Der Agent hatte „Hilfe" gesehen, aber nicht als Effekt-Seite erkannt — seine „Funktionalität weg"-These war falsch. Fix: „Helper"→„Hilfe" durchgängig + Klarstellung **Snap vs. Szene** (Chaser nimmt Szenen, nicht Snaps → „Programmer → Szene") im Intro. |
| UXT-05 | P2 | ✅ done (dieser PR) | **Laser-Endnutzer-Anleitung geschrieben** | NEU `docs/anleitung_laser/ANLEITUNG_LASER.md`: zwei Laser-Klassen (DMX-Muster vs. Netzwerk), Patchen, Laser-Tab + Betriebsart, Bank/Muster + Klartext-Wertebereiche, Muster-Palette speichern, Werksmuster-Kacheln + Foto, VC-Knopf + Tempo-Fader (`gobo_rotation` 192–223), Zeichen-Studio, Sicherheit. In `ANLEITUNGEN.md` (Nr. 12) verlinkt. |
| UXT-06 | P2 | ✅ done ([#190](https://github.com/ixamgames-droid/lightos/pull/190)) | **VC-Werkzeugleiste: neue Elemente staffeln + auswählen** | **Ursache:** `virtual_console_view._add_widget` legte JEDES neue Widget exakt in der Canvas-Mitte ab → der zweite lag deckungsgleich auf dem ersten (wirkte wie „nichts passiert"; ein Element WURDE erzeugt, nur unsichtbar gestapelt). Fix: diagonale **Kaskade** (Versatz `GRID*3` je Klick, nach 8 Stufen Wrap, Reset je Bearbeiten-Sitzung) + das neue Widget wird **ausgewählt** (Inspector bindet → sichtbare Bestätigung). Tests in `test_views.py`. (Der „Toolbar-Drag"-Punkt war kein Bug: die Toolbar ist Klick-zum-Anlegen; Drag gibt es nur aus der rechten Bibliothek — der Tooltip „auf eine Taste ziehen" gehört zur Bibliothek.) |
| UXT-07 | P2 | ✅ done (dieser PR) | **„Großes Fenster"-Popout wirkte folgenlos** — behoben | **Ursache:** die Popout-Fenster (`_toggle_editor_popout` in 7 Editoren + EFX-Vorschau-Popout) riefen `win.show()` **ohne** `raise_()`/`activateWindow()` → das nicht-modale Fenster erschien HINTER dem maximierten Hauptfenster (der Editor-Bereich wurde leer → „kein Effekt"). Fix: nach jedem `show()` `raise_()`+`activateWindow()` (rgb_matrix, chaser, audio, carousel, effect_layer, scene, sequence, efx — inkl. EFX-Vorschau-Erstöffnung). Der „Großes Fenster"-Knopf ist damit die Lösung für das Höhenproblem (voll scrollbares Fenster, Buttons sichtbar). Test `test_matrix_meta_view.py` verifiziert raise+activate+isVisible. |
| UXT-08 | P3 | ✅ done (dieser PR) | **Patch-Validierungswarnung entschärft (Modus-Umbenennung)** | Ist der gespeicherte Modusname nur eine Kurzform des vollen DDF-Namens (z. B. „34-Kanal" ⊂ „34-Kanal (Professional DMX)"), meldet die Validierung (`sync.py`) das jetzt als harmlose Umbenennung („Modus … aktualisiert / wird als … geführt") statt als alarmierendes „Mode … fehlt". Beide Warn-Pfade. Test in `test_uxt_kleinvieh.py`. |
| UXT-09 | P3 | ✅ done (dieser PR) | **NOT-AUS bestätigt jetzt sichtbar** | Neuer SyncEvent `LASER_ESTOP`, zentral in `laser_output.estop_all()` emittiert (egal ob VC-Button oder Laser-Steuerseite auslöst). Das Hauptfenster zeigt darauf einen **prominenten rot eingefärbten Statuszeilen-Alarm** („⏹ LASER NOT-AUS ausgelöst — Netzwerk-Ausgabe verriegelt & unscharf", 6 s, selbst zurücksetzend) — bewusst nicht-blockierend (kein wegzuklickender Dialog im Ernstfall). Test in `test_laser_vc_safety.py`. |
| UXT-12 | **P2** | ✅ done (dieser PR) | **NOT-AUS macht DMX-Muster-Laser (L2600) jetzt wirklich dunkel** | `estop_all()` verriegelte nur den Netzwerk-Streamer; ein L2600 lief über DMX weiter. Fix: Latch `app_state.laser_estop_active` (gesetzt zentral in `estop_all` via `set_laser_estop(True)`); `_rebuild_render_plan` cacht die DMX-Adressen aller Laser-Fixtures (`_laser_estop_addrs`) + deren fids (`_laser_fids`); `_render_frame` zwingt bei aktivem Latch als **oberste Ebene** (nach allen Merges, vor Commit) alle Laser-Kanäle auf 0 — überschreibt auch Effekte/Simple-Desk. Latch löst beim nächsten bewussten Laser-Programmerwert (Muster-Abruf/Regler = „wieder an"); Show-Load lässt ihn stehen (dunkel = sicher). Annahme: Betriebsart/Shutter 0 = aus (gilt L2600). Tests `test_laser_dmx_estop.py` (5, inkl. echtem Render: Laser 0, PAR unberührt). |
| UXT-10 | P3 | ✅ done (dieser PR) | **Intensity-Tab erklärt RGBW-Dimmen** | `programmer_view.py`: bei einem Gerät ohne Intensity-Kanäle, das aber Farbkanäle hat, steht statt „Keine Intensity-Kanäle gefunden" jetzt „Dieser Modus hat keinen separaten Dimmer — die Helligkeit läuft über die Farbe (Color-Tab)." |
| UXT-11b | P3 | ✅ done (dieser PR) | **Show-Dialoge starten in sinnvollem Ordner** | `main_window._default_show_dir()`: Speichern/Öffnen starten im Ordner der aktuellen Show, sonst in einem angelegten `…/LightOS/shows` — statt im Arbeitsverzeichnis/Repo-Root (wo Test-Shows neben `main.py` landeten). Test in `test_uxt_kleinvieh.py`. |
| UXT-11a | P3 | ✅ done (dieser PR) | **Zahlenfelder: selectAll bei Fokus** | NEU `src/ui/widgets/select_all_spinbox.py` (`SelectAllSpinBox`): markiert bei Fokus den ganzen Inhalt (verzögertes selectAll, kein GC-Pin), sodass das erste Eintippen den Wert ersetzt statt anhängt. Gezielt umgestellt: Patch-Kopierdialog (Anzahl/Offset), Matrix (Spalten/Reihen). Wiederverwendbar für weitere Zahlenfelder. Tests `test_select_all_spinbox.py`. |

### 🧱 Aus Live-Show-Bau 2026-07-07 (Patch → Fixture-Gruppen, Grid-Editor)
_David baute live eine Show (neue Show, 13 Fixtures, PARs/MovingHeads/Spiders-Gruppen im Grid-Editor) und stieß auf zwei reproduzierbare UX-Fallen, die Zeit kosteten. Datei: `src/ui/views/fixture_group_view.py` (Tab „Fixture-Gruppen")._

| ID | Prio | Status | Titel | Akzeptanzkriterium (Definition of Done) |
|----|------|--------|-------|------------------------------------------|
| PATCH-GRP-01 | **P1** | ✅ done (dieser PR) | **Gruppen-Auswahl bleibt stabil + Drag-Drop rastet ein (kein stiller Overwrite)** | `_reload_group_list` sprang bei jedem Neuaufbau auf `groups[0]` (alphabetisch erste) → nach „+ Neu"/„Speichern" (via `GROUP_CHANGED`) traf die Auswahl unbemerkt die falsche Gruppe (Spiders→MovingHeads überschrieben). Fix: Auswahl per ID erhalten (`select_gid`), „+ Neu" selektiert die neue Gruppe. Externer Drop: `place_fixture`/`_nearest_free_cell` weichen belegten Zellen auf die nächste freie aus (kein stilles Überschreiben), Rand geklemmt, grünes Live-Ziel-Highlight (`resolve_drop_cell`). Plus Shortcut „Alle → Raster" (`_add_all_fixtures`). Tests `tests/test_fixture_group_grid_ux.py` (16). |

### 👁️ Aus UI-Visual-Audit 2026-07-02 (headless MainWindow-Renderpass + 2-Linsen-Panel)
_Autonome Runde (Davids Auftrag: Optik + Bedienbarkeit). Methode + alle Belege: [`docs/UI_VISUAL_AUDIT_2026_07_02.md`](docs/UI_VISUAL_AUDIT_2026_07_02.md). Alle P1/P2-Befunde am Screenshot verifiziert; interaktiver GUI-Nachpass (Computer Use) stand nicht zur Verfügung (Freigabe-Dialog 2× Timeout) und bleibt offen. **UI-15..18 + UI-23 als Quick-Win-Polish-Runde erledigt** (`tests/test_ui_polish_audit.py`, PR folgt)._

| ID | Prio | Status | Titel | Akzeptanzkriterium (Definition of Done) |
|----|------|--------|-------|------------------------------------------|
| UI-15 | P2 | done ([PR #135](https://github.com/ixamgames-droid/lightos/pull/135)) | Sektions-Tab-Beschriftungen clippen in der Haupt-Navigation | `ui/main_window.py` (Section-Buttons) — bei 1440 px sind fast alle Titel abgeschnitten („Bühnen-Lay", „Programm", „Virtual Cons", „Simple Des", „Eingabe / Aus"). Fix: Breite aus `sizeHint` der Beschriftung statt fixer Breite; bei echtem Platzmangel elidieren + Tooltip (kein harter Clip). Screenshot-Vorher/Nachher im PR. |
| UI-16 | P2 | done ([PR #135](https://github.com/ixamgames-droid/lightos/pull/135)) | Grand-Master-Prozentwert liest sich als „00%" | `ui/main_window.py` (Kopfzeile, GM-Slider) — der Slider-Griff überlappt die erste Ziffer des Labels. Fix: Label mit fester Mindestbreite RECHTS neben dem Slider (nie unterm Griff). Sicherheitsrelevant (wichtigster globaler Regler). |
| UI-17 | P2 | done ([PR #135](https://github.com/ixamgames-droid/lightos/pull/135)) | EFX-View: Buttonleisten unlesbar geclippt | `ui/views/efx_view.py` — Buttons unter der Effektliste zeigen „Ne/eic/sch/Sta/St"; auch „Custom Path"-Mini-Buttons betroffen. Fix: `minimumSizeHint` statt harter Breiten (Vorbild: Matrix-View daneben, dort voll lesbar). |
| UI-18 | P2 | done ([PR #135](https://github.com/ixamgames-droid/lightos/pull/135)) | Checkbox-Indikatoren im Dark-Theme unsichtbar | Live-Edit-QSS (`vc_multi_live_editor.py`) + globales QSS prüfen — unangehakte `QCheckBox::indicator` haben keinerlei sichtbare Box. Fix: 1 px heller Rand (`#8b949e`) auch im unchecked-Zustand. |
| UI-19 | P2 | todo | Rohe Tokens app-weit eindeutschen (Options-Labels in die Meta-Schicht) | Der Live-Editor hat seit VCL-03/PR #132 eine Options-Label-Map — Programmer-Matrix-Combos (`rgb_matrix_view.py`, z. B. „normal"), VCStepper-Untertitel (`runner_count`) und VCEncoder („speed") zeigen weiter Roh-Bezeichner. Fix: Label-Quelle nach `rgb_matrix_meta`/`vc_effect_meta` hoisten und in allen drei Verbrauchern nutzen (inkl. kontextabhängiger Overrides wie `loop_mode:"reverse"`). |
| UI-20 | P3 | done | Programmer-Leerzustand: doppeltes „Kein Gerät ausgewählt" ohne Anleitung | `ui/views/programmer_view.py` — Meldung erscheint 2× übereinander, ohne Hinweis, dass links ein Gerät zu wählen ist. Fix: eine Meldung mit Handlungsanweisung; Doppel-Label entfernen. |
| UI-21 | P3 | ✅ done (dieser PR) | Live-View-Kopfzeile: Gerätezähler ohne Initial-Pull | `LiveView.__init__` (`src/ui/views/live_view.py`) rief `_refresh_info()` nie selbst — der Zähler „0 Geräte im Patch" wurde nur über den `_info_timer` (alle 500 ms) aktualisiert (die Präzisierung ggü. der Notiz: es ist der Timer, nicht `PATCH_CHANGED` — `_on_patch_changed` ruft `_refresh_info` gar nicht). Beim Bau einer bereits gepatchten Show blieb der Zähler bis zum ersten Tick (≤500 ms) auf 0 (Bug-Klasse UI-05/UI-09). Fix: `_refresh_info()` am Ende von `__init__` (Initial-Pull). Test `test_live_view_fixes.py::test_info_label_initial_pull`. |
| UI-22 | P3 | todo | Sprachkonsistenz: englische Kategorie-/Widget-Titel neben deutschen | Preset-Browser-Kategorien („Color", „Beam"), VC-Widget-Titel („Cue List" vs. „Cueliste" im Playback). Fix: Anzeige-Mapping vereinheitlichen (keine Datenmigration). |
| UI-23 | P3 | done ([PR #135](https://github.com/ixamgames-droid/lightos/pull/135)) | Live-Edit breit: Vorschau oben ausrichten | `vc_multi_live_editor.py` (Body-Split seit PR #131) — Vorschau-Spalte zentriert vertikal; oben ausrichten (AlignTop), damit kein toter Leerraum über der Vorschau entsteht. |
| UI-24 | P3 | todo | Kleinvieh-Batch aus dem Visual-Audit | Sammel-Item, je 1-Zeilen-Fixes: (a) DMX-Monitor: Rot in Legende erklären + Kontrast; (b) Empty-State-Hinweise für Show Manager/Playback/BPM; (c) BPM „Erkennungsqualität"-Balken mit Kontur bei 0 %; (d) VCColorList: zwei Leerzustands-Hinweise zu einem zusammenfassen; (e) Statuszeile „Enttec: nicht gefunden" klickbar → Output-Einstellungen; (f) Bibliothek-Punktfarben (gelb/blau) Tooltip. |
| UI-25 | P3 | todo | Sektions-Tab-Breitenverteilung bei Davids realer Fensterbreite (1344 px) unfair | Live-GUI-Verifikation 2026-07-02: kein Overlap mehr (PR #135 wirkt), aber unterhalb 1440 px verteilt QHBoxLayout ungleich - `Virtual Console` (laengster Titel) bleibt voll, `Patchen` elidiert, **`E/A` kollabiert zu reinem `...`**. Fix-Idee: faire Verteilung (Shrink proportional zum Ueberhang) oder Kurz-Labels duerfen nie unter ihre Volltext-Breite eliden, solange sie in den Klick-Floor passen. Akzeptanz: bei 1344x896 zeigt jeder Tab mindestens ein sinnvolles Kuerzel (kein reines `...`). |

### 🔎 Aus Demo-/Anleitungs-Bau (Findings, Session 2026-06-29) — ✅ alle erledigt
_Beim Bauen der `Hochzeit_Komplett_2026`-Show + Minianleitungen entdeckt. Volle Details (Problem/Fix/DoD) in [`docs/LOOP_FINDINGS_DEMO_BUILD.md`](docs/LOOP_FINDINGS_DEMO_BUILD.md). **DEMO-02/03/05** → [PR #90](https://github.com/ixamgames-droid/lightos/pull/90), **DEMO-04** → [PR #88](https://github.com/ixamgames-droid/lightos/pull/88) (alle im „Erledigt"-Log unten). Follow-up **DEMO-06** (Generator-Modul-Import) steht in der Codex-Follow-up-Tabelle weiter unten._

### 🤖 Aus Codex-Reviews (Stand 2026-06-24 — noch offene Befunde)
_Befunde der Codex-CLI unter den PR-Kommentaren, gegen aktuellen `main` geprüft. Der STAB-/QA-/VIZ-Teil dieses Audits ist via [PR #55](https://github.com/ixamgames-droid/lightos/pull/55) bereits erledigt (siehe „Erledigt" unten) — hier bleiben die 9 noch offenen UI-/ENG-Punkte. **Audit 2026-06-30 (10-Agent-Workflow):** alle 9 mit High-Confidence als noch real bestätigt, exakte Stellen + Minimal-Fixes verifiziert; keiner braucht eine Design-Entscheidung. Verschieb einzelne Zeilen nach oben in die „Offen"-Liste, um sie für den Loop zu priorisieren._

| ID | Prio | Status | Titel | Akzeptanzkriterium (Definition of Done) |
|----|------|--------|-------|------------------------------------------|
| UI-05 | P2 | done | Programmer-Fokus beim Start initialisieren ([PR #95](https://github.com/ixamgames-droid/lightos/pull/95)) | `ui/views/programmer_view.py` — `programmer_focus` bleibt `None` (Intensity-Tab feuert kein `currentChanged`) → „Intensity gewinnt" erst nach Tab-Wechsel. Fix: einmal `_on_main_tab_changed(currentIndex())` nach connect; Test. [Codex #9](https://github.com/ixamgames-droid/lightos/pull/9#discussion_r3455648382) |
| UI-06 | P2 | ⏸ defer | Color-Tab: Default-Köpfe nicht persistieren — **Design-Entscheidung nötig** | `ui/views/programmer_view.py` (`_seed_separate_head`). **Trade-off (Audit 2026-06-30):** Codex' Fix („nur Anzeige-Fallback, nicht persistieren") **kollidiert mit Davids bewusstem Feature** der Getrennt-Kopf-Unabhängigkeit: Das Tab-Bau-Seeding ist der EINZIG robuste Weg, dass Bewegen von Kopf 0 die anderen Köpfe NICHT mitzieht (deckt Slider, Color-Picker, Palette, VC ab — der DMX-Flush fällt sonst auf Kopf 0 zurück). Ohne Seeding kehrt Davids ursprünglich gemeldeter Bug zurück. → Siehe „🎨 Design-Entscheidungen" unten. Details: [Codex #35](https://github.com/ixamgames-droid/lightos/pull/35#discussion_r3462619556) |
| UI-07 | P2 | done | QuickBars: Representative-Range nur auf kompatible Fixtures ([PR #95](https://github.com/ixamgames-droid/lightos/pull/95)) | `ui/views/programmer_view.py` — gemischte Auswahl nutzt einen Fixture-Range als Template für alle → „open"/Strobe schreibt fremden Range-Mittelwert in inkompatiblen Kanal. Fix: QuickBars auf range-kompatible Fixtures filtern; Test. [Codex #28](https://github.com/ixamgames-droid/lightos/pull/28#discussion_r3462419284) |
| UI-08 | P2 | done | Orientierungs-Tri-State auf Pan/Tilt filtern ([PR #95](https://github.com/ixamgames-droid/lightos/pull/95)) | `ui/views/programmer_view.py` (`_build_orientation_bar`/`_set_orientation`) — statische Fixtures verfälschen Tri-State + Flags werden auf nicht-fähige Fixtures persistiert. Fix: nur Pan/Tilt-fähige berücksichtigen; Test. [Codex #28](https://github.com/ixamgames-droid/lightos/pull/28#discussion_r3462419288) |
| UI-09 | P2 | done | Externe Auswahl in ProgrammerView spiegeln ([PR #95](https://github.com/ixamgames-droid/lightos/pull/95)) | `ui/views/programmer_view.py` — abonniert `SELECTION_CHANGED` nicht → Preset-Browser-Gruppenauswahl im Attribut-Tab unsichtbar/funktionslos. Fix: subscriben + `_selected_fids`/Liste nachziehen; Test. [Codex #13](https://github.com/ixamgames-droid/lightos/pull/13#discussion_r3459492513) |
| ENG-03 | P2 | done | Palette: Phantom-Köpfe vermeiden ([PR #96](https://github.com/ixamgames-droid/lightos/pull/96)) | `core/engine/palette.py` (`apply_to_programmer`) — Mehrkopf-Keys auf Einkopf-Fixture schreiben Bogus-`attr#N`, landen in Snaps/Paletten. Fix: Head-Keys gegen echte Kanal-Vorkommen der Ziel-Leuchte filtern; Test. [Codex #20](https://github.com/ixamgames-droid/lightos/pull/20#discussion_r3461923078) |
| ENG-04 | P2 | done | Palette-Overwrite: stale Werte bereinigen ([PR #96](https://github.com/ixamgames-droid/lightos/pull/96)) | `ui/views/palette_view.py` + `Palette.record_from_programmer` — selektives Überschreiben merged nur, alte Fixture-Werte bleiben und werden später angewendet. Fix: bei Auswahl-Overwrite nicht-Ziel-fids leeren/ersetzen; Test. [Codex #22](https://github.com/ixamgames-droid/lightos/pull/22#discussion_r3462027119) |
| ENG-05 | P2 | done | Preset-Browser: Gruppen-Identität (ID/fids statt Name) ([PR #96](https://github.com/ixamgames-droid/lightos/pull/96)) | `core/engine/preset_search.py` + `app_state` — gleichnamige Gruppen → `select_group_by_name` (`scalar_one_or_none`) scheitert („Gruppe ohne Geräte"). Fix: Gruppen-ID/fids im Eintrag statt nur Name; Lookup per ID; Test. [Codex #13](https://github.com/ixamgames-droid/lightos/pull/13#discussion_r3459492508) |
| ENG-06 | P2 | done | EFX: Spider-Modus nach Auto-Zuweisung aktualisieren ([PR #96](https://github.com/ixamgames-droid/lightos/pull/96)) | `ui/views/efx_view.py` (`_auto_assign_if_empty`) — Auto-Zuweisung nur von Spider-Fixtures ruft `_update_spider_mode()` nicht → Spider-Controls bleiben versteckt. Fix: nach Auto-Zuweisung `_update_spider_mode()` in `_add_efx`/`_start_efx` aufrufen; Test. [Codex #11](https://github.com/ixamgames-droid/lightos/pull/11#discussion_r3458551131) |

### 🤖 Aus Codex-Reviews (Stand 2026-06-30 — Follow-ups auf #87–#90)
_Beim Harvesten der Codex-Kommentare auf den frisch gemergten PRs #87–#90 entdeckt + gegen aktuellen `main` verifiziert. Es sind echte Folge-Bugs der jeweiligen Fixes (kein Reopen der Hauptfindings)._

| ID | Prio | Status | Titel | Akzeptanzkriterium (Definition of Done) |
|----|------|--------|-------|------------------------------------------|
| STAB-09 | P1 | done | `reset_show`: re-entranten `patch_changed`-Emit unterdrücken ([PR #93](https://github.com/ixamgames-droid/lightos/pull/93)) | `core/show/show_file.py` (`reset_show`) — der DEMO-03-`clear_patch()` läuft NACH `_replace_patch_from_data` (das `_suppress_emits` im `finally` auf `False` zurückstellt) → feuert mitten im Reset `patch_changed`, während programmer/functions/VC noch alt sind → re-entranter Refresh (genau der STAB-07-Pfad, AV-Gefahr). Fix: den harten `clear_patch()` im `_suppress_emits`-Fenster ausführen; der finale gebündelte `patch_changed`-Emit bleibt einzige Notification; Test. [Codex #90](https://github.com/ixamgames-droid/lightos/pull/90#discussion_r3495290942) |
| ENG-09 | P2 | done | `classify_attr`: `#N`-Kopf-Suffix vor Klassifikation strippen ([PR #97](https://github.com/ixamgames-droid/lightos/pull/97)) | `core/attr_groups.py` (`classify_attr`) — Mehrkopf-Schlüssel `prism_rotation#1` verfehlt den Exact-Match und fällt über den Substring `prism` in **Beam** statt **Effect** → falsches Snap/Szenen-Label für den 2. Kopf. Fix: `attr.split("#",1)[0]` vor der Klassifikation (generell für ALLE Multi-Head-Attribute); Test. [Codex #89](https://github.com/ixamgames-droid/lightos/pull/89#discussion_r3495260316) |
| ENG-10 | P2 | done | `sync_phase`: Stall-Bus-Cache beim Re-Ankern löschen ([PR #97](https://github.com/ixamgames-droid/lightos/pull/97)) | `core/engine/rgb_matrix.py` (`sync_phase`) — nach Bus-Stall gilt `_last_bus_pos == pos`; `sync_phase` re-ankert `_beat_anchor`, lässt aber `_last_bus_pos` stehen → der DEMO-04-Stall-Check überspringt im nächsten Frame die Bus-Sync und free-runt → Sync-Aktion/Phasen-Offset ignoriert, bis der Bus vorrückt. Fix: `_last_bus_pos=None` im Bus-Zweig von `sync_phase`; Test. [Codex #88](https://github.com/ixamgames-droid/lightos/pull/88#discussion_r3495186868) |
| UI-14 | P2 | done | VC-Badge-Farben nach Live-Color-Edit invalidieren ([PR #97](https://github.com/ixamgames-droid/lightos/pull/97)) | `ui/virtualconsole/vc_button.py` (`_color_badge_colors`) — Cache-Key `(action, snap_ids, function_id)` ignoriert den Inhalt der live-editierbaren `ColorSequence` → nach Live-Edit (VCColor `EFFECT`/`add_color`/`toggle_color`) bleibt `_badge_colors` stale (zeigt entfernte/alte Farben + falschen Cycle-Zustand). Fix: Sequence-Signatur (`enabled_colors`) in den Cache-Key; Test. [Codex #87](https://github.com/ixamgames-droid/lightos/pull/87#discussion_r3495059354) |
| DEMO-06 | P3 | done | Generatoren: `_gen_env`-Import auch im Modul-Import-Pfad ([PR #97](https://github.com/ixamgames-droid/lightos/pull/97)) | `tools/build_*.py` — `import _gen_env` resolved nur bei Direkt-Ausführung (Python legt `tools/` auf `sys.path`), nicht bei `python -c "import tools.build_x"` (nur Repo-Root auf Path). Fix: `tools/`-Verzeichnis vor dem Import zu `sys.path` ergänzen (oder paket-relativer Fallback) — zentral in `tools/_gen_env.py`-Importpfad; Test. [Codex #90](https://github.com/ixamgames-droid/lightos/pull/90#discussion_r3495290945) |

> **Geprüfte False-Positives (NICHT wieder öffnen):** PR #91 (reine Docs-PR) erhielt 2 Codex-„Reopen"-Kommentare (DEMO-02/04 + ENG-07), weil die `docs/backlog-sync`-Branch die Merges #88/#89/#90 noch nicht enthielt (stale Review-Base). Gegen aktuellen `main` am 2026-06-30 verifiziert: `tools/_gen_env.py` + `import _gen_env`, `prism_rotation` exakt in `attr_groups.ATTR_GROUPS['Effect']`, und die `_last_bus_pos`-Stall-Logik in `rgb_matrix.py` existieren alle → DEMO-02/04 + ENG-07 sind korrekt erledigt.

### 🧪 Test-Infrastruktur

| ID | Prio | Status | Titel | Akzeptanzkriterium |
|----|------|--------|-------|--------------------|
| QA-24 | P2 | ✅ done (2026-07-08, Runner-Tooling) | Lock-Runner klassifizierte nativen Crash nicht-deterministisch als Failure → falsch-rote Gates | `run_tests.ps1 -Isolate` zählte nur `rc=139`/`-1073741819` als CRASH; **jeder** andere Nicht-Null-Code → FAIL/rot. Dieselbe viz13-QtWebEngine-Teardown-Instabilität crasht aber mit wechselndem nativem Code (mal `-1073741819` AV → grün, mal `-2147483645` 0x80000003 Breakpoint → FAIL/rot) → identische Baseline kippte das Gate zufällig. **Fix:** Crash-Erkennung auf die ganze Familie erweitert — Unix 128+Signal (`129..192`) ODER Windows-NTSTATUS (große negative Codes, `rc < -100000000`); echte pytest-Failures (Exit 1..5) bleiben FAIL/rot. Verifiziert (alle Codes + E2E: `2 ok, 0 Failures, 5 Crashes → exit 0`). **Hinweis:** `run_tests.ps1` liegt AUSSERHALB des Repos (Davids sitzungsübergreifendes Tooling im äußeren Ordner) → nicht versioniert; Doku: SecondBrain `reference_pytest_lock`. |
| QA-23 | P2 | ✅ done ([PR #199](https://github.com/ixamgames-droid/lightos/pull/199)) | MainWindow-Tests hängen am Autosave-Recovery-Dialog (Baseline-Bruch, entdeckt 2026-07-07 im PR-#198-Gate) | Lag eine Autosave-Datei neuer als alle Recents vor, öffnete `_check_autosave_recovery` beim MainWindow-Bau (via `singleShot(500)`) ein modales `QMessageBox.question` → headless blockierte in den pytest-Timeout: `test_ui_polish_audit.py` + `test_vc_canvas_clear_on_new_show.py` (die einzigen zwei MainWindow-bauenden Tests) fielen zustandsabhängig. **Fix:** `_recovery_prompt_suppressed()` (`LIGHTOS_NO_RECOVERY_PROMPT`-Env, conftest-gesetzt, ODER `QT_QPA_PLATFORM=offscreen`) — Guard VOR jedem Dateizugriff + `singleShot` gar nicht erst geplant; Live-Recovery in der echten App unverändert. Regressionstest `tests/test_autosave_recovery_headless.py` (A: headless fragt nie/kein Modal; B/C: Live-Logik fragt genau einmal, Yes stellt wieder her; `_autosave_path` strikt auf Temp). |
| QA-06 | P2 | done | `test_vc_tempo_live_coupling` fixtures.db-Setup-Kollision ([PR #104](https://github.com/ixamgames-droid/lightos/pull/104)) | Root-Cause war eine **TOCTOU-Lücke in `create_all(checkfirst=True)`**: greifen zwei Verbindungen/Läufe auf dieselbe SQLite-Datei zu, reflektiert die eine `sqlite_master`, bevor der parallele CREATE der anderen committet ist → eigener CREATE kollidiert mit „already exists". `checkfirst` allein verhindert das nicht. Fix: neuer Helfer `models.create_all_idempotent` schluckt nur „already exists", verdrahtet in `open_show`/`create_db`/`get_engine` (conftest darf fixtures.db NICHT umlenken — feste `fixture_profile_id`). Baseline danach 274/274 grün. |

### 🖥️ Aus VC-Code-Audit (Stand 2026-06-30)
_10-Agent-Audit des `src/ui/virtualconsole/*`-Subsystems (+ `effect_live.py`, `show_file.py`-VC-Teil). **Volle Details, alle 31 Bugs + 14 Verbesserungen + Design-Fragen + F-26b-Plan:** [`docs/VC_AUDIT_2026_06_30.md`](docs/VC_AUDIT_2026_06_30.md). Gesamtbild: Code strukturell solide; Schwerpunkte = VCFrame-Seitensteuerung, Bank-Propagierung, zu nachsichtige `apply_dict`-Pfade. Die **10 P1-Bugs** (alle Aufwand S) werden vom Loop zuerst gefixt:_

| ID | Prio | Status | Titel | Stelle |
|----|------|--------|-------|--------|
| VCB-01 | P1 | done | VCFrame.switch_page traversiert Grandchildren → versteckt Kinder innerer Frames ([PR #100](https://github.com/ixamgames-droid/lightos/pull/100)) | `vc_frame.py:switch_page` |
| VCB-02 | P1 | done | add_child_to_page: `widget.show()` überschreibt `setVisible` (falsche Seite sichtbar) ([PR #100](https://github.com/ixamgames-droid/lightos/pull/100)) | `vc_frame.py:add_child_to_page` |
| VCB-03 | P1 | done | _open_properties: Seitenzahl-Reduktion klemmt `_current_page` nicht ([PR #100](https://github.com/ixamgames-droid/lightos/pull/100)) | `vc_frame.py:_open_properties` |
| VCB-04 | P1 | todo ⏸ | VCFrame-Kinder erben Bank-Index statt -1 → MIDI/Hotkey stumm nach Bank-Wechsel. **Deferiert:** der einfache Fix (`bank=-1` beim Umhängen) ist nur korrekt, wenn der Frame selbst `bank=-1` ist; bei Frame auf fester Bank würde das Kind auf allen Bänken reagieren. Braucht saubere Lösung (MIDI-Dispatch wertet die Bank des Eltern-Frames für Frame-Kinder) → genauere Analyse. | `vc_canvas.py:_add_widget` / `vc_frame` |
| VCB-05 | P1 | done | `fixture_dimmers` (F-25) bei reset/load nicht geleert → Ghost-Dimmer ([PR #100](https://github.com/ixamgames-droid/lightos/pull/100); `feature_dimmers`/F-26 nicht auf main → Teil von F-26b) | `show_file.py:reset_show`/`load_show` |
| VCB-06 | P1 | done | „Alle löschen" (Kontextmenü) nicht Undo-bar ([PR #100](https://github.com/ixamgames-droid/lightos/pull/100)) | `vc_canvas.py:_context_menu` |
| VCB-07 | P1 | done | `set_param_normalized`: color/action-Kinds → AttributeError (`spec.min/max`) ([PR #100](https://github.com/ixamgames-droid/lightos/pull/100)) | `effect_live.py:set_param_normalized` |
| VCB-08 | P1 | done | Negativer `function_id` → letzter Executor statt Stop ([PR #100](https://github.com/ixamgames-droid/lightos/pull/100)) | `vc_button.py:_trigger_primary` |
| VCB-09 | P1 | done | Negativer `snapshot_index` → letzter Snapshot statt Abbruch ([PR #100](https://github.com/ixamgames-droid/lightos/pull/100)) | `vc_button.py:_apply_snapshot` |
| VCB-10 | P1 | done | VCEffectColors.mousePressEvent: fehlendes `event.accept()` → Klick verschiebt Widget ([PR #100](https://github.com/ixamgames-droid/lightos/pull/100)) | `vc_effect_colors.py:mousePressEvent` |

_**Stand 2026-06-30:** P1 (VCB-01..10) → [PR #100]; **P2 (VCB-11/12/13/14/15/16/17/20)** → [PR #105]; **P3 (VCB-24/25/26/28/29/30/31)** → [PR #106]; **Verbesserungen (VCI-01..08/10/11/12/14)** → [PR #107]. **Offen:** VCB-04 (deferiert, Frame-Bank-Vererbung, braucht saubere MIDI-Dispatch-Lösung). **VCI-09** = verifizierter False-Positive (keine Loop-Closure), **VCI-13** bewusst ausgelassen (Refactor-Risiko). 6 Design-Fragen + F-26b: siehe unten / das Doc._

## 🎨 Design-Entscheidungen

### ✅ Von David entschieden (2026-06-30)
**Erledigt:**
- **F-26 / F-26b + DQ-6** · Feature-Dimmer-Master komplett: Backend (`b8b43f3`) cherry-gepickt auf main + VC-Bindung (`SliderMode.FEATURE_DIMMER`, feature_attr = **ComboBox aus Capabilities**, Slot-Sync/Re-Apply/clear bei load/reset). → [PR #109](https://github.com/ixamgames-droid/lightos/pull/109).

**Freigegeben, offen (nächste Session):**
- **UI-06** · Color-Tab Getrennt-Modus → **Save-Filter:** beim Snap/Szene/Palette-Speichern Kopf>0-Farbwerte verwerfen, die exakt dem aktuellen effektiven Kopf-0-Wert entsprechen (lebenszyklus-freier Gleichheits-Filter; Ansatz mit David noch final abzunicken). Kopf-Unabhängigkeit bleibt. Save-Pfade: `programmer_to_scene_values`, `palette.record_from_programmer`, Snap-Capture.
- **DQ-3** · VCStepper **absoluter MIDI-Modus** (analog Encoder `midi_mode`).
- **DQ-4** · VCFrame-Kontextmenü **MIDI/Key-Teach + Live-Param**.
- **DQ-5** · ~~VCColorList/VCChaseBuilder als Drop-Ziele~~ — **hinfällig:** `VCChaseBuilder` wurde 2026-07 komplett entfernt (Davids VC-Umbau, [PR #116](https://github.com/ixamgames-droid/lightos/pull/116)). `VCColorList` bleibt reine Anzeige.

### ⏳ Noch offen / niedrige Prio
- **VIZ-05** · **Frische Farb-Matrix macht dunkles Licht (Default-Kollision)** — verifiziert 2026-07-02 (Beweis: [`docs/UI_VISUAL_AUDIT_2026_07_02.md`](docs/UI_VISUAL_AUDIT_2026_07_02.md)): `drive_intensity=False` (Default neuer Matrizen, bewusst „reine Farb-Ebene") × `implicit_brightness=False` (Default neuer Shows seit 2026-06-24, bewusste Farbe/Dimmer-Trennung) ⇒ neue RGB-Matrix auf Dimmer-PARs in frischer Show = Farbe im DMX, Dimmer 0, Licht aus — ohne Hinweis. Beide Defaults sind einzeln Davids bewusste Entscheidungen, daher **keine autonome Änderung**. Optionen: **(a)** Heuristik beim Matrix-ERSTELLEN über die UI: wenn kein Dimmer-Treiber (Cue/Programmer/anderer Effekt) für die Zielgeräte existiert → `drive_intensity=True` vorschlagen oder Hinweis zeigen; **(b)** 4a²-Grundhelligkeit präziser triggern (nur wenn Farbe aktiv UND keinerlei explizite Dimmer-Absicht), statt globalem Bool; **(c)** Nur-UI: Warnbanner im Matrix-Editor/Live-View, wenn ein laufender Farb-Effekt auf komplett ungedimmte Fixtures schreibt. → David wählt.
- **DQ-1** · FUNCTION_TOGGLE Multi-Gruppe — Ausschalten nimmt clear_programmer/exclusive/solo NICHT zurück. Default: als **bewusst mechanisches Stop dokumentieren** (kein Code-Change), außer David will Symmetrie.
- ~~**DQ-2** · VCSlider PLAYBACK — `function_id` als Executor-Slot~~ → **✅ done (dieser PR): saubere Trennung gebaut.** Dediziertes `playback_slot`-Feld (Spinbox „Playback Executor-Slot", nur im PLAYBACK-Modus) statt `function_id`-Zweckentfremdung; Persistenz + **Migration** alter Shows (Slot lag früher in `function_id`) → rückwärtskompatibel; Bonus-Guard `0 <= slot < len`. ✅ **Von David bestätigt (2026-07-08): die saubere Trennung wird behalten** — Entscheidung geschlossen (war die geparkte Design-Frage „nur dokumentieren vs. saubere Trennung"; David hat für die Trennung entschieden). Bleibt trivial revertierbar, falls je gewünscht.
- **VCB-04** · VCFrame-Kinder erben Bank-Index → MIDI/Hotkey stumm nach Bank-Wechsel. Braucht saubere Lösung (MIDI-Dispatch wertet die Bank des Eltern-Frames für Frame-Kinder).
- **PROC-01** · Changelog-Policy — der Loop pflegt `CHANGELOG.md` pro Runde (Loop-Schritt 7); aktuell aktiv praktiziert. Kein WORKFLOW.md-Zwang; bei Bedarf formalisieren.
- **STAB-11** · **`sync.subscribe_widget` auf schwach gehaltene Callbacks umstellen (Re-Landing nach dem STAB-10-Sweep)** — der starke Callback-Halt im globalen Bus pinnt weiterhin jedes abonnierende Widget (STAB-03-Falle: Wrapper überlebt den C++-Tod via Eltern-Kaskade). Der erste Versuch (#142, per [#145](https://github.com/ixamgames-droid/lightos/pull/145) revertiert) nutzte ein Anker-Design (`_sync_cb_pins` = GC-barer Selbst-Zyklus) — das schickt JEDEN abonnierenden Owner durch die zyklische GC (AV-Klasse). **Plan stattdessen:** Call-Site-Lambdas auf Bound-Methods/`weak_slot` umstellen + Bus hält `weakref.WeakMethod` → kein Zyklus, Views sterben weiter per Refcount. **Blocker vorab klären:** `test_matrix_dirty_save` deckt eine Zerstörungs-Fragilität der `RgbMatrixView` auf (Heap-Corruption 0xC0000374 beim Refcount-Tod MITTEN im Test; nur ohne Bus-Pin erreichbar). **Abnahme:** die 6 Canary-Dateien aus #145 + `test_ui_teardown_gc`/`test_snapshot_teardown_gc`. Details: SecondBrain `entry_crash_logging` (STAB-10).

## ✅ Erledigt (Kurz-Log)
_(der Loop verschiebt fertige Items mit PR-Link hierher; Details stehen in [CHANGELOG.md](CHANGELOG.md))_

- **VIZ-FIX-DECIMAL** (+ VIZ-STAGE-PANEL a) · 3D-Panel-Zahlenfelder akzeptieren Punkt UND Komma als Dezimaltrenner: neues wiederverwendbares `LocaleTolerantDoubleSpinBox` (`src/ui/widgets/decimal_spinbox.py`) ersetzt alle 14 `QDoubleSpinBox` in `visualizer_window.py` (Fixture-Position/-Ausrichtung, Stage-Größe, Raster). Vorher verwarf die Standard-Spinbox unter dt. Locale eine Eingabe mit Punkt („5.7" → Datenverlust). +5 Tests (`test_decimal_spinbox.py`). (dieser PR)

- **UI-21** · Live-View-Kopfzeile: Gerätezähler zeigt die Anzahl jetzt sofort beim Bau (`_refresh_info()`-Initial-Pull am Ende von `LiveView.__init__`) statt erst nach dem ersten 500 ms-`_info_timer`-Tick (Bug-Klasse UI-05/UI-09). Präzisierung: der Zähler hing am Timer, nicht an `PATCH_CHANGED`. +1 Test (`test_live_view_fixes.py`). (dieser PR)

- **VIZ-BUILDER-FIXTYPE** · `ShowBuilder.patch()` übernimmt `fixture_type` aus dem `FixtureProfile` (statt Default `'other'`) → 3D-Visualizer färbt/bewegt Skript-gepatchte Fixtures korrekt (vorher PAR-Fallback ohne DMX→Farbe/Pan/Tilt-Abbildung). Neuer Helfer `_lookup_profile` → `(id, fixture_type)`; redundanter manueller Nachzieh-Block in `tools/build_grosses_rig.py` entfernt. +1 Regressionstest (`test_showbuilder.py`, querabgesichert gegen den echten Profil-Typ). Gate grün. (dieser PR)

- **DQ-2** · VCSlider-PLAYBACK nutzt dediziertes `playback_slot` statt `function_id` (saubere Trennung — Davids geparkte Design-Entscheidung aufgelöst): eigene Spinbox „Playback Executor-Slot" (nur PLAYBACK-Modus), Persistenz + Migration alter Shows (Slot lag in `function_id`), Guard `0 <= slot < len`. **Resume-Falle vermieden:** die WIP lag auf einem Stale-Branch (`#111`-Base) — sauber auf aktuelles `main` (`#201`, mit UXT-01/LAS-Speed) neu angewandt **statt gemerged**, sonst hätte der Merge diese `vc_slider.py`-Arbeit revertiert. +7 Tests (`test_vc_slider_playback_slot.py`), Gate grün. (dieser PR)

- **FLA-1/2/3** · Fixture-Library-Audit-Fixes (Davids Prüfauftrag 2026-07-04, 1 PR): vollständiges Audit aller ~30 Builtins × 4 Achsen (6-Agenten-Workflow, adversarial verifiziert) → Bibliothek ~90 % schlüssig. Behoben: **PARTYLASER** wurde wegen zweier `color_r`-Kanäle als Spider statt Laser gerendert → `is_spider_fixture` gatet `fixture_type=='laser'` zentral aus (fixt 3D + beide 2D-Pfade + Patch-Spiegel auf einen Schlag; DMX unberührt; adversariale Review SICHER); **CMY**-Kanäle (`cmy_c/m/y`) fielen mangels Klassifikation aus dem Color-Tab → in `ATTR_GROUPS['Color']` + `_DIM_COLOR_ATTRS` (wie `palette.py`); **DOTZTPAR**-Strobe ohne Range → `_SIMPLE_STROBE`. +3 Regressionstests, Gate grün (314 ok, 0 Failures). Offen (P3): FLA-4 (`other`-Klasse ohne 3D-Modell/Icon), FLA-5 (Kleinvieh). Die 2D-Bar-Icon-Findings = FM-6/7 (via #174 erledigt). [PR #176](https://github.com/ixamgames-droid/lightos/pull/176)

- **STAB-10** · GC-Teardown-AV-Klasse (PySide6 6.11/Py 3.14: zyklische GC dealloziert Owner-Wrapper → native AV, faulthandler „Garbage-collecting") ursächlich gefixt + repo-weit ausgesweept, 3 PRs: **(1)** SnapshotsView-Baum — `SnapshotButton._view` → weakref, `snap_file_panel`-Lambda-Slots → Bound-Methods; +3 Regressionstests (`test_snapshot_teardown_gc.py`). [PR #142](https://github.com/ixamgames-droid/lightos/pull/142) _(in SecondBrain/Commit als „STAB-09 (GC)" geführt — Nummern-Kollision mit dem älteren reset_show-STAB-09 unten, PR #93.)_ **(2)** Der zusätzliche `subscribe_widget`-weak-Callback-Umbau aus #142 crashte 6 vorher grüne Testdateien (das starke Bus-Leck war eine versehentliche Schutzschicht für Views mit eigenen Zyklen) → revertiert in [PR #145](https://github.com/ixamgames-droid/lightos/pull/145); Re-Landing = **STAB-11** (oben). **(3)** Sweep über `src/ui/` [PR #147](https://github.com/ixamgames-droid/lightos/pull/147): alle starken Kind→Owner-Refs (EfxPopoutDialog, AttributeSlider, vc_drop_panel, EfxView-Preview-Callback, RenderCrashGuard-status_cb, `ColorButton.color_changed`, follow-Subscribes) → weakref; ~90 self-fangende Lambda-Slots in 38 Dateien → Bound-Methods/sender()-Adapter/neue `weak_slots`-Helfer (empirisch: auch functools.partial pinnt GC-unsichtbar; Shibokens Parent→Kind-Kante ist GC-sichtbar → Kind→Owner-Ref zykelt den Owner ohne Python-Container); Kontextmenü-/Modal-/`destroyed`-Lambdas bewusst belassen. Matrix-/Programmer-/EFX-Views sterben per Refcount (5/6 Canaries grün sogar unterm weak-sync-Stand). +10 Regressionstests (`test_ui_teardown_gc.py`; fand beim Schreiben selbst einen 4. Owner-Zyklus per nativem Crash). Volles Gate 298 Dateien, 0 Failures, **0 Crashes**.

- **Live-Edit-Widget · Farb-/Dimmer-Sequenz-Auswahl + responsives Layout + Anzeige-Toggles + Immer-Vorschau** (Davids Auftrag Chat 2026-07-01, 1 PR): **Farbwechsel-Auswahl** — `color_sequence`-Params („Farben") als neuer Regler-Typ im Live-Editor (`ColorSequenceField`, Swatch-Strip + Popout; flüchtig via `effect_live`-Baseline). **Dimmer-Stufen** — neue Engine-ParamSpec `dimmer_levels` (kind `dimmer_sequence`, Gate = Renderer: Dimmer-Style + CHASE + `dimmer_cycle`), im Editor als `DimmerSequenceField`; Guards in `set_param_normalized`/`adjust_param`. **Responsiv** — Tempo-Zeile einzeilig ab 430 px (Multiplikator neben Aus/BPM/Tap), Vorschau links neben den Reglern ab 560 px (deferred Relayout, Widgets zwischen zwei permanenten Containern umgehängt). **Anzeige-Toggles** — Vorschau/Tempo-Kontrolle pro Effekt an-/abwählbar („Anzeige:"-Zeile im Bearbeiten-Modus), persistiert als `hidden` (Waisen-Guard, Back-Compat). **Rahmenlos** — Scroll-Bereich ohne Border. **Immer-Vorschau** — Matrix animiert auch gestoppt (`_advance_step`-Selbstantrieb, VCEffectDisplay-Muster; DMX-sicher). **Tempo pro Effekt** — Verhalten bestand schon, jetzt beschriftet „Tempo (dieser Effekt)". Adversariale 3-Linsen-Review fand 1 HIGH (Tempo nach Ab-/Wieder-Anhaken dauerhaft unsichtbar — `_apply_display_visibility` entschied nur Richtung „verstecken") → gefixt + Regressionstests. +22 Tests (50 im Editor-File + 6 Engine). [PR #131](https://github.com/ixamgames-droid/lightos/pull/131)

- **Live-Edit-Widget · Canvas-Umbau + Bearbeiten/Bedienen-Trennung + visuelle Regler** (Davids Folge-Feedback 2026-07-01, 4 PRs): Aus dem frei schwebenden Fenster (unten) wurde ein **persistentes VC-Canvas-Widget** — `VCMultiLiveEditor(VCWidget)`, in `WIDGET_REGISTRY`, sitzt/skaliert wie jedes VC-Widget, im Layout gespeichert; **Effekt-Zuweisung (`fids`) + Regler-Auswahl (`checked`) persistent, die eingestellten Werte flüchtig** (`effect_live`-Baseline). Quick-Add „Live-Edit", Inspector-Label. [PR #126](https://github.com/ixamgames-droid/lightos/pull/126); Doku floating→canvas [PR #127](https://github.com/ixamgames-droid/lightos/pull/127). Danach **Bearbeiten/Bedienen-Trennung** (an den VC-Bearbeiten-Modus gekoppelt: Bearbeiten → Häkchen-Auswahl, *welche* Regler ein Effekt zeigt; Bedienen → nur die angehakten, ohne Kästchen — Content in beiden Modi bedienbar, verschoben über Header, skaliert über Randring) + **visuelle Regler statt Textform** (Slider · −/+-Stepper · Pfeil-Richtungswähler →/←/↔/Mitte↔außen · Segment-Buttons für Auswahl ≤5 · An/Aus-Toggle · Combo erst ab 6 Optionen), Auswahl je Effekt gespeichert (`checked` + Waisen-Guard für abgewiesene fids), Tempo Aus/BPM/Tap mit Multiplikator. Deferred Rebuild (`QTimer.singleShot(0)`+`shiboken6.isValid` gegen Use-after-free). [PR #128](https://github.com/ixamgames-droid/lightos/pull/128). Anleitung `docs/anleitung_testshow_liveedit/` — 3-Seiten-Testshow (Farbe/Dimmer/Tempo, 11 Effekte) **live via Computer Use gebaut + getestet** — + `LIVE_EDIT_FENSTER.md` nachgezogen [PR #129](https://github.com/ixamgames-droid/lightos/pull/129). Je Branch grünes Gate + adversariale 3-Lens-Review (1 LOW-Waisen-Auswahl direkt mitgefixt). **Feinschliff-Follow-ups** (VCL-01..04/VIZ-05) oben unter „Offen".
- **VC-Umbau · Live-Edit-Fenster + 3 Entfernungen** (Davids Auftrag 2026-06-30/07-01, 6 PRs): **Entfernt** — Chase Builder (`VCChaseBuilder`, [PR #116](https://github.com/ixamgames-droid/lightos/pull/116)); Color-Chase + Chase-Bereich + Controller-Knopf inkl. Canvas-Aufzieh-Werkzeug ([PR #118](https://github.com/ixamgames-droid/lightos/pull/118)). **Neu** — `VCMultiLiveEditor`: frei schwebendes, **nicht-persistentes** Live-Edit-Fenster — Effekte per Drag&Drop, Navigation (–/Dropdown/+), Vorschau je Typ (Matrix/EFX/Chaser), Checkbox-Parameter-Editor (live über `effect_live`, baseline-geschützt), Tempo-Modus Aus/BPM/Tap. Foundation [PR #119](https://github.com/ixamgames-droid/lightos/pull/119), Parameter [PR #120](https://github.com/ixamgames-droid/lightos/pull/120), Vorschau [PR #121](https://github.com/ixamgames-droid/lightos/pull/121), Tempo [PR #122](https://github.com/ixamgames-droid/lightos/pull/122). Doku: `docs/LIVE_EDIT_FENSTER.md`. Je Branch adversariale 3-Lens-Review + grünes Gate. _(Erste voll-autonome Loop-Strecke ohne Zwischen-„go".)_

- **F-26 / F-26b** · Feature-Dimmer-Master (Backend + VC-Bindung, 1 PR): effekt-unabhängiger per-Slot-Master, der eine wählbare Feature-Gruppe (Intensity/Color/Gobo/Beam/Position/Effect) einer festen Gruppe multiplikativ am fertigen Output skaliert (Render 4b²). Backend `b8b43f3` cherry-gepickt auf aktuellen main (konfliktfrei, 13 Tests). VC-Bindung: `SliderMode.FEATURE_DIMMER`, feature_attr-ComboBox aus Fixture-Capabilities, Slot-Sync (enter/leave), Re-Apply beim Laden, `clear_feature_dimmers` bei reset/load, destroyed-Cleanup, Capability-Manifest regeneriert. +9 VC-Tests, Suite 277/277 grün. [PR #109](https://github.com/ixamgames-droid/lightos/pull/109)

- **VCI-01..08/10/11/12/14** · VC-Audit-Verbesserungen (1 PR): Lit-Indikator für Tempo-Toggles (VCI-01), Assign-Overlay-Hinweise (VCI-02), `normalize_color_target`-Log (VCI-03), Encoder-/Slider-Modus-Validierung (VCI-04/05), `mappable_param_choices`-Konsistenz (VCI-06), totes `_lp_fired` raus (VCI-07), `aspect_caption`-Token (VCI-08), VCColor-W-Fold im Swatch (VCI-10), Kommentar+Guard (VCI-11/12), `snap_ids`-Dedup beim Save (VCI-14). **VCI-09** verifizierter False-Positive, **VCI-13** bewusst ausgelassen. +14 Tests, Suite 275/275 grün. [PR #107](https://github.com/ixamgames-droid/lightos/pull/107)
- **VCB-24/25/26/28/29/30/31** · VC-Audit P3-Bugfix-Batch (1 PR): _teach_midi-Sentinel, `_spec_for` try/except, ColorList-Hit-Test spiegelt Paint-Layout, SpeedDial-`_bpm` float-coerce, `add_live_controls` dynamischer y-Offset, `replace_widget_type` Undo-erst-nach-Erfolg (außerhalb `_restoring`), VCButton snap_id-Cleanup. +10 Tests, 274/274 grün. [PR #106](https://github.com/ixamgames-droid/lightos/pull/106)
- **VCB-11/12/13/14/15/16/17/20** · VC-Audit P2-Bugfix-Batch (1 PR): XYPad-Rundung (konsistent zum 16-bit-Pfad), SpeedDial min/max-BPM-Persistenz + invert-bewusster Probe, Stepper per-Fixture-Key, Slider-ZeroDivision-Guard, VCColor `begin_live_edit`, negative function_id ablehnen, Slider `_effect_targets` via `_all_target_fids()`. +9 Tests, 273/273 grün. [PR #105](https://github.com/ixamgames-droid/lightos/pull/105)
- **QA-06** · `create_all` TOCTOU-idempotent (Test-Baseline wieder voll grün): siehe Test-Infrastruktur oben. +3 Tests. [PR #104](https://github.com/ixamgames-droid/lightos/pull/104)
- **VCB-32/33/34 + UI-14c** · Frische Codex-Folgebugs auf #100/#101 (1 PR): GROUP_DIMMER/SUBMASTER beim Show-Laden re-applien (VCB-32, Folge von VCB-05), `range_max=0` als Mute-Konfig erhalten (VCB-33), GROUP_DIMMER-Retarget-Geist (VCB-34, `_post_dialog_mode_sync`), `refresh_effect_badges(None)` löst aktiven Effekt auf (UI-14c, fixt VCColor+VCEffectColors). +8 Tests. [PR #103](https://github.com/ixamgames-droid/lightos/pull/103)

- **UI-14b + VCB-18/19/21/22/23/27** · VC-Audit P2-Robustheits-Batch (1 PR): **UI-14b** (Codex-Follow-up auf #97) — neue `VCCanvas.refresh_effect_badges(fid)` + `VCWidget._notify_effect_colors_changed`; VCColor/VCEffectColors rufen sie nach einem Live-Color-Edit → gebundene VCButton-Badges repainten sofort (statt erst beim Bank-Wechsel). **VCB-18** `VCButton.apply_dict` setzt `_snap_active`/`_snap_prev` zurück (kein stale Snap-Toggle nach Reload). **VCB-19** `VCSlider._open_properties` setzt beim Verlassen von GROUP_DIMMER die Fixture-Dimmer der Gruppe auf 1.0 (kein Geister-Dimmer; komplementär zu VCB-05). **VCB-21** `apply_dict` ignoriert korrupte Hex-Farben (`isValid`). **VCB-22** `apply_dict` erzwingt `MIN_SIZE` (kein 0×0-Widget). **VCB-23** `VCSlider.apply_dict` klemmt `_value` auf 0..255. **VCB-27** `apply_dict` überlebt JSON-`null` bei range_min/max. +7 Tests, 277 VC-Regressionstests grün. [PR #101](https://github.com/ixamgames-droid/lightos/pull/101)
- **VCB-01/02/03/05/06/07/08/09/10** · VC-Audit P1-Bugfix-Batch (9 Bugs, 1 PR): aus dem VC-Code-Audit, alle gegen den echten Code verifiziert. **VCB-01** `VCFrame.switch_page` nutzt `FindDirectChildrenOnly` (versteckte sonst Kinder verschachtelter innerer Frames). **VCB-02** bedingungsloses `widget.show()` in `add_child_to_page` entfernt (Widget erschien auf falscher Seite). **VCB-03** `_open_properties` ruft nach Seitenzahl-Reduktion `switch_page` (klemmt `_current_page`). **VCB-05** `reset_show`/`load_show` leeren `state.fixture_dimmers` (F-25 GROUP_DIMMER-Ghost nach Show-Wechsel). **VCB-06** „Alle löschen" pusht jetzt Undo. **VCB-07** `effect_live.set_param_normalized` gibt für color/color_sequence/action-Kinds `False` statt AttributeError (symmetrisch zu `adjust_param`). **VCB-08** negativer Executor-Slot-Guard. **VCB-09** negativer Snapshot-Index-Guard. **VCB-10** `VCEffectColors.mousePressEvent` `event.accept()` bei input-blocked. +7 Tests, 271 VC/Show-Regressionstests grün. VCB-04 (Frame-Bank-Vererbung) deferiert (nuancierter). [PR #100](https://github.com/ixamgames-droid/lightos/pull/100)
- **ENG-09 / ENG-10 / UI-14 / DEMO-06** · Codex-Follow-up-Batch (#87–#90, 1 PR): **ENG-09** `classify_attr` strippt das Mehrkopf-Suffix (`attr.split("#",1)[0]`) vor der Zwei-Pass-Klassifikation → `prism_rotation#1` (2. Kopf) landet exakt in **Effect** statt über den Substring `prism` in Beam; gilt generell für alle Multi-Head-Attribute. **ENG-10** `RgbMatrixInstance.sync_phase` setzt `_last_bus_pos=None` beim Bus-Re-Ankern → der DEMO-04-Stall-Check überspringt nach einem Sync nicht mehr die Bus-Synchronisation (sonst free-run + Sync/Phasen-Offset ignoriert, bis der Bus vorrückt). **UI-14** `VCButton._color_badge_colors` nimmt eine Signatur der Effekt-`ColorSequence` (`enabled_colors`) in den Cache-Key → ein Live-Color-Edit (VCColor `EFFECT`/`add_color`/`toggle_color`) invalidiert das Badge sofort statt erst beim Bindungswechsel. **DEMO-06** neues `tools/__init__.py` legt `tools/` auf `sys.path`, sobald ein Generator als **Modul** importiert wird → `import _gen_env` resolved auch im `python -c "import tools.build_x"`-Pfad (Direkt-Ausführung war nie betroffen). +6 Tests (`test_codex_followups.py`), Regression grün. [PR #97](https://github.com/ixamgames-droid/lightos/pull/97)
- **ENG-03 / ENG-04 / ENG-05 / ENG-06** · Engine Codex-Batch (4 Befunde, 1 PR): **ENG-03** `Palette.apply_to_programmer` zählt die Kopf-Vorkommen je Attribut der Zielleuchte und schreibt `attr#N` (Kopf>0) nur, wenn die Leuchte so viele Köpfe hat → kein Bogus-`attr#N` auf Einkopf-Fixtures (das in Snaps/Paletten weiterwanderte). **ENG-04** `record_from_programmer` räumt beim selektiven Overwrite die alten Pro-Fixture-Werte der Ziel-fids zuerst (`fixture_values.pop`) → ein geleertes Ziel hinterlässt keine stale Werte mehr (fids außerhalb der Auswahl unberührt). **ENG-05** gleichnamige Fixture-Gruppen: `PresetEntry.ref` trägt jetzt `(gid, name)`, `_group_lookup` löst bei gid eindeutig per ID auf und fällt im Namens-Pfad nur bei echtem `MultipleResultsFound` auf `.first()` zurück (statt `scalar_one_or_none`-Crash → „Gruppe ohne Geräte"); `list_fixture_groups` liefert die `id` mit. **ENG-06** `EfxView._add_efx`/`_start_efx` rufen nach `_auto_assign_if_empty` `_update_spider_mode()` → Spider-Controls erscheinen auch bei auto-zugewiesenen Spider-Fixtures. +9 Tests, 222 Regressionstests grün. [PR #96](https://github.com/ixamgames-droid/lightos/pull/96)
- **UI-05 / UI-07 / UI-08 / UI-09** · Programmer-View Codex-Batch (4 Befunde, 1 PR): **UI-05** `programmer_focus` wird beim Bau via `_on_main_tab_changed(currentIndex())` initialisiert (vorher None bis manuellem Tab-Wechsel → „aktiver Tab gewinnt" griff am Start nicht). **UI-07** range-basierte QuickBars (Shutter/Gobo) bekommen nur range-kompatible Fixtures (`_range_signature`/`_range_compatible_fixtures`) → kein fremder Range-Mittelwert in inkompatiblen Kanal. **UI-08** Orientierungs-Leiste (Pan/Tilt invert/swap) filtert auf Pan/Tilt-fähige Fixtures → statische Geräte verfälschen Tri-State nicht mehr + bekommen keine bedeutungslosen Flags. **UI-09** ProgrammerView abonniert `SELECTION_CHANGED` (+ `_sync_follow_selection` ohne Re-Publish) → Preset-Browser-Gruppenauswahl wird im Attribut-Tab sichtbar. +7 Tests, 79 Regressionstests grün. [PR #95](https://github.com/ixamgames-droid/lightos/pull/95)
- **QA-05** · Lokales Show-Lint-Gate wieder grün + Generator-Root-Cause: `tests/test_show_lint.py::test_committed_shows_have_no_errors` lief auf dem **lokalen** Loop-Gate (run_tests.ps1 glob't ALLE 51 lokalen `shows/*.lshow`) rot — die (gitignorte, nicht im Repo liegende) Demo `Hochzeit_Komplett_2026.lshow` band den VCEncoder „EFX-Tempo" (widget[104], EFX fid 46) an `param_key "speed_hz"`. Die EFX-Animationsrate wird aber unter dem Key **`"speed"`** exponiert (`EfxInstance.set_param("speed")` → `self.speed_hz`); `"speed_hz"` ist kein gültiger Effekt-Param → Encoder wirkungslos, Validator (VC-PARAMKEY) meldete zu Recht. **Die committeten Demo-Shows (`demo_*.lshow`) waren bereits sauber → CI war grün.** Root-Cause-Fix (committet): Generator `tools/build_hochzeit_komplett.py:974` auf `"speed"` korrigiert (künftige Rebuilds korrekt). Zusätzlich die lokale Show chirurgisch gepatcht (nur der `param_key`-String; die 10 EFX-`speed_hz`-ATTRIBUTwerte unberührt) → lokales Gate grün (0 Fehler über 51 Shows). Entdeckt beim autonomen Loop-Audit 2026-06-30.
- **STAB-09** · `reset_show` unterdrückt re-entranten `patch_changed`-Emit: der DEMO-03-`clear_patch()` lief nach `_replace_patch_from_data` (das `_suppress_emits` im `finally` auf `False` zurücksetzt) → ein zweites, re-entrantes `patch_changed` mitten im Reset (programmer/Funktionen/VC/Snaps noch alt) = der STAB-07/BUG-01-AV-Pfad. Jetzt läuft der harte `clear_patch()` im Suppress-Fenster; der DELETE (DEMO-03) bleibt, nur der Emit wird gedrosselt; finaler gebündelter Emit ist einzige Notification. +1 Test. Codex-Befund auf #90. [PR #93](https://github.com/ixamgames-droid/lightos/pull/93)
- **UI-02** · Undo im Patch: Backlog-Bereinigung (Audit 2026-06-30) — der Test `tests/test_patch_undo.py` (Fixture-Löschen via globalem Ctrl+Z) liegt seit [PR #7](https://github.com/ixamgames-droid/lightos/pull/7) auf `main`; der Backlog-Eintrag stand fälschlich noch auf „review" mit nicht-existentem Branch `feature/patch-undo-test`. Verifiziert erledigt, kein Code-Change nötig.

- **ENG-07** · `prism_rotation` korrekt als **Effect** statt Beam klassifiziert: Der real emittierte Attributname `prism_rotation` (QXF-Import `qxf_import.py`, eingebauter Generic-MH `fixture_db.py`) fiel per Substring-Match `prism` in die **Beam**-Gruppe → falsche Snap/Szenen-Labels für echte Fixtures. Der 2026-06-24-Fix deckte nur die **synthetische, nie emittierte** Kurzform `prism_rot` ab. Jetzt sind beide exakt in der Effect-Menge (Exact-Match in Pass 1 schlägt den Beam-Substring) + deutsches Label „Prisma-Rotation". +2 Tests (classify + Label inkl. Mehrkopf-Suffix `#1`→„Kopf 2"). Volle Suite grün (262 Dateien, 0 Failures/Crashes). Aus den offenen Codex-Befunden (alle 9 verbleibenden am 2026-06-30 als noch-real verifiziert). [PR #89](https://github.com/ixamgames-droid/lightos/pull/89)

- **DEMO-02 / DEMO-03 / DEMO-05** · Demo-Bau-Findings als Batch: **DEMO-02** Generator-Spawn-Korruption — neues Single-Point-Bootstrap `tools/_gen_env.py` setzt vor `app_state`/`output_manager` die spawn-sicheren Env-Schalter (`LIGHTOS_SERIAL_INPROC=1` etc.), `import _gen_env` in allen 30 bauenden `tools/build_*.py` → kein `multiprocessing`-`spawn`-Re-Import des guardlosen Skripts mehr (voller Patch). **DEMO-03** `reset_show()` ruft zusätzlich hart `state.clear_patch()` (verwaiste `current_show.db`-Patch-Zeilen nach Absturz). **DEMO-05** `ColorSequence` iterierbar/indexierbar (`__iter__`/`__getitem__`). +11 Tests, volle Suite grün (264/265, 1 batch-fremder Flake). Parallel via 3 Subagents. [PR #90](https://github.com/ixamgames-droid/lightos/pull/90)

- **DEMO-04** · Bus-gekoppelte Matrix friert nicht mehr **dunkel** ein: Ein an einen Tempo-Bus gekoppelter Matrix-Effekt fror auf der statischen Bus-Position ein, wenn der Bus `bpm>0` hatte, aber nicht vorrückte (Render ohne `advance_frame`-Schleife: Vorschauen/`render_probe`/Validierung/Generatoren/Headless; pausierte Uhr) → bei Dimmer-Style = Intensität 0 = dunkel. `RgbMatrixInstance._advance_step` erkennt den stehenden Bus jetzt am Positions-Delta über einen echten Zeitschritt (`dt>0` + Position unverändert) und fällt auf **Free-Run** (`matrix_speed`) zurück; `dt==0`-Re-Evaluationen (z. B. nach „Jetzt synchronisieren") rechnen weiter Bus-Sync; Bus-Wiederanlauf snappt zurück; globaler Freeze hält an. Live byte-identisch. +6 Tests. [PR #88](https://github.com/ixamgames-droid/lightos/pull/88)

- **UI-13** · VC-Button-Politur (3-in-1): (a) **quadratische Default-Größe** — neu angelegte Buttons sind `72×72` (Pad-Look, grid-aligned) statt länglich `120×60`; nur Neuanlage, Alt-Shows unverändert. (b) **Farb-/Effekt-Vorschau-Badge oben rechts** (analog Gobo): einfarbig = Kreis, Farbwechsel = **animiert durchwechselnd** (Timer nur aktiv wenn sichtbar+mehrfarbig); Dimmer-/Shutter-Style (`has_colors=False`) → kein Badge. (c) **RGBW-Weiß-Erkennung** — zentraler Qt-freier Helfer `color_utils.rgbw_to_display`/`display_rgb_from_attrs` faltet den W-Kanal additiv in die Anzeige-RGB; Snap-Swatch + Badge zeigen reines Weiß als weiß statt schwarz, und `VCColor` faltet W auch beim Senden an Effekt-Farb-Slots (Wurzel von „weißer Effekt = schwarzer Knopf"). +17 Tests, volle Suite grün (261 Dateien). [PR #87](https://github.com/ixamgames-droid/lightos/pull/87)

- **ENG-01** · Cue-Delay **In/Out auf Attribut-Ebene**: Cues hatten bereits eine Pro-Attribut-Verzögerung beim Hineinfaden (`attr_delays`); neu ist das symmetrische `attr_delays_out` fürs Ausfaden. `CueStack._fade_to` wählt jetzt **richtungsabhängig** Fade-Zeit, Cue-Delay-Basis **und** die Pro-Attribut-Delays (GO → `fade_in`/`delay_in`/`attr_delays`, BACK → `fade_out`/`delay_out`/`attr_delays_out`). Nebenbei behoben: der BACK-Fade nahm bisher fälschlich `delay_in` statt `delay_out` als Verzögerungs-Basis. Alt-Shows ohne den neuen Schlüssel verhalten sich unverändert (defensive Deserialisierung). +6 Tests. [PR #78](https://github.com/ixamgames-droid/lightos/pull/78)

- **STAB-08** · Serial-Ausgabe **prozess-isoliert**: die Enttec-Ausgabe läuft jetzt in einem eigenen Prozess (`EnttecProcessProxy` + `serial_process`-Worker). Eine native, in Python **unfangbare** Access Violation im USB-/FTDI-Kerneltreiber killt damit nur den Worker; der Parent respawnt ihn gedrosselt, GUI/Engine laufen weiter. Parent schreibt latest-wins in einen shared `Array` → der 44-Hz-Thread hängt nie mehr an einem Serial-Write. Default an, `LIGHTOS_SERIAL_INPROC=1` erzwingt den In-Prozess-Pfad, Fallback bei Spawn-Fehler. +10 Tests. [PR #75](https://github.com/ixamgames-droid/lightos/pull/75)

- **OUT-02** · Enttec-Serial-Stabilisierung (Fehler-Watchdog): nach `FAIL_LIMIT` (20) aufeinanderfolgenden Schreib-Fehlern wird der Port geschlossen + als tot markiert → kein 44-Hz-Hammern auf ein abgezogenes/wackliges USB-Gerät (drastisch weniger native Access-Violation-Gelegenheiten); ein erfolgreicher Frame resettet den Zähler. Gedrosselter Reconnect (alle 3 s) reaktiviert die Ausgabe automatisch, sobald das USB zurück ist; `is_disabled()` für UI-Status. Reines Python kann eine native Kerneltreiber-AV nicht fangen — der Fix senkt die Exposition. +6 Tests. [PR #73](https://github.com/ixamgames-droid/lightos/pull/73)

- **STAB-07** · Häufigste native Access Violation (crash.log Jun 2026, GUI-Thread im Programmer-Refresh) **ursächlich** behoben: `programmer_view._refresh_fixture_list` kapselt `clear()`+Neuaufbau in `blockSignals` → keine re-entrante `itemSelectionChanged`→`_rebuild_attr_editor`-Kaskade mehr; **und** `_on_state_change` behandelt `patch_changed` nicht mehr → kein Doppel-Rebuild über Legacy- **und** Sync-Pfad. Befund nebenbei: die BUG-01-Bremse `_suppress_emits` ist inert (nie auf `True`). +4 Regressionstests. [PR #70](https://github.com/ixamgames-droid/lightos/pull/70)
- **NET-01 / QA-04** · Quick Wins aus der crash.log-Analyse: Web-Remote startet wieder (`allow_unsafe_werkzeug=True` an `sio.run` — der `WebServer`-Thread starb sonst still beim Start, Remote nie erreichbar) + RGB-Matrix-Style-Sichtbarkeit gegen `AttributeError`-Regression abgesichert (Prod war längst gefixt: `_shut_form_label`; neuer Headless-Test über **alle** `MatrixStyle`×`RgbAlgorithm`). [PR #71](https://github.com/ixamgames-droid/lightos/pull/71)
- **STAB-03** · AV im `programmer_view`-Refresh (Zombie-Subscriber) defensiv abgesichert: `subscribe_widget` mit `weakref` + `shiboken6.isValid`-Guard überspringt/meldet tote Widgets ab. (Die verbleibende **re-entrante** Refresh-Ursache wurde separat als STAB-07 gelöst.) +2 Tests. [PR #37](https://github.com/ixamgames-droid/lightos/pull/37)

- **UI-12 / ENG-08** · Programmer-Matrix: „Farbe pro Runde wechseln" (`color_cycle`) hoch in die Farben-Gruppe (auf RGB/RGBW gegated) **und** neue **Dimmer-Sequenz** für den Dimmer-Chase — explizite Dimmerwerte (z. B. 255/50/100) pro Runde, Pendant zur Farbauswahl: `DimmerSequence` + Checkbox „Dimmer pro Runde wechseln" + Graustufen-Editor, `dimmer_order`/`dimmer_interval`, Cycle-Werte ohne Min/Max-Remap (abwärtskompatibel). +29 Tests, Manifest regeneriert. [PR #60](https://github.com/ixamgames-droid/lightos/pull/60)

- **STAB-04 / STAB-05 / STAB-06** · DMX-Output- & Crash-Erkennungs-Stabilität gehärtet: getimeouteten Output-Thread weitertracken (kein zweiter, konkurrierender DMX-Thread → AV), fatale Exception wird nicht mehr als „sauberer Exit" markiert (Absturz beim nächsten Start erkennbar), Running-Flag pro PID + Windows-sicherer Liveness-Check (OpenProcess statt os.kill). +15 Tests. [PR #55](https://github.com/ixamgames-droid/lightos/pull/55)
- **QA-01 / QA-02 / QA-03** · Test-Gate/CI gehärtet: `pytest-timeout` in den CI-Install-Pfad, sACN-Loopback `fail` statt `skip` nach erfolgreichem Bind, `_delete_selected`-UI-Pfad direkt getestet. [PR #55](https://github.com/ixamgames-droid/lightos/pull/55)
- **VIZ-01 / VIZ-02 / VIZ-03 / VIZ-04** · Visualizer-Lows: 2D-Live-View-Positionen beim Unpatch aufräumen, Spider-Tilt-Default 180° statt 270°, Dock beim Ausrichten/Verteilen lösen, Laser-Fächer-Sichtbarkeit bei View-/Settings-Wechsel reapplien. +5 Tests. [PR #55](https://github.com/ixamgames-droid/lightos/pull/55)
- **UI-03** · Fixture-Kopieren mit Offset: Toolbar-Button „Mit Offset kopieren…" (Dialog Anzahl + Offset), testbare `plan_offset_copies`-Logik (Universe-Überlauf übersprungen), jede Kopie einzeln undoable. +6 Tests. [PR #55](https://github.com/ixamgames-droid/lightos/pull/55)
- **UI-10 / UI-11** · EFX-RANDOM-Vorschau folgt jetzt dem echten `_random_xy`-Walk (wandernde Spur statt geschlossener Schleife; Fixture-Punkte + Spider-Bars numerisch == `EfxInstance._values`) + Fan-Fallback `phase_mode`-bewusst. Letzte Punkte des Programmer-Audits 2026-06-23. _Rein visuelle Feinabnahme im laufenden Programm noch offen._ [PR #51](https://github.com/ixamgames-droid/lightos/pull/51)
- **STAB-02** · Access Violation beim Beenden behoben: `OutputManager.stop()` schließt DMX-Geräte nur nach bestätigtem Thread-Ende (sonst CloseHandle neben WriteFile → AV); `EnttecPro` mit `is_open`-Guard + Purge vor close. +8 Tests. [PR #36](https://github.com/ixamgames-droid/lightos/pull/36)
- **STAB-01** · Crash-Logging „ausgereift": man erkennt jetzt **wann/wie** abgestürzt — Start-/Clean-Exit-Marker, „vorige Sitzung abgestürzt"-Erkennung (deckt native Crashes ab), Standby≠Freeze, `threading.excepthook`, Sturm-Drossel, Qt-Message-Handler, Rotation. Neu `src/core/crash_logging.py` + 18 Tests. [PR #33](https://github.com/ixamgames-droid/lightos/pull/33)
- **UI-01** · Preset-Browser: ein Suchfeld über Paletten **+** Fixture-Gruppen (Name/Typ/Ordner/Tag, Mehrwort-UND), Doppelklick/Enter wendet an. Filterlogik Qt-frei + getestet. [PR #13](https://github.com/ixamgames-droid/lightos/pull/13)
- **OUT-01** · sACN / E1.31: echter UDP-Loopback-Test für die Universe-Ausgabe (`tests/test_sacn_loopback.py`). [PR #14](https://github.com/ixamgames-droid/lightos/pull/14)
- **UI-04** · EFX-Tab: „▶ Start" lief stumm ohne Geräte — neue Bewegung bekommt automatisch Geräte (Auswahl → sonst alle Movingheads), klare Warnung statt No-Op. Root-Cause: `fixtures=[]` → `write()` bricht ab. [PR #11](https://github.com/ixamgames-droid/lightos/pull/11)
- **ENG-02** · Dimmer-Matrix vom Programmer-Intensity entkoppeln („aktiver Tab gewinnt"): Dimmer-Effekt besitzt seinen Kanal wert-unabhängig; Intensity-Tab+Auswahl gewinnt manuell. [PR #9](https://github.com/ixamgames-droid/lightos/pull/9)

---

## 🌱 Neue Kandidaten (Survey 2026-06-30, von Claude vorgeschlagen)

_Ergebnis eines 6-Agenten-Surveys + Kritiker-Pass über das ungeauditierte Terrain (Render, Output/Input, Show-Datei, MIDI/OSC/MTC, Audio/BPM, Web-Remote, Fixture-Import, Multi-Universe) sowie über Doku- und Verifikations-Lücken. **Alle Items sind in echten Dateien geerdet und gegen den aktuellen Code/Backlog verifiziert** (kein Doppel mit Offenem oder Erledigtem)._

> **Bewusst ganz ans Ende gestellt — niedrigste Prio.** Oben werden gerade die Codex-/VC-Befunde abgearbeitet; diese Kandidaten warten dahinter. **David: einzelne Zeilen zum Priorisieren einfach nach oben in „📋 Offen" ziehen.** Die Prio-Spalte (P1/P2/P3) ist die *interne* Reihenfolge innerhalb dieses Blocks, nicht relativ zu den oberen Listen.

> **Zwei neue Bereichs-Präfixe:** **`AUD-NN`** = Subsystem-Tiefen-Audit (Doc analog [`docs/VC_AUDIT_2026_06_30.md`](docs/VC_AUDIT_2026_06_30.md): verifizierte Bug-/Risiko-Liste mit `file:line`, Befunde fließen als STAB-/ENG-/NET-Items zurück). **`DOC-NN`** = Pro-Komponente-Referenzdoku (`docs/components/**`). Bestehende Präfixe laufen weiter (OUT-03+, QA-07+, STAB-10+, NET-02+, ENG-11+).

### 🎚️ Mehrere Universen mit verschiedenen Adaptern (Davids explizitester Wunsch)

_Kern funktioniert bereits (OutputManager hält pro Universum getrennte Registries `_enttec/_artnet/_sacn_outputs`, `_send_all` sendet je Universum an das dort registrierte Gerät). Echte Lücken: inkonsistenter/toter externer Universe-Versatz, globale „Übernehmen"-Buttons überschreiben die Per-Universum-Zuweisung, null Tests/Doku für den Mixed-Fall._

| ID | Prio | Status | Titel | Akzeptanzkriterium (Definition of Done) |
|----|------|--------|-------|------------------------------------------|
| QA-07 | P1 | ✅ done (dieser PR) | Test: 2 Universen, 2 verschiedene Adapter — beide bekommen ihre Frames (S) | `tests/test_output_manager.py::TestOutputManagerMixedSend`: `add_universe(1)`+`add_universe(2)`, `_enttec_outputs[1]`=FakeEnttec (`send_dmx(data)`), `_artnet_outputs[2]`=FakeArtNet (`send_dmx(universe,data)`), Kanal-1-Werte 11/22; ein `_send_all()` → Enttec bekam genau U1 (11), Art-Net genau U2 (22) mit externer Universe-Nr `univ_num-1`=1, keiner fremde Daten. Nagelt den zentralen Mixed-Send-Pfad (`output_manager.py:303-321`) gegen Routing-/Universe-Vertausch-Regression fest. |
| AUD-01 | P2 | todo | Audit: Multi-Universe + gemischte Adapter (Zuweisung, Persistenz, Render-Pfad) (M) | Neues `docs/OUTPUT_MULTI_UNIVERSE_AUDIT.md`: beschreibt je Universum den Pfad `universes.json` → `apply_output_config` (`app_state.py`) → `add_enttec/add_artnet/add_sacn` → `_send_all`; belegt mit `file:line` die 3 Befunde — (a) Art-Net sendet `univ_num-1` vs. sACN `univ_num` (`output_manager.py:314` vs `:319`); (b) totes UI-Feld „Art-Net Startuniversum" (`output_config.py:102-104`, nirgends gelesen); (c) `_apply_artnet/_apply_sacn`-Loop überschreibt ALLE Universen (`output_config.py:285-287/362-364`); nennt die abgeleiteten OUT-/QA-Items. |
| QA-08 | P2 | ✅ done (dieser PR) | Test: `universes.json` Round-Trip — `apply_output_config` richtet Mixed-Adapter ein (S) | `tests/test_output_manager.py::TestApplyOutputConfigRoundtrip`: temporäre `universes.json` (Enttec/ArtNet/sACN auf U1/U2/U3) → `AppState.apply_output_config` (auf einem Stub, der nur `output_manager`+`universes` nutzt; Enttec-Port-Open via `_make_enttec_device`-Mock umgangen) → alle 3 Universen existieren, jeder Adapter im RICHTIGEN Registry-Dict, keine Kreuz-Einträge; zweiter Test: ein Enttec-Fehler bricht den Loop NICHT ab (ArtNet danach trotzdem gesetzt). Schließt die ungetestete Start-Rekonstruktion (`app_state.py:apply_output_config`). |
| OUT-03 | P2 | todo | Art-Net/sACN: externe Universe-Nummer pro Universum konfigurierbar (M) | `universes.json`-Zeile trägt optional `out_universe`; `apply_output_config` reicht sie an OutputManager durch; `_send_all` nutzt die konfigurierte Nummer statt fix `univ_num-1`/`univ_num` (Default abwärtskompatibel: Art-Net=num-1, sACN=num); Universe-Manager-Tab zeigt/editiert die Spalte. Test: ArtNetSender/SACNSender bekommen die erwartete externe Universe-Nr. Macht das tote „Startuniversum"-Feld nutzbar (`output_config.py:102-104`). |
| OUT-04 | P2 | ✅ done (dieser PR) | „Übernehmen"-Buttons überschreiben nicht mehr alle Universen (per-Universum) (S) | Art-Net- und sACN-Tab haben jetzt je ein **Ziel-Universe-Feld** (`_spin_artnet_univ`/`_spin_sacn_univ`, analog `_spin_enttec_univ`); `_apply_artnet`/`_apply_sacn` wirken nur auf **dieses eine** Universum (legen es bei Bedarf an) statt per Schleife über alle — `_persist_output` aktualisiert nur diese eine Zeile, andere `universes.json`-Zuweisungen bleiben erhalten. Das behebt zugleich AUD-01-Befund (c) (Apply-Loop überschrieb alle). Das separate „Startuniversum"-Feld (externe Nr.) blieb unangetastet → gehört zu **OUT-03**. Test NEU `tests/test_output_config.py` (4: Art-Net/sACN nur 1 Universum, deaktiviert=noop, fehlendes Zieluniversum wird angelegt). |
| DOC-01 | P3 | todo | Anleitung: 2 Universen über 2 verschiedene Adapter (Schritt für Schritt) (S) | Neues `docs/anleitung_multi_universe.md` (Stil der `docs/anleitung_*`): (1) 2. Universum im Universe-Manager-Tab anlegen, (2) U1=Enttec+COM, U2=ArtNet+IP setzen+Speichern (→`universes.json`), (3) Fixtures auf U2 patchen, (4) im Output-Monitor (`output_view.py`, Universe-Spinner) prüfen, dass beide Universen Werte zeigen; nennt explizit die Stolperfalle der globalen „Übernehmen"-Buttons. |

### 🔬 Fehlende Subsystem-Audits (AUD)

_Der EINZIGE dedizierte Tiefen-Code-Audit ist `docs/VC_AUDIT_2026_06_30.md`; `docs/PROJECT_AUDIT.md` ist High-Level-Architektur ohne Bug-Liste, `ANLEITUNGEN_AUDIT_*` sind reine Doku-Audits. Per `ls docs/` verifiziert: KEIN `*AUDIT*` für Render, Output-Sender, Show-Datei, MIDI, OSC/MTC, Audio/BPM, Web-Remote, DMX-Eingang oder Fixture-Import. Nach Risiko priorisiert (Live-Output/Threading & Persistenz vor reinen Eingangsadaptern)._

| ID | Prio | Status | Titel | Akzeptanzkriterium (Definition of Done) |
|----|------|--------|-------|------------------------------------------|
| AUD-02 | P1 | ✅ done (dieser PR) | Audit: Render-Pfad & Channel-Merge (`_render_frame`) (L) | [`docs/RENDER_AUDIT_2026_07_08.md`](docs/RENDER_AUDIT_2026_07_08.md) — 6-Dimensionen-Workflow (36 Agenten, 6 CONFIRMED, 2 PLAUSIBLE, 7 adversarial zurückgewiesen). Kern: Clamp lückenlos (`set_channel`), kein Thread-Death (Callback-Isolation), GM/WP-6-Merge **by design** (2× widerlegt), Coverage **breit** (≥8 Suiten — Backlog-Annahme „nur test_render_frame.py" war veraltet). Echte Defekte als **STAB-13/14/15 + QA-25** abgeleitet. |
| AUD-03 | P1 | ✅ done (dieser PR) | Audit: DMX-Output & Netzwerk-Sender (ArtNet/sACN/Serial) (M) | [`docs/DMX_OUTPUT_AUDIT_2026_07_08.md`](docs/DMX_OUTPUT_AUDIT_2026_07_08.md) — 5-Dimensionen-Workflow (19 Agenten, 6 CONFIRMED + 1 PLAUSIBLE). Kern: **Bytes spec-konform, Loop doppelt abgesichert (kein Loop-Tod), Locks robust, Universe→Wire korrekt** (send-Robustheit-Hypothese adversarial widerlegt). Ein harter Defekt **OUT-05** (P2), Rest P3 → **OUT-05/06, NET-04, SERIAL-01/02** abgeleitet. |
| AUD-04 | P1 | ✅ done (dieser PR) | Audit: Show-Datei-Persistenz (.lshow Round-Trip) (M) | [`docs/SHOW_FILE_AUDIT_2026_07_08.md`](docs/SHOW_FILE_AUDIT_2026_07_08.md) — 5-Dimensionen-Workflow (33 Agenten, 12 CONFIRMED, 2 zurückgewiesen → 9 distinkte Defekte). Kern: **Round-Trip vollständig** (alle 29 Keys symmetrisch, kein stiller Feld-Verlust), `reset_show` sauber. Echte Defekte in der **Fehler-Behandlung** → **STAB-16..20** abgeleitet (2× P1: nicht-atomarer Save + functions leer bei to_dict-Fehler). |
| AUD-05 | P1 | ✅ done (dieser PR) | Audit: Web-Remote (Flask + SocketIO, `src/web/app.py`) (M) | [`docs/WEB_REMOTE_AUDIT_2026_07_08.md`](docs/WEB_REMOTE_AUDIT_2026_07_08.md) — 4-Dimensionen-Workflow (21 Agenten, 16 CONFIRMED, jeder Befund adversarial gegen den Code verifiziert). Kern: **kein** Out-of-range-/Index-/Clamp-Bug (Guards korrekt), `SECRET_KEY` korrekt (random). Echte Befunde als **NET-/WEB-Items** unten abgeleitet. |

### 🌐 Aus Web-Remote-Audit 2026-07-08 (AUD-05)
_Verifizierte Befunde aus [`docs/WEB_REMOTE_AUDIT_2026_07_08.md`](docs/WEB_REMOTE_AUDIT_2026_07_08.md). Der Web-Remote ist ein auth-loser externer Steuer-Eingang._

| ID | Prio | Status | Titel | Stelle / DoD |
|----|------|--------|-------|--------------|
| NET-01 | **P1** | todo | **Web-Remote UND OSC ohne Auth im ganzen LAN erreichbar** | `app.py:170` bindet `0.0.0.0` ohne Token/PIN → jeder im WLAN kann Blackout/Kanäle/GO/Fader auslösen. **AUD-08-Parität (OSC-02):** `osc_server.py:27/48` bindet ebenso `0.0.0.0:7770` ohne Auth → dieselbe LAN-Exposition. Fix: Bind default `127.0.0.1`, LAN nur per Option; optional PIN/Token; **querschnittlich für ALLE externen Eingänge** (Web + OSC, ggf. Art-Net/sACN-Input). Dialog benennt die Exposition (NET-02). |
| NET-02 | P2 | todo | **Verbindungs-Dialog zeigt „localhost:5000" trotz `0.0.0.0`-Bind** | `main_window.py:1606` — irreführend, Nutzer unterschätzt LAN-Exposition. Fix: echte LAN-IP + Warnung; koppelt an NET-01. |
| NET-03 | P2 | todo | **`cors_allowed_origins="*"` ohne CSRF/Origin-Schutz** | `app.py:36` — beliebige Webseite kann sich mit dem SocketIO verbinden. Fix: Origins beschränken; mit Auth (NET-01) entschärft. |
| WEB-01 | P2 | todo | **`/api/channel` UND `/lightos/ch` schreiben am 44-Hz-Renderer vorbei (halten auf gepatchten Kanälen nicht)** | `app.py:108` — `set_channel` direkt; `_render_frame` überschreibt gepatchte Spans jeden Frame → Web-Wert flackert/hält ~23 ms. **AUD-08-Parität (OSC-01):** `osc_server.py:125-138` `_handle_channel` hat exakt dasselbe Verhalten. Fix (gemeinsam für beide Remote-Kanal-Schreiber): über eine Input-/Override-Ebene leiten (analog `input_layer`), oder dokumentieren/auf ungepatchte Adressen beschränken. |
| WEB-02 | P3 | ✅ done (dieser PR) | **Ungekapselte `int()/float()`-Coercion → HTTP 500** | Neuer Helfer `_num(data,key,default,cast)` in `app.py` fängt `TypeError/ValueError/OverflowError` (auch `int(inf)/int(NaN)`) → Default statt 500; verdrahtet in `api_fader`, `api_channel`, `on_fader`. Clamp bleibt einzige Wert-Leitplanke. |
| WEB-03 | P3 | ✅ done (dieser PR) | **`on_fader`/`on_blackout` crashen bei leerem SocketIO-Payload** | `def on_fader(data=None)`/`on_blackout(data=None)` + `data = data or {}` → leerer Emit crasht nicht mehr (analog `on_go`/`on_back`). |
| WEB-04 | P3 | ✅ done (dieser PR) | **TOCTOU auf `state.cue_stacks[0]`** | `stacks = _get_state().cue_stacks; if stacks: stacks[0]...` in `api_go`/`api_back`/`on_go`/`on_back` → lokale Referenz, kein IndexError bei nebenläufigem Leeren. |
| WEB-05 | P3 | ✅ done (dieser PR) | **`/api/status` iteriert `cue_stacks`/`universes` ohne Lock** | `for s in list(state.cue_stacks)` (Snapshot-Kopie); `universes` nutzte bereits `list(...)`. |
| WEB-QA | P3 | ✅ done (dieser PR) | **0 Tests für den Web-Remote** | NEU `tests/test_web_app.py` (17 Tests, Flask `test_client` + SocketIO-`test_client`, `get_state` gemockt): Clamping (Fader 0–1/Kanal 0–255), Bereichs-Guards (out-of-range/slot=0 = No-op), Payload-Fehlertoleranz (WEB-02, kein 500), SocketIO ohne/kaputtem Payload (WEB-03), Routing, leerer Cue-Stack. |
| — | — | ⏸ Querschnitt | **Cross-Thread: Web-Kommandos in den Qt-Thread marshallen** | Alle Handler mutieren AppState direkt aus dem WebServer-Thread ggü. dem 44-Hz-Renderer (WEB-04/05 sind Ausprägungen). Sauber: wie MIDI/OSC per Signal/`invokeMethod` marshallen — größeres Follow-up. |

### 🎚️ Aus Render-Pfad-Audit 2026-07-08 (AUD-02)
_Verifizierte Befunde aus [`docs/RENDER_AUDIT_2026_07_08.md`](docs/RENDER_AUDIT_2026_07_08.md). Alle drei echten Defekte sitzen an der Grenze zwischen dem lock-freien Render-Thread und den UI-Threads, die Patch/Feature-Slots mutieren._

| ID | Prio | Status | Titel | Stelle / DoD |
|----|------|--------|-------|--------------|
| STAB-13 | **P2** | ✅ done (dieser PR) | **`feature_dimmers` im Renderer ungelockt iteriert → Dropped-Frame-Stutter** | `app_state.py:1542` baut `active = [s for s in self.feature_dimmers.values() …]` ohne Snapshot/Lock; UI-Thread mutiert die dict-Größe (`set_feature_dimmer` Slot anlegen/`pop` bei Schwelle 0.999; `clear_feature_dimmers` beim Show-Load, Z. 1777/1779/1784) → `RuntimeError: dictionary changed size during iteration` (Block nicht in try/except) → Commit (Schritt 5) entfällt, alle Universen behalten den Vorframe. Einzige Render-Ebene ohne Lock-Snapshot. **Fix:** `_fd_lock` (analog `_prog_lock`), Setter unter Lock, Renderer snapshottet vor Z. 1542. **DoD:** Regressionstest (nebenläufige Mutation wirft nicht mehr / Frame committet). |
| STAB-14 | **P2** | ✅ done (dieser PR) | **Repatch verwirft Engine-Extra-Freigabeliste → Roh-Kanal-Zombie (bleibt an)** | `_rebuild_render_plan` setzt `_engine_extra_prev = {}` (`app_state.py:675`) ohne die Live-Werte zu nullen. Eine `ScriptFunction` (setdmx auf ungepatchte Adresse) committet einen Roh-Kanal (gemerkt in `_engine_extra_prev`, Z. 1662); nach einem Patch-Change + Skript-Stopp ist `prev=None` → `if cur or prev` (Z. 1656) false → **keine Freigabe** → `live[addr]` bleibt dauerhaft an (Strobe/Shutter/Beam sicht-/sicherheitsrelevant). **Fix:** Tracking über Repatch erhalten ODER beim Repatch die gemerkten Roh-Adressen aktiv auf 0 committen; `_engine_extra_prev` cross-thread-sicher stellen. **DoD:** Test (Roh-Kanal committen → Repatch → Skript stoppt → Adresse wird auf 0 freigegeben). |
| STAB-15 | P3 | todo | **Patch-Render-Plan wird ungelockt & nicht-atomar getauscht → 1-Frame-Glitch beim Umpatchen** | `_rebuild_render_plan` rebindet `_fix_index`/`_default_frame`/`_commit_spans`/`_patched_set`/`_engine_extra_prev` als 5 separate Statements ohne Lock (`app_state.py:669-675`), während `_render_frame` sie lock-frei quer über den Frame liest (Z. 1259/1267 früh, 1648/1653 spät). Torn Read → Spurious-/dunkler Wert an einer eben umgepatchten Adresse; selbstheilend, nur bei **manuellem** Umpatchen, kein Crash/Datenverlust. **Fix:** Plan-Strukturen zu einem `RenderPlan`-Snapshot bündeln, **einen** atomaren Rebind; Renderer zieht `plan = self._plan` am Frame-Anfang. |
| QA-25 | P3 | todo | **Render-Coverage: Fail-Safe + Multi-Universe-Engine-Extra + EE-02-Color-only** | (a) Universe-Stub, dessen `set_channel` in einer späten Stage wirft → asserten, dass der zuletzt committete Frame steht (Fail-Safe, kein Halb-Commit). (b) 2 Universen mit Roh-Script, in einem stoppen → nur dort Release; + Repatch-Zombie-Regression (nach STAB-14). (c) Color-only-Fixture (RGB, kein Dimmer), Effekt→`color_r=200`, programmer intensity=128 → assert ~100 (EE-02-Multiply). _Der Rest des Render-Pfads ist bereits breit getestet (≥8 Suiten) — dies sind die einzigen echten Lücken._ |

### 💾 Aus Show-Datei-Persistenz-Audit 2026-07-08 (AUD-04)
_Verifizierte Befunde aus [`docs/SHOW_FILE_AUDIT_2026_07_08.md`](docs/SHOW_FILE_AUDIT_2026_07_08.md). Der Round-Trip selbst ist vollständig — die Defekte sitzen alle in der **Fehler-Behandlung** von `show_file.py` (Save schreibt still lossy/korrupt über gut; Load amplifiziert einen kaputten Wert zum ganzen Block)._

| ID | Prio | Status | Titel | Stelle / DoD |
|----|------|--------|-------|--------------|
| STAB-16 | **P1** | ✅ done (dieser PR) | **Nicht-atomarer Save → korrupte `.lshow` + vorherige Show zerstört** | `show_file.py:424-425` — `zipfile.ZipFile(path,"w")` truncatet die gute Datei sofort, `json.dumps` (ohne `default=`) läuft erst danach im offenen Handle. Crash/voller Datenträger/`TypeError` (programmer/base_levels by-ref) → halb geschrieben + Vorversion weg. Kein `temp+os.replace`, auch nicht im Autosave. **Fix:** `json.dumps` **vor** ZIP-Open; Temp-Datei + `os.replace()` atomar; programmer/base_levels defensiv kopieren. **DoD:** Test (Serialisierungsfehler lässt Zieldatei unangetastet; erfolgreicher Save ersetzt atomar). Zusammen mit STAB-17 fixen. |
| STAB-17 | **P1** | ✅ done (dieser PR) | **`functions`-Block bei `to_dict()`-Fehler still LEER gespeichert → Totalverlust (inkl. EFX/Matrix)** | `show_file.py:301-305` — `function_manager.to_dict()` ohne Per-Funktion-Guard; eine kaputte Funktion → `except` (Z.304, kein `_lenient`) → `functions_data` bleibt `{"functions":[]}`, Save schreibt leer. EFX/Matrix leben **nur** hier. **Fix:** bei Serialisierungsfehler eines nicht-leeren Managers Save **abbrechen** statt leer speichern (fällt mit STAB-16 serialize-before-open zusammen). **DoD:** Test (werfender Manager → Save wirft, Zieldatei unverändert). |
| STAB-18 | P2 | ✅ done (dieser PR) | **Ein kaputter Wert löscht den GESAMTEN programmer/base_levels-Block still** | `show_file.py:711` (programmer) / `722-730` (base_levels) — `int(v)`-Comprehension nicht pro-Wert geschützt → äußerer `except` → `state.programmer={}` / `base_levels={}` (alle Fixtures weg, still). **Zusatz:** `_rebuild_render_plan()` (Z.727) steht **im** base_levels-`try` → ein unabhängiger Render-Fehler löscht base_levels + kippt `implicit_brightness`. **Fix:** `int(v)` pro Wert kapseln (wie fid/attrs schon); `_rebuild_render_plan()` aus dem `try` ziehen. **DoD:** Test (ein null-Wert → nur dieser Eintrag fällt, Rest bleibt). |
| STAB-19a | P2 | ✅ done (PR #222) | **`palettes` bleedet aus Vorshow** | `palettes` war der einzige Manager ohne `else`-Reset → ohne `palettes`-Key überlebten die Paletten der Vorshow. Fix: `else: pm.from_dict({})` (analog `reset_show`). +2 Tests. |
| STAB-19b | P3 | todo | **`load_show` nicht reset-first/atomar → Frankenstein-Show (Restrisiko)** | Patch zuerst ersetzt, ~20 Blöcke folgen; Fehler an einer der wenigen **ungefangenen** Stellen → neuer Patch + alter Rest inkonsistent. **Einschätzung (2026-07-08):** geringes Restrisiko — `load_show` setzt bereits **alle** State-Felder inline zurück (Z. 621-645/754-757/787/814/896-897) und kapselt jeden Block in `_lenient`; nur ein Absturz an einer der raren ungefangenen Zeilen träfe. Ein voller `reset_show()`-first/Snapshot+Rollback-Umbau am **kritischsten Ladepfad** ist hoch-riskant für P3-Nutzen → bewusst als eigene, sorgfältige Runde geparkt (nicht im Dauer-Loop mit-erledigt). **DoD:** wenn angegangen — `reset_show()`-first ODER Rollback bei Fehler; Test „Fehler in Block N → kein halb-alter Zustand". |
| STAB-20 | P3 | ✅ done (dieser PR) | **`.lshow`-Robustheit gegen alt/korrupt** | (a) Non-Object-JSON (`list`/`null`/`string`) → unbehandelter `AttributeError` statt sauberem Fehler; (b) kein Versions-Gate (zu neue `SHOW_VERSION` still fehlinterpretiert); (c) Legacy-EFX/RGB-Migration (`~829-840`) bricht beim ersten kaputten Eintrag ab, verliert alle folgenden. **Fix:** `isinstance(data, dict)`-Guard am Load-Anfang; `read_show_version` gegen `SHOW_VERSION` prüfen/warnen; Migrationsschleifen pro Eintrag kapseln (wie cue_stacks). |
### 🔌 Aus DMX-Output-Sender-Audit 2026-07-08 (AUD-03)
_Verifizierte Befunde aus [`docs/DMX_OUTPUT_AUDIT_2026_07_08.md`](docs/DMX_OUTPUT_AUDIT_2026_07_08.md). Byte-/Loop-Ebene ist solide — der harte Defekt ist der Adapter-Lifecycle; der Rest P3 (Netz-Egress braucht Produkt-Entscheidung)._

| ID | Prio | Status | Titel | Stelle / DoD |
|----|------|--------|-------|--------------|
| OUT-05 | **P2** | ✅ done (dieser PR) | **Kein Remove/Disable eines Adapters pro Universe → Typ-Wechsel/„Disabled" sendet weiter (Doppel-Output + Leak)** | `output_manager.py:163-188`/`app_state.py:754-772` — `add_*` schreiben nur in ihre eigene Registry, kein `remove_*`/Disable, kein „Disabled"-Zweig. Typ-Wechsel (ArtNet→sACN) → beide Adapter senden; „Disabled"-Universe gibt weiter Licht aus (nur per Neustart stoppbar); Alt-Adapter-Handle nie `close()`t. **Fix:** `remove_output(universe)`/`disable_universe` (pop unter `_io_lock` + `close()`, Muster wie `_swap_device`); `apply_output_config` entfernt Alt-Typ vor dem Einrichten + behandelt „Disabled". **DoD:** Test (Typ-Wechsel → nur neuer Adapter aktiv, alter `close()`t; „Disabled" → kein Adapter für das Universe). |
| NET-04 | P3 | todo | **Kein explizites Egress-Interface / Broadcast-Default → falsches NIC auf Multi-NIC-PC** | sACN-Multicast ohne `IP_MULTICAST_IF` (`sacn.py:93-100`) + ArtNet-Default `255.255.255.255` Limited Broadcast (`artnet.py:25`) → auf WLAN+LAN-Venue-PC lautlos falsches Interface / nicht geroutet, Fixtures schwarz trotz „Aktiv". **Braucht Produkt-Entscheidung** (Interface-Auswahl-UI + Default; kein Limited-Broadcast-Default). Workaround heute: Ziel-IP (Unicast) im Patch-Feld. **Fix:** `setsockopt(IP_MULTICAST_IF, …)` + NIC-Auswahl; ArtNet Unicast/gerichteter Broadcast; `bind()` an gewählte NIC. |
| SERIAL-01 | P3 | todo | **Toter/falscher COM-Port zeigt dauerhaft „Verbunden" (kein Fehlersignal)** | `serial_process.py:167` — `is_open()` gibt `p.is_alive()` (Prozess lebt) zurück, nicht Port-offen; Worker setzt `ST_DISABLED`, aber kein UI-Code liest `is_disabled()`. Rig bleibt dunkel, UI meldet „Verbunden". **Fix:** `is_open()`/`output_live` am `ST_OK/ST_DISABLED`-Status ausrichten; Output-Config `is_disabled()` pollen → „Kein Signal". |
| SERIAL-02 | P3 | todo | **Reconnect auf feste COM-Nummer festgenagelt → USB-Replug mit neuer COM heilt nie** | `serial_process.py:94`/`enttec_pro.py:131` öffnen nur `self.port`; `find_enttec_port()` (VID/PID) beim Reconnect ungenutzt; Docstring verspricht „ohne App-Neustart". Randfall (FTDI behält meist COM-Nr). **Fix:** Reconnect optional per VID/PID neu auffinden; sonst UI-Warnung. |
| OUT-06 | P3 | todo | **sACN-Quellen-Hygiene: CID nicht persistent + keine Stream-Termination** | `sacn.py:88` CID `uuid.uuid4()` neu pro Instanz (jeder Neustart = neue Quelle für CID-trackende Empfänger); `close()` sendet keine Stream-Termination (~2,5 s Zwei-Quellen-Merge-Fenster beim Adapter-Wechsel). E1.31-Interop-Hygiene, kein falsches Paket. **Fix:** CID persistieren (Config/Maschinen-ID); `close()` sendet Options-Bit „Stream_Terminated". |
| AUD-06 | P1 | ✅ done (dieser PR) | Audit: Art-Net/sACN-EINGANG & RX-Thread-Lebenszyklus (M) | [`docs/DMX_INPUT_AUDIT_2026_07_08.md`](docs/DMX_INPUT_AUDIT_2026_07_08.md) — 5-Dimensionen-Workflow (15 Agenten, 5 CONFIRMED). Kern: **Parser robust, Locks sauber, Multicast-Join korrekt** (2 Dimensionen sauber). Defekte im **Quellen-Lebenszyklus** → **NET-05** (P1, kein Source-Timeout → eingefrorene Kanäle), **NET-06** (P2, RX-Thread-Tod ohne `_running`-Reset), NET-07/08 (P3, Merge-Config). |

### 📡 Aus DMX-Eingang-Audit 2026-07-08 (AUD-06)
_Verifizierte Befunde aus [`docs/DMX_INPUT_AUDIT_2026_07_08.md`](docs/DMX_INPUT_AUDIT_2026_07_08.md). Die Parser/Locks sind solide — die Defekte betreffen den „was passiert, wenn der externe Sender weg ist"-Bereich._

| ID | Prio | Status | Titel | Stelle / DoD |
|----|------|--------|-------|--------------|
| NET-05 | **P1** | ✅ done (dieser PR) | **Kein Source-Timeout → verlorene Art-Net/sACN-Quelle friert Kanäle dauerhaft ein** | `app_state.py:1631-1654` — Receiver rufen `apply_input_merge` nur bei Paketempfang; hört die Quelle auf, bleibt der letzte Wert in `input_layer` und der 44-Hz-Renderer mischt ihn weiter (HTP-Boden/REPLACE-Freeze). **Sicherheitsrelevant:** Input wird nicht vom Blackout/Submaster skaliert. `clear_input_merge` existiert (Docstring verspricht genau das), hat aber **keinen** Produktiv-Aufrufer; `input_layer` hat keinen Zeitstempel. **Fix:** `time.monotonic()`-Stempel pro `out_univ` in `apply_input_merge`; in `_render_frame` 4b-Input Universen älter ~2,5 s (E1.31 Network-Data-Loss) überspringen + verwerfen (bzw. Watchdog). **DoD:** Test (Quelle stempelt, dann „Zeit vergeht" → Kanal fällt auf 0/Default statt zu hängen). |
| NET-06 | **P2** | ✅ done (dieser PR) | **RX-Thread-Tod ohne `_running`-Reset → `is_running()` lügt, Eingang bleibt stumm** | `artnet_input.py:153-159`/`sacn_input.py:171-176` — `except OSError: break`/äußeres `except: break` beenden den RX-Thread ohne `self._running=False` → `is_running()` (nur `return _running`) bleibt True → UI-Restart-Guard `if not rx.is_running(): rx.start()` verpufft, `start()` no-oppt. Nach einem Netz-Blip permanent stumm, UI sagt „Aktiv". **Fix:** in beiden `break`-Pfaden `_running=False`; `is_running()` an `_thread.is_alive()` koppeln. **DoD:** Test (Thread-Tod → `is_running()` False → Restart-Guard greift). |
| NET-07 | P3 | todo | **Merge in nicht konfiguriertes `out_universe` still verworfen (Status lügt „Aktiv")** | `app_state.py:1642-1644` — `scratch.get(out_univ) is None → continue`; empfangene Kanäle verpuffen, UI zeigt „Aktiv". **Fix:** `out_univ`-Existenz prüfen (Warnung/anlegen) bzw. Status-Label. |
| NET-08 | P3 | todo | **Umkonfigurieren des Eingangs-Universums lässt alte Merge-Config + Multicast-Join aktiv** | `output_config.py:428-429` ruft nie `remove_merge`/`clear_merges` → alte Quelle (z. B. U5) mischt nach Umstellung auf U7 weiter in dasselbe out-Universe. **Fix:** vor neuem Merge den alten `in_u` räumen. |

| AUD-07 | P2 | todo | Audit: MIDI (Manager, Mapper, APC-Feedback) (M) | `docs/MIDI_AUDIT_<datum>.md`: verifizierte Bug-Liste (`file:line`) zu Cross-Thread-Marshalling der Callbacks (`midi_manager.py` MidiDispatch-Thread → UI), Queue-Overflow-/Reconnect-Verhalten (bounded Queue `maxsize=4096`) und Mapping-/Teach-Korrektheit (`midi_mapper.py`, `apc_mk2/mini_feedback.py`). Nur `tests/test_midi_mapper.py` existiert. P1-Bugs als Items. |
| AUD-08 | P2 | ✅ done (dieser PR) | Audit: OSC- & Timecode/MTC-Remote-Eingang (S) | [`docs/OSC_TIMECODE_AUDIT_2026_07_08.md`](docs/OSC_TIMECODE_AUDIT_2026_07_08.md) — 4-Dimensionen-Workflow (22 Agenten, 7 CONFIRMED). **Kein neuer P1/P2** — OSC/MTC ist solide (robuste Handler, Cross-Thread gelockt, MTC spec-konform). Die 2 P2 sind **Parität** zu WEB-01 (`/lightos/ch` am Renderer vorbei) + NET-01 (`0.0.0.0` auth-los) → dort vermerkt. Neu nur P3 → **OSC-04, MTC-01/02/03**. |

### 🎛️ Aus OSC/MTC-Audit 2026-07-08 (AUD-08)
_Verifizierte Befunde aus [`docs/OSC_TIMECODE_AUDIT_2026_07_08.md`](docs/OSC_TIMECODE_AUDIT_2026_07_08.md). Kein neuer P1/P2 (die 2 P2 sind Parität → in NET-01/WEB-01 vermerkt); nur P3-Robustheit._

| ID | Prio | Status | Titel | Stelle / DoD |
|----|------|--------|-------|--------------|
| OSC-04 | P3 | ✅ done (dieser PR) | **`_handle_blackout` invertiert bei String-Args (`bool("0")`==True)** | `osc_server.py:89` — `val = bool(args[0])` → String-Typetag „0"/„off" → Blackout AN statt AUS (getypte int/float von TouchOSC/Lemur sind ok). **Fix:** typ-tolerant (numerisch `float(raw)>=0.5`, String gegen `{"0","off","false","no",""}`). |
| MTC-02 | P3 | ✅ done (dieser PR) | **MTC feuert bei `piece==7` ohne Vollständigkeitsprüfung → gemischte Nibbles** | `mtc_reader.py:117` — bei Mid-Stream-Attach/Rückwärts-Transport Frame aus alten+neuen Nibbles → kurz falscher Timecode. **Fix:** Bitmaske der empfangenen Pieces, nur bei vollständigem 0..7-Satz feuern. |
| MTC-01 | P3 | todo | **MTC `+2`-Frame ohne Wrap → Frame-Nr ≥ fps** | `mtc_reader.py:131` — `adj_frames = frames+2` ohne Sekunden-Carry → „:31" bei 30 fps. **Fix:** Timecode normalisieren — **Achtung Drop-Frame (29,97)**, eigene sorgfältige Runde. |
| MTC-03 | P3 | todo | **Lock-freie `time()`/`format()` → Cross-Thread-Torn-Read (latent)** | `mtc_reader.py:167/173` — heute kein Live-Defekt (einziger Consumer nutzt die konsistenten `_fire`-Callback-Args), aber Falle für künftige Poll-Consumer. **Fix:** Tupel unter Lock lesen/kopieren. |
| AUD-09 | P2 | todo | Audit: Audio/BPM-Engine (Beat-Detektor, Capture, BPM-Manager) (M) | `docs/AUDIO_BPM_AUDIT_<datum>.md`: verifizierte Bug-Liste (`file:line`) zu Thread-Safety/Callback-Marshalling in `bpm_manager.py` (`_emit_beat`/Timer-Thread, „im Qt-Mainthread"-Garantie) & `audio/capture.py`, Beat-Anker-/Drift-Korrektheit (`beat_detector.py`) und `offline_timeline.py`-Persistenz. BPM treibt EFX/Matrix/Chaser-Sync (DEMO-04/ENG-10-Kopplung). `BPM_MANAGER_PLAN.md` ist Plan, kein Audit. P1-Bugs als ENG-Items. |
| AUD-10 | P2 | todo | Audit: QXF/QXW-Fixture-Import & Attribut-Mapping (M) | `docs/FIXTURE_IMPORT_AUDIT_<datum>.md`: verifizierte Bug-/Risiko-Liste (`file:line`) zu Kanal-/Capability-Parsing und Attribut-Mapping in `qxf_import.py` (Vollständigkeit QXF-Kanaltyp→Attribut, Multi-Head-`#N`-Ableitung), QLC+-Workspace-Import `qxw_importer.py`, Fehlertoleranz bei korrupten QXF; mind. 1 Mapping-/Klassifikations-Verdachtsfall belegt/widerlegt (analog ENG-07, dessen Attributname aus genau diesem Import stammt). Nur `tests/test_qxf_import_color_attr.py` existiert. Befunde als ENG-/QA-Items. |

### 📚 Pro-Komponente-Doku (DOC)

_Es gibt kein `docs/components/`-Verzeichnis (per Glob verifiziert) und kein Pro-Modul/Pro-Widget-Template — nur SHOW-Anleitungen und High-Level-Inventare (`FEATURE_MAP.md`, `CAPABILITIES.md`). Gestaffelter Plan: Index+Template zuerst, dann Batch-Items je Gruppe, plus ein CI-Gate gegen Doku-Drift._

| ID | Prio | Status | Titel | Akzeptanzkriterium (Definition of Done) |
|----|------|--------|-------|------------------------------------------|
| DOC-02 | P1 | todo | DOC-Index + Konvention + Template (`docs/components/README.md`) (S) | Neu: `docs/components/README.md` mit (a) Zweck/Scope, (b) Pfadkonvention (`views/<modul>.md`, `vc/<widget>.md`, `engine/<typ>.md`, `output/<modul>.md`, `input/<modul>.md`), (c) verbindliches Markdown-Template mit fixen Abschnitten (Zweck · Bedienung/Optionen · Verknüpfungen · zugehörige Tests · Quelle `file:line`), (d) lebende Status-Tabelle (Komponente → Doc ja/nein). Plus 1 voll ausgefülltes Beispiel (`docs/components/vc/vc_button.md`). Grundlage für DOC-03..07 + QA-10. |
| DOC-03 | P2 | todo | Doku für alle VC-Widgets (`docs/components/vc/<widget>.md`) (L) | Pro `vc_*.py`-Widget (außer Infrastruktur `vc_widget/vc_canvas`, im Index als „Basis" markiert) eine Doc nach Template: Zweck, Modi/Optionen (z.B. `SliderMode` bei vc_slider, action-Kinds bei vc_button), Drop-/Bind-Verhalten (`_droppable_types`), Serialisierung (`to_dict/apply_dict`), gekoppelte Engine-Teile (`effect_live`, `show_file`), Tests. DoD: jede `vc_*.py` hat eine Doc oder ist im Index begründet ausgenommen. Hochgekoppeltes Subsystem (vgl. VC-Audit). |
| DOC-04 | P2 | todo | Doku für alle Views (`docs/components/views/<view>.md`) (L) | Pro `src/ui/views/*.py`-View (außer Helfer-Widgets) eine Doc nach Template: Zweck, Navigations-Ort (Sektion/Sub-Tab laut `main_window.py`/`FEATURE_MAP.md`), Bedienelemente, abonnierte/emittierte Bus-Signale (z.B. `SELECTION_CHANGED`, `patch_changed`), gekoppelte AppState-/Engine-Teile, Tests. DoD: jede Nicht-Helper-View hat eine Doc oder ist als Helper/Basis markiert. |
| DOC-05 | P2 | todo | Doku für alle Engine-Funktionstypen (`docs/components/engine/<typ>.md`) (M) | Pro `FunctionType`-Wert (`function.py`: Scene, Chaser, Sequence, Collection, Show, EFX, RGBMatrix, Audio, Script, MappedChannelChange) eine Doc nach Template: Zweck/Verhalten, Parameter (Dataclass-Felder + `set_param`-Keys, z.B. EFX `speed` nicht `speed_hz`), Render-Beitrag (Schritt in `show_engine`), Serialisierung, gekoppelte Module, Tests. DoD: alle 10 FunctionType-Werte haben eine Doc, im Index verlinkt. |
| DOC-06 | P2 | todo | Doku für DMX-/Output-Komponenten (`docs/components/output/<modul>.md`) (M) | Pro `src/core/dmx/*.py`-Modul (output_manager, universe, artnet, sacn, enttec_pro, serial_process, artnet_input, sacn_input) eine Doc nach Template: Zweck, Konfiguration/Schalter (z.B. `LIGHTOS_SERIAL_INPROC`, `FAIL_LIMIT`, Universe-Mapping), Threading-/Prozessmodell, Fehler-/Watchdog-Verhalten, gekoppelte Module, Tests. DoD: alle 8 Nicht-`__init__`-Module haben eine Doc. |
| DOC-07 | P3 | todo | Doku für Input-Komponenten (MIDI/OSC/Timecode/Hotkeys) (M) | Pro Input-Modul (`midi_manager`, `midi_mapper`, `midi_backend_winmm`, `osc_server`, `mtc_reader`, `input/keyboard_hotkeys`, `input/profile`) eine Doc nach Template: Zweck, unterstützte Nachrichten/Adressen (MIDI-CC/Note, OSC-Pfade, MTC-Frames), Mapping-/Learn-Mechanik, gekoppelte VC-/Engine-Teile, Tests. DoD: jedes Modul hat eine Doc, im Index verlinkt. `KEYBOARD_MAPPING.md` deckt nur Tastatur ab. |
| DOC-08 | P2 | todo | Referenz-Doku: Env-Flags & Config-Dateien (`docs/CONFIG_REFERENCE.md`) (S) | Neu: `docs/CONFIG_REFERENCE.md` mit (a) Tabelle aller `LIGHTOS_*`-Env-Flags (LIGHTOS_DEBUG, _STRICT, _SHOW_DB, _NO_OUTPUT_THREAD, _NO_AUDIO_AUTOSTART, _SERIAL_INPROC, _FLASK_SECRET): Name·Wirkung·Default·wo gelesen (`file:line`)·Test-Relevanz; (b) Tabelle der `data/`-Configs (universes.json, midi_mappings.json, channel_groups.json): Pfad·Schema-Kurzform·Subsystem. Per `ls docs/` verifiziert: kein `*CONFIG*`-Doc. Aus README/INSTALL.md verlinkt. |
| DOC-09 | P3 | todo | Anleitung: Web-Remote (Handy als Konsole) einrichten & bedienen (S) | Neu: `docs/anleitung_web_remote.md` (Stil `docs/anleitung_*`): (1) Server starten (Port 5000, Bind `0.0.0.0`, ggf. `LIGHTOS_FLASK_SECRET`), (2) eigene IP finden + am Handy im WLAN öffnen, (3) was die Buttons tun (GO/BACK/Blackout/Fader/Executor — Bezug auf `src/web/app.py`-Routen), (4) Sicherheitshinweis (offenes Netz, keine Auth). Aus README.md + `docs/ANLEITUNGEN.md` verlinkt; `REMOTE_KONSOLE_PLAN.md` (reines Konzept) davon abgegrenzt. |

### ✅ UI-Verifikations-Routine (QA)

_Bestehend: schmales Smoke-Set (`test_views.py` baut nur ~4-6 Views; `test_vc_dialogs_smoke.py` deckt 6 der 19 WIDGET_REGISTRY-Widgets) + die MANUELLE, hardware-abhängige `docs/SMOKE_TEST.md`. Es fehlt ein enumerierender Headless-Smoke über ALLE Views/Widgets, eine maschinen-prüfbare Pro-Element-Checkliste und gezielte Tests für acht völlig ungetestete Views (AudioInputView und ShowManagerView haben seit #238/#235 eigene Tests)._

| ID | Prio | Status | Titel | Akzeptanzkriterium (Definition of Done) |
|----|------|--------|-------|------------------------------------------|
| QA-09 | P1 | todo | Enumerierender Headless-Smoke: ALLE Views + alle WIDGET_REGISTRY-Widgets bauen ohne Crash (M) | Neuer `tests/test_ui_smoke_enumerated.py`: (1) `@parametrize` über alle no-arg QWidget-Views in `src/ui/views/*.py` (per `importlib/inspect` ausgelesen; arg-pflichtige Editoren via dokumentierter Skip-Liste) — jede headless (`QT_QPA_PLATFORM=offscreen`) ohne Exception; (2) `@parametrize` über `WIDGET_REGISTRY` (`vc_canvas.py`) — jeder Typ instanziiert + `to_dict()`→`apply_dict()`-Roundtrip wirft nicht. Künstliches Umbenennen einer View lässt den Test rot werden. Grün via `tools/verify_loop.ps1`. **Merge der zwei Survey-Vorschläge zu einem.** |
| QA-10 | P1 | todo | Smoke-Tests für die 8 Views ohne jede Test-Abdeckung (S) | `grep` gegen `tests/` zeigt 8 Views mit 0 Treffern: AudioEditor, CarouselEditor, CollectionEditor, EffectLayerEditor, MidiView, OutputView, SceneEditor, ScriptEditor. Die 2 no-arg + im MainWindow verdrahteten (OutputView, MidiView) headless ohne Crash bauen + je 1 Assertion auf ein zentrales Kind-Widget; die 6 arg-pflichtigen Editoren mit Minimal-Fixture (leere Scene/Script/Carousel/Collection/LayeredEffect/AudioFunction). Alle grün via `verify_loop.ps1`. |
| QA-11 | P2 | todo | CI-Gate auf Show-Lint + View-Smoke ausweiten (S) | `.github/workflows/ci.yml` ruft nur `pytest tests/test_show_file.py tests/test_core_engine.py` (verifiziert). Zusätzlich `tests/test_show_lint.py` + `tests/test_views.py` ausführen (mit `QT_QPA_PLATFORM=offscreen`, PySide6 im Install-Schritt); ein absichtlich kaputter `param_key` in einer Test-Show lässt CI rot werden. So fällt eine kaputte committete Show/View auch auf GitHub auf, nicht nur lokal (vgl. QA-05, lokal entdeckt). |
| QA-12 | P2 | todo | `docs/UI_VERIFICATION_CHECKLIST.md`: maschinen-prüfbare Pro-Element-Checkliste (M) | Neue `docs/UI_VERIFICATION_CHECKLIST.md` mit (1) Tabelle aller Views (`src/ui/views/*.py`) + VC-Widgets (WIDGET_REGISTRY), Spalten: headless-instanziierbar / hat Tooltip-od.-Label / Aktion-od.-Signal verdrahtet / Regressionstest (Pfad) / Doc; (2) je Zeile ein ausführbarer Verifikationspfad (Testname od. „manuell: SMOKE_TEST.md §x"). Offene Zellen als Lücke markiert. Löst die rein manuelle `docs/SMOKE_TEST.md` als Backing-Store für QA-13 ab. |
| QA-13 | P3 | todo | Tooltip-/Label-Coverage-Audit für interaktive Steuerelemente (M) | Headless-Test (oder `tools/`-Skript) baut jede no-arg View, durchläuft `findChildren(QAbstractButton)` und meldet Buttons mit leerem `text()` UND leerem `toolTip()` als Verstoß; Ergebnis in `docs/UI_VERIFICATION_CHECKLIST.md`. Abdeckung heute ungleich: `show_manager_view.py` (7 Buttons, 0 Tooltips), `dmx_monitor_view.py` (0) vs. `efx_view.py` (28). Mindestziel: show_manager + dmx_monitor dokumentiert/behoben. |

### 🐛 Gezielte Bugfix-Jagden (Bug-Klassen-Audits)

_Systematische Klassen-Abdeckung statt Einzel-Pfade. `hypothesis` ist NICHT in den Deps → kein Property-Test existiert. Verifizierte Drift-Quelle u.a.: `show_file.py` klemmt `address/channel_count` beim LADEN auf ≤512, beim DUMP nicht._

| ID | Prio | Status | Titel | Akzeptanzkriterium (Definition of Done) |
|----|------|--------|-------|------------------------------------------|
| STAB-10 | P1 | todo | .lshow Save→Load→Save Round-Trip-Identitätsjagd (Property/Fuzz) (M) | Neuer `tests/test_show_roundtrip_identity.py`: lädt jede committete `shows/*.lshow` (oder baut Fixture-Shows), führt save→load→save aus und assertet die zweite `show.json` == erste (nach Normalisierung der Key-Typen). Verifizierte Drift: `_fixture_to_dict` klemmt beim Dump nur `max(1,…)`, `_patched_fixture_from_data` beim Load `min(512,…)` (`show_file.py:42/66` vs `:94/95`) → Fixture mit address/channel_count>512 driftet still. Gefundene Drifts als STAB-Sub-Items; Fix-Vorschlag = symmetrische Klemmung. Komplementär zu AUD-04. |
| ENG-11 | P1 | todo | Multi-Head-Attribut (`attr#N`) Pfad-Sweep über alle Erzeuger/Verbraucher (M) | Neuer `tests/test_multihead_sweep.py`: parametrisiert über eine 3-Kopf-Fixture jeden `#N`-Pfad (set→get, classify, label, palette apply+record, snap roundtrip, efx write) und assertet: kein `attr#N` auf nicht vorhandenen Köpfen, Kopf-Werte unabhängig, `#N`-Keys überleben save/load. Das `#N`-Suffix wird an >12 Stellen unabhängig erzeugt/geparst und hat schon 3 echte Bugs geliefert (ENG-03/07/09). Verdachtsstelle: `app_state.py:839` (Fallback Kopf 0) vs. occurrence-zählende Schreiblogik `efx.py:578`/`mapped_channel.py:266`. Inkonsistenzen als ENG-Sub-Items. |
| STAB-21 | P1 | todo | Threading/native-AV-Härtungsaudit (re-entrante Emits, close/send-Races) (M) | `docs/THREADING_AV_AUDIT_<datum>.md`: tabellarische Liste aller Cross-Thread-Zugriffe (UI↔Output↔MIDI-RX) mit Lock/Marshalling-Status + Reentrancy-Risiko; mind. 2 konkrete Verdachtsstellen als STAB-Sub-Items mit Minimal-Fix-Skizze + Regressionstest-Idee. Kandidaten: weitere re-entrante `_emit`-Pfade (analog STAB-07/09) und `vc_canvas` teardown vs. laufender MIDI-Dispatch (`_on_midi_raw`/`_teardown_midi`). Prüft gegen den „nur `_render_frame` schreibt"-Invariant (OUTPUT_MERGE_CONTRACT Inv.1). |
| QA-14 | P2 | todo | Render-Vertrag (WP-6/EE-02/LTP) als Property-Tests statt Einzelfälle (M) | `hypothesis` als Test-Dep + neuer `tests/test_render_contract_props.py` mit `@given`-Strategien über (Anzahl Funktionen, Kanal-Typ Intensität/Farbe/Position, Werte, Start-Reihenfolge), der WP-6 (Funktions-Kanalschutz), EE-02 (Programmer-Dimmer multiplikativ) und LTP (später gestartete gewinnt) als Eigenschaften assertet. Mind. 3 Properties grün; jede generativ gefundene Vertragsverletzung als ENG/STAB-Sub-Item. ARCHITECTURE/OUTPUT_MERGE_CONTRACT nennen selbst einen by-design-Grenzfall. |
| STAB-12 | P2 | todo | MIDI-Mapping (global) vs VC-Binding Konflikt-Audit (S) | Audit + Test `tests/test_midi_vc_conflict.py`: simuliert eine MIDI-Message, die sowohl ein VC-Widget-Binding als auch ein globales `MidiMapper`-Mapping trifft (beide abonnieren denselben `get_midi_manager()`-Bus, `vc_canvas.py:_setup_midi`); dokumentiert das beobachtete Verhalten (beide feuern?) + DoD = klare Aussage „erwartet/unerwartet" mit Empfehlung (Priorität VC vor global, oder Konflikt-Warnung beim Learn). ARCHITECTURE markiert die globalen Mappings ausdrücklich als Konfliktquelle. Doppel-Trigger als STAB-Sub-Item. |
| QA-15 | P2 | todo | Undo-Abdeckungs-Matrix — jede mutierende Aktion auf Undo-barkeit prüfen (S) | `docs/UNDO_COVERAGE_MATRIX.md`: alle mutierenden AppState/Engine/UI-Aktionen mit Spalte „undo-bar? (ja/nein/teilweise)" + Quellzeile; identifizierte Nein-Lücken als priorisierte STAB/UI-Sub-Items. Heute nur punktuell `undoable=` (add/remove/update_fixture, auto_patch, set_programmer_value); Verdachtskandidaten `clear_programmer` (`app_state.py:674`, kein undoable-Param) + base-level-Setter. DoD: mind. 3 echte Lücken benannt. Bestehende Lücken (VCB-06, UI-02) belegen die Klasse. |

### 🛠️ Verifizierbarkeit / CI / Werkzeuge (Querschnitt)

_Kleine, günstige Gates gegen Doku-/Backlog-/Performance-Drift. Alle Lücken per grep/ls verifiziert._

| ID | Prio | Status | Titel | Akzeptanzkriterium (Definition of Done) |
|----|------|--------|-------|------------------------------------------|
| QA-16 | P1 | done ([#213](https://github.com/ixamgames-droid/lightos/pull/213), [#231](https://github.com/ixamgames-droid/lightos/pull/231)) | Web-Remote-Endpunkte headless abgesichert | `tests/test_web_app.py` deckt Flask- und SocketIO-Routing, Clamp-/Bereichs-Guards, fehlerhafte Payloads, leere Cue-Stacks sowie STOP und Fader-Initial-Sync ab. Die ursprüngliche Behauptung „0 Tests" ist damit überholt. |
| QA-17 | P2 | todo | Doc-Link-Checker für tote `*.md`-Querverweise (S) | Neues `tools/check_doc_links.py` + `tests/test_doc_links.py`: parst alle relativen Markdown-Links (`](…md…)`) in `docs/*.md`, `BACKLOG.md`, `ROADMAP.md`, `CHANGELOG.md` und meldet nicht existierende Ziele; grün, sobald aktuell tote Links (z.B. die neuen `docs/components/*` aus DOC-02..07) existieren oder als bekannt-ausgenommen gelistet sind. ~65 Docs / ~168 Querverweise, heute ungeprüft. |
| QA-18 | P1 | todo | Backlog-Linter: ID-/PR-/Status-Konsistenz in `BACKLOG.md` (S) | Neuer `tests/test_backlog_lint.py`: prüft die kanonische Arbeitswarteschlange und alle Tabellenzeilen auf eindeutige IDs, zulässige Status (`todo`, `decision`, `blocked`, `wip`, `review`, `done`), gültige PR-Links bei `done`/`review` und intakte Markdown-Links. Historische Detailnotizen werden entweder normalisiert oder explizit als Log-Zeilen ausgenommen. Verhindert, dass wieder erledigte oder doppelte Items als offen erscheinen. |
| DOC-10 | P2 | todo | AUDIT_COVERAGE-Tracker: welche Subsysteme sind auditiert (M) | Neues `docs/AUDIT_COVERAGE.md` mit Tabelle Subsystem → letztes Audit-Doc/Datum/Status (auditiert/teilweise/offen), verlinkt aus `BACKLOG.md`; optionaler Test prüft, dass jede referenzierte Audit-Doc existiert. Macht die nächste sinnvolle AUD-Aufgabe (AUD-01..10) sofort sichtbar und verhindert Doppel-Audits. Heute muss der Loop das aus Backlog/Changelog rekonstruieren. |
| QA-19 | P2 | todo | Show-Lint: `param_key` gegen die echten Params der gebundenen Funktion prüfen (M) | `validate.py`: bei gesetzter `function_id` wird `param_key` gegen die Param-Keys des konkreten gebundenen Funktionstyps geprüft (neuer Code, z.B. `VC-PARAMKEY-FN`) statt nur gegen `caps.all_param_keys` (Union aller Effekte). Test: Slider `param_key='speed'` gebunden an eine Funktion ohne speed-Param → ERROR-Finding; korrekt gebundener bleibt sauber. Genau die QA-05-Bugklasse (`speed_hz` global gültig, für die Funktion wirkungslos) rutscht heute durch. |
| QA-20 | P2 | todo | Performance-Regression-Gate: 44-Hz-Render-Budget im Test prüfen (S) | Erweiterung von `tests/test_benchmark_universes.py` (oder neuer Test): misst `_render_frame` über N Frames bei kleiner Universe-Zahl (1-2, `LIGHTOS_NO_OUTPUT_THREAD=1`) und assertet p95 unter einer großzügigen, dokumentierten Schwelle (Konstante mit Begründung, deutlich < 22,7 ms bei 1 Universum); ein künstliches `sleep` im Render lässt den Test rot werden. Heute prüft der Test nur Struktur (p50>0, fps>0) — eine Render-Regression im heißen Pfad fängt nichts. Headless im Test-Gate. |
| QA-21 | P3 | todo | `/lightos-loop`-Routine-Task: wiederkehrende UI-Element-Verifikation (S) | Neue Mini-Spec `.claude/commands/ui-verify-round.md`: definiert einen Loop-tauglichen Task, der (1) die oberste rote Zeile aus `docs/UI_VERIFICATION_CHECKLIST.md` (QA-12) wählt, (2) Headless-Test + ggf. Tooltip/Doc ergänzt, (3) die Checkliste-Zeile auf grün setzt, (4) via `verify_loop.ps1` testet; mit Auswahlkriterium + Abbruchbedingung (alle grün). Macht die Verifikation zur Routine statt Einmal-Doc. Hängt an QA-12. |
| QA-22 | P2 | todo | UI-Drive-Toolset konsolidieren: `tools/app.ps1` (restart/stop/wait/shot) + EIN Klick/Screenshot-Werkzeug (M) | Transcript-Mining 2026-07-06: ~700 ad-hoc getippte Kill-Start-Sleep-/Klick-/Shot-Ketten über `docs/_walkthrough/lo.ps1` UND `docs/tutorial_matrix/_ui.ps1`/`_cap.ps1`/`_fg.ps1` (zwei redundante Toolsets für denselben Zweck). Neu: `tools/app.ps1` mit Subkommandos `restart/stop/wait/shot` (kapselt den Get-CimInstance-Kill per CommandLine-Filter mit Worktree-Pfad-Parameter, venv-Start, Fenster-Wartezeit) + Konsolidierung der zwei Walkthrough-Toolsets zu einem (`fg/click/dclick/key/type/shot/crop/scroll`, einheitliche Screenshot-Ordner-Konvention, `dlg.ps1`-Modal-Handling integriert). DoD: beide alten Toolsets rufen intern das neue auf (oder sind ersetzt); ein kompletter Mini-Walkthrough (Show laden, 3 Screenshots) läuft nur über das neue Tool; `/lightos-demoshow` + `/lightos-audit` (`.claude/commands/`) verweisen darauf. |
